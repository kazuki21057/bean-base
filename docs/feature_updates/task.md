# タスクリスト

## Dashboard (ダッシュボード)
- [x] **Inventory (インベントリ/履歴)**: リスト表示からグリッド表示（3列など）に変更する。

## Master (マスタ管理)
- [x] **Grid View (グリッド表示)**: 全マスタリスト（Beans, Grinders, Drippers, Filters）をグリッド表示に変更する。
- [x] **Image Fix (画像表示修正)**: アイコンではなく登録された画像が正しく表示されるように修正する（現状プレースホルダーが優先されている可能性があるため見直し）。
- [x] **Method Steps (メソッドステップ)**: 入力方式を「増分（＋秒、＋ml）」から「合計値（経過時間、総湯量）」に変更する。
  - [x] ステップ編集箇所の特定（なければ作成）。
  - [x] 入力された「合計値」からモデルの「増分」へ変換するロジックの実装。
  - [x] **Time Format (時間表記)**: 編集時の時間入力を「分:秒」形式に変更する。

## Coffee Logs (抽出記録)
- [x] **Editing (編集機能)**: 既存のコーヒーログを編集できるようにする。
  - [ ] 編集画面の作成（`LogEditScreen` または既存画面の流用）。
  - [ ] 詳細画面→編集画面への遷移。
  - [x] **Score Order (スコア順序)**: Overall（総合評価）をスライダーリストの一番下に移動する。

## Verification (検証)
- [x] `flutter test` の実行（リグレッションがないか確認）。
- [x] `flutter run` での実機動作・ログ確認。
