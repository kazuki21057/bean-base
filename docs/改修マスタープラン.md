# BeanBase 2.0 改修マスタープラン（進捗表示付き）

> 本書は今回の大規模改修の **全体設計と進捗の単一の真実**。毎日の改修タスクはここから選ぶ。
> 出典: [`docs/Beanbase改修案.md`](./Beanbase改修案.md) / 運用ルール: `CLAUDE.md`「日次改修ループ運用ルール」
> 最終更新: 2026-06-28

## 凡例
| 記号 | 意味 |
|---|---|
| ⬜ | 未着手 |
| 🟦 | 進行中 |
| ✅ | 完了 |
| ⏸ | 保留／後回し |

---

## 1. 改修の全体方針

| # | 柱 | 概要 | 優先 |
|---|---|---|---|
| A | データ基盤変更 | Firestore → **Google Sheets** に戻す（管理を楽に） | 1 |
| B | 画面構成・遷移の再編 | 抽出と評価を分離、14画面構成へ再設計 | 2 |
| C | UI全面刷新 | ダッシュボード/抽出/評価/スタッツを一から。黒板風・瓶ビジュアル等 | 3 |
| D | その他軽微な修正 | レスポンシブ確認・DRY化・仕上げ | 4 |
| E | AI活用拡大 | PCAの説明範囲拡大など（**全改修完了後**） | 5（最後） |

**データ基盤の決定（A）:** 個人利用・データ量小のため、以前の `SheetsService`（`lib/services/sheets_service.dart`）を再活用して Google Sheets に戻す。スプレッドシートで直接データ確認・手動編集できる点が「管理が楽」の要件に最適。Cycle 18 で入った `FirestoreService` 経由の読み書きを Sheets 経由へ差し戻す。画像保存先は別途方針決定（後述 T0-4）。

---

## 2. 全体進捗サマリ（フェーズ別）

| フェーズ | 内容 | 対応Cycle | 状態 |
|---|---|---|---|
| Phase 0 | データ基盤を Sheets に戻す | Cycle 19 | ✅ |
| Phase 1 | 画面構成・ナビ再編（14画面の骨組み） | Cycle 20–22 | ⬜ |
| Phase 2 | UI全面刷新（黒板風・瓶・タイマー・評価・スタッツ） | Cycle 23–26 | ⬜ |
| Phase 3 | 軽微な修正・仕上げ | Cycle 27 | ⬜ |
| Phase 4 | AI活用拡大 | Cycle 28+ | ⏸ |

> Cycle番号は目安。実際の粒度に応じて分割・増減してよい（番号は連番厳守、`docs/`と`docs/archive/`の最大値+1）。

---

## 3. フェーズ詳細＆タスク分解（日次タスクのベース）

### Phase 0 — データ基盤を Sheets に戻す（Cycle 19）
**終了条件:** Sheets 経由で一覧表示・登録・編集・削除が動き、`flutter run` で接続成功。

| ID | タスク | 状態 |
|---|---|---|
| T0-1 | `SheetsService` の現状調査・API/認証の再有効化方針を決める（抽象 `DataService` 採用） | ✅ |
| T0-2 | `data_providers.dart` の読み取りを Firestore→Sheets に切替 | ✅ |
| T0-3 | 各CRUD（records/beans/grinders/drippers/filters/methods/pouringSteps）の書込を Sheets に戻す | ✅ |
| T0-4 | 画像保存先の方針決定（**Google Drive 保存**に決定。実装は GAS拡張方式・別タスク T0-4b） | ✅ |
| T0-4b | Drive 画像保存の実装（GAS拡張方式） | ✅ |
| T0-5 | `analyze`/`test`/`run` で動作・接続確認、`[Antigravity]`ログ整備 | ✅ |

### Phase 1 — 画面構成・ナビ再編（Cycle 20–22）
**終了条件:** 14画面のルーティングが通り、抽出(030)と評価(031)が別画面で遷移する。

| ID | タスク | 状態 |
|---|---|---|
| T1-1 | 14画面のルーティング骨組み（`NavigatorKey`/`navIndexProvider` 再編） | ⬜ |
| T1-2 | **抽出(030)と評価(031)を別画面に分離** | ⬜ |
| T1-3 | ダッシュボード001の骨組み（残豆量・直近5件のプレースホルダ＋遷移） | ⬜ |
| T1-4 | 抽出履歴 リスト002／詳細003（スワイプで評価継承→021） | ⬜ |
| T1-5 | 豆管理 010/011/012（カード一覧・詳細・新規＋ボタン） | ⬜ |
| T1-6 | ドリッパー013–015／フィルター016–018（画像左・名前右のリスト） | ⬜ |
| T1-7 | メソッド019–021／グラインダー022–024 | ⬜ |

### Phase 2 — UI全面刷新（Cycle 23–26）
**終了条件:** 黒板風ダッシュボード・瓶ビジュアル・タイマー付き抽出・評価・スタッツが見た目通りに動作。

| ID | タスク | 状態 |
|---|---|---|
| T2-1 | ダッシュボード001を**黒板風**デザインに | ⬜ |
| T2-2 | **残豆量の瓶ビジュアル**（10%刻みで中身減、空は非表示、チェックボックスで表示切替） | ⬜ |
| T2-3 | 抽出レシピ030（豆名・豆量・メソッド選択→Pouring Steps表示、タイマー連動ハイライト） | ⬜ |
| T2-4 | 030のPouring Steps編集（既存更新／新規メソッド継承→021、評価時に豆残量を自動計算） | ⬜ |
| T2-5 | 評価画面031（抽出情報登録＋評価、新規登録対応、登録後031に戻る） | ⬜ |
| T2-6 | スタッツ040の刷新 | ⬜ |
| T2-7 | 設定090（メインカラー・APIキー・データ保存先情報） | ⬜ |

### Phase 3 — 軽微な修正・仕上げ（Cycle 27）
| ID | タスク | 状態 |
|---|---|---|
| T3-1 | モバイル実機でのレイアウト確認・調整 | ⬜ |
| T3-2 | 画像アップロード部品の共通化（DRY） | ⬜ |
| T3-3 | メソッド詳細020の動画再生（可能なら） | ⬜ |
| T3-4 | 全体のUIおしゃれ化・微調整 | ⬜ |

### Phase 4 — AI活用拡大（Cycle 28+、最後）
| ID | タスク | 状態 |
|---|---|---|
| T4-1 | PCA説明を第3主成分以降へ拡大／活用幅拡大 | ⏸ |

---

## 4. 画面インベントリ（14画面）

| 画面 | 名称 | 主な要素 | 状態 |
|---|---|---|---|
| 001 | ダッシュボード | 残豆量(瓶)・直近5件履歴・黒板風 | ⬜ |
| 002 | 抽出履歴(リスト) | 日時/豆/メソッド/点数、スワイプで021 | ⬜ |
| 003 | 抽出履歴(詳細) | 全情報表示・編集 | ⬜ |
| 010 | 豆管理(カード) | 焙煎所/豆名/煎り度/画像/残量、0%表示切替、＋ | ⬜ |
| 011 | 豆管理(詳細) | 全情報・編集・関連履歴5件 | ⬜ |
| 012 | 新規豆追加 | 入力フォーム | ⬜ |
| 013 | ドリッパー管理 | 画像左/名前右リスト、＋ | ⬜ |
| 014 | ドリッパー詳細 | 全情報・編集・関連履歴5件 | ⬜ |
| 015 | 新規ドリッパー | 入力フォーム | ⬜ |
| 016 | フィルター管理 | 画像左/名前右リスト、＋ | ⬜ |
| 017 | フィルター詳細 | 全情報・編集・関連履歴5件 | ⬜ |
| 018 | 新規フィルター | 入力フォーム | ⬜ |
| 019 | メソッド管理 | 名前/発案者(小)/抽出回数、＋ | ⬜ |
| 020 | メソッド詳細 | 全情報・動画再生(可能なら)・編集・履歴5件 | ⬜ |
| 021 | 新規メソッド | 入力フォーム | ⬜ |
| 022 | グラインダー管理 | 画像左/名前リスト、＋ | ⬜ |
| 023 | グラインダー詳細 | 全情報・編集・履歴5件 | ⬜ |
| 024 | 新規グラインダー | 入力フォーム | ⬜ |
| 030 | 抽出レシピ | Pouring Steps・タイマー・編集・評価ボタン→031 | ⬜ |
| 031 | 抽出結果の評価 | 抽出情報登録＋評価・新規登録対応 | ⬜ |
| 040 | 統計情報 | スタッツ・歯車→090 | ⬜ |
| 090 | 設定 | メインカラー/APIキー/保存先 | ⬜ |

---

## 5. 日次ループ運用（要点）

- **終了条件**（いずれかで停止）: ①タスク完了 ②連続3回失敗 ③当日コスト>$1.5 ④当日ターン数>=30
- コスト・ターン数は `.claude/hooks/loop_guard.js` が transcript から算出し `.claude/loop_state.md` に出力。**この数値が真実**。
- 連続失敗は `.claude/loop_failures.txt`（整数）を自分で更新（失敗+1／成功0）。
- 停止時は新規着手せず、`NEXT_SESSION.md`（引き継ぎ）と本書の進捗表を更新してから終了。
- 詳細は `CLAUDE.md`「日次改修ループ運用ルール」を参照。

### 5.1 自動ループの省コスト運用方針（$0.5/日 厳守）
コスト上限 $0.5/日 を変えずに自動化するため、**1日=最小1サブタスク**に限定し、**実装日と評価日を分離**する。役割をクラウドとローカルで分担:

| 担当 | 起動 | 役割 | やること | やらないこと |
|---|---|---|---|---|
| **クラウド schedule** | 毎日 05:00 JST（PC不要） | **実装日** | 最小1サブタスクのコード実装 → `flutter analyze` → `flutter test` → commit/push | `flutter run`（外部通信不可・高コスト） |
| **ローカル loop** | PC起動時 05:00 JST | **評価日** | `git pull` → `flutter run` → Playwright で疎通・見た目を評価 → 結果を `NEXT_SESSION.md` に記録 | 新規コード改修（NG項目は次の実装日へ回す） |

**省トークン徹底（コスト抑制の要）:**
- ファイルは全読みせず対象箇所のみ読む。大きな画面は「骨組み→部品→評価」と分割。
- Playwright の `snapshot` は最小限、スクショは要所のみ。
- $0.5 到達で `loop_guard` が停止 → その時点までを commit し引き継ぎ、翌日継続。

**競合回避:** クラウドは実装してpush、ローカルは pull して評価のみ（feature コードを commit しない）ので、同時刻でも git 競合は起きない。ローカルが評価するのは「直近 push 済みコミット」。

### コスト算出式（参考）
```
cost = Σ_model (
    input_tokens          × 単価_in
  + cache_creation_tokens × 単価_in × 1.25
  + cache_read_tokens     × 単価_in × 0.1
  + output_tokens         × 単価_out
) / 1e6        （当日分のみ）
```
| モデル | in($/1M) | out($/1M) |
|---|---|---|
| Opus 4.8 | 5.00 | 25.00 |
| Sonnet 4.6 | 3.00 | 15.00 |
| Haiku 4.5 | 1.00 | 5.00 |
