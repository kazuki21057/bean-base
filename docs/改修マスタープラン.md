# BeanBase 2.0 改修マスタープラン（進捗表示付き）

> 本書は今回の大規模改修の **全体設計と進捗の単一の真実**。毎日の改修タスクはここから選ぶ。
> 出典: [`docs/Beanbase改修案.md`](./Beanbase改修案.md) / 運用ルール: `CLAUDE.md`「日次改修ループ運用ルール」
> Phase 4 詳細の出典: `statistics_feature_design.md`(単一の正本) / `CLAUDE.md`「統計解析・予測機能の実装ルール」
> 最終更新: 2026-07-28

## 凡例
| 記号 | 意味 |
|---|---|
| ⬜ | 未着手 |
| 🟦 | 進行中 |
| ✅ | 完了 |
| ⏸ | 保留／後回し |

---

## 1. 改修の全体方針

| # | 柱 | 概要 | 優先 |
|---|---|---|---|
| A | データ基盤変更 | Firestore → **Google Sheets** に戻す（管理を楽に） | 1 |
| B | 画面構成・遷移の再編 | 抽出と評価を分離、14画面構成へ再設計 | 2 |
| C | UI全面刷新 | ダッシュボード/抽出/評価/スタッツを一から。黒板風・瓶ビジュアル等 | 3 |
| D | その他軽微な修正 | レスポンシブ確認・DRY化・仕上げ | 4 |
| E | AI活用拡大 | PCAの説明範囲拡大など（**全改修完了後**） | 5（最後） |

**データ基盤の決定（A）:** 個人利用・データ量小のため、以前の `SheetsService`（`lib/services/sheets_service.dart`）を再活用して Google Sheets に戻す。スプレッドシートで直接データ確認・手動編集できる点が「管理が楽」の要件に最適。Cycle 18 で入った `FirestoreService` 経由の読み書きを Sheets 経由へ差し戻す。画像保存先は別途方針決定（後述 T0-4）。

**モバイル利用の決定(D、2026-07-13):** ネイティブアプリ(Android/iOS)は**作らない**。Web 版をホスティングし、スマホ(Android)のブラウザからアクセスして**ホーム画面にピン留め(PWA として standalone 起動)**して使う。ホスティング先は既存 Firebase プロジェクト(`beanbase-app-2016`)の **Firebase Hosting** を第一候補とする(無料枠で十分・`firebase.json` が既にある)。デプロイは `flutter build web --release` → `firebase deploy` の手動運用から始める。注意: Gemini API キーは `shared_preferences`(ブラウザごとの localStorage)保存のため、スマホ側でも設定画面から再入力が必要。

**公開URL(T3-11で確定、2026-07-18):** https://beanbase-app-2016.web.app — 認証なしで誰でもアクセス可能。再デプロイは `flutter build web` → `firebase deploy --only hosting`。

---

## 2. 全体進捗サマリ（フェーズ別）

| フェーズ | 内容 | 対応Cycle | 状態 |
|---|---|---|---|
| Phase 0 | データ基盤を Sheets に戻す | Cycle 19 | ✅ |
| Phase 1 | 画面構成・ナビ再編（14画面の骨組み） | Cycle 20–22 | 🟦 |
| Phase 2 | UI全面刷新（黒板風・瓶・タイマー・評価・スタッツ） | Cycle 23–26 | ✅ |
| Phase 3 | 軽微な修正・仕上げ | Cycle 27 | 🟦 |
| Phase 4 | 統計解析・予測機能拡張（数値基盤/データ基盤/回帰/PCA拡張/好み/提案/GP） | Cycle 28+ | 🟦 |

> Cycle番号は目安。実際の粒度に応じて分割・増減してよい（番号は連番厳守、`docs/`と`docs/archive/`の最大値+1）。

---

## 3. フェーズ詳細＆タスク分解（日次タスクのベース）

### Phase 0 — データ基盤を Sheets に戻す（Cycle 19）
**終了条件:** Sheets 経由で一覧表示・登録・編集・削除が動き、`flutter run` で接続成功。

| ID | タスク | 状態 |
|---|---|---|
| T0-1 | `SheetsService` の現状調査・API/認証の再有効化方針を決める（抽象 `DataService` 採用） | ✅ |
| T0-2 | `data_providers.dart` の読み取りを Firestore→Sheets に切替 | ✅ |
| T0-3 | 各CRUD（records/beans/grinders/drippers/filters/methods/pouringSteps）の書込を Sheets に戻す | ✅ |
| T0-4 | 画像保存先の方針決定（**Google Drive 保存**に決定。実装は GAS拡張方式・別タスク T0-4b） | ✅ |
| T0-4b | Drive 画像保存の実装（GAS拡張方式） | ✅ |
| T0-5 | `analyze`/`test`/`run` で動作・接続確認、`[Antigravity]`ログ整備 | ✅ |

### Phase 1 — 画面構成・ナビ再編（Cycle 20〜22）

**Phase 終了条件:** 22画面すべてにルーティングが通り、抽出(030)と評価(031)が別画面として遷移する。

> **2026-07-10: Phase 1 終了条件を満たした(T1-7完了)。** T1-1a〜T1-7が全て✅になり、本番ナビ(NavigationRail/Bar)から Masters ハブ経由で豆/ドリッパー/フィルター/メソッド/グラインダーの全実装画面に到達できる。次はPhase 2(Cycle 23〜)。

サイズ: S=半日余裕、M=1ループ標準、L=1ループぎりぎり(着手前に分割を検討)

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T1-1a | 画面ID enum(001〜090)とルート定数の定義、`NavigatorKey`/`navIndexProvider` の再編方針決定 | enum とルートテーブルが定義され `analyze` パス | — | S | ✅ |
| T1-1b | 全22画面のプレースホルダ画面生成(画面IDと名称を表示するだけの Scaffold) | `run` で全画面にコードから遷移可能 | T1-1a | M | ✅ |
| T1-1c | `MainLayout`(NavigationRail/Bar)を新ナビ構成に再マップ | ナビ操作で主要タブ(001/002/010/040 等)へ遷移できる | T1-1b | M | ✅ |
| T1-2a | 抽出030の画面骨組み(既存記録画面から抽出パートを分離) | 030 が単独画面として表示・遷移する | T1-1c | M | ✅ |
| T1-2b | 評価031の画面骨組みと 030→031 のデータ受け渡し | 030 の「評価」ボタンで抽出情報を引き継いで 031 へ遷移 | T1-2a | M | ✅ |
| T1-3 | ダッシュボード001の骨組み(残豆量・直近5件のプレースホルダ+各遷移) | 001 から 010/002/003/011 へ遷移できる | T1-1c | M | ✅ |
| T1-4a | 抽出履歴リスト002(日時/豆/メソッド/点数の実データ表示) | Sheets の実データがリスト表示される | T1-1c | M | ✅ |
| T1-4b | 抽出履歴詳細003(全情報表示・編集) | 002 の行タップで 003 に遷移し編集保存できる | T1-4a | M | ✅ |
| T1-4c | 002 のスワイプ→評価継承で 031 へ遷移 | スワイプで評価値を引き継いだ 031 が開く | T1-4a, T1-2b | S | ✅ |
| T1-5a | **汎用マスター画面テンプレート**(リスト型: 画像左・名前右/詳細: 全情報+関連履歴5件/新規フォーム)の共通ウィジェット化 | ドリッパー013/014/015 がテンプレートで動作 | T1-1c | L | ✅ |
| T1-5b | テンプレートをフィルター016/017/018 へ適用 | フィルター3画面が実データで動作 | T1-5a | S | ✅ |
| T1-5c | テンプレートをグラインダー022/023/024 へ適用 | グラインダー3画面が実データで動作 | T1-5a | S | ✅ |
| T1-5d | テンプレートをメソッド019/020/021 へ適用(名前/発案者小書き/抽出回数のリスト仕様差分を吸収) | メソッド3画面が実データで動作 | T1-5a | M | ✅ |
| T1-6a | 豆管理カード一覧010(焙煎所/豆名/煎り度/画像/残量、0%表示チェックボックス、＋ボタン) | 010 がカード形式で実データ表示 | T1-1c | M | ✅ |
| T1-6b | 豆詳細011・新規豆012(テンプレート応用) | 011 で編集保存、012 で新規登録ができる | T1-6a, T1-5a | M | ✅ |
| T1-7 | 本番ナビ切替: `lib/layout/main_layout.dart` の「Masters」タブを新画面群(010/013/016/019/022)へ接続 | 左レール/ボトムバーの「Masters」から新実装のマスター画面(旧`MasterListScreen`ではない)に到達できる | T1-5a〜d, T1-6a〜b | M | ✅ |

> **2026-07-10 発見(T1-6a検証時):** `main_layout.dart` の `_screenFor(AppScreen.beanList)` は今も旧実装 `MasterListScreen`(Beans/Methods/Grinders/Drippers/Filtersのタブ切替UI、`master_list_screen.dart`/`master_detail_screen.dart`/`master_add_screen.dart`)を返しており、T1-5a〜d・T1-6a〜bで作った新画面群は090設定→画面一覧ギャラリーからしか到達できない。旧実装には新テンプレートに未移植の機能(画像一括インポート `_handleImageImport`)があるため、単純差し替えではなく機能移行の検討が必要。Phase 1終了条件「22画面すべてにルーティングが通り」を満たすにはこのT1-7が必要。詳細は`NEXT_SESSION.md`参照。

> **2026-07-05 補足(ユーザー依頼による先行UIモック):** **全22画面**のUIモックを作成済み(見た目のみ・データ未接続・保存はダミー)。作成系6画面(012/015/018/021/024/031)は `lib/screens/create/`、既存系16画面(001/002/003/010/011/013/014/016/017/019/020/022/023/030/040/090)は `lib/screens/mock/` 配下。`lib/routing/screen_registry.dart` が画面ID→Widgetの解決テーブルで、ギャラリー(090旧設定→画面一覧)から全画面に遷移可能。モック間遷移も一部実装済み(010→011/012、013系リスト→詳細/新規、030→031、040→090)。T1-2a以降の本実装(実データ接続)はこのモックを土台にできるが、各タスクの状態(⬜)はデータ接続が完了するまで変更しない。

> 旧 T1-1〜T1-7 との対応: T1-1→T1-1a/b/c、T1-2→T1-2a/b、T1-4→T1-4a/b/c、T1-5→T1-6a/b、T1-6→T1-5a/b、T1-7→T1-5c/d。

### Phase 2 — UI全面刷新（Cycle 23〜26）

**Phase 終了条件:** 黒板風ダッシュボード・瓶ビジュアル・タイマー付き抽出・評価・スタッツが見た目どおり動作。

> **2026-07-11: Phase 2 終了条件を満たした(T2-7完了)。** T2-1a〜T2-7が全て✅になり、黒板風ダッシュボード(001)・瓶ビジュアル・タイマー付き抽出レシピ(030、実際のメソッド保存対応)・評価画面(031、実際のrecords登録・連続記録対応)・スタッツ(040)・設定(090、メインカラー/APIキー/データ保存先)が実データで動作する。次はPhase 3(Cycle 27〜、軽微な修正・仕上げ)。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T2-1a | 黒板風テーマ(配色・フォント・背景テクスチャ)を ThemeData/共通ウィジェットとして定義 | 001 に黒板風背景が適用されて表示される | T1-3 | M | ✅ |
| T2-1b | ダッシュボード001の本実装(黒板風レイアウトに残豆量・直近5件を配置) | デザインどおりの 001 が実データで動作 | T2-1a, T2-2b | M | ✅ |
| T2-2a | 瓶ビジュアル・ウィジェット(静的: 10%刻み11段階の描画) | 任意の残量%を渡すと対応する瓶が描画される(単体テスト付き) | — | M | ✅ |
| T2-2b | 残豆量の計算ロジック(抽出履歴から豆ごとの残量算出)と瓶への接続 | 実データで豆ごとの瓶が正しい残量で表示 | T2-2a | M | ✅ |
| T2-2c | 空瓶の非表示+チェックボックスでの表示切替(001/010 両方) | チェックボックスで 0% 豆の表示が切り替わる | T2-2b | S | ✅ |
| T2-3a | 抽出レシピ030: 豆名・豆量・メソッド選択フォーム | 選択に応じて Pouring Steps が読み込まれる | T1-2a | M | ✅ |
| T2-3b | Pouring Steps の表示とタイマー実装 | タイマー開始で経過時間が表示される | T2-3a | M | ✅ |
| T2-3c | タイマー連動の Steps ハイライト | 経過時間に応じて該当ステップが強調表示される | T2-3b | S | ✅ |
| T2-4a | 030 からの Pouring Steps 編集(既存メソッド更新) | 編集内容が Sheets に保存され再読込で反映 | T2-3b | M | ✅ |
| T2-4b | 編集内容を新規メソッドとして保存(021 へ継承遷移) | 「新規として保存」で 021 に編集内容が引き継がれる | T2-4a | S | ✅ |
| T2-5a | 評価画面031の本実装(抽出情報+評価入力、新規登録対応) | 031 から登録すると records に保存される | T1-2b | M | ✅ |
| T2-5b | 評価時の豆残量自動計算と登録後の 031 復帰フロー | 登録で使用豆量が残量に反映され 031 に戻る | T2-5a, T2-2b | M | ✅ |
| T2-6 | スタッツ040の刷新 | 新デザインで PCA/KPI が表示される | T2-1a | L | ✅ |
| T2-7 | 設定090(メインカラー・APIキー・データ保存先情報) | 各設定が保存・反映される | T2-1a | M | ✅ |

### Phase 3 — 軽微な修正・仕上げ（Cycle 27）

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-1 | モバイル実機でのレイアウト確認・調整(**ユーザー実施の確認結果を受けて修正**) | ユーザー確認で NG 項目ゼロ | T3-12 | M | ⬜ |
| T3-2 | 画像アップロード部品の共通化(DRY) | 全マスターの画像アップロードが共通部品経由 | T1-5a〜d | M | ✅ |
| T3-3 | メソッド詳細020の動画再生(可能なら) | 動画URLが再生される or 見送り判断を記録 | T1-5d | M | ✅ |
| T3-4 | 全体のUIおしゃれ化・微調整 | ユーザーレビューで完了判断 | T3-1 | M | ⬜ |
| T3-5 | 030(抽出レシピ)の入力項目整理 — メソッド・豆量以外(豆選択/グラインダー/ドリッパー/フィルター)の入力欄を削除し、031(評価画面)側に移動 | 030の入力欄がメソッド・豆量のみになり、移動した項目が031で入力でき CoffeeRecord に正しく保存される | T2-5b | M | ✅ |
| T3-6 | 数値表示の小数点桁数統一(021含む全画面) | 小数点を表示する全項目が小数点以下1桁で統一表示される | なし | S | ✅ |
| T3-7 | 設定(090)への導線を共通化 — 新規登録画面・詳細画面以外のどの画面からでも設定ボタンを押せるようにする | 新規登録・詳細画面以外の全画面に設定への導線があり遷移できる | なし | S | ✅ |
| T3-8 | 本番ナビの Masters／Logs タブ順序を入れ替え | ナビの並びが Home→Logs→Masters→Calc→Stats になる | なし | S | ✅ |
| T3-9 | メインカラーの反映範囲拡大 — T2-7で Material標準UIのみに限定した反映範囲を見直し、黒板風テーマ等も含め全体に反映されるようにする | メインカラー変更が主要画面(黒板風含む)に反映される、または技術的制約で対応不可な範囲を明記しユーザーの判断を仰ぐ | T2-7, T2-1a | L | ✅ |
| T3-10 | PWAマニフェスト・アイコン整備 — `web/manifest.json` の name/short_name を「BeanBase」に、theme_color/background_color をアプリのテーマ色に変更。アイコン(192/512/maskable)を BeanBase 用画像に差し替え。`web/index.html` の title/description も更新 | manifest がアプリ実態を反映し、`flutter build web` 後にローカルで standalone 表示・アイコンが確認できる | なし | S | ✅ |
| T3-11 | Firebase Hosting 環境構築 — `firebase.json` に hosting 設定(public: `build/web`)を追加、`firebase-tools` でログイン(**ユーザー操作**)・`firebase deploy --only hosting` で初回デプロイ。GAS/Drive への疎通をデプロイ先URLで確認 | デプロイ先URLでアプリが表示され、Sheets のデータ読み書きができる | T3-10 | M | ✅ |
| T3-12 | スマホからのアクセス・ホーム画面ピン留め確認(**ユーザー実施**) — Android実機のChromeでデプロイURLを開き「ホーム画面に追加」、standalone起動・アイコン表示を確認。Gemini APIキーをスマホ側の設定画面で再入力 | ホーム画面のアイコンからアプリ風に起動し、データ表示とAI分析が動作する | T3-11 | S | ✅ |
| T3-13 | デプロイ手順のドキュメント化 — build→deploy の手順とURLを `rules/` または `docs/` に記録し、更新反映の運用(手動deploy)を明文化 | 手順書を見て再デプロイが再現できる | T3-11 | S | ✅ |
| T3-14 | 抽出履歴(002)の一覧行、左側のアイコンを豆の画像に変更 | 002の各行左側に、該当する豆の画像(未設定ならプレースホルダ)が表示される | なし | S | ✅ |
| T3-15 | 030→031遷移で、メソッド未選択でも評価画面(031)へ進めるようにする | 030でメソッドを選ばずに031へ遷移でき、エラーにならない | T3-5 | S | ✅ |
| T3-16 | 031(評価画面)で豆/グラインダー/ドリッパー/フィルター選択リストの各項目左側に画像を表示 | 031の各選択リストの項目に、該当マスターの画像(未設定ならプレースホルダ)が表示される | T3-5 | M | ✅ |
| T3-17 | 031で030から引き継いだ項目(豆/メソッド/器具/湯量等)も編集可能にする。湯温は030から引き継がず、031側で最初から入力する運用に変更 | 031画面上で引き継いだ全項目を編集でき、CoffeeRecordに正しく保存される。湯温は031での新規入力値が保存される | T3-5 | M | ✅ |
| T3-18 | 031の「味わい」入力欄を4:6メソッド選択時のみ表示・適用する | 4:6メソッド以外を選んだ場合は味わい欄が表示されず、CoffeeRecordにも保存されない | T3-5, T3-17 | S | ✅ |
| T3-19 | 豆管理(010)・グラインダー/ドリッパー/フィルター等の各マスター管理画面間を相互に行き来できるようにする | いずれのマスター一覧/詳細画面からも、他マスターの一覧へ遷移できる導線がある | なし | M | ✅ |
| T3-20 | Ubuntu環境の並行セットアップ(**ユーザー実施**) — Flutter/Dart SDK・Chrome(またはChromium)・Node.js(hooks用)・`gh` CLI導入、GitHub用SSH鍵の新規発行＆登録(リモートがSSH URLのため)、`flutter pub get`→`flutter analyze`/`flutter test`→`flutter run -d chrome`で疎通確認、Gemini APIキーを設定画面(090)で再入力(ブラウザのshared_preferencesはマシンごとに独立するため) | Ubuntu上で`flutter run -d chrome`が起動し、Sheets/Driveへの疎通とAI分析が確認できる | なし | S | ⬜ |
| T3-21 | 001(ダッシュボード)「直近の抽出」セクション内に日本語でない表記がある箇所を特定し日本語表記に修正する(着手時に画面を確認して対象箇所を特定すること。現状のコード上では明確な該当箇所が見当たらず、ウェルカムボードの `"Today's BeanBase ☕"` である可能性もある) | 該当箇所が日本語表記になる | なし | S | ✅ |
| T3-22 | 001(ダッシュボード)「直近の抽出」一覧、各行左側のアイコンを豆の画像に変更(T3-14で002に行った対応を001にも適用) | 001の直近の抽出一覧の各行に該当する豆の画像(未設定ならプレースホルダ)が表示される | なし | S | ✅ |
| T3-23 | 001(ダッシュボード)の残豆量表示確認用に、残量50%相当となるダミーの抽出データ(または在庫データ)をSheetsに登録する(**本番Sheetsへの書き込みを伴うため、実施前にユーザーに確認**) | 001の残豆量セクションに残量50%の豆が表示される | なし | S | ✅ |
| T3-24 | 020(メソッド詳細)でのYouTube動画再生対応を再検討する(T3-3で一度「見送り」判断済み。追加パッケージ導入の是非も含め再度ユーザーと相談のうえ判断すること) | 再生を実装する、または改めて見送り理由を提示しユーザーの合意を得る | なし | S | ✅ |
| T3-25 | Mastersの各詳細画面(011/014/017/020/023、共通`MasterDetailTemplate`経由)の「関連する抽出履歴」一覧、各行左側のアイコンを豆の画像に変更(T3-14と同様の対応。`lib/screens/master_template.dart`の共通実装1箇所の修正で全マスター詳細画面に反映される見込み) | 全マスター詳細画面の関連抽出履歴一覧に豆画像が表示される | なし | S | ✅ |
| T3-26 | 003(抽出履歴詳細)の評価表示のデザイン改善。現状のデザインが良くないとの指摘あり。設計書`statistics_feature_design.md`§12①の運用方針(画面デザインの新規検討は上位モデルで実施)に従い、まず上位モデル(Opus等)でデザイン案を検討してからユーザー確認のうえ実装する | デザイン案が提示されユーザー承認を得たうえで実装が完了する | なし | M | ✅ |
| T3-27 | 統計処理の理論説明ページを新設する。回帰(F1)・PCA(F2)・GP/EI(F4)・好み検定(F5)・提案(F3)など統計処理を行っている各機能の理論的な背景を日本語で説明する専用ページ(画面)を用意し、各統計機能付近(040の各セクション・030のレシピ探索・003の評価表示など)から該当説明へ飛べる導線(情報アイコン/リンク)を設ける。数式・説明の正確さが要るため設計書§12①に従い内容は上位モデル(Opus等)で検討する。正本の統計仕様は`statistics_feature_design.md`。 | 理論説明ページが表示され、各統計機能付近のリンクから該当説明箇所へ遷移できる。説明内容が実装の統計処理と整合している | Phase4完了(済) | L | ✅ |
| T3-28 | 日本語ではない漢字表記の全画面点検・修正。アプリ全体で日本語ではない漢字(中国語字形/簡体字等)や不自然な表記が使われている箇所を洗い出し正しい日本語表記に修正する。T3-21が001「直近の抽出」限定だったのを全画面へ拡大。**着手時に原因の切り分けが必要**: (a)ソース中の実際に誤った文字列なのか、(b)Flutter WebのCJKフォント/ロケール未指定により漢字が中国語字形で描画される既知問題(日本語フォント指定・`supportedLocales`/`locale`のja設定・`fontFamily`の見直しで対処)なのかを先に特定する。 | 主要画面を実データで確認し、非日本語表記(誤字または中国語字形)が日本語表記で表示される | なし | M | ✅ |
| T3-29 | 評価記録時の注意点をダイアログ表示する。評価記録画面(031)等で、情報アイコン等をクリックすると評価記録時の注意点がダイアログとして表示されるようにする。**専用ページは用意しない**。既存の回帰セクション「分析上の注意」ダイアログ(`regression_section.dart`、§2.1.5)と同じパターンを流用する。**表示する注意点の具体的な文面はユーザーに要確認**。 | 031(または該当箇所)の注意アイコンをタップすると注意点ダイアログが表示される | なし | S | ✅ |
| T3-31 | 020のYouTube埋め込み再生(T3-24)がモバイル実機で正しく表示されない不具合の修正(ユーザーが実機で確認・報告) | モバイル実機で埋め込みプレーヤーの再生領域が表示される | なし | S | ✅ |
| T3-32 | 本番環境の各画面AppBarに表示されている画面コードバッジ(例:「001」「012」)を削除する。開発時の画面特定用途で本番UIとしては不要なため | 各画面のAppBarから画面コードバッジが消え、画面名のみ表示される | なし | S | ✅ |
| T3-33 | 設定(090)から統計解説ページ(041「統計の理論と読み方」)へ直接遷移できるボタンを追加する | 090画面のボタンから041へ遷移できる | 041(T3-27)完了済み | S | ✅ |
| T3-30 | 豆の説明カード等の画像から豆情報を抽出する機能を追加する。豆の説明カード/パッケージ等の画像を選択すると、Gemini Vision(マルチモーダル)で豆名・焙煎所・産地・焙煎度・焙煎日等を抽出し、新規豆追加(012)フォームにプリフィルする。抽出はDartローカルではなくGeminiに委ねてよい(数値統計計算の絶対規則の対象外。テキスト抽出のため)。**抽出対象項目とフォーム項目のマッピング・抽出失敗時の扱い(手動修正前提)を設計する必要がある**。APIキーは既存同様`shared_preferences`(`gemini_api_key`)を利用。 | 012等で画像を選択するとGeminiが豆情報を抽出しフォーム各欄に反映され、ユーザーが確認・修正して登録できる | なし | L | ✅ |
| T3-34 | 豆マスターの画像を「パッケージ画像」「豆画像」「情報画像(説明書き等)」の3種類に分離する。`BeanMaster`に画像フィールドを3種追加、GAS `bean_master`シートに列追加、既存の単一`豆画像URL`は移行によりパッケージ画像として扱う(データ変換 or 読み込み時フォールバック)。012(新規)・011(詳細)のUIを3枚のアップロード欄に変更 | 012で3種類を個別アップロードでき、011で3種類とも表示され、既存データの旧`豆画像URL`がパッケージ画像として表示される | なし | L | ✅ |
| T3-35 | 豆情報読取AI(T3-30)にカメラ撮影での画像取得を追加する。既存の「ファイルから選択」に加え「カメラで撮影」を選べるようにし、撮影した画像はAI抽出に使うと同時に**情報画像(T3-34)として保存し豆に紐付ける** | 012でカメラ撮影→Gemini抽出→フォーム反映が動作し、撮影画像が情報画像として保存される | T3-34 | M | ✅ |
| T3-36 | 統計処理(F1回帰/F2 PCA/F4 GP/F5好み等)の稼働on/off状況を一覧表示する新規ページを作成し、設定(090)から遷移できるようにする。各項目から該当する説明(041の該当セクション)へ遷移できる。offの場合は有効化に必要な条件(設計書§1.3の最小データ条件)を表示する。稼働有無は信号機のように色分け表示する | 新規ページで各機能のon/off状況・条件・041への導線が確認でき、090から遷移できる | 041(T3-27)完了済み | M | ✅ |
| T3-37 | 020のYouTube埋め込み再生がT3-31の修正後も機能していないとの報告があり、再調査・修正する。症状の詳細(環境・見え方)をユーザーに確認した上で原因を特定する | 埋め込みプレーヤーの再生領域が正しく表示され、ユーザーが再生を確認できる | なし | S〜M | ✅ |
| T3-38 | `original-data/`配下のCSVにユーザーが投入した最新データを本番Sheetsへ移植する。`original-data/old/`(2026-06-28時点の旧版)との比較で、新規追加分は coffee_data(抽出記録)27件・豆マスター4件(ドリッパー/フィルター/ミル/メソッド/pouring_stepsは旧版と同一で対象外)。**移植時は本番Sheetsの既存データと突き合わせ、二重登録が発生しないように確認・対応すること**(IDや内容一致での重複判定方法は着手時に検討) | original-data内の新規データ(coffee_data新規抽出記録・豆マスター新規4件)が本番Sheetsに反映され、既存データとの重複登録がないことが確認できる | なし | M | ✅ |
| T3-39 | Gemini APIのモデルを設定(090)から選べるようにする。現状は`_kGeminiModels`(2.5-flash→2.0-flash-lite→1.5-flash)のコード内固定フォールバック順のみで、ユーザーがモデルを選択・変更する手段が無い。モデル一覧はWeb検索で取得できるようにし(`https://ai.google.dev/gemini-api/docs/pricing?hl=ja`を参照してモデル名を最新化)、専用ページを新設のうえ090のAPIキー入力欄下あたりにモデル設定ページへの遷移ボタンを追加する。**優先度は低め(ユーザー指定)** | 090からモデル設定ページへ遷移でき、Web検索で取得したモデル一覧から選択・保存でき、選択したモデルがGemini呼び出しに反映される | なし | M | ✅ |

#### Phase 3 追加分（2026-07-26 ユーザー報告の不具合・要望、T3-40〜T3-53）

着手順の目安: **①不具合(T3-44→T3-45→T3-40) → ②焙煎度8段階化(T3-42→T3-43→T3-51) → ③画像UI統一(T3-41) → ④おすすめレシピ改修(T3-47→T3-48→T3-49) → ⑤最適条件探索(T3-50→T3-52→T3-53) → ⑥データ整理(T3-46)**。

**依存関係(必ず守る順序)**:
- `T3-42`(焙煎度8段階) → `T3-43`(AI抽出に反映) / `T3-51`(説明ページ) / `T3-47`(メソッドの推奨焙煎度)
- `T3-47`(メソッドに推奨焙煎度) → `T3-48`(おすすめにメソッド追加) → `T3-49`(遷移先を030へ)
- `T3-50`(探索フラグ) → `T3-52`(F4の次元拡張) → `T3-53`(検証状況の可視化)
- `T3-40`(カメラ起動) → `T3-41`(全画像欄へ展開)

**すぐ着手できる(依存なし)**: T3-46(S、削除承認済み。**T3-56誤検知判明によりupdateRow/deleteRowが正常動作すると確認済みのため、これまでの懸念なく実行可能**)・T3-50(M)・T3-43(L)・T3-51(M)・T3-47(M)。**T3-45完了(2026-07-27)によりタスク表の①不具合グループは全件完了。T3-42完了(2026-07-26)によりT3-43(AI抽出反映)・T3-51(説明ページ)・T3-47(メソッドの推奨焙煎度)が着手可能。T3-56は2026-07-27の再調査で誤検知(調査ツール側のシェル文字化けが原因)と判明し解決済み。T3-57(Youth豆画像、2026-07-27追加)は写真素材がユーザー提供待ちのため実質着手不可(ユーザー入力待ち)。**

**モデル選定(2026-07-26確定、2026-07-29 T3-54完了により更新)**: T3-52・T3-53は上位モデルで実施(下記参照)。**T3-54は2026-07-29に上位モデル(Opus 5)で設計完了し、実装用の T3-54a / T3-54b に分解済み(設計書=`docs/roast_slider_design.md`)。T3-54a は依存なしでSonnet 5が即着手できる。** それ以外(T3-40〜T3-51、T3-54a、T3-54b)はSonnet 5で`/loop`による自動実施の対象。

**`/clear`問題は解決済み(2026-07-27)**: 下記「`/loop`定期実行の`/clear`問題」への対応として、`/clear`を呼ぶ前提を諦め、`loop_guard.js`のコスト・ターン数・連続失敗判定を「当日累計」から「直近の`/start`・`/full_loop`呼び出し以降の1ループ単位」に変更する対応で決着した(2026-07-25のユーザー指示で導入済み、`CLAUDE.md`§日次改修ループ運用ルール参照)。`full_loop`スキルからも実行不可能な`/clear`手順を削除済み(2026-07-27)。**`/schedule`への切り替えは見送り、セッション内蔵`/loop`を継続する運用で確定。以降のセッションはこの節の確認・ユーザー回答待ちを不要とする。**

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-40 | **不具合**: 豆情報読取AI(012)で「カメラで撮影」を選んでもカメラが起動しない(ユーザー実機=Android確認)。**根本原因を特定・修正、ユーザーの実機再確認で解消を確認済み(下記2026-07-26参照)** | 実機(本番URL)で「カメラで撮影」からカメラが起動し、撮影画像がAI抽出と情報画像保存に渡る | T3-35 | M | ✅ |
| T3-41 | 画像を登録するすべての場所で「ファイルから選択」/「カメラで撮影」を選べるようにする。現状カメラ選択はT3-35で012の豆情報読取AIボタンにのみ実装されており、共通ウィジェット`ImageUploadField`(豆パッケージ/豆/情報画像、ドリッパー/フィルター/グラインダー/メソッドの各画像欄)は`FilePicker`固定。**T3-35で`bean_create_screen.dart`にローカル実装した選択ダイアログを共通部品化し`ImageUploadField`へ移設する**(CLAUDE.md「全マスタータブへの一律適用」規約に沿う) | 全マスターの画像アップロード欄でファイル/カメラを選択でき、どちらでも画像がDriveへ保存される | T3-40 | M | ✅ |
| T3-42 | 焙煎度を**8段階**(浅い順に ライト/シナモン/ミディアム/ハイ/シティ/フルシティ/フレンチ/イタリアン。**2026-07-26ユーザー確認済み: 業界標準の8段階を採用、9段階は不要**)に統一し、日本語・英語(アルファベット表記、例`Light`/`Cinnamon`…。ユーザー了承済み)どちらでも読み取れるようにする。現状は`roastOrdinalMap`(`lib/services/math/encoding.dart`)が5段階(浅煎り/中浅煎り/中煎り/中深煎り/深煎り。ライト〜イタリアンは別名として5段階に集約されている)、012のUIは4段階(`_roastOptions`)とバラバラ。**影響範囲(重要)**: `roastOrdinalMap`の順序値は統計処理(F1回帰・F4 GP・F5好み検定)が直接参照するため、1.0〜5.0 → 1.0〜8.0への変更は既存の分析結果の意味を変える。**既存の本番データ(bean_master/coffee_dataの焙煎度文字列は「中煎り」「中深煎り」等の旧5段階表記)が欠測扱いにならないよう、旧表記→新8段階へのマッピングを必ず用意すること**(例: 中煎り→ハイ、中深煎り→シティ 等。マッピング方針は着手時にユーザーへ確認) | 焙煎度が8段階で統一され、日本語・英語どちらの表記でも同じ段階として解釈され、既存データが欠測にならず統計処理も従来どおり動作する | なし | L | ✅ |
| T3-51 | 焙煎度8段階それぞれの説明ページを新設する。各段階(ライト/シナモン/ミディアム/ハイ/シティ/フルシティ/フレンチ/イタリアン)について、日本語名・英語表記・味わいの特徴(酸味/苦味/コクのバランス)・見た目の色味・適した抽出方法などを解説する。既存の041(統計の理論と読み方)と同じ構成パターン(`MockScreenScaffold`+`FormSection`+目次チップ)を踏襲すると実装が早い。**画面IDは`044`を割り当てる**(2026-07-29 T3-54で確定。`lib/routing/app_screen.dart`の使用済みは040=統計/041=統計の理論/042=統計処理の稼働状況/043=Geminiモデル設定まで)。**導線**: 012/011の焙煎度選択欄の近くに情報アイコン(`StatsTheoryLink`と同型: `Icons.menu_book_outlined`, `size:20`, `color:kAccent`, `visualDensity: VisualDensity.compact`)を置いて遷移できるようにする。**T3-54a完了後は`RoastLevelSlider`に`trailing`スロットが用意されているので、012側の呼び出しを`RoastLevelSlider(trailing: const RoastGuideLink(), ...)`にするだけでよい(`RoastLevelSlider`自体の改修は不要)。詳細は`docs/roast_slider_design.md`§6。**内容はWeb検索で調査すること(参考: `https://crowdroaster.com/ja/story/79`、`https://hollys-corp.jp/about_coffee/20191115950/`) | 焙煎度説明ページが表示され、8段階すべての特徴が確認でき、012/011の焙煎度欄から遷移できる | T3-42 | M | ⬜ |
| T3-43 | 豆情報のAI自動入力(T3-30)で焙煎度も確実に入力されるようにする。`extractBeanInfoFromImage`の`responseSchema`は既に`roastLevel`を`Schema.enumString(enumValues: ['浅煎り','中煎り','中深煎り','深煎り'])`で定義済みだが、ユーザー報告では反映されない。**enumValuesが4段階固定でパッケージの実際の表記(英語表記・9段階表記)と一致せずGeminiがnullを返している可能性が高い**ため、T3-42の段階定義に合わせてenumValuesとプロンプトを更新する | パッケージ画像に焙煎度の記載(日本語/英語いずれか)があれば、012のフォームの焙煎度が自動選択される | T3-42 | S | ⬜ |
| T3-44 | **不具合**: 豆情報を登録する際、画像アップロード後に「Image uploaded!」が表示されると登録ボタンが押せなくなる。**原因の第一候補は`SnackBar`が画面下部の保存ボタン(`CreateFormScaffold`のボタンは画面下部固定)に重なりタップを奪っていること**(T3-39の既存テスト修正時にも同種のレイアウト問題を確認済み)。SnackBarの表示位置・`behavior`の見直し、または保存ボタンとの重なり回避で対処する。あわせて「Image uploaded!」「Uploading image...」等の英語メッセージを日本語化する | 画像アップロード直後でも登録ボタンがタップでき、豆を登録できる。メッセージが日本語表示になる | なし | S | ✅ |
| T3-45 | **完了(2026-07-27)**: 豆を追加して豆管理画面(010)に戻った際、追加した豆が一覧に反映されるまで時間がかかる不具合。**実測でGAS `getBeans`往復が単体で約2.5秒**かかると確認、`ref.invalidate(beanMasterProvider)`はRiverpodの仕様上invalidate直後`AsyncLoading`へ戻り一覧全体がスピナー表示になる(体感速度低下の直接原因)ことも確認した。**対応(①楽観的更新を採用)**: `beanMasterProvider`等5マスタープロバイダ(Bean/Method/Grinder/Dripper/Filter、CLAUDE.md「全マスタータブへの一律適用」規約により全種に展開)を`FutureProvider`から`AsyncNotifierProvider`(`OptimisticListNotifier<T>`基底クラス、`data_providers.dart`)へ移行。`addOptimistic`/`updateOptimistic`/`removeOptimistic`が`state`へ直接代入(`invalidateSelf`を使わずスピナーへ戻さない)した上でバックグラウンド再取得を行う。各マスターの登録/更新/削除画面(bean・grinder・dripper・filter・method の create/detail各画面、brew_recipe_screen.dartの030由来メソッド更新も含む)の`ref.invalidate(xxxMasterProvider)`をこれらの呼び出しに置換。**本番デプロイは2026-07-28に完了**(2026-07-27はハーネスの安全分類器にブロックされ未実施だったが、別セッションでの再実行で成功。`docs/deploy.md`の教訓欄参照) | 豆登録後、一覧に新しい豆がほぼ即時に表示される(または反映中であることがユーザーに分かる) | なし | M | ✅ |
| T3-46 | **一部完了(2026-07-27)**: 本番Sheetsから動作確認用のテストデータを削除する。**T3-56誤検知判明を受け、アプリの実UI(ローカル配信+claude-in-chrome、本番GAS実データ)経由で削除を実行し、豆マスター「残量50%テスト豆(T3-23)」(豆ID=1784633291938)+`mill_master`の「Test Grinder」2件(ID=1771594642152/1771671286428)、計3件の削除に成功(コンソールで`{"status":"success","action":"delete",...}`を確認)**。**残タスク(4件、未着手)**: ①`bean_master`の「検証用テスト豆(T4-1e確認・削除予定)」(豆ID=1784590301174)②同「検証用テスト豆2(T4-1b修正確認・削除予定)」(豆ID=1784590715190)③`coffee_data`の孤児レコード「TEST-REC-NO-REDIRECT」④同「REC-1770290905531」。**さらに今回新たに判明**: 削除済みの「残量50%テスト豆」に紐づいていた抽出記録(2026/07/21付、詳細画面の「関連する抽出履歴」に表示されていたもの)が豆削除後に孤児化した可能性があるため、次回`coffee_data`確認時に併せて洗い出すこと。**残タスクが完了しなかった理由(環境固有の制約、アプリ不具合ではない)**: このセッションのclaude-in-chromeブラウザ自動化で、豆一覧(グリッド表示、31件)のスクロールがホイール・ドラッグ・キーボード・CDPマウスイベント・JS合成wheelイベントのいずれでも一切反応しない現象に遭遇し、対象2豆(グリッド後方に位置)を画面に表示できなかった(削除できた「残量50%テスト豆」とグラインダー2件はいずれもスクロール不要な短いリストだったため成功)。抽出履歴一覧(coffee_data、100件超)も同様の理由で未着手。次回は診断の起点としてまずスクロール操作自体を再確認すること。**2026-07-26 ユーザーより削除の承認済み。実行直前に削除対象の一覧(件数・ID・内容)を提示してから削除する運用は今回も遵守した** | テストデータが本番Sheetsから削除され、ダッシュボード・豆管理・統計に表示されなくなる。削除対象と実行結果がユーザーに報告される | なし(削除承認は取得済み) | S | 🟡(3/7件完了) |
| T3-47 | メソッドマスタ(`MethodMaster`)に「推奨焙煎度」項目を追加する。モデルへのフィールド追加・GAS `methods_master`シートへの列プロビジョニング(`EXISTING_SHEET_EXTRA_COLUMNS`)・`SheetsService`のkeyMap/reverseMap両方の更新・021(新規メソッド)/020(メソッド詳細)のUI対応が必要。**過去に頻発した「モデル追加時の列プロビジョニング漏れ」バグ(T4-1b/T4-2d/T3-23と同型)に注意**、`rules/verification.md`のチェックリストに従うこと。焙煎度の選択肢はT3-42で確定する段階定義に合わせる | 021で推奨焙煎度を設定でき、020で表示され、本番Sheetsに保存される | T3-42 | M | ⬜ |
| T3-48 | おすすめレシピ(F3、001ダッシュボードの`recipe_suggestion_card.dart`)の提案内容に**メソッドを追加**する。提案するメソッドは**T3-47で追加した推奨焙煎度が、対象豆の焙煎度と一致するもの**から選ぶ。あわせて**湯温はメソッドによって決まるため、湯温に関する提案ロジックをすべてメソッド依存に修正する**(現状F3/F4は湯温を独立した連続変数として扱っている。`suggestion_service.dart`・`gp_service.dart`・設計書§7.4/§7.5の該当箇所を確認し、メソッドの基準湯温を優先する形へ改める)。**統計処理の変更を伴うため`statistics_feature_design.md`との整合を確認し、設計書の記述と食い違う場合はユーザーに相談すること**(CLAUDE.md絶対規則: 設計書に無い仕様を発明しない) | おすすめレシピが「豆+メソッド+抽出条件」の組で提案され、提案される湯温がメソッドの基準湯温と整合する | T3-47 | L | ⬜ |
| T3-49 | おすすめレシピカードから遷移する先を**抽出画面(030 calc)**に変更する。現状の遷移先を030へ変更したうえで、**030の画面に無い項目(湯温・比率・抽出時間など、提案には含まれるがcalcページのUIには表示されない情報)を破棄せず保持**し、**保持されていることがユーザーに分かるように表示**する(例: 「おすすめレシピから引き継いだ条件」バッジ/カードで湯温等を表示)。引き継いだ値は031(評価画面)まで持ち越して記録に残せるようにする | おすすめレシピをタップすると030へ遷移し、引き継いだ湯温等が画面上で確認でき、そのまま抽出→評価まで進める | T3-48 | M | ⬜ |
| T3-50 | 豆マスタに「**最適条件を探索するか**」(旧称: A/Bテストを希望するか)の項目を追加する。`BeanMaster`へのフィールド追加+GAS列プロビジョニング+SheetsServiceのマッピング(T3-47と同じチェックリスト)、012/011のUI対応。さらに**この項目が未設定の豆があれば、ダッシュボード(001)で「この豆の最適な淹れ方を探しますか?」と尋ねるUI**を追加する。ONにした豆はT3-52(探索)・T3-53(進捗表示)の対象になる。**2026-07-26 ユーザーヒアリング結果(仕様確定)**: やりたいことは「同じ豆で最適なメソッド・湯温・粒度を知る」+「現在の検証状況が分かる」。**A/Bテスト(2案の対照実験)という形式は本アプリには適さないと判断** — 抽出は1回ごとに条件が変わる逐次実験であり、2群に分けて統計的有意差を見るより、既存のF4(ガウス過程回帰+期待改善量EIによるベイズ最適化、`gp_service.dart`)を「次に試すべき条件の提案」として使うほうが少ない試行回数で最適条件に到達できる。この方針をT3-52/T3-53として具体化した | 012/011で探索するか否かを設定でき本番Sheetsに保存される。未設定の豆があるとダッシュボードで質問が表示され、そこから設定できる | なし | M | ⬜ |
| T3-52 | **⚠️上位モデルで実施(下記参照、`full_loop`の自動選定対象外)** F4(GP+EIによるレシピ探索)の探索次元を拡張し、「同じ豆で最適なメソッド・湯温・粒度を知る」を実現する(T3-50のヒアリング結果に基づく)。現状`GpService`が扱う入力は**湯温・brew ratio・抽出時間の3次元**のみで(`gp_service.dart` の `xsRaw.add([r.temperature, ratio, r.totalTime])`)、ユーザーが知りたい**粒度・メソッドが対象外**。対応方針: ①**粒度**を4つ目の連続次元として追加する。**ただし粒度の目盛りはミルごとにスケールが異なる**(本番`mill_master`: Timemore c3 pro=20段階 / Kingrinder K6=180段階 / ドリップバッグ=0)ため、`GrinderMaster.grindRange`(挽き目調整段階)で正規化してから投入する必要がある。②**メソッドはカテゴリ変数のためGPの連続次元には入れられない**。メソッドごとに別々のGPをフィットし、各メソッドの最良予測スコア(と不確実性)を比較して「このメソッドが有望」と提示する方式にする。**設計書`statistics_feature_design.md`§2.3・§7.5の仕様変更を伴うため、実装前に設計書との整合を確認し、必要なら設計書側を改訂すること**(CLAUDE.md絶対規則: 設計書に無い仕様を発明しない)。最小データ条件(n_eff≥10)も次元追加により見直しが必要 | 030のレシピ探索で、豆を選ぶと「推奨メソッド・湯温・粒度」が根拠(予測スコアと不確実性)付きで提示される | T3-50, T3-47 | L | ⬜ |
| T3-53 | **⚠️上位モデルで実施(下記参照、`full_loop`の自動選定対象外)** 検証状況の可視化(T3-50のヒアリング結果「現在の検証状況がわかると嬉しい」に対応)。探索対象(T3-50でONにした豆)ごとに、**これまで何通りの条件を試したか・各条件の評価がどうだったか・探索空間のどこが未試行か・次に試すべき条件(EI最大点)は何か**を一覧/可視化する画面またはセクションを追加する。表示案: ①試行回数と評価スコアの推移(折れ線)②試した条件の分布(既存のGPヒートマップ上に実測点を重ねる)③「次に試すと良い条件」カード④最適条件に到達したと言える状態か(EIが十分小さくなったか)の判定。**配置場所は要検討**(030のレシピ探索セクション内に統合するか、専用ページを新設して011豆詳細から遷移するか) | 探索対象の豆について、試行回数・実測条件の分布・次の推奨条件・探索の進捗度合いが1画面で確認できる | T3-52 | L | ⬜ |
| T3-54 | **✅設計完了(2026-07-29、上位モデル(Opus 5)で実施。成果物=`docs/roast_slider_design.md`。実装は T3-54a / T3-54b に分解済み、コード変更なし)** — 以下は当初の指示内容の記録。**⚠️上位モデルで実施(`full_loop`の自動選定対象外、2026-07-26ユーザー指示)** 焙煎度の入力UIをスライダーに変更する。**2026-07-26ユーザー指摘**: 現状(012/011)は`ChoiceChip`等による8択の選択式UIだが、順序尺度(1.0〜8.0、T3-42で確定)であることを踏まえるとスライダーの方が直感的に分かりやすいのではという提案。**検討事項**: ①スライダーの目盛りラベル表示(8段階の名称をどう配置するか、日英どちらの表記も考慮)②既存のチップ選択UIとの見た目の一貫性(他マスタ項目とのデザイン統一)③041の焙煎度説明ページ(T3-51)への導線をスライダー上でどう確保するか④F1回帰予測フォーム・F4レシピ探索の焙煎度選択(現状ドロップダウン)も同様にスライダー化すべきかの判断。**画面デザインの新規検討を伴うため`statistics_feature_design.md`§12①のルールに基づき上位モデルで実施**(スライダー化の要否・具体的なUI設計自体をこのタスクの中で検討すること、本行はあくまで「検討して」という指示の記録) | 焙煎度の入力(012/011、対象を広げる場合は041/030も)がスライダーで行え、既存の8段階・日英表記・旧データ互換性(T3-42)を損なわない | T3-42 | M〜L | ✅(設計のみ完了。実装はT3-54a/T3-54b) |
| T3-54a | 焙煎度スライダーの**ウィジェット新規作成と012への適用**。**設計は`docs/roast_slider_design.md`で完全に確定済み(2026-07-29、T3-54)。同書§3(ウィジェット仕様)・§4(文字列↔順序値の変換規則)・§5.1(012の置換手順)・§7.1(テスト6件)をそのまま実装し、レイアウトやAPIを新規に検討しないこと。** 作業: ①`lib/services/math/encoding.dart`に`roastLevels8En`(英語8ラベル、`roastLevels8`とindex1対1)を追加(§4.1)。**`roastOrdinalMap`は絶対に変更しない**(F1回帰/F4 GP/F5好み検定が直接参照)。②`lib/widgets/roast_level_slider.dart`を新規作成。**必ず`StatelessWidget`(制御コンポーネント、内部状態を持たない)**にする — `MockChoiceChips`が`initialValue`を`initState`でしか読まないためAI自動入力が画面に反映されない既存バグ(T3-58と同型の地雷)を再発させないため。`min:1 max:8 divisions:7`、未設定は淡色サム+「クリア」ボタンで表現(0=未設定案は不採用)、トラックは焙煎色グラデーション(グラデーションContainerの上に`activeTrackColor/inactiveTrackColor: Colors.transparent`のSliderを`Stack`で重ねる方式、§3.3にコード骨子あり)。`compact`分岐もこのタスクで作り込む(T3-54bで使う)。③`lib/screens/create/bean_create_screen.dart`(012)の`MockChoiceChips`(煎り度)を`RoastLevelSlider(value: _roastLevel, onChanged: (v) => setState(() => _roastLevel = v))`に置換。**`setState`必須**(現状の`onChanged: (v) => _roastLevel = v`をそのまま写さない)。あわせて`_roastOptions`/`_roastChoices`/`_withCurrentValue`(**このファイル内のもののみ。dripper/filterの同名メソッドは使用中なので消さない**)/314行目の`_roastChoices`代入/未使用になる`encoding.dart`のimportを削除(§5.1の表のとおり)。④`test/roast_level_slider_test.dart`を新規作成(§7.1の6件。**`tester.drag`は不安定なので使わず`tester.widget<Slider>(...).onChanged!(5.0)`でコールバックを直接呼ぶ**)。**副次効果として、AI自動入力で抽出した焙煎度がチップに反映されない既存バグが直る**(walkthrough/NEXT_SESSIONに記載すること)。**011豆詳細は変更不要**(表示専用で編集は012の編集モードを使うため自動的にカバーされる、§5.2)。`dart format`をファイル全体にかけないこと(-4.69の教訓) | 012の煎り度がスライダーで入力でき、旧5段階表記(`中煎り`等)の既存豆を編集しても正しい段階(ハイ)が表示され、AI自動入力で抽出された焙煎度がスライダーに即座に反映される。`flutter analyze`新規issue 0・既存203件+新規6件パス | なし | M | ⬜ |
| T3-54b | 040(統計/回帰予測フォーム)と030(抽出/レシピ探索)の焙煎度入力を**コンパクト版スライダーに統一**する。**設計は`docs/roast_slider_design.md`§5.3・§3.5で確定済み。** 作業: ①`lib/widgets/statistics/regression_section.dart`の`_roastDropdown()`(565行目付近)を`RoastLevelSlider(value: _roastLabel, compact: true, onChanged: (v) => setState(() => _roastLabel = v ?? _roastLabel))`に置換。②`lib/widgets/brew/gp_explorer_section.dart`(123行目付近)の焙煎度`DropdownButtonFormField`を`RoastLevelSlider(value: _selectedRoast, compact: true, onChanged: (v) => setState(() => _selectedRoast = v ?? 'ハイ'))`に置換。同ファイル38〜48行目の`static const _roastOptions`はドロップダウン構築専用なら削除するが、**102行目の`roastOrdinalMap[_selectedRoast]`による順序値変換は残す**。③`test/roast_level_slider_test.dart`に`compact: true`の表示テスト2件(端ラベル「浅い/深い」が無い・クリアボタンが無い)を追加。④既存の`test/regression_section_test.dart`/`test/gp_explorer_section_test.dart`が通ることを確認(ドロップダウンを探すアサーションがあれば差し替え)。**両画面とも`Row`+`Expanded`の中にあるため、縦幅増加によるオーバーフロー(黄黒ストライプ)が出ていないかブラウザで必ず目視すること** | 040の回帰予測フォームと030のレシピ探索の焙煎度選択がスライダーになり、予測結果・ヒートマップが従来どおり更新され、レイアウト崩れが無い | T3-54a | S〜M | ⬜ |
| T3-55 | **不具合**(2026-07-27ユーザー指示、`/start`を経由しない単発依頼): 本番環境の表示文字のうち日本語化されていない箇所を修正する。本番ナビ5タブすべて("Home"/"Masters"/"Logs"/"Calc"/"Stats")、マスターハブ画面AppBar、設定(090)のDebugセクション見出し・Firebase動作確認項目、`image_upload_field.dart`(全マスター共通の画像アップロード部品)、`method_steps_editor.dart`(注湯ステップ表)、`log_edit_screen.dart`(抽出記録編集画面、画面全体が完全に未翻訳だった)が対象。詳細はNEXT_SESSION.md「-4.67」節参照 | 該当箇所すべてが日本語表記になる。`flutter analyze`/`test`パス、ブラウザ確認でナビ・設定・注湯ステップ表・記録編集画面が日本語表示であることを確認する | なし | M | ✅(commit/push済み、`firebase deploy --only hosting`で https://beanbase-app-2016.web.app へ反映済み) |
| T3-56 | **誤検知と判明・解決(2026-07-27再調査)**: 「本番GASの`deleteRow`/`updateRow`が正しいIDでも失敗する」という2026-07-27発見の報告は**実際にはGASコードのバグではなく、調査時のcurlコマンドがWindows Git Bashのシェル経由で日本語リテラル(`豆ID`等のJSONキー)をUTF-8以外(Shift-JIS相当)で送信してしまい、GAS側でキーが一致しなくなっていたテスト手法側の誤りだった**と判明。UTF-8のバイト列を保証したファイル経由(`--data-binary @file`)で同一リクエストを送ると、`bean_master`/`coffee_data`/`origin_master`いずれの日本語ID列(豆ID/記録ID/産地ID)でも`update`/`delete`が正常動作することを確認(存在しないIDでは正しく「Record not found」、実在するテストレコード(豆ID=1784633291938)への実更新も`{"status":"success"}`で成功)。**Flutter本体(`sheets_service.dart`)は`dart:convert`の`json.encode`経由で常に正しいUTF-8を送信するため、アプリ自体はこの誤検知の影響を受けていなかったと考えられる**(未確認点: アプリUIからの実削除操作そのものは今回も確認していないため、実機/ブラウザでの最終確認は引き続き推奨)。付随タスクの`bean_master`テスト豆3件+`coffee_data`孤児レコードの削除(ユーザー承認済み、T3-46で対応)は、本バグが存在しないことが判明したため通常のアプリUI経由で問題なく実行できる見込み | 本番アプリのUI(削除ボタン等)から実際に豆/抽出記録を削除・更新でき、成功することを確認する(直接API検証は完了、UI経由の最終確認が残タスク) | なし | S(再調査により縮小) | ✅(誤検知と判明。UI経由の最終確認のみ任意で残置) |
| T3-57 | **不具合(2026-07-27ユーザー報告)**: 「Youth」の豆(パッケージ画像)が表示されない。**調査結果(2026-07-27、下記note参照)**: 表示側のバグではなく、対象3件(`5a57cb8d`/`bf2d1f8d`/`5549c4ad`、いずれも産地違いのYouthシリーズ)は本番`bean_master`上で`豆画像URL`/`豆粒画像URL`/`情報画像URL`のいずれも元々空欄と確認。原因はT3-38(original-data移植)で、CSVが参照する画像がユーザーのローカルファイルパス(`豆マスター_Images/`配下)のみでリポジトリ・本番のどこにも実体が無く、移植時に画像だけ意図的に対象外とされたこと(T3-38完了ログに明記済み)。**修正には対象3件それぞれの実際のパッケージ写真が必要**(Claude側で生成・代用はできない)。画像URLの設定は`updateBean`経由でGASの`updateRow`を呼ぶが、**T3-56の再調査(2026-07-27)により`updateRow`自体は正常動作すると確認済み**(誤検知だった)ため、保存経路側の懸念は解消。**残る唯一の障害はユーザーからの写真提供待ち**。写真が揃い次第アプリUI(011編集)からアップロードして設定する | 3件のYouth豆すべてでパッケージ画像が本番アプリに表示される | なし(写真素材のみユーザー提供待ち) | S | ⬜(ユーザーからの写真提供待ち) |

#### Phase 3 追加分（2026-07-28 ユーザー要望、T3-58〜T3-69）

**要望の原文(6件)**: ①calcページで豆量を変更してもpouring stepsに反映されない・線形でお湯量を変更して ②豆残量も手動で調整できるようにする ③そもそも追加購入を想定した設計になっていない。豆詳細画面に追加購入ボタンを追加し、購入日・焙煎日・購入量を入力するダイアログで既存の購入日・焙煎日を更新し購入量を残量に加算する(**画面デザイン・遷移は上位モデルで検討**) ④豆の管理項目に保存場所(職場/家)を追加、既存は全て「職場」にしてあとでユーザーが手動で振り分ける ⑤豆の購入履歴を追えるページを新設、リスト形式とカレンダー形式の両方で購入日・購入豆・購入先が分かるように(**運用方法・画面構成は上位モデルで検討**) ⑥購入店を管理できるページを追加、管理項目も検討。豆管理項目から店を自動抽出しネットから情報を自動取得(**上位モデルで実行**)。

**着手順の目安**: **①独立・即着手(T3-58 → T3-59) → ②在庫基盤(T3-60) → ③設計(T3-61、上位モデル) → ④購入履歴基盤〜UI(T3-62 → T3-63 → T3-64 → T3-65) → ⑤購入店(T3-66設計(上位) → T3-67 → T3-68 → T3-69)**。

**依存関係(必ず守る順序)**:
- `T3-60`(在庫基準点) → `T3-61`(追加購入+購入履歴の統合設計、上位モデル) → `T3-62`(購入履歴データ基盤) → `T3-63`(追加購入UI実装) / `T3-64`(購入履歴リスト) → `T3-65`(購入履歴カレンダー)
- `T3-66`(購入店設計、上位モデル。**2026-07-28完了**) → `T3-67`(購入店データ基盤) → `T3-68`(購入店UI) → `T3-70`(新規購入店のAI自動取得) / `T3-69`(豆マスタのstore→storeId移行)
- `T3-62` + `T3-68` → `T3-69`

**すぐ着手できる(依存なし)**: T3-59(M)・T3-60(M)。**T3-58は2026-07-29に完了済み**。**T3-66は2026-07-28に上位モデル(Opus 5)で完了済み**(成果物: `docs/store_master_design.md`)。これによりT3-67(→T3-68→T3-69)が着手可能になった。**T3-67以降はSonnet 5の`/full_loop`で実施してよい**(設計判断は設計書で完結しているため)。

**モデル選定の一般ルール(2026-07-28ユーザー指示で恒久化)**: **上位モデル(Opus等)がやるのは「方針検討」と「実装内容の検討」まで。実装は必ず下位モデル(Sonnet 5)に回す。** したがって上位モデル指定のタスクは、成果物が必ず**設計書+実装タスクへの分解**になり、コードは1行も書かない。逆に、上位モデルはSonnet 5が迷わない粒度(フィールド名・シート列名・画面ID・引数表・突合規則・既知の地雷への注意まで確定)で情報を整理する責任を負う。T3-66はこのルールに沿って実施した(成果物`docs/store_master_design.md` → T3-67/T3-68/T3-69/T3-70はすべてSonnet 5で実装可能)。

**このグループのモデル選定(2026-07-28、ユーザー指示「上位モデルで検討して」に基づく)**: **T3-61・T3-66の2件のみ上位モデル(Opus等)で実施**し、`full_loop`の自動選定対象外とする(`statistics_feature_design.md`§12①「画面デザインの新規検討は上位モデルで実施」に該当。T3-61=追加購入ダイアログと購入履歴の統合という新規の運用設計、T3-66=購入店マスタの管理項目そのものの決定とWeb情報収集を伴うため)。**それ以外(T3-58〜T3-60、T3-62〜T3-65、T3-67〜T3-69)はすべてSonnet 5で`/full_loop`による自動実施の対象**。そのため設計判断が必要な箇所は本タスク表側で先に確定させてある(下記各行の「実装方針」を発明せずそのまま従うこと)。

**共通の注意(全タスク共通)**:
- モデルにフィールドを追加したら**必ず** ①`lib/models/*.dart` ②`*.g.dart`(build_runnerがこのマシンで不安定なため手動編集可、T3-34の教訓参照) ③`SheetsService`の**keyMapとreverseMapの両方** ④GAS `gas/Code.gs`の`EXISTING_SHEET_EXTRA_COLUMNS`(既存シートへの列追加)または`ALLOWED_SHEETS`+`NEW_SHEET_HEADERS`(新規シート) ⑤`clasp push`+`clasp redeploy` の5点をセットで実施する。**過去に頻発した「列プロビジョニング漏れ」バグ(T4-1b/T4-2d/T3-23/T3-47)と同型**。`rules/verification.md`のチェックリストに従う。
- 新規画面を足す場合は `lib/routing/app_screen.dart`(画面ID enum)+`lib/routing/screen_registry.dart`(ID→Widget解決)+導線(遷移ボタン)の3点セット。既存例はT3-36(042)・T3-39(043)。
- 画面IDの空き: **025〜029, 032〜039, 044〜089**。本グループでは **025=購入履歴、026=購入店管理(リスト)、027=購入店詳細、028=新規購入店** を割り当てる(着手時に`app_screen.dart`で重複が無いことを再確認すること)。
- CLAUDE.md「全マスタータブへの一律適用」規約: 豆マスタだけの変更で済むか、他マスターにも展開すべきかを毎回判断すること(本グループは豆固有の要件が多いため、原則として豆のみで良い。共通ウィジェットに手を入れる場合のみ全マスターへの影響を確認)。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-58 | **不具合**: 030(calc/抽出レシピ)で豆量を変更しても注湯ステップ(Pouring Steps)の湯量表示が変わらない。豆量に対して**線形にスケール**した湯量を表示すること。**根本原因は調査済み(着手時に再調査不要)**: ①`MethodStepsEditor`(`lib/widgets/method_steps_editor.dart`)は030から`isEditing: true`で使われており、各セルが`TextFormField(initialValue: ...)`で描画されている。`initialValue`は**内部コントローラの初回生成時にしか反映されない**ため、豆量変更で`setState`→再ビルドされても表示中の湯量テキストが更新されない(`_beanWeightController`の`onChanged: (_) => setState((){})`自体は既に実装済みで、再ビルドは起きている)。②さらに`MethodStepsEditor`の湯量計算は`s.waterRatio`がある場合のみ`waterRatio * baseBeanWeight`でスケールし、**`waterRatio`が未設定(null/0)のステップは`s.waterAmount`をそのまま表示しておりスケールされない**。一方、030本体の`_stepAmount()`は`waterAmount * _scaleFactor`(=`_currentWeight / method.baseBeanWeight`)でフォールバックしており、**両者のロジックが食い違っている**。**実装方針**: (a)`MethodStepsEditor`に`baseBeanWeight`(現在の豆量)と別に`methodBaseBeanWeight`(メソッド基準豆量)を渡せるようにし、湯量計算を030の`_stepAmount()`と同一のロジック(ratioがあれば`ratio*currentWeight`、無ければ`waterAmount*(currentWeight/methodBase)`)に統一する。ロジックは`lib/utils/`等の共通関数に切り出して030とエディタの両方から呼ぶ(二重定義を残さない)。(b)`TextFormField`の`initialValue`を`TextEditingController`管理へ変更するか、`ValueKey`に湯量の値を含めて豆量変更時にウィジェットを作り直す(**ただし編集中のフォーカス・カーソル位置が飛ばないこと**を必ず確認。ユーザーが湯量セルを手入力している最中に再生成されないよう、豆量が変わったときのみ再構築されるキー設計にする) | 030でメソッドを選び豆量を15g→30g等に変更すると、注湯ステップ表の湯量(累計湯量)が**即座に比例して(線形に)変化する**。`waterRatio`を持たない既存メソッドのステップでも同様にスケールされる。湯量セルを手入力した際にカーソル位置が飛ばない。`flutter analyze`新規0・`flutter test`全パス(スケーリングの単体テストを新規追加すること) | なし | S〜M | ✅ 2026-07-29 `/full_loop`(Sonnet 5)で完了。共通関数`lib/utils/pouring_step_scaling.dart`の`scaledStepWaterAmount`に一本化+`MethodStepsEditor`の水量セルに豆量依存のValueKeyを付与。新規テスト7件追加、既存210件全パス、本番デプロイ・確認済み |
| T3-59 | 豆マスタに**保存場所**(職場/家)を追加する。**実装方針(発明せずこの通りに)**: ①`BeanMaster`に`String storageLocation`(`@JsonKey(defaultValue: '')`)を追加、`copyWith`にも追加。②選択肢は`lib/services/math/encoding.dart`の`roastLevels8`に倣って**定数リスト`beanStorageLocations = ['職場', '家']`**を適切な場所(例: `lib/utils/bean_storage.dart`新規、または既存の定数置き場)に定義し、UIはこの定数を参照する(文字列直書き禁止)。③`SheetsService.getBeans()`のkeyMapに`'保存場所': 'storageLocation'`、`_reverseMapBean()`のreverseMapに`'storageLocation': '保存場所'`を**両方**追加。④`gas/Code.gs`の`EXISTING_SHEET_EXTRA_COLUMNS['bean_master']`に`'保存場所'`を追加し`clasp push`+`clasp redeploy`。⑤012(`bean_create_screen.dart`)・011(`bean_detail_screen.dart`)の基本情報セクションに選択UI(既存の焙煎度と同じChoiceChip/ドロップダウンの流儀に合わせる)を追加。⑥010(`bean_list_screen.dart`)に**保存場所での絞り込み**(全て/職場/家)を追加し、カード上にも保存場所が分かる表示(小さなラベル/アイコン)を出す。⑦**既存データの一括設定**: 本番`bean_master`の既存レコードすべてに`保存場所='職場'`を設定する。`tools/`配下に冪等な移行スクリプト(`tools/migrate_original_data.dart`と同じくSheetsServiceを使わない独立HTTPスクリプト、**302リダイレクトの手動フォロー必須**=既知の教訓)を新規作成して実行し、既に値が入っているレコードは上書きしないこと。**ユーザーがあとで手動で「家」に振り分ける前提**なので、スクリプトは一度きりの実行でよい | 012で保存場所を選んで登録でき、011で表示・編集でき、本番Sheetsの`保存場所`列に保存される。010で職場/家の絞り込みができる。既存の全豆が「職場」になっている。`analyze`新規0・`test`全パス(新規テスト: 保存場所の選択UI描画と絞り込み動作) | なし | M | ⬜ |
| T3-60 | 豆の**残量を手動で調整**できるようにする。現状の残量は`lib/utils/bean_stock_calculator.dart`の`calculateBeanRemainingPercent()`が`initialQuantityGrams - Σ(該当豆の抽出記録のbeanWeight)`で**完全に自動算出**しており、実測とのズレ(こぼした・量り誤差・記録漏れ)を補正する手段が無い。**実装方針(在庫基準点方式。T3-63の追加購入もこの基盤に乗るため、この設計を必ず先に入れること)**: ①`BeanMaster`に`double? stockBaselineGrams`(基準時点での残量g)と`DateTime? stockBaselineAt`(基準時点)を追加。②残量計算を`残量g = stockBaselineGrams - Σ(brewedAt > stockBaselineAt の該当豆記録のbeanWeight)`に変更し、**`stockBaselineAt`がnullの豆は従来どおり`initialQuantityGrams`基準で算出**(既存データの後方互換を必ず維持)。③残量%の分母は`stockBaselineGrams ?? initialQuantityGrams`とする(=基準点設定直後は100%表示になる)。④`calculateBeanRemainingPercent()`はシグネチャを維持しつつ内部を上記に差し替え、あわせて**残量をグラム単位で返す`calculateBeanRemainingGrams()`を新設**(手動調整ダイアログの初期値と追加購入の加算に使うため)。⑤011(豆詳細)の残量表示付近に「残量を調整」ボタンを置き、ダイアログで現在の残量g(初期値=算出値)を入力→保存で`stockBaselineGrams`=入力値・`stockBaselineAt`=現在時刻を`updateBean`で保存。⑥`SheetsService`のkeyMap/reverseMap両方に`'在庫基準量(g)': 'stockBaselineGrams'`・`'在庫基準日時': 'stockBaselineAt'`を追加、`gas/Code.gs`の`EXISTING_SHEET_EXTRA_COLUMNS['bean_master']`に同2列を追加して`clasp push`+`clasp redeploy`。⑦`T3-45`で導入済みの`updateOptimistic`を使い、保存後に一覧・ダッシュボードの瓶表示が即時更新されること | 011で残量を手動入力すると瓶の表示(011/010/001)が即座に入力値どおりになり、その後の抽出記録は基準点以降の分だけが差し引かれる。基準点未設定の既存豆の表示は従来と変わらない。本番Sheetsに2列が保存される。`analyze`新規0・`test`全パス(新規テスト: 基準点あり/なしの残量算出、基準点より前の記録が差し引かれないこと) | なし | M | ⬜ |
| T3-61 | **⚠️上位モデルで実施(`full_loop`の自動選定対象外、2026-07-28ユーザー指示「上位モデルで画面デザインや遷移を検討して」)** **追加購入フローと購入履歴の統合設計**。現状のデータモデルは**豆1件=1回の購入**を前提としており(`purchaseDate`/`roastDate`/`initialQuantityGrams`が各1つずつ)、同じ豆を買い足す運用が表現できない。ユーザー要望は「011(豆詳細)に**追加購入ボタン**を置き、押すと**購入日・焙煎日・購入量**を入力するダイアログが出て、入力すると既存の購入日・焙煎日が**更新**され、購入量が現在の残量に**加算**される」+「**購入履歴**をリスト形式・カレンダー形式で追えるページ(購入日・購入豆・購入先が分かる)」。**この2つは同じ購入イベントを扱うため統合して設計すること**(追加購入のたびに購入履歴へ1行追記される形が自然)。**検討・決定すべき事項**: ①購入イベントの保存先(新規シート`bean_purchases`を推奨。`ALLOWED_SHEETS`+`NEW_SHEET_HEADERS`で自動生成できる既存の仕組みに乗る)と列定義(購入ID/豆ID/購入日/焙煎日/購入量(g)/購入店(将来のstoreId、T3-69)/メモ 等)②豆マスタ側の`purchaseDate`/`roastDate`/`initialQuantityGrams`を「最新購入の値」として上書きし続ける方針でよいか、それとも履歴からの導出に切り替えるか(**後方互換=既存の統計処理が`roastDate`から鮮度を算出している点(`BeanMaster.roastDate`のコメント参照)を壊さないこと**)③**初回購入(012での新規登録)も購入履歴の1行として記録するか**、既存豆の遡及登録をどうするか(既存23件超の豆をどう扱うか、移行スクリプトの要否)④追加購入ダイアログのUI(入力項目・バリデーション・デフォルト値・確認表示)と011からの導線・遷移⑤購入履歴ページ(025)の画面構成: リスト形式とカレンダー形式の切替方法(タブ/セグメント)、カレンダーの実装方式(**外部パッケージ`table_calendar`を追加するか自前実装か。追加パッケージの是非はユーザーに確認**)、1日に複数購入がある場合の表示、行タップ時の遷移先(011か購入詳細か)⑥購入履歴からの導線を置く場所(010/011/ナビ/設定のどこからか)。**成果物**: 決定内容を`docs/改修マスタープラン.md`の本タスク行(または`docs/cycle_<N>_bean_purchase/implementation_plan.md`)に追記し、T3-62〜T3-65が**設計判断なしで実装だけできる粒度**まで具体化する(Sonnet 5が実行するため、フィールド名・シート名・画面ID・UI構成をすべて確定させること)。**実装はこのタスクでは行わない(設計とタスク詳細化のみ)** | 上記①〜⑥がすべて決定され、ユーザー承認を得たうえでT3-62〜T3-65のタスク記述が実装可能な粒度に更新されている | T3-60 | M | ⬜ |
| T3-62 | 購入履歴の**データ基盤**を作る(T3-61で確定した設計に従う。**設計が未確定なら着手しないこと**)。想定作業: ①新規モデル`lib/models/bean_purchase.dart`(+`.g.dart`)を追加。②`gas/Code.gs`の`ALLOWED_SHEETS`に`bean_purchases`を追加し、`NEW_SHEET_HEADERS`に列定義を追加(`ensureSheet_`が初回POST時に自動生成する既存の仕組みに乗る)、`clasp push`+`clasp redeploy`。③`DataService`(`lib/services/data_service.dart`)に`getBeanPurchases`/`addBeanPurchase`/`updateBeanPurchase`/`deleteBeanPurchase`を追加し`SheetsService`に実装(keyMap/reverseMapの両方を必ず定義。**IDの`.toString()`キャストを`fromJson`で行うこと**=既知のクラッシュ要因)。④`lib/providers/data_providers.dart`にプロバイダを追加(T3-45で導入済みの`OptimisticListNotifier<T>`基底クラスを踏襲) | 本番Sheetsに`bean_purchases`シートが自動生成され、購入履歴の追加・取得・更新・削除がアプリのDataService経由で動作する。`analyze`新規0・`test`全パス(新規テスト: モデルのfromJson/toJson往復、数値IDの文字列化) | T3-61 | M | ⬜ |
| T3-63 | 011(豆詳細)に**追加購入ボタン**とダイアログを実装する(T3-61で確定したUI設計に従う)。想定作業: ①ボタン押下で購入日(既定=今日)・焙煎日・購入量(g)を入力するダイアログを表示。②保存時に **(a)`bean_purchases`へ1行追記(T3-62)** **(b)豆マスタの`purchaseDate`/`roastDate`を入力値で更新** **(c)在庫基準点を更新して残量に購入量を加算**(`stockBaselineGrams = calculateBeanRemainingGrams(現時点) + 購入量`、`stockBaselineAt = 現在時刻`。T3-60で作った基盤をそのまま使う)を**1操作で行う**。③保存はT3-45の`updateOptimistic`経由で一覧・ダッシュボードに即時反映。④失敗時は日本語のエラーSnackBar(`SnackBarBehavior.floating`+下マージン、T3-44の教訓)を出し、部分的に書き込みが成功した場合の扱いをログ(`[Antigravity]`)に残す | 011の追加購入ボタンから購入日・焙煎日・購入量を入力すると、豆の購入日/焙煎日が更新され、残量が購入量ぶん増え、購入履歴に1行追加される。すべて本番Sheetsに保存される。`analyze`新規0・`test`全パス(新規テスト: ダイアログ描画+保存で残量加算と履歴追記の両方が呼ばれること) | T3-62 | M | ⬜ |
| T3-64 | 購入履歴ページ **025** を新設し、**リスト形式**を実装する(T3-61で確定した画面構成に従う)。想定作業: ①`app_screen.dart`に`beanPurchaseHistory('025', '購入履歴')`を追加、`screen_registry.dart`にcase追加、導線(010の豆管理からのボタン等、T3-61で確定した場所)を設置。②購入日の新しい順に **購入日・豆(画像+豆名、`MockListRow`の`imageUrl`を使う=T3-14/T3-22と同じパターン)・購入先・購入量(g)・焙煎日** を1行で表示。③行タップで011(豆詳細)へ遷移。④豆マスタ側の絞り込み(保存場所T3-59、期間)があると使いやすいが**必須ではない**(スコープを膨らませないこと) | 025がリスト形式で購入履歴を表示し、購入日・購入豆・購入先が一覧で分かる。行タップで011へ遷移する。`analyze`新規0・`test`全パス(新規テスト: 実データ相当のフェイクで行が描画されること・タップ遷移) | T3-62 | M | ⬜ |
| T3-65 | 購入履歴ページ **025** に**カレンダー形式**を追加する(リスト/カレンダーの切替はT3-61で確定した方式=タブまたはセグメントで実装)。想定作業: ①月表示のカレンダー上で、購入があった日にマーカー(豆画像サムネイル or ドット)を表示。②日付タップでその日の購入内訳(豆名・購入先・購入量)をボトムシート/リストで表示、そこから011へ遷移。③**カレンダーの実装方式はT3-61の決定に従う**(外部パッケージ追加が承認されていればそれを使い、承認されていなければ`GridView`ベースの自前実装で済ませる。**独断でパッケージを追加しないこと**) | 025でカレンダー形式に切り替えられ、購入があった日が視覚的に分かり、日付タップで内訳が確認できる。`analyze`新規0・`test`全パス(新規テスト: 購入がある日にマーカーが出ること・日付タップで内訳が出ること) | T3-64 | M | ⬜ |
| T3-66 | **⚠️上位モデルで実施(`full_loop`の自動選定対象外、2026-07-28ユーザー指示「これは上位モデルで実行して」)** **購入店マスタの設計 + 既存店の抽出 + Web情報の自動収集**。現状、購入店は`BeanMaster.store`(自由入力文字列、シート列`購入店舗`)としてしか持っておらず、店ごとの管理・集計ができない。**このタスクでやること**: ①**管理項目の検討・決定**(候補: 店名/正式名称/URL/所在地/都道府県/オンライン販売の有無/実店舗の有無/焙煎所併設か/取扱豆の傾向/SNS/メモ/画像。**ユーザーに確認のうえ確定**)②**本番`bean_master`の`購入店舗`列から既存の店名を重複排除して抽出**する(表記ゆれの名寄せ方針も決めること。現データを実際に取得して一覧化する)③**抽出した各店の情報をWeb検索(`WebSearch`/`WebFetch`)で収集**し、①で決めた項目を埋めた初期データ案を作る(**不確かな情報を断定で埋めないこと**。出典URLを併記し、確証が無い項目は空欄にしてユーザーが埋められるようにする)④購入店の画面構成(026一覧/027詳細/028新規)のUI設計。既存の`MasterListTemplate`/`MasterDetailTemplate`(`lib/screens/master_template.dart`)を流用できるかを判断する(流用できれば実装コストが大幅に下がる)⑤027(詳細)に「この店で買った豆」「この店の購入履歴」を出すか、購入履歴(025)との関係をどう見せるか⑥導線(Mastersハブ`masters_hub_screen.dart`に追加するのが自然)。**成果物**: 決定した管理項目・初期データ案(出典付き)・画面構成をユーザーに提示して承認を得たうえで、T3-67〜T3-69のタスク記述を実装可能な粒度に更新する。**実装はこのタスクでは行わない(設計・調査とタスク詳細化のみ)。本番Sheetsへの初期データ投入もこの時点では行わない(T3-67で実施)** | 管理項目・初期データ案(既存店の抽出結果+Web収集情報、出典付き)・画面構成がユーザー承認を得て、T3-67〜T3-69が実装可能な粒度に更新されている | なし | L | ✅ 2026-07-28 **成果物=`docs/store_master_design.md`**。管理項目=フル13項目(→シート17列)、名寄せ=提案どおり(7店に集約、非店舗値3件は除外)、画面=既存テンプレート流用、027の中身=4セクション全部、をユーザー承認済み |
| T3-67 | 購入店マスタの**データ基盤**を作る。**設計は`docs/store_master_design.md`で完全に確定済み(2026-07-28、T3-66)。同書§2の列定義・§4の初期データをそのまま使い、発明しないこと**。作業: ①新規モデル`lib/models/store_master.dart`(+`.g.dart`。**`build_runner`がこのマシンで不安定なため`.g.dart`は手書きでよい**=T3-34の教訓)。フィールドは設計書§2の表のとおり19個(`id`/`name`/`formalName`/`url`/`prefecture`/`address`/`hasOnlineShop`/`hasPhysicalStore`/`hasRoastery`/`beanTendency`/`memo`/`imageUrl`/`snsUrl`/`businessHours`/`closedDays`/`phone`/`openedYear`/`sourceUrl`/`infoFetchedAt`。**末尾2つはT3-70(AI自動取得)のメタ列。あとから足すと列プロビジョニング漏れを踏むので最初から入れること**)。**`id`は`@JsonKey(defaultValue: '', fromJson: _parseString)`で必ず`.toString()`キャスト**、boolは`BeanMaster._parseBool`をコピーして流用(`TRUE`/`FALSE`表記が通ることを確認)、`openedYear`は`int`ではなく`String`。`copyWith`も全フィールド分用意する。②`gas/Code.gs`の`ALLOWED_SHEETS`に`'store_master'`を追加し、`NEW_SHEET_HEADERS['store_master']`に設計書§2の日本語列名19個(`購入店ID`/`店名`/`正式名称`/`URL`/`都道府県`/`住所`/`オンライン販売`/`実店舗`/`焙煎所併設`/`取扱豆の傾向`/`メモ`/`店舗画像URL`/`SNS`/`営業時間`/`定休日`/`電話番号`/`開業年`/`情報取得元`/`情報取得日`)を**この順で**定義 → `clasp push`+`clasp redeploy`。③`DataService`に`getStores`/`addStore`/`updateStore`/`deleteStore`を追加し`SheetsService`に実装(**keyMapとreverseMapの両方**に17列分のマッピングを書く=列プロビジョニング漏れと同型の頻出バグ)。④`data_providers.dart`に`storeMasterProvider`を追加(T3-45の`OptimisticListNotifier<T>`基底を踏襲)。⑤設計書§4の**7店**(`store_navy`/`store_kobe_coffee`/`store_heisei`/`store_sora`/`store_misaki`/`store_akekure`/`store_youth`)を本番Sheetsへ投入する冪等な移行スクリプトを`tools/migrate_stores.dart`として作成・実行(`tools/migrate_original_data.dart`と同型、**302リダイレクト手動フォロー必須**、`購入店ID`が既にあればスキップ)。**IDは設計書のとおり固定スラッグを使い、タイムスタンプ生成にしないこと**(再実行で重複するため)。**空欄の項目は空欄のまま投入する(推測で埋めない)** | 本番Sheetsに`store_master`シートが17列で自動生成され、設計書§4の7店が登録され、アプリのDataService経由でCRUDできる。移行スクリプトを2回実行しても8件目が増えない(冪等)。`analyze`新規0・`test`全パス(新規テスト: `StoreMaster`のfromJson/toJson往復、数値IDの文字列化、`TRUE`文字列のbool化) | T3-66 | M | ⬜ |
| T3-68 | 購入店の**一覧026/詳細027/新規028**の3画面を実装する。**画面構成は`docs/store_master_design.md`§5で完全に確定済み。同書の引数表・セクション順をそのまま実装し、レイアウトを新規に検討しないこと**。作業: ①`app_screen.dart`に`storeList('026', '購入店管理')`/`storeDetail('027', '購入店詳細')`/`storeNew('028', '新規購入店')`を追加(**`025`は購入履歴T3-64用の予約なので使わない**)、`screen_registry.dart`に3件のcase追加。②026は`MasterListTemplate<StoreMaster>`をそのまま使う(設計書§5.2の引数表どおり。アイコンは`Icons.storefront_outlined`、`subtitleOf`は都道府県+業態ラベル、**絞り込みは実装しない**)。③027は`MasterDetailTemplate`を使い、`fields`に設計書§2の13項目(空欄は`'-'`)、`extraSections`に**設計書§5.3の順で**「この店で買った豆」→「統計(購入回数/総購入量/平均評価)」の2つ(「この店の購入履歴」はT3-62未完了のためこの時点では作らず、T3-69で追加する)、`relatedLogFilter`は**先に豆IDのSetを作ってから**`(log) => beanIds.contains(log.beanId)`とする。「この店で買った豆」の突合は**T3-69完了までのフォールバック**として`b.storeId == store.id || (b.storeId.isEmpty && (b.store == store.name || (store.id == 'store_akekure' && b.store == '明暮焙煎研')))`を使う。④028は`lib/screens/create/store_create_screen.dart`を新設(`dripper_create_screen.dart`と同構成。必須は`店名`のみ、業態3つは`SwitchListTile`、画像は既存の`lib/widgets/image_upload_field.dart`、保存は`addOptimistic`/`updateOptimistic`、エラーSnackBarは`SnackBarBehavior.floating`+下マージン=T3-44の教訓)。⑤**CLAUDE.md「全マスタータブへの一律適用」規約**により、`MasterSwitcherButton._entries`と`_categoryOf`(`lib/screens/master_template.dart`)、および`MastersHubScreen`の`entries`(`masters_hub_screen.dart`)の**3箇所すべて**に購入店を追加する | 026で購入店7件が一覧表示され、028で新規登録、027で13項目の詳細表示・編集・削除ができ、027に「この店で買った豆」と統計が出る。Mastersハブと他マスターの切替メニューの両方から到達できる。本番Sheetsに保存される。`analyze`新規0・`test`全パス(新規テスト: 3画面の描画と遷移、027の「この店で買った豆」が`store`文字列フォールバックでも一致すること) | T3-67 | M | ⬜ |
| T3-70 | **新規購入店の情報をAIで自動取得する**(2026-07-28ユーザー要望「今後新しい店で購入した場合、自動で情報を取得する仕組み」)。**方式・プロンプト制約・UI・スコープはすべて`docs/store_master_design.md`§8で確定済み。設計を発明せず、同節をそのまま実装すること**。要点: ①**新しい外部サービス・新しいパッケージ・自前スクレイピングは採用しない**(Flutter WebからのCORSで確実に失敗するため)。既存の`google_generative_ai`+`AiAnalysisService`にメソッドを1つ足すだけ。②`AiAnalysisService`に`fetchStoreInfo({required String storeName, String? hintPrefecture, required String apiKey, String? preferredModel})`を追加。既存の`extractBeanInfoFromImage`と同じく`GenerationConfig(responseMimeType: 'application/json')`+`_modelOrder`のフォールバックを使う。③**プロンプトの絶対規則**(設計書§8.3): 確信が無い項目は必ず空で返す/推測で埋めない・同名の別店舗が複数あるときは勝手に選ばず`ambiguous: true`と候補一覧を返す・項目ごとに`confidence`(high/medium/low)と`sourceUrls`を必ず併記。④**取得結果は絶対に自動保存しない**。項目ごとにチェックボックス付きの確認ダイアログを出し(設計書§8.4)、`confidence: low`と**既に値がある項目は既定OFF**、反映時に`sourceUrl`と`infoFetchedAt`(T3-67で作る2列)も書く。⑤発動は2箇所: 028の店名欄横の「AIで自動入力」ボタン(012の「パッケージ画像から自動入力(AI)」と同じ流儀)と、**T3-69完了後に012/011から「新規購入店を追加」した直後の自動起動**。⑥Google検索グラウンディングは**パッケージが対応していれば使い、対応していなければ非グラウンディングのまま完成とする**(パッケージの追加・更新は独断で行わずユーザーに確認)。⑦APIキー未設定・取得失敗・JSONパース失敗はいずれも日本語SnackBar(`SnackBarBehavior.floating`+下マージン)で通知し**手入力に落とせること**。ログは`[Antigravity] Action: 購入店情報のAI取得 …`。**スコープ外(設計書§8.5)**: 既存店の定期自動更新・店舗画像の自動取得・他マスタへの展開 | 028で店名を入れて「AIで自動入力」を押すと、確認ダイアログに取得値と出典URLが出て、チェックした項目だけが反映され`情報取得元`/`情報取得日`が本番Sheetsに保存される。確信の低い項目・既存値のある項目は既定でチェックが外れている。APIキー未設定時や取得失敗時もエラーが日本語で出て手入力を継続できる。`analyze`新規0・`test`全パス(新規テスト: `confidence: low`と既存値ありの項目が既定OFFになること、`ambiguous: true`のとき何も反映されずに候補選択が出ること、取得失敗時に手入力へ落ちること) | T3-68 | M | ⬜ |
| T3-69 | 豆マスタの購入店を**自由入力文字列から購入店マスタ参照へ移行**する。**実装方針**: ①`BeanMaster`に`String storeId`を追加(`OriginMaster`導入時(T4-1b)と**完全に同じパターン**: 自由入力の`store`は後方互換のため残し、保存時に選択した店名を`store`にもコピーする。`BeanMaster.originId`のコメント参照)。②`SheetsService`のkeyMap/reverseMap両方に`'購入店ID': 'storeId'`、`gas/Code.gs`の`EXISTING_SHEET_EXTRA_COLUMNS['bean_master']`に`'購入店ID'`を追加して`clasp push`+`clasp redeploy`。③012/011の購入店入力欄を、購入店マスタからの選択式(+「新規購入店を追加」ボタン、既存の産地選択UIと同じ流儀)に変更。④既存豆の`store`文字列を`store_master`と突き合わせて`storeId`を埋める冪等な移行スクリプト`tools/migrate_bean_store_id.dart`を作成・実行する。**突合規則は`docs/store_master_design.md`§3.2で確定済み、これをそのまま実装すること**: `Navy`→`store_navy` / `神戸珈琲物語`→`store_kobe_coffee` / `HEISEI COFFEE The Factory`→`store_heisei` / `SORA`と`そら`→`store_sora` / `岬の焙煎所`と**豆名が`岬焙煎所`で始まり店が空欄の1件**→`store_misaki` / `明暮焙煎研`(誤記)→`store_akekure` / **豆名が`Youth `で始まり店が空欄の3件**→`store_youth`。**`ドリップバッグ`/`コロンビア`/`グアテマラ`の3件は店名ではないので空のまま残す**(ユーザーが後で手動で紐付ける)。**302リダイレクト手動フォロー必須**、実行前に対象一覧をユーザーに提示。⑤027(購入店詳細)にT3-68で保留していた「この店の購入履歴」セクションを追加し、あわせて「この店で買った豆」の`store`文字列フォールバックを削除して`storeId`一致のみに単純化する。⑥購入履歴(`bean_purchases`、T3-62)の購入先も`storeId`で保持し、025のリスト/カレンダーで店名を表示する | 012/011で購入店を選択式で設定でき、既存豆の購入店がマスタに紐付き(3件の非店舗値のみ空欄)、027に購入履歴が出て、025の購入履歴で購入先の店名が表示される。`analyze`新規0・`test`全パス | T3-62, T3-68 | M | ⬜ |

> **2026-07-28 タスク追加(T3-58〜T3-69、ユーザー要望6件):** 実装はせず記録のみ(ユーザー指示「今回は何も修正を実行せずタスク登録に限定して」)。原文6件を12タスクに細分化した。内訳: **不具合1件** = 030で豆量を変えても注湯ステップの湯量が線形にスケールされない(T3-58、**根本原因まで調査済みで着手時の再調査不要**: `MethodStepsEditor`の`TextFormField(initialValue:)`が再ビルドで更新されないこと+`waterRatio`が無いステップが030本体の`_stepAmount()`と違ってスケールされないロジック不一致の2点)。**豆マスタ項目追加2件** = 保存場所(職場/家、既存は全て職場に一括設定、T3-59)/在庫基準点方式による残量の手動調整(T3-60)。**追加購入・購入履歴5件** = 統合設計(T3-61、上位モデル)→購入履歴データ基盤`bean_purchases`(T3-62)→011の追加購入ボタン(T3-63)→購入履歴リスト025(T3-64)→カレンダー形式(T3-65)。**購入店マスタ4件** = 設計+既存店抽出+Web情報収集(T3-66、上位モデル)→データ基盤`store_master`(T3-67)→026/027/028の3画面(T3-68)→豆マスタの`store`→`storeId`移行(T3-69)。**ユーザー指示「`/full_loop`で実行することを想定、かつSonnet 5でも実行できるように細分化・詳細化」に従い、上位モデル指定の2件(T3-61/T3-66)以外は設計判断を本タスク表側で先に確定させてある**(モデルのフィールド名・シート名・列名・画面ID・計算式・既知の教訓への言及まで各行に明記済み。実装時は発明せずそのまま従うこと)。**追加購入(要望③)と購入履歴(要望⑤)は同じ購入イベントを扱うため、T3-61で統合設計してから実装する順序にした**(追加購入が既存の購入日・焙煎日を上書きする仕様のままでは履歴が残らないため)。
>
> **2026-07-26 T3-40調査・修正(カメラが起動しない不具合、`/loop`定期実行)**: ユーザーヒアリング(症状=「何も起きない」、環境=Android実機のホーム画面ピン留めPWA)を踏まえ調査。**根本原因は本番ビルドが使っていたFlutterのWebプラグイン登録ファイル(`.dart_tool/flutter_build/<hash>/web_plugin_registrant.dart`)が`image_picker_for_web`の登録を欠落したまま古いキャッシュを使い回していたこと**(直接確認: 修正前の当該ファイルには`FirebaseFirestoreWeb`/`FilePickerWeb`/`FirebaseCoreWeb`/`SharedPreferencesPlugin`/`UrlLauncherPlugin`/`WebYoutubePlayerIframePlatform`のみが並び`ImagePickerPlugin`が無かった)。T3-35で`image_picker`をpubspec.yamlに追加した際、その後の`flutter build web`がこのキャッシュを正しく再生成せず、**T3-35以降(T3-36/T3-38/T3-39含む)の本番デプロイはすべてこの欠落状態のビルドだった**と判明。`ImagePickerPlatform.instance`がweb実装に差し替わらないため`ImagePicker().pickImage()`が機能せず、例外もキャッチされず(呼び出し元に`try/catch`が無い)ユーザーには「何も起きない」ように見えていたと考えられる。**修正はコード変更ではなく`flutter clean`→`flutter pub get`→`flutter build web`によるキャッシュの強制再生成のみ**(再生成後のファイルで`import 'package:image_picker_for_web/image_picker_for_web.dart';`と`ImagePickerPlugin.registerWith(registrar);`を直接確認)。`flutter analyze`44件(新規0)・`flutter test`194件全パス(コード無変更のため既存件数のまま)、本番デプロイ完了。**未確認事項**: このセッションのclaude-in-chrome自動操作では、012の「パッケージ画像から自動入力(AI)」ボタン押下→選択ダイアログ表示、という操作自体が(座標を変えても・タブを作り直しても)反応しない自動化上の問題に複数回遭遇し、実際のカメラ起動フローをブラウザ上で最終確認できなかった(同じページの他要素(産地ドロップダウン・テキスト入力)は正常に操作できており、この特定のボタンのみ何らかの理由でヒットテストが成立しなかった。過去のT3-35実装時には同じボタンが正常にクリックできていたため、今回の自動化不調はコード側の問題ではなく環境/セッション固有の可能性が高いが未解明)。**2026-07-26追記: ユーザーが実機で再確認し「カメラOK」と報告、T3-40完了。**
> **2026-07-26 モデル選定方針(ユーザー確認)**: `statistics_feature_design.md`§12①「画面デザインの新規検討は上位モデルで実施」の既存ルールに照らし、T3-40〜T3-53のうち**T3-52・T3-53のみ上位モデル(Opus等)での実施が必要**と判断した。理由: 両タスクとも既存の統計UI(`gp_explorer_section.dart`等、§1.2.1で名指しされているコンポーネント)を拡張する**新規の可視化・比較UI**(メソッド別GP比較結果の提示、探索進捗のダッシュボード)の新規検討を伴うため。**T3-51(焙煎度説明ページ)は041と同じ既存テンプレートを踏襲するだけのため対象外、T3-49(引き継ぎ情報の可視化)は既存パターン(バッジ/カード)の応用で新規デザイン検討の程度が小さいため対象外**と判断(迷う場合は着手時に再検討してよい)。**T3-40〜T3-51(T3-52/T3-53を除く全件)はSonnet 5で`/loop 1h /full_loop`による自動実施の対象とする。T3-52/T3-53はこのタスク表に登録するのみで、`full_loop`は選定しない(ユーザーが上位モデルで別途着手する)。**

> **2026-07-25 T3-35完了(豆情報読取AIへのカメラ撮影追加、`/loop`定期実行1回目):** `image_picker ^1.1.2`を追加(Web版はブラウザの`<input capture>`経由でモバイルのネイティブカメラを起動、file_pickerには相当機能が無いため新規依存)。012(`bean_create_screen.dart`)の「パッケージ画像から自動入力(AI)」ボタンをタップすると`SimpleDialog`で「ファイルから選択」「カメラで撮影」を選べるようにし(`_chooseBeanImageSource`)、共通の抽出処理(`_runBeanImageExtraction`)に一本化。カメラ撮影時のみ、撮影画像を`ImageService.saveImage`(既存のDriveアップロード経路、`PlatformFile`を撮影バイト列から直接構築)で情報画像として保存し`_infoImageUrl`を更新(終了条件どおり、ファイル選択時は従来どおりフォーム反映のみで画像保存はしない)。GAS/シート列の変更は無し(既存T3-34の`infoImageUrl`をそのまま利用)。検証: `flutter analyze`44件(新規0)。`flutter test`188件全パス(既存187+新規1、ダイアログの選択肢描画を確認するテストを追加)。`flutter build web`成功。ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ)で012のボタンから「画像の取得方法」ダイアログ(ファイルから選択/カメラで撮影)が正しく表示されることを確認、コンソールエラー0件。**カメラ実撮影自体(`ImageSource.camera`のネイティブカメラ起動)はこのサンドボックスにカメラデバイスが無く自動確認不可**、実機での撮影→AI抽出→情報画像保存の一連の流れはユーザーのローカル/実機確認が必要。**本番デプロイ**: `firebase deploy --only hosting`で https://beanbase-app-2016.web.app へ反映(34ファイル)。**ハマった点(セッション中のブラウザ操作トラブル)**: `zoom`アクションでCDPスクリーンショットがタイムアウトした後、同一タブのビューポートが332×37に固定されたまま戻らなくなった(`resize_window`でも復帰せず)。新規タブを作成して同一URLを開き直すことで復旧した。**運用面**: このセッションは`/loop 1 /full_loop`(cron、毎時7分、1時間間隔)による定期実行の1回目。ユーザー指示によりfull_loopスキルに「5分超過のバックグラウンドタスクは進捗確認のみ」「着手タスクが無ければユーザー承認を待つ」「`/end`相当の締め後に`/clear`でセッションを軽量化する」の3点を追記済み。
> **2026-07-26 T3-41完了(全画像欄でファイル/カメラ選択、`/loop`定期実行):** T3-35で`bean_create_screen.dart`にローカル実装していた「画像の取得方法」ダイアログ(ファイルから選択/カメラで撮影)と取得ロジックを`lib/widgets/image_upload_field.dart`へ共通化した。`ImagePickSource` enum・`showImageSourceDialog()`・`pickImageFile()`(ダイアログでの選択に応じて`FilePicker`または`image_picker`のカメラから画像を取得し共通の`PlatformFile`として返すヘルパー、結果はレコード型`({PlatformFile file, ImagePickSource source})`)を新設。`ImageUploadField._pickImage()`をこの共通関数を使うよう置き換え(豆パッケージ/豆/情報画像、ドリッパー/フィルター/グラインダーの全画像欄が同じ経路を通るため一度の修正で全マスターに反映される。メソッドマスタには画像欄自体が存在しないため対象外)。`bean_create_screen.dart`側もローカルの`_BeanImagePickSource` enum・`_chooseBeanImageSource()`を削除しこの共通関数を呼ぶだけに簡素化(重複コード解消)。検証: `flutter analyze`44件(新規0)、`flutter test`195件全パス(既存194+新規1、`test/image_upload_field_test.dart`=共通アップロードアイコンから選択ダイアログが出ることを確認)、`flutter build web`成功。**ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ)**: 015(新規ドリッパー)の画像欄アップロードアイコンをタップし「画像の取得方法」(ファイルから選択/カメラで撮影)が正しく表示されることを確認、コンソールエラー0件。本番デプロイ完了。
> **2026-07-26 T3-42完了(焙煎度8段階統一、`/loop`定期実行):** `roastOrdinalMap`(`lib/services/math/encoding.dart`)を旧5段階(浅煎り〜深煎り、1.0〜5.0)から**新8段階**(ライト/シナモン/ミディアム/ハイ/シティ/フルシティ/フレンチ/イタリアン、1.0〜8.0)に再構成。各段階に英語アルファベット表記のエイリアス(`Light`/`Cinnamon`/…/`Italian`)を追加し日英どちらでも同じ順序値に解決できるようにした。**既存の本番データが欠測にならないよう、ユーザー確認済みの対応表どおり旧5段階を新8段階へのエイリアスとして残した**(浅煎り→シナモン(2.0)、中浅煎り→ミディアム(3.0)、中煎り→ハイ(4.0)、中深煎り→シティ(5.0)、深煎り→フレンチ(7.0)。ライト・フルシティ・イタリアンは旧データに無いため今後の新規登録でのみ使用)。UI選択肢を新設した`roastLevels8`定数に統一し、012(`bean_create_screen.dart`)・041のF1回帰予測フォーム(`regression_section.dart`)・041のF4レシピ探索(`gp_explorer_section.dart`)の`_roastOptions`をいずれも8段階へ置換(デフォルト値は旧'中煎り'相当の新'ハイ'に統一)。`stats_status_screen.dart`のF4判定用フォールバックも同様に4.0へ更新。**影響範囲の確認**: `preference_service.dart`の`roastLabelByOrdinal`(各順序値の代表ラベルを`roastOrdinalMap`の宣言順で決定)は新8段階の正式名を旧エイリアスより先に宣言したため、旧表記の記録も新8段階の代表名でグルーピング・表示されるようになった(グルーピング自体=順序値は不変、表示名のみ新名称になる)。この副作用で好み分析(F5)の表示ラベルが変わる3件のテスト(`preference_service_test.dart`/`preference_section_test.dart`/`recipe_suggestion_card_test.dart`)を、期待値を新代表名(例: 'シナモン')に更新し理由コメントを付記して対応(設計書のフィクスチャ値そのものは変更せず、表示ラベルの解釈のみ更新)。新規に`test/math/encoding_test.dart`を追加(新8段階の順序値・英語エイリアス・旧5段階の後方互換解決・未知表記の欠測扱いを検証)。検証: `flutter analyze`44件(新規0)、`flutter test`200件全パス(既存195+新規5)、`flutter build web`成功。**ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ)**: 041のレシピ探索(F4)で焙煎度ドロップダウンがデフォルト「ハイ」・全8段階(ライト〜イタリアン)選択可能を確認、ダッシュボードのおすすめレシピで実データに基づく「この産地はシティが高評価です」の表示(新代表名が実際に本番データで機能していることを確認)、豆管理で旧データ「中煎り」の豆を編集フォームで開くと選択済みチップとして「中煎り」が保持されたまま新8段階の選択肢と共存表示され、欠測やクラッシュが無いことを確認。コンソールエラー0件。本番デプロイ完了。
> **2026-07-26 T3-44完了(画像アップロード後に登録ボタンが押せなくなる不具合の修正+英語メッセージの日本語化、`/loop`定期実行):** `lib/widgets/image_upload_field.dart`の`_pickImage()`が使う4つの`SnackBar`(アップロード中/成功/失敗/エラー)はいずれもデフォルトの`SnackBarBehavior.fixed`で表示されており、`CreateFormScaffold`が`bottomNavigationBar`スロットに固定表示している登録ボタンと同じ画面下部領域に重なってタップを奪っていた可能性が高いと判断(タスク表の既存仮説どおり)。**対策**: 共通ヘルパー`_showSnack()`を新設し、`behavior: SnackBarBehavior.floating`+`margin: EdgeInsets.fromLTRB(16, 0, 16, 96)`(下96pxのマージンで登録ボタンの領域を確実に避ける)、`duration`を2秒に短縮して指定。あわせて4メッセージすべてを日本語化(`Uploading image...`→`画像をアップロード中...`、`Image uploaded!`→`画像をアップロードしました`、`Failed to upload image`→`画像のアップロードに失敗しました`、`Error picking image: $e`→`画像の取得に失敗しました: $e`)。**この共通ウィジェットは豆(パッケージ/豆/情報画像)・ドリッパー・フィルター・グラインダーの全画像アップロード欄が経由するため、1箇所の修正で全マスターに反映される**(CLAUDE.md「全マスタータブへの一律適用」規約どおり、メソッドマスタには画像欄自体が無いため対象外)。**検証**: テスト環境にはfile_pickerのプラットフォームチャンネルが無く「ファイルから選択」を選ぶと`MissingPluginException`が送出されることを利用し、実ファイル選択なしで`_pickImage`のcatch節経由のエラーSnackBar表示を検証する新規テストを追加(`test/image_upload_field_test.dart`、日本語メッセージの表示と`SnackBarBehavior.floating`の両方をアサート)。`flutter analyze`44件(新規0)、`flutter test`201件全パス(既存200+新規1)、`flutter build web`成功。**ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ)**: 012(新規豆追加)でパッケージ画像のアップロードアイコンをタップし「画像の取得方法」ダイアログが引き続き正常に表示されることを確認(実際のファイル選択はネイティブOSダイアログのためブラウザ自動化では完了できない既知の制約、T3-40と同様)。コンソールエラー0件。本番デプロイ完了。
> **2026-07-26 `/loop`定期実行の`/clear`問題(要最優先対応、ユーザー指示):** T3-44完了直後にloop_guardが2回連続でコスト超過($74.77/$24、$32.05/$24)を報告。調査の結果、**根本原因は`/full_loop`実行手順に書かれている「次回のループ起動前に`/clear`を実行する」がそもそも実行不可能な指示だったこと**と判明。`/loop 1h /full_loop`は`CronCreate`で登録しており、そのツール説明に明記されているとおり**セッション内蔵(session-only)** — 発火のたびに同一セッションへ`/full_loop`を再投入するだけで、新しい独立セッションは生成されない。一方、`/clear`はターミナルでユーザーが打つCLIコマンドであり、**アシスタントが自分自身に対して呼び出せるツールは存在しない**(`ToolSearch`で確認済み、`EndConversation`はセッション自体を終了させてしまいcronループも道連れに消えるため代替にならない)。つまり毎時の発火が同一の会話へひたすら積み上がり続けており、これが`cacheR`(キャッシュ読み込みトークン)の異常な増大とコスト超過の直接原因。**対応方針は保留中**: `/loop`スキル自体が案内する`/schedule`(クラウド定期実行)への切り替えを提案したが、ユーザーから「デプロイや本番確認(`claude-in-chrome`によるブラウザ確認)がクラウド実行環境からできるのか」との妥当な懸念が出た。**Flutter/Firebaseのローカルツールチェーンや、ユーザーの実ブラウザに拡張機能経由で接続する`claude-in-chrome`は、いずれもこのマシン/セッションに強く紐づいており、`/schedule`のクラウド実行がそこへ到達できる保証は無い**(このセッションからは確認不可能、`docs/deploy.md`が既に本サンドボックスの類似の接続制約を記録している点からも、クラウド実行はさらに隔離されている可能性が高いと判断)。**ユーザーへの提案**: ①些細な内容で`/schedule`を試験登録し、実際にFlutter/Firebase CLIやブラウザに到達できるか事実確認してから本番の日次ループを移行するか判断する、②または`/clear`が効くという前提を諦めた上でセッション内蔵`/loop`を継続し、コスト上限($24/ループ)を安全弁として運用する(ただし発火のたびにコストが伸び続け、上限到達のたびに手動でのセッション`/clear`/再起動が必要になる)。**ユーザーの最終回答待ち。次回セッションはこの節を必ず確認し、回答があればそれに従い、無ければ通常のタスク選定を進めつつこの問題への対応が依然未解決であることを`NEXT_SESSION.md`で申し送ること。**
> **2026-07-27 追記(解決)**: ユーザー指示「スキルの`/clear`問題は削除して。別のアプローチで解決する」を受け、`/clear`を呼ぶ前提そのものを放棄。既に2026-07-25に導入済みだった「コスト・ターン数・連続失敗を当日累計ではなく直近の`/start`・`/full_loop`呼び出し以降の1ループ単位で判定する」`loop_guard.js`の仕組みが、実質的な代替解決策として機能していると再確認し、これを正式な解決策として確定した。`full_loop`スキル(`.claude/skills/full_loop/SKILL.md`)から実行不可能だった`/clear`手順を削除し、この仕組みの説明に置き換えた。`/schedule`への移行検討は打ち切り。
> **2026-07-26 タスク追加(T3-40〜T3-50、ユーザー報告の不具合・要望11件):** 実装はせず記録のみ。内訳: **不具合4件** = カメラが起動しない(T3-40)/画像アップロード後に登録ボタンが押せない(T3-44)/豆登録後の一覧反映が遅い(T3-45)/AI自動入力で焙煎度が入らない(T3-43)。**機能改善・新規5件** = 全画像欄でカメラ選択(T3-41)/焙煎度9段階化・日英対応(T3-42)/メソッドに推奨焙煎度(T3-47)/おすすめレシピにメソッド追加+湯温のメソッド依存化(T3-48)/おすすめレシピの遷移先を030へ変更し引き継ぎ情報を保持・可視化(T3-49)/豆のA/Bテスト希望フラグ+ダッシュボードでの質問(T3-50)。**データ整理1件** = テスト豆等の本番データ削除(T3-46)。**着手前にユーザー確認が必要だった3件は同日中にすべて解消(下記)。**
>
> **2026-07-26 ユーザー確認の結果を反映(T3-42/T3-46/T3-50を改訂、T3-51〜T3-53を追加):** ①**焙煎度は業界標準の8段階を採用**(9段階は不要とユーザー了承)、英語表記はアルファベット表記でOK。あわせて各段階の特徴を解説するページの新設要望を受け**T3-51**を追加。②**テストデータ削除はユーザー承認済み**(実行直前に対象一覧を提示する運用は維持)。③**「A/Bテスト」の狙いは「同じ豆で最適なメソッド・湯温・粒度を知る」+「検証状況の可視化」**と判明。**2案の対照実験(A/Bテスト)という形式は本アプリに適さない**(抽出は1回ごとに条件が変わる逐次実験であり、2群比較より既存F4のベイズ最適化=GP+EIで「次に試すべき条件」を提案するほうが少ない試行で最適に到達できる)と判断し、代替案として**T3-52**(F4の探索次元に粒度を追加+メソッドはメソッド別GPの比較として扱う。**現状F4は湯温・比率・時間の3次元のみで粒度とメソッドが対象外**であることを確認済み)と**T3-53**(試行回数・実測条件の分布・次の推奨条件・探索の進捗度合いの可視化)に分解した。
> **2026-07-25 T3-39完了(Geminiモデル選択設定、`/loop`定期実行、Phase 3残タスク完了):** `https://ai.google.dev/gemini-api/docs/pricing?hl=ja`をWebFetchで参照し実装時点の現行モデル一覧を確認したところ`gemini-1.5-flash`が既に廃止されていたため、既定フォールバック順`_kGeminiModels`を`gemini-2.5-flash→gemini-2.0-flash-lite→gemini-2.0-flash`に更新(1.5-flashを2.0-flashへ置換)。テキスト/画像入力対応の汎用モデルを7件厳選し`kSelectableGeminiModels`として公開(画像/動画/音声生成・embedding・実験的preview限定モデルは除外)。`AiAnalysisService`に`_modelOrder(preferredModel)`ヘルパーを追加し、4つの公開メソッド(`extractBeanInfoFromImage`/`interpretRegression`/`analyzeComponents`/`analyzeComponentsDeep`)に`preferredModel`任意引数を追加(指定時は先頭に挿入、既存フォールバック順は変更なしで後続)。呼び出し元4箇所(`bean_create_screen.dart`/`pca_detail_panel.dart`/`pca_scatter_plot.dart`/`regression_section.dart`)で`shared_preferences`の`gemini_model`キーを読み取り渡すよう統一。新規ページ**043「Geminiモデル設定」**(`lib/screens/gemini_model_screen.dart`)を新設、`RadioGroup`(Flutter新API、`RadioListTile`のgroupValue/onChangedは3.32で非推奨のため使用)で選択・保存し、090のAPIキー欄下に遷移ボタンを追加。検証: `flutter analyze`44件(新規0)、`flutter test`194件全パス(既存191+新規3)。**既存の`test/settings_screen_test.dart`のAPIキー保存テストが新ボタン追加による高さ増分で一時的に壊れ、固定ピクセル数の`tester.drag`では安定した位置に収束しなかったため、`ScrollableState.jumpTo`を使った決定的スクロールループに書き換えて修正**(教訓化、`rules/verification.md`参照)。ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ): 090→「使用するモデルを設定」→043でモデル一覧表示、`gemini-2.5-pro`選択→保存→リロード後も選択状態が永続化されていることを確認、コンソールエラー0件。本番デプロイ完了。**モデル一覧は実装時点でのWeb検索結果を静的にハードコードしたもの(ライブAPI経由の動的取得ではない)。SDK(`google_generative_ai` 0.4.7)にモデル一覧取得APIが無く、Generative Language APIの`models.list`REST直叩きは本タスクの優先度(低)に対してスコープ過大と判断し見送った。** これでPhase 3の細分化タスク(T3-1/T3-4/T3-20のユーザー作業主体を除く)がすべて完了。
> **2026-07-25 タスク追加(T3-39):** ユーザー指示によりGemini APIのモデルを設定(090)から選択できるようにするタスクを追加(実装未着手、タスク追加のみ)。モデル一覧はWeb検索で取得(`https://ai.google.dev/gemini-api/docs/pricing?hl=ja`参照)、専用ページ新設、090のAPIキー欄下にモデル設定ページへの遷移ボタンを追加。ユーザー指定により優先度は低め。
> **2026-07-25 T3-36完了(統計処理の稼働状況一覧ページ新設、`/loop`定期実行):** 新規ページ **042「統計処理の稼働状況」**(`lib/screens/stats_status_screen.dart`)を新設。`AppScreen.statsStatus('042',…)`を追加(トップレベルタブには含めずpush遷移のみ)、`screen_registry.dart`に対応case追加、090「ヘルプ」`FormSection`に「統計処理の稼働状況」`MockListRow`を追加(041のリンクの下)。F1(重回帰)/F2(PCA)/F4(GP)/F5(好み検定)の4機能について、設計書§1.3の最小データ条件をそのまま流用した判定関数(`_f1Status`〜`_f4Status`、いずれも既存サービスの判定ロジック(`buildRegressionMatrix`のn/p算出、`StatisticsService.calculatePca`のn<3ガード、`PreferenceService.build`のグループn、`GpService.fit`のn_eff)を呼ぶだけで新規計算ロジックは発明していない)で「稼働中(緑)/未稼働(赤)」を判定し、未稼働時は必要件数の案内文を表示。各行右端に`StatsTheoryLink`(既存、041の該当セクションへ自動スクロール遷移)を配置。**F4の判定について**: `GpService.fit`の重み付け(設計書§7.5)は非一致データにも最低0.2の重みを与えるため、有効サンプル数n_effは特定の産地×焙煎度の組み合わせによらずほぼ全記録数に連動する。本ページでは「最新記録の産地×焙煎度」を代表値としてフィットを試みる簡略化で判定した(コメントに理由を明記)。検証: `flutter analyze`44件(新規0)、`flutter test`191件全パス(既存188+新規3、`test/stats_status_screen_test.dart`=データ不足時4機能とも未稼働/十分なデータで4機能とも稼働中/本アイコンから041へ遷移)、`flutter build web`成功。**ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ)**: 090→「統計処理の稼働状況」で4機能すべて緑ドット「稼働中」(F1=77件, F2=146件, F4=n_eff47.7, F5=最大グループ16件)と表示、F1の本アイコンから041の重回帰セクションへ正しく自動スクロール遷移、コンソールエラー0件。**本番デプロイ**: `firebase deploy --only hosting`で https://beanbase-app-2016.web.app へ反映(34ファイル)。
> **2026-07-25 T3-38完了(original-dataの本番Sheets移植、`/loop`定期実行):** 着手前にPythonで重複検証を実施: ①`original-data/old/`との差分から新規分がcoffee_data 27件・豆マスター4件と特定(ドリッパー/フィルター/ミル/メソッド/pouring_stepsは差分無し) ②新規27件の`記録ID`は本番coffee_data(146件)と完全重複ゼロ ③ID一致に頼らない安全策として、新規27件の日時(CSVはPacific想定のローカル時刻・本番はUTC ISO、既存143件の突き合わせで+7〜+8h=米国夏時間相当のズレと確認)を±10時間の許容窓で本番全件と突き合わせ、内容面でも近接候補ゼロを確認 ④新規4豆(`f58067af`/`5a57cb8d`/`bf2d1f8d`/`5549c4ad`)も本番bean_masterに不在、新規27件が参照するミル/ドリッパー/フィルター/抽出方法IDはすべて本番マスタに既存で欠落なしを確認。**移植は`tools/migrate_original_data.dart`(新規、`tools/seed_origin_masters.dart`と同じくSheetsServiceを使わない独立HTTPスクリプト)で実施**: 本番の既存ID一覧を取得し、CSVにあって本番に無いIDのみ`action:add`でPOSTする冪等設計(重複登録防止の恒久的な安全策として、一度きりの手動チェックではなくスクリプト自体に組み込んだ)。**ハマった点**: 初回実行時、POSTの302リダイレクトを`package:http`が自動追従せずHTML本文を`json.decode`しようとして例外(rules/verification.md既存教訓と同種の再発)。副作用(1件目の豆追加)はGAS側で成立済みだったため、`curl`で実在確認したうえで`_addRow`に`seed_origin_masters.dart`と同じ302手動フォロー処理を追加し再実行(冪等設計により1件目は自動スキップ、残り3豆+27記録を正常追加)。再実行で「0件追加」を確認し冪等性を検証。最終確認: 本番bean_master 23→27件、coffee_data 146→173件。豆画像(CSVはローカルファイルパス参照でファイル実体が手元に無い)・産地ID(既存の空欄が大半という既知の制約)は移植対象外とした。**ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ)**: ダッシュボードの「直近の抽出5件」に新規記録(パプアニューギニア コルブラン アルージャ、Youth エチオピア等)が正しく表示、豆管理一覧(残量0%表示ON)でも新規豆が確認できる、コンソールエラー0件。コード変更は`tools/`配下の新規スクリプトのみで`lib/`は無変更のため、Flutter Web本番デプロイは不要と判断(データはGAS/Sheets経由で即時反映)。
> **2026-07-25 タスク追加(T3-38):** ユーザーが`original-data/`配下のCSVに最新データ(coffee_data 27件・豆マスター4件を追加、他マスターは旧版`original-data/old/`と同一)を投入。本番Sheetsへの移植をT3-38として記録(実装未着手、タスク追加のみ)。既存本番データとの重複登録防止が必須要件。
> **2026-07-24 T3-34完了(豆マスター画像3分類化+本番デプロイ):** `BeanMaster`に`beanImageUrl`(豆画像)・`infoImageUrl`(情報画像)を追加(既存`imageUrl`はパッケージ画像として維持、データ移行不要)。GAS `bean_master`シートに`豆粒画像URL`・`情報画像URL`列を追加(`EXISTING_SHEET_EXTRA_COLUMNS`、`ensureColumns_`により次回書き込み時に自動プロビジョニング)。012(`bean_create_screen.dart`)を「パッケージ画像」「豆画像」「情報画像(説明書き等)」の3つの`ImageUploadField`(ラベル任意パラメータを新設)に変更、011(`bean_detail_screen.dart`)は`MasterDetailTemplate.extraSections`で豆画像・情報画像のサムネイル2枚を表示するセクションを追加(削除時は3画像ともDriveから削除)。**`dart run build_runner build`がこのマシンのDart SDK(3.10.7)と`analyzer`(7.6.0、Dart言語3.9系までしか対応)のミスマッチで`lib/firebase_options.dart`のリンク時にクラッシュ/ハングする問題に遭遇**(詳細は`rules/verification.md`教訓)。`--delete-conflicting-outputs`で全`*.g.dart`が消えたため、無関係なものは`git checkout`で復元し、`bean_master.g.dart`のみ既存パターンに倣って手動編集(json_serializableの生成コードは定型的で手編集が安全)。検証: `flutter analyze`44件(新規0)、`flutter test`187件全パス(既存181+新規6)、`flutter build web`成功。ブラウザ確認(ローカル配信+claude-in-chrome、本番GAS実データ): 012で3つのアップロード欄(パッケージ画像/豆画像/情報画像(説明書き等))が表示、011で「豆画像・情報画像」セクションが表示、コンソールエラー0件(Service Workerキャッシュが一度残っていたため既存の教訓通りunregister+cache削除で解消)。**本番デプロイ**: `clasp push`+`clasp redeploy`で既存GAS Web App(URLは維持)に新2列のプロビジョニングを反映(@11)、`firebase deploy --only hosting`で https://beanbase-app-2016.web.app へ反映。本番GAS実データ(残量50%テスト豆)で011/012とも新UIが正常表示されコンソールエラー0件を確認(実際に画像をアップロードして本番Sheets/Driveへ書き込むE2Eはユーザーのローカル確認に委ねる)。
> **2026-07-24 タスク追加(T3-34〜T3-37):** ユーザーから5件の追加要望を受け、4タスクに分解して記録。①豆マスター画像のパッケージ/豆/情報の3分類化(T3-34)②豆情報読取AI(T3-30)へのカメラ撮影追加+撮影画像の情報画像としての保存(T3-35、T3-34に依存)③統計処理のon/off状況一覧ページ新設(T3-36)④YouTube埋め込み(T3-31で一度対応済みだが依然不具合との報告)の再修正(T3-37)。
> **2026-07-24 T3-37完了(YouTube埋め込み再修正):** ユーザー報告「灰色の背景はあるがそれ以外は何も表示されない」を、ローカルでビルド+claude-in-chromeで実データ(本番GAS)を使い再現。**`flutter run`(debugモード/DDC)では再現せず`flutter build web`(release/dart2js)でのみ再現**することを突き止め、コンソールに出ていた`Null check operator used on a null value`のスタックトレースを`--source-maps`付きビルドと自作VLQデコーダで解析した結果、`youtube_player_iframe`のコントローラ構築時に呼ばれる`webview_flutter`の`NavigationDelegate()`内部で参照する`WebViewPlatform.instance!`が、Flutter Webの自動プラグイン登録(`WebYoutubePlayerIframePlatform.registerWith`)の反映前にreleaseビルドでのみnullのまま評価されクラッシュしていたことが根本原因と判明(T3-31のClipRRect説は誤りだったと判明)。`lib/utils/youtube_web_platform_fix_web.dart`(+`_io.dart`のno-op/条件分岐)を新設し、`YoutubeEmbed.initState()`で`WebViewPlatform.instance ??= WebYoutubePlayerIframePlatform()`を明示的に保険としてセットする防御的修正で対応(自動登録が効いていればno-op)。`webview_flutter_platform_interface`・`youtube_player_iframe_web`をpubspec.yamlの直接依存に追加(既存の解決バージョンのまま、transitive→directへの明示化のみ)。修正後、同一のreleaseビルドで再現テストしたところクラッシュが消え、`state=unStarted→cued`まで正常遷移し、YouTubeのサムネイル・タイトルが正しく表示されることを確認(**実際のクリック→再生自体は、このサンドボックスのCDP制御下ブラウザではクロスオリジンiframeへの操作が反映されない既知の制約のため確認できず、ユーザーのローカル/実機での最終確認が必要**)。`flutter analyze`44件(新規0)・`flutter test`181件全パス。ユーザー指示「ユーザーが操作することなく全て自分でできるように」に従い、実機確認を求めずにclaude-in-chromeでの再現・修正・検証まで完結させた。
> **2026-07-22 T3-9・T3-13・T3-31・T3-32・T3-33完了(Phase 3の残タスク一括対応):** ユーザー指示で残タスク一覧提示後にT3-9/T3-13を一括着手、加えて①モバイル実機で確認されたYouTube埋め込み不具合の修正②本番画面コードバッジの削除③設定→統計解説ページへの導線追加、の3件を新規タスク(T3-31〜33)として記録・実装した。
> - **T3-9完了(メインカラー反映拡大)**: 影響範囲調査の結果、コーヒートーン定数(`kEspresso`等)は27ファイルに直接ハードコードされており全面的な動的化は過大なリスク(L超の別タスク相当)と判断。**AppBar背景(`MockScreenScaffold`/`CreateFormScaffold`、全22画面の骨格)・保存ボタン色・黒板風背景(ダッシュボード)** の3点を`mainColorProvider`から導出するように変更し、技術的制約(カードの罫線・チップ・グラフ配色等の細部は対象外)を090の説明文に明記する形で完了とした(タスクの完了条件が許容する代替パス)。`theme_provider.dart`に`boardBackgroundFor(mainColor)`(色相はメインカラー由来、明度はチョーク文字が読めるよう0.22に固定)を追加。`mainColorPresets`の1色目を実際の`kEspresso`値(`0xFF3E2723`、従来`0xFF6D4C41`とズレていた)に修正し、未変更時の見た目が変わらないようにした。`MockScreenScaffold`/`CreateFormScaffold`を`ConsumerWidget`化。`BlackboardTexture`に`background`引数(デフォルト`kBoardBg`)を追加。
> - **T3-32完了(本番の画面コードバッジ削除)**: `MockScreenScaffold`・`CreateFormScaffold`のAppBarタイトルから画面コード(例:「001」)のバッジ表示を削除し、画面名のみに変更(T3-9のConsumerWidget化と同時に実施)。090「Debug」内の「画面一覧」(`screen_gallery_screen.dart`)は開発者向けの画面インベントリ表示が目的のため対象外のまま残した。バッジ表示に依存していた`test/master_switcher_test.dart`・`test/screen_transition_test.dart`のアサーションをタイトル文字列ベースに更新。
> - **T3-33完了(設定→統計解説ページへの導線)**: 090に新設「ヘルプ」`FormSection`から`StatsTheoryScreen`(041)へ`MockListRow`タップで直接遷移できるようにした。
> - **T3-31完了(YouTube埋め込みのモバイル実機不具合修正)**: ユーザーがモバイル実機で確認したところ埋め込みプレーヤーが表示されなかった。WebSearchで先行事例を調査した結果、`lib/widgets/youtube_embed.dart`が`YoutubePlayer`(Web版はHtmlElementViewでiframeを描画するプラットフォームビュー)を`ClipRRect`で角丸クリップしていたことが原因と判断(`HtmlElementView`を`ClipRRect`/`ClipRect`で囲むとプラットフォームビューが描画されなくなる既知のFlutter課題、flutter/flutter#91191・#91805・#161094)。`ClipRRect`を除去し直接描画するように修正(角丸が無くなる以外の見た目・動作への影響は無い)。**実機での再生確認自体はユーザーのローカル環境でのみ可能**(このサンドボックスはクロスオリジンiframeのプラットフォームビューを描画できないため)。
> - **検証**: `flutter analyze`44件で不変(新規0)。`flutter test`**181件全パス**(既存テストの修正のみで新規追加は無し。上記バッジ削除・`ConsumerWidget`化に伴い`test/stats_theory_screen_test.dart`の3ケースに`ProviderScope`ラップを追加、`test/master_switcher_test.dart`・`test/screen_transition_test.dart`のバッジアサーションを更新)。`flutter build web`成功。**未確認(要ユーザーローカル確認)**: YouTube埋め込みの実機再生確認、T3-9のAppBar/黒板背景の実際の見た目確認(このサンドボックスはCanvasKitのプラットフォームビュー・黒板背景色の目視は可能だがモバイル実機固有の描画までは確認できない)。
> - **別途、T3-13完了(デプロイ手順のドキュメント化)**: `docs/deploy.md`新規。build→deployの2コマンド・公開URL・デプロイ後確認手順・既知の教訓(Service Workerキャッシュ・サンドボックスからの本番確認代替手順)をまとめた。
> - **本番デプロイ+本番確認+commit/push完了(ユーザー指示)**: `firebase deploy --only hosting`で https://beanbase-app-2016.web.app へ反映後、同一`build/web`をローカル配信(未使用ポート)し本番GAS実データで再確認(001のバッジ消失+黒板背景色味変化、090「ヘルプ」→041遷移、020のYouTube埋め込み領域とプレーヤー初期化ログ、いずれもコンソールエラー0件)。commit `9a01836`でpush済み。
>
> **2026-07-22 T3-30完了(豆情報のGemini Vision抽出):** `lib/services/ai_analysis_service.dart`に`ExtractedBeanInfo`(name/store/origin/roastLevel/type/roastDate、各項目nullable)と`extractBeanInfoFromImage()`を追加。`GenerationConfig(responseMimeType:'application/json', responseSchema: Schema.object(...))`で構造化出力を強制し(`google_generative_ai` 0.4.7で対応確認済み)、`Content.multi([TextPart(prompt), DataPart(mimeType, imageBytes)])`で画像+プロンプトを送信。読み取れない項目は必ずnullにする指示をプロンプトに明記し、既存の`_kGeminiModels`フォールバック順(2.5-flash→2.0-flash-lite→1.5-flash)を踏襲。**012(`bean_create_screen.dart`)にボタン「パッケージ画像から自動入力(AI)」を新設**(基本情報セクション先頭)。`file_picker`でローカル画像を選択→Gemini抽出→抽出できた項目のみ(nullは既存値を維持)フォームへ反映。産地は`OriginMaster`一覧との完全一致/部分一致で`_selectedOriginId`に解決を試み、一致しなければ未選択のままスナックバーで「産地「X」は既存産地に一致しなかったため未選択」と案内(新規産地追加は既存の「新規産地追加」ボタンに委ねる、専用ページは作らない設計どおり)。APIキー未設定時は既存のPCA深掘り解釈(`pca_detail_panel.dart`)と同じダイアログパターンでその場で入力・保存できる。抽出失敗(ネットワークエラー・JSON不正等)は赤スナックバーでエラー表示し、フォームは一切変更しない(手動修正前提の設計どおり)。`test/bean_create_screen_test.dart`にボタン描画の1ケース追加。検証: `flutter analyze`44件で不変(新規0)、`flutter test`170件全パス(+1)、`flutter build web`成功。**ブラウザ確認**: ローカル配信+claude-in-chromeで012画面を開き、基本情報セクション先頭に新ボタンが正しく描画されコンソールエラー0件を確認(初回スクリーンショットでボタンが写らなかったのはナビゲーション直後でレイアウトが未確定だった一時的な事象で、1秒待機後の再確認では正常描画)。**未確認(要ユーザーローカル確認)**: `file_picker`はOSネイティブのファイル選択ダイアログを開くため、この環境(claude-in-chrome)では実際の画像選択→Gemini Vision呼び出し→抽出結果のフォーム反映という一連の流れを自動操作できない。実際のパッケージ画像・有効なAPIキーでの抽出精度・産地マッチング挙動は`flutter run -d chrome`でのユーザー確認が必要。
> **2026-07-22 T3-27完了(統計理論説明ページ新設+デプロイ+本番確認):** 統計処理の理論を機能ごとに解説する専用ページ **041「統計の理論と読み方」**(`lib/screens/stats_theory_screen.dart`新規)を新設。`AppScreen`に`statsTheory('041',…)`を追加(トップレベルタブには含めずpush遷移のみ)、`screen_registry.dart`に対応caseを追加。ページは目次(ActionChip)+7セクション(はじめに/信頼区間と予測区間/重回帰F1/PCA F2/好み検定F5/GP・EI F4/レシピ提案F3)で構成し、各式は設計書§2の式番号(T-1〜T-25)をそのまま引用して実装と整合。導線(`StatsTheoryLink`=本アイコン、`FormSection.trailing`スロットに配置)を **040のPCA/回帰/好みセクション・030のレシピ探索(gp_explorer)・003の評価表示** に設置。タップで該当セクションへ自動スクロール(`Scrollable.ensureVisible`)して開く。**実装上の注意**: `MockScreenScaffold`内のListViewは画面外の子を遅延生成しGlobalKeyのcontextがnullになるため、全セクションを単一の`Column`で一括ビルドしてensureVisibleを確実に効かせた。`test/stats_theory_screen_test.dart`新規3ケース(全セクション+式番号描画/initialSection起動/リンク遷移)。検証: `flutter analyze`44件で不変(新規0)、`flutter test`169件全パス(+3)、`flutter build web`成功。**本番確認**: `firebase deploy --only hosting`で https://beanbase-app-2016.web.app へデプロイ後、本番ビルドをローカル配信し実データ(146記録)で確認。040の回帰セクション見出し右端の本アイコンをクリック→041が回帰セクションへ自動スクロールして開き、目次ジャンプ・全7セクション(GP EI式/F3提案含む)末尾まで正常描画、コンソールエラー0件。**ハマった点(教訓)**: Flutter Web(CanvasKit)のListViewはCDPのマウスホイール/キーボードイベントで安定してスクロールしない。`flt-glass-pane`要素へJSで直接`WheelEvent`をdispatchする方法(`document.querySelector('flt-glass-pane').dispatchEvent(new WheelEvent('wheel',{deltaY,clientX,clientY,bubbles,cancelable}))`)なら実機同様にスクロールできたが、多数連投すると描画アーティファクト/レンダラ一時フリーズ(CDP screenshot timeout)が起きるため、少量ずつ+間に待機を挟むこと。
> **2026-07-22 T3-29・T3-28完了:** T3-29は評価画面(031、`brew_evaluation_screen.dart`)のスコアセクションに情報アイコン(`FormSection`にオプションの`trailing`引数を新設して配置)を追加し、タップで「評価記録時の注意点」ダイアログ(初期値7バイアス/主観スコアの基準一貫性/同一環境での評価/好みプロファイル自動更新の4点、文面はユーザー承認済み)を表示。専用ページは作らず既存の回帰「分析上の注意」ダイアログと同型。`test/brew_evaluation_test.dart`に1ケース追加。T3-28は原因が **`main.dart`の`GoogleFonts.outfitTextTheme()`(ラテン専用)+ロケール未指定** で、CanvasKitのHan統合フォント選択が中国語字形(Noto Sans SC)を選んでいたためと特定。`flutter_localizations`を追加し`MaterialApp`に`locale: Locale('ja')`/`supportedLocales`/`localizationsDelegates`(Global*Localizations)を設定して日本語字形(Noto Sans JP)を優先させた(`intl`も0.19→0.20.2にバンプ、`flutter_localizations`の要求)。`flutter analyze`44件で不変(新規0)、`flutter test`166件全パス(+1)、`flutter build web`成功、`widget_test.dart`のApp launch smoke testが新ローカライゼーション込みの`MyApp`で起動成功。**字形の最終目視確認(zh→jaの字形差)はCanvasKit実行時+人の目でしか判定できないため、ユーザーのローカル`flutter run -d chrome`に委ねる。** T3-27(統計理論ページ)・T3-30(画像から豆情報抽出)は未着手。
> **2026-07-22 ユーザーからの追加修正要望4件(T3-27〜T3-30)を記録:** ①統計処理の理論説明ページ新設(各機能付近から遷移、上位モデルで内容検討)②日本語でない漢字表記の全画面点検・修正(フォント/ロケール起因の可能性を着手時に切り分け)③評価記録時の注意点をクリックでダイアログ表示(専用ページは作らない、文面は要確認)④豆の説明カード等の画像からGemini Visionで豆情報を抽出し012フォームにプリフィル。いずれも未着手、実装はせず記録のみ。

> **2026-07-22 T3-24完了(YouTube埋め込み実装、T3-3の「見送り」判断を撤回):** ユーザー相談の結果「埋め込みしたい・追加パッケージOK・保存先は既存`sourceUrl`流用・埋め込み+リンク併記」で合意し実装した。**パッケージ`youtube_player_iframe ^6.0.2`を追加**(Web/Android/iOS/macOS対応・IFrame Player API・APIキー不要。T3-3の見送り理由=追加パッケージ or `dart:html`自作iframeが必要というリスクを、確立されたクロスプラットフォームパッケージで解消)。実装: (1)`lib/utils/youtube_util.dart`新規=`youtubeVideoId(url)`/`isYoutubeUrl(url)`。抽出は`YoutubePlayerController.convertUrlToId`に委譲するが、同関数が`^https://`固定でスキーム無し/`http://`を弾くため、先に`_normalizeScheme`で正規化してから渡す(watch/youtu.be/embed/shorts/music対応、`?si=`等の追加クエリも先頭11文字IDを抽出)。(2)`lib/widgets/youtube_embed.dart`新規=`YoutubeEmbed`(StatefulWidgetでコントローラのlifecycle保持、`fromVideoId(autoPlay:false)`でcue、16:9・全画面ボタン付き、`dispose`で`close`)。(3)`method_detail_screen.dart`の「参考URL」`FormSection`で、`youtubeVideoId(sourceUrl)`が非nullなら埋め込みプレーヤーを表示し**その下に従来の外部リンクも残す**(YouTube以外はリンクのみ=従来どおり)。**データ層(モデル/GAS/シート列)の変更は一切なし**=既存の頻発バグ「モデル追加時の列プロビジョニング漏れ」を回避。検証: `flutter analyze`44件で不変(新規0)、`flutter test`170→181件全パス(+11、`test/youtube_util_test.dart`新規=watch/youtu.be/embed/shorts・http正規化・スキーム無し救済・空白トリム・非YouTube/null判定)、`flutter build web`成功(Windowsのsymlink警告はネイティブプラグイン用でWebは`youtube_player_iframe_web`が実装を提供するため無影響)。**ブラウザ確認(本番ビルドをローカル配信+claude-in-chrome)**: 020「ORIGAMI ウェーブ 基本」(本番`sourceUrl=https://youtu.be/dpYaU8LfwG4?si=…`)を開くと注湯ステップ下に**16:9のプレーヤー領域が確保・表示**され、コンソールに`[Antigravity] Action: YouTube埋め込みプレーヤー初期化 (videoId=dpYaU8LfwG4)`=実データの`youtu.be/…?si=…`から動画IDが正しく抽出されコントローラ初期化まで成功。**ハマった点(教訓)**: 最初プレーヤーが出なかったのはFlutterの**Service Workerが旧`main.dart.js`をキャッシュ**していたため。`navigator.serviceWorker.getRegistrations()`→`unregister()`+`caches.delete()`してから再読込で新ビルドが反映された(ローカル配信で新機能を確認するときは要注意)。**未確認(ユーザーのローカル確認が必要)**: 実際の動画再生映像のDOM上のiframe(platform-view)は、CanvasKitのplatform-view+クロスオリジンYouTube iframeがこのCDP/拡張サンドボックスでは描画されないため目視できず(引き継ぎ既出の制約)。Dart側結線は実データで全て正常動作しているため、実再生の最終目視は`flutter run -d chrome`に委ねる。実装commit/push後、ユーザー指示で`firebase deploy --only hosting`により**https://beanbase-app-2016.web.app**へ本番デプロイ(34ファイル)、デプロイ成果物をローカル再配信して本番データで020埋め込みを再確認(同一ビルドのため修正不要)。
> **2026-07-21 T3-23完了(本番書き込み・デプロイ・本番確認、ユーザー承認済み):** ダッシュボード残豆量の動作確認用ダミーデータを本番Sheetsに登録した。**着手時に判明した根本原因: 本番`bean_master`シートに`初期購入量(g)`列そのものが存在せず**(Cycle 20 T2-2bで`BeanMaster.initialQuantityGrams`とSheetsServiceの`reverseMap('初期購入量(g)')`は実装済みだったが、GAS `EXISTING_SHEET_EXTRA_COLUMNS`への列プロビジョニングが漏れていた。T4-1b/T4-2dと同型の「モデル追加時に列追加を忘れる」バグの再発)、全豆で初期量が未保存=`calculateBeanRemainingPercent`が常に0を返す状態だった。対応: (1)`gas/Code.gs`の`EXISTING_SHEET_EXTRA_COLUMNS['bean_master']`に`初期購入量(g)`を追加→`clasp push`+`clasp redeploy`(デプロイ`@9`→`@10`、URL維持)。(2)ダミー豆「残量50%テスト豆(T3-23)」を初期購入量200gで登録し、当該豆に`豆の量(g)=100`の抽出記録1件を登録(=残量50%)。列追加は次POST時の`ensureColumns_`で自動反映。(3)`flutter build web`→**Firebase Hosting(`beanbase-app-2016.web.app`)へデプロイ**。(4)本番ビルドをローカル配信して実データで確認(拡張機能が本番ドメインをブロックするため。ビルド成果物・データ(本番GAS)は本番と同一): 001残豆量に50%の豆ジャー表示、001おすすめレシピカード(T4-6c)描画、003評価ヒーローカード+レーダー(T3-26)描画、030「レシピ探索(実験的)」GPヒートマップ(T4-6b)が産地×焙煎度選択で実データ予測を描画、いずれもコンソールエラー0件。`flutter analyze`44件(不変)、`flutter test`165件全パス(Dart変更なし)。**別途発見(未対応、要記録)**: `bean_master`の`type`(豆の種類)のreverseMapは`'豆の種類'`だが本番シート列名は`'豆種類'`(のなし)で不一致、かつシートに`'豆の説明'`列があるがモデルに対応フィールド無し。T3-23スコープ外のため未修正だが、豆の種類が本番で永続化されない既存バグの可能性あり。
> **2026-07-21 T3-21・T3-22・T3-25完了:** T3-21は実データをブラウザ確認したところ「直近の抽出」セクション自体にはハードコードされた非日本語文字列は無く(そこに表示される"Navy"等は豆銘柄・メソッド名に含まれるユーザー登録データの固有名詞のため対象外と判断)、タスク文で示唆されていたウェルカムボードの見出し`"Today's BeanBase ☕"`→`"今日のBeanBase ☕"`に修正した。T3-22は001「直近の抽出」各行、T3-25は`master_template.dart`(共通実装)経由の全マスター詳細画面「関連する抽出履歴」各行のアイコンを、T3-14(002)と同じパターン(`MockListRow`の`imageUrl`引数)で豆画像化した。`flutter analyze`新規issue0件(44件のまま)、`flutter test`全件パス(155件)、`flutter build web`後にローカル配信+ブラウザで001・011の実データ表示(画像読み込み・見出し変更)を確認済み。T3-23(本番書き込み要確認)・T3-24(要再相談)・T3-26(上位モデルでのデザイン検討要)は未着手のまま。
> **2026-07-21 ユーザーからの追加修正要望6件(T3-21〜T3-26)を記録:** 001(ダッシュボード)の直近抽出セクションの表記言語・アイコン画像化・残豆量ダミーデータ登録、020のYouTube再生再検討、Masters全詳細画面の関連抽出履歴アイコン画像化、003の評価表示デザイン改善(上位モデルでの検討が前提)。いずれも未着手、実装はせず記録のみ。

> **2026-07-20 Ubuntu並行開発の要望を記録:** プロジェクトルール・グローバル相当の設定(`CLAUDE.md`/`rules/verification.md`/`.claude/`一式)は元々すべてリポジトリにコミット済みで追加pushは不要と確認(git statusはclean)。Ubuntu側で新たに必要となるのはマシンローカルな環境構築(SDK・ブラウザ・SSH鍵・APIキー再入力)のみのため、T3-20として追加。詳細はNEXT_SESSION.md参照。
> **2026-07-20 ユーザーからの追加要望6件(T3-14〜T3-19)を記録:** 画像一括インポート完了のフィードバックと合わせて、抽出履歴のアイコン画像化・030→031のメソッド未選択遷移許可・031選択リストの画像表示・031での引き継ぎ項目編集可+湯温入力方式変更・味わい欄の4:6メソッド限定・マスター管理画面間の相互遷移、の6件を新規タスクとして追加。いずれも未着手(コストガードレール超過のため本日は実装せず記録のみ)。
> **2026-07-20 T3-15〜T3-18完了:** 030/031関連の4タスクをまとめて実装。`PendingBrewInfo.method`を`nullable`化し030でメソッド未選択でも031へ進めるようにした(T3-15)。031に豆/グラインダー/ドリッパー/フィルター選択リストの画像サムネイル表示を追加した(T3-16、`BeanImage`ウィジェット流用)。031にメソッド・豆量・総湯量の編集欄を追加し、湯温は030から引き継がず031で新規入力する運用に変更した(T3-17)。「味わい」欄はメソッド名に「4:6」を含む場合のみ表示・保存するようにした(T3-18)。詳細はNEXT_SESSION.md参照。
> **2026-07-20 T3-14完了:** 002(抽出履歴)の一覧行左側アイコンを、該当する豆マスターの画像(`MockListRow`が既に対応していた`imageUrl`引数を渡すだけ)に変更。ブラウザで実データ(本番Sheets/Drive)接続確認済み、画像ありの豆は実際の商品写真、未設定の豆はプレースホルダのまま表示されることを確認。
> **2026-07-20 T3-19完了:** これでユーザー要望6件(T3-14〜T3-19)すべて完了。`lib/screens/master_template.dart`に`MasterSwitcherButton`(AppBarのアイコン→他マスター一覧へのポップアップメニュー、自分自身の種別は除外)を新規実装し、`MasterListTemplate`/`MasterDetailTemplate`のAppBarへ自動的に組み込んだ(ドリッパー/フィルター/グラインダー/メソッドの一覧・詳細、豆の詳細が対象)。テンプレートを使わない`BeanListScreen`(豆一覧)のみ1行追加で個別対応。ブラウザで実データ確認済み(010→013への切り替え遷移)。
> **2026-07-13 方針追加:** モバイルはネイティブアプリ化せず、Web版ホスティング+ホーム画面ピン留め(PWA)で対応する(§1「モバイル利用の決定」参照)。これに伴い T3-10〜T3-13 を追加し、T3-1 の依存を「Phase 2 完了」→「T3-12」に変更(実機確認はデプロイ先URLで行うため)。
> **2026-07-18 T3-12関連の実機指摘対応(T3-12自体は未完了のまま):** ユーザーがスマホ実機でデプロイ先URLを確認した際の指摘4件を修正しデプロイ済み。詳細は NEXT_SESSION.md 参照。ホーム画面ピン留め・Gemini APIキー再入力・AI分析動作確認はまだ未実施のため T3-12 は引き続き⬜。
> **2026-07-19 画像一括インポート不具合:** 原因はGAS側のDrive OAuth権限未承認と判明(コードのバグではない)。ユーザー対応1回目は未解決。詳細・次の手順は NEXT_SESSION.md 参照。T3-12は「スマホでホーム画面ピン留め」まで進捗(ユーザー報告)、APIキー再入力・AI分析確認は未実施のため引き続き⬜。
> **2026-07-20 画像一括インポート不具合(続報):** Drive権限問題はGoogleアカウントのアクセス権リセットで解消。残る原因は`DRIVE_FOLDER_ID`の値の誤り(正しくは`1Hs8d36riqqkl9qrojuGlZpkIAMim-fou`)と判明、修正・再デプロイ手順を提示済み(ユーザー実施待ち)。詳細は NEXT_SESSION.md 参照。Flutter側のコード変更は無し。
> **2026-07-20 画像一括インポート「常にSkipped」の別バグを発見・修正:** `importMasterImages`がグラインダー/ドリッパー/フィルターのProviderをread時にawaitせず、未fetch時にnullを空リスト扱いしていたため常にスキップされていた不具合。`ref.read(xxxProvider.future)`で修正・ブラウザで再現確認・commit済み(`aadc2fc`)。DRIVE_FOLDER_ID修正(上記)が完了すればアップロードも成功する見込み。詳細は NEXT_SESSION.md 参照。
> **2026-07-20 T3-12完了:** ユーザーがDRIVE_FOLDER_ID修正・GAS再デプロイを実施し、Gemini APIによるAI分析の動作も実機で確認できたため、T3-12の終了条件(ホーム画面アイコンからの起動・データ表示・AI分析動作)を満たした。あわせて判明した「実際に権限修正が反映されていたのは新しく作成したデプロイ側のURLのみ」だったため、`kGoogleSheetsApiUrl`をそちらに更新・commit・push・`firebase deploy`まで完了。
> **2026-07-20 画像一括インポート「全件Failed」の別バグを発見・修正:** T3-12完了後もユーザーが実際に試すと(旧Skippedから)全9件Failedに悪化していた。原因は`image_service.dart`の`uploadImage`/`deleteImage`がGAS WebAppへのPOSTで`Content-Type: application/json`を使っており、実ブラウザからはCORSプリフライト(OPTIONS)がGAS側で処理されず`TypeError: Failed to fetch`していたため(`curl`はプリフライトをしないため同じリクエストが常に成功し、`_postData`側は既に同種の対策済みだったため見落とされていた)。両メソッドを`sheets_service.dart`の`_postData`と同じ`text/plain`に変更し、ローカルビルド+実データでドリッパー「HARIO V60 NEO 02」(5bf221c7)への実アップロードで`Success: 1, Failed: 0`・Sheets側`ドリッパー画像URL`がDrive URLに更新されることを確認済み。詳細は`rules/verification.md`の教訓・NEXT_SESSION.md参照。
> **2026-07-12 T3-2:** 検証のみで完了。豆/ドリッパー/フィルター/グラインダー全4マスターの新規・編集画面は既に共通ウィジェット`ImageUploadField`(`lib/widgets/image_upload_field.dart`)経由でアップロードしており、追加のDRY化は不要と判断。
> **2026-07-12 T3-3:** 見送り。動画埋め込み(YouTube等)には追加パッケージまたは`dart:html`によるWeb専用iframe実装が必要で、既存の依存にはなく安定性リスクが高いため実装を見送った。020の「参考URL」は引き続き外部ブラウザで開くリンクのまま。
> **2026-07-18 T3-10:** 完了。`web/manifest.json`のname/short_nameを「BeanBase」に、theme_colorをkEspresso(#3E2723)、background_colorをkLatte(#D7CCC8)に変更(既存のコーヒートーン配色`create_form_widgets.dart`と統一)。`web/index.html`のtitle/description/apple-mobile-web-app-titleも更新。アイコン(192/512/maskable 192/512)とfaviconは、外部画像素材が無かったため、コーヒー豆をモチーフにした図形をPythonの標準ライブラリ(zlib/struct)のみでPNGとして生成(kEspresso背景+kLatte豆型+クレース線)。maskable版は安全領域(中心80%)に収まるよう豆を小さめに配置。`flutter build web`→ローカルHTTPサーバー+Playwrightで実データ接続の起動・タイトル表示・コンソールエラー無しを確認。
> **2026-07-18 T3-11:** 完了。`firebase-tools`は既にログイン済み(Cycle 18のFirestore設定時の認証が残存、`beanbase-app-2016`プロジェクトへアクセス可能だったため今回のセッションでは`firebase login`不要だった)。`firebase.json`に`hosting`セクション(public: `build/web`、SPA向けrewrite: 全パス→`/index.html`)を追加、`.firebaserc`を新規作成(default project: `beanbase-app-2016`)。`flutter build web`→`firebase deploy --only hosting`で初回デプロイし、**https://beanbase-app-2016.web.app** が公開された(ユーザーに公開の了承を得た上で実施。認証なしで誰でもアクセス可能な状態)。Playwrightでデプロイ先URLに実際にアクセスし、実データ(本番Sheets、GAS経由)のダッシュボードが正常表示・コンソールエラー0件(Service Workerの起動待ちタイムアウト警告のみ、Flutter Web PWAの既知の無害な事象)を確認、GAS/Driveへの疎通も問題なし。

### Phase 4 — 統計解析・予測機能拡張（Cycle 28+、最後）

> **2026-07-20 タスク分解:** 旧 T4-1(PCA拡大)・T4-2(AI提案)は、ユーザーから提示された設計書 `statistics_feature_design.md`(版1.0)により、F0〜F6の7機能・詳細仕様(数式・クラス名・テスト期待値まで指定)に具体化された。旧T4-1はF2、旧T4-2はF3/F4に相当するため、下表に統合し置き換える。
>
> **設計書は単一の正本**(CLAUDE.md「統計解析・予測機能の実装ルール」参照)。本表と食い違う場合は設計書が優先。**設計書§0の絶対規則によりサブPhase順(0→1→2→3→4→5→6)を厳守**し、各サブPhaseはテスト全パス(設計書§9)まで次に進まない。ID の数字はこのサブPhase番号に対応(例: T4-2xはサブPhase2=F1回帰分析)。
>
> **Phase 終了条件:** F0〜F6すべてが実装され、設計書§9の全テストがパスし、Phase 1完了時点で必要なユーザー作業(実データ移行の確定、GAS `scriptId` 記入と初回`clasp login`)が完了している。
>
> **2026-07-21 T4-6a完了(F4 GP推薦エンジン、サブPhase6開始):** `lib/services/gp_service.dart`新規(設計書§7.5)。`GpModel`/`GpPrediction`/`GpService`(fit/predict/optimize)。RBFカーネル(T-16)+観測ごとの重み付きノイズ(σ_n²/wᵢ、同一グループ1.0/同産地・焙煎差1以内0.5/その他0.2)でK行列を組み、Cholesky分解(既存`linear_solve.dart`)でα=K⁻¹y・予測分散(T-19)を計算。固定グリッド(ℓ/σ_f/σ_n各3値、計27通り)で対数周辺尤度(T-20)最大のハイパラを選択。EI(期待改善量、T-21)は独立関数`expectedImprovement`として公開。**設計書のクラス定義には無いが、テスト容易性のため`fitWithParams`(θ固定・グリッド探索やCoffeeRecordパイプラインを介さず直接フィットする入口)を追加した(regression_service.dartの`fitDesign`と同じ理由づけ)。** §9.5の3テストケース(訓練点上でmean≈y・sd<1e-2、遠方点でsd≈σ_f、EIの3性質)はいずれも数値実例をこちらで構築する必要があったため、事前に`python`(numpy)でフィクスチャを検証してからDartテストを作成(誤差1e-12〜1e-6で一致)。`test/gp_service_test.dart`新規5ケース全パス。`flutter analyze`新規issue0件(44件のまま)、`flutter test`全件パス(150→155件)、`flutter build web`成功。UI未接続(T4-6bで030画面`gp_explorer_section.dart`に接続予定)のためブラウザ確認は対象外(既存の教訓通り)。
>
> **2026-07-21 T4-5b完了(F3レシピ提案カード、サブPhase5完了):** `lib/widgets/dashboard/recipe_suggestion_card.dart`新規(設計書§7.4)。**表示対象豆の選定方針(設計書に明記が無く着手時に決定)**: 在庫豆(`calculateBeanRemainingPercent`>0)のうち`SuggestionService.suggestFor`が提案を返せる豆を、最終使用日が古い順(未設定はさらに古い扱い)に最大3件、縦積みカードで表示。カード内容は豆名+湯温/湯:豆比/時間チップ+推奨焙煎度(§7.4後半、F5 `PreferenceProfile`から当該産地で最も平均が高くn≥3のグループの焙煎ラベル)+一致バッジ。`[この条件で淹れる]`は`accepted='yes'`で`saveRecipeSuggestion`→031へプリフィル遷移、`[今回はパス]`は`accepted='no'`で保存しカード非表示。ダッシュボード(001)の残豆量より前に配置。**提案→淹れる→記録→resultRecordId紐付け**: `PendingBrewInfo.temperature`(任意)を追加し031の湯温をプリフィル、`BrewEvaluationScreen`に`pendingSuggestion`(任意)を追加して最初の記録保存成功時に`updateRecipeSuggestion`で`resultRecordId`を書き戻す(`_suggestionLinked`で連続記録の2件目は非上書き、失敗は記録保存を妨げない)。`test/recipe_suggestion_card_test.dart`新規4ケース+`test/brew_evaluation_test.dart`に書き戻しE2E 1ケース追加で終了条件のE2E経路を担保。`flutter analyze`新規issueなし(44件のまま)、`flutter test`全件パス(145→149件)、`flutter build web`成功。実データでの実ブラウザ手動E2EはサンドボックスがGASへ到達できないためユーザーローカル(`flutter run -d chrome`)に委ねる。**これでサブPhase5(F3、T4-5a・T4-5b)が完了**、設計書§0のPhase順によりサブPhase6(F4 GP推薦、T4-6a `gp_service.dart`)へ進める。
>
> **2026-07-21 T4-5a完了(F3提案、group_bestロジック):** 在庫豆定義は`lib/utils/bean_stock_calculator.dart`の`calculateBeanRemainingPercent`(既存、残量>0判定)で解決済みと確認。`lib/services/suggestion_service.dart`新規、`SuggestionService.suggestFor(bean, records, originById)`(originId×roastOrdinalMapの一致でグルーピングし、同グループの過去最高スコア記録の条件を提案、rationale='group_best')。**GP推薦エンジン(T4-6a〜c)未接続のためフォールバック経路のみ実装**、GP接続時に手順1(n_eff≥10でμ最大点)を追加予定。`test/suggestion_service_test.dart`新規(7ケース)。`flutter analyze`新規issueなし(44件のまま)、`flutter test`全件パス(138→145件)、`flutter build web`成功(UI未接続のためブラウザ確認は対象外)。次はT4-5b(recipe_suggestion_card.dart、ダッシュボード)。
>
> **2026-07-21 T4-4b・T4-4c完了(F5好みプロファイルの自動保存フック+UI、サブPhase4完了):** `brew_evaluation_screen.dart`の評価登録処理(`_submit()`)に、記録保存成功後`PreferenceService.build()`→`AnalysisSnapshot(type:'preference')`保存の自動フックを追加(失敗は記録本体の保存を妨げずSnackBar通知のみ)。`PreferenceProfile`/`PreferenceGroupStat`に直列化用`toJson()`を追加(設計書に無いが`payloadJson`要件を満たすため必須、T4-2aと同種の拡張)。`lib/widgets/statistics/preference_section.dart`新規(statementsカード、産地×焙煎グループ統計テーブル(n<5は「n不足」バッジ)、履歴タブ(`preferenceSnapshotsProvider`新設+fl_chart LineChartの`betweenBarsData`で95%CI帯を表示))。040画面に「好みの傾向」として結線。`test/brew_evaluation_test.dart`に2ケース・`test/preference_section_test.dart`新規4ケース追加。`flutter analyze`新規issueなし(44件のまま)、`flutter test`全件パス(132→138件)、`flutter build web`成功。ブラウザでの040到達確認はT4-3bと同じ制約(CanvasKitのNavigationRailにDOM露出なし)で見送り、widgetテストで担保。**これでサブPhase4(F5、T4-4a〜c)が完了**、設計書§0のPhase順によりサブPhase5(F3レシピ提案、T4-5a)へ進める。
>
> **2026-07-21 T4-4a完了(F5好みプロファイル、サブPhase4開始):** `lib/services/preference_service.dart`新規(`PreferenceGroupStat`/`PreferenceProfile`/`PreferenceService`、設計書§7.1)。産地(originId→OriginMaster.nameJa解決、無ければ自由入力origin)×焙煎度(roastOrdinalMap)でグルーピングし、平均・95%CI(T-22)・n≥5グループのみWelch t検定(T-23)+Welch-Satterthwaite自由度(T-24)・Bonferroni補正(m=検定可能グループ数)によるsignificant判定・statements固定テンプレート生成を実装。**設計書§9.6のWelch検定期待値の誤記を発見**(`tools/verify_preference.py`でscipy.stats.ttest_ind(equal_var=False)と突き合わせた結果、旧値t=10.2899/ν≈10.68が実際の値t=9.788265/ν=7.816449と不一致、ユーザー確認のうえ設計書・テストとも訂正。有意という結論自体は変わらず)。`test/preference_service_test.dart`新規(6ケース)。`flutter analyze`新規issueなし(44件のまま)、`flutter test`全件パス(126→132件)、`flutter build web`成功(UI未接続のためブラウザ確認は対象外)。次はT4-4b(評価登録後の自動更新フック)。
>
> **2026-07-21 T4-3b完了(F2 PCA拡張のUI、サブPhase3完了):** `lib/widgets/statistics/pca_detail_panel.dart`新規(設計書§6.2: 寄与率バー+Kaiser基準線、負荷量テーブル(|L|≥0.5強調)、「AIで深掘り解釈する」ボタン)。`AiAnalysisService.analyzeComponentsDeep`(§8.2)を新設、PC1スコア上位/下位5件の産地・焙煎度・湯温要約(`_summarizePc1Extremes`、Dart側で計算)をプロンプトに含める。統計画面(040)の既存`PcaScatterPlot`直後に結線。`test/pca_detail_panel_test.dart`新規(3ケース、非縮退フィクスチャで寄与率バー・負荷量テーブル・除外軸メッセージ・AIボタン表示を検証)。`flutter analyze`新規issueなし(44件のまま)、`flutter test`全件パス(123→126件)、`flutter build web`成功。ブラウザでの040画面到達確認はCanvasKitのNavigationRailにDOM/aria-labelが露出せず特定できず断念(座標推定はスクリーンショット目視のズレが原因と判明、詳細はNEXT_SESSION.md)。**これでサブPhase3(F2、T4-3a・T4-3b)が完了**、設計書§0のPhase順によりサブPhase4(F5好みプロファイル、T4-4a)へ進める。
>
> **2026-07-21 T4-3a完了(F2 PCA拡張、相関行列ベース化、サブPhase3開始):** `calculatePca()`の固有値分解を独自Jacobi実装から`eigenSymmetric()`(T4-0a)に差し替え、共分散行列→相関行列化(標準偏差0の軸は除外し`PcaResult.excludedFeatures`に記録)。`PcaComponent`に`eigenvalue`/`contributionRatio`(T-13)/`cumulativeRatio`(T-14)を追加、負荷量をT-15で再定義。`PcaResult.components`は全主成分(最大6件)を保持するよう変更したが、既存`pca_scatter_plot.dart`の表示は`take(2)`でPC1/PC2のみに維持(全件表示はT4-3bのpca_detail_panel.dartで対応)。既存`mockRecords`フィクスチャがランク1の縮退データと判明したため、`tools/verify_pca.py`(numpy)で事前検証し、符号に依存しない量(固有値・寄与率・スコア絶対値等)でテスト期待値を確定(`eigen_test.dart`と同じ考え方)。`flutter analyze`新規issueなし(51→44件、旧実装削除等で減少)、`flutter test`全件パス(122→123件)、`flutter build web`成功。ブラウザでの040画面到達確認は`NavigationRail`の選択状態依存レイアウトのためPlaywright座標クリックが不安定で断念(詳細はNEXT_SESSION.md参照)。
>
> **2026-07-21 T4-2d完了(coffee_dataのoriginIdバックフィル):** `coffee_data`のSheetsService `keyMap`/`reverseMap`に`'産地ID':'originId'`マッピングが無く(`bean_master`で発生した既知バグと同じパターンの再発)読み書きどちらでも反映されていなかったことが根本原因と判明。GAS `EXISTING_SHEET_EXTRA_COLUMNS`に`coffee_data`を追加してclasp push/deploy、`sheets_service.dart`にマッピング追加、`tools/backfill_coffee_origin_ids.dart`(スタンドアロンスクリプト)で本番実行(ユーザー承認済み)。結果: 145件中77件に`産地ID`をバックフィル(残り68件はbeanId無し2件+参照先beanのoriginId未設定66件で今回は対象外、`焙煎度`欠測は3件のみ)。Python側での再計算により、バックフィル後の77件がF1回帰の行フィルタを全て通過し最小データ条件を満たすことを確認、040の回帰セクションが実データでフル表示可能な状態になった。`flutter analyze`新規issue3件(新規スクリプトの`avoid_print`のみ、既存パターンと同種)、`flutter test`全件パス(122件、変更なし)。詳細はNEXT_SESSION.md参照。
>
> **2026-07-21 T4-2c2完了(F1回帰UI後半、サブPhase2/F1完了):** `regression_section.dart`に設計書§5.2の項目5・6を追加。項目6「このモデルで予測」ミニフォーム(`_RegressionPredictionForm`、湯温/湯量比/総抽出時間/焙煎度(roastOrdinalMap)/産地(design.baseLevel+dummyLevels)を入力→`RegressionService.predict`で点推定+95%予測区間(T-25)を表示、0〜10範囲外は外挿注意を併記)。項目5「AIで解釈」(`_RegressionAiSection`、`AiAnalysisService.interpretRegression`を新設し設計書§8.1のプロンプトテンプレートを固定使用、既存PCA AI分析と同じ操作感・モデルフォールバック。数値はDart側計算済みでGeminiには解釈のみ要求)。`test/regression_section_test.dart`に2ケース追加(予測フォーム/AIボタンの表示、予測実行で点推定+区間が出ること)。`flutter analyze`新規issue0件(既存48件のまま)、`flutter test`全件パス(120→122件)、`flutter build web`成功。**これでT4-2c1/c2が揃いサブPhase2(F1重回帰分析、T4-2a〜c2)が完了**、設計書§0のPhase順によりサブPhase3(F2 PCA拡張、T4-3a)へ進める。ただし実データでのフル動作は`CoffeeRecord.originId`未投入のため`T4-2d`(バックフィル)完了後(下記T4-2c1エントリ参照)。UIロジックはwidgetテストで担保。
>
> **2026-07-21 T4-2c1完了(F1回帰UI前半):** `lib/widgets/statistics/regression_section.dart`新規(設計書§5.2項目1〜4: 情報アイコン(§2.1.5の注意3点ダイアログ)・モデルサマリ(n/調整済みR²/AIC/除外行数、デフォルトスコア7が3割超で未編集バイアス警告)・係数リスト(β̂/SE/t/p/VIF、Bonferroni補正で有意行を太字+*、VIF>5警告バッジ、1単位あたり点数の副文)・残差vs予測値散布図(fl_chart、y=0強調)。統計画面(040)のランキングの後ろに`FormSection`で結線。データ不足(§1.3、n<max(30,5p))と線形従属(Cholesky失敗)を計画行列から出し分けて案内文を表示。計算は`RegressionService`に委譲し表示のみ担当。**項目5(AIで解釈)・項目6(予測ミニフォーム)はT4-2c2の範囲のため未実装。** `test/regression_section_test.dart`新規(4ケース: データ不足案内・フル表示・バイアス警告・注意ダイアログ)。`flutter analyze`新規issue0件(既存48件のまま)、`flutter test`全件パス(117→120件)、`flutter build web`成功。**重要な発見: 実データ`coffee_data`(145件)には`産地ID`列が存在せず全記録の`originId`が空**のため、現状の実データでは計画行列で全行が除外され、040の回帰セクションは「データが不足しています(現在:0件)」の案内文のみを表示する(挙動は正しい)。フル表示(サマリ/係数/散布図)を実データで確認するには`CoffeeRecord.originId`の投入(coffee_dataへの`産地ID`列追加+バックフィル)が別途必要。ブラウザ確認では040画面が実データ145件で例外なく描画されることまで確認したが、回帰セクションは画面最下部にあり本環境のFlutter Webスクロール制約で直接の目視到達は不可、全描画分岐はwidgetテストで担保した。詳細はNEXT_SESSION.md参照。
>
> **2026-07-21 T4-1f実データ移行実行完了、サブPhase1(F6・データ基盤)終了:** Firebase Hosting本番(https://beanbase-app-2016.web.app)へ最新ビルドをデプロイ後、ユーザー承認のもと本番環境で設定画面(090)の「産地データ移行を実行」を実施。結果: 対象22件・設定済み(スキップ)1件・自動突合成功12件・未突合9件(ドロップダウンでの手動確定UIも正常表示)。未突合分の手動確定は産地名の解釈判断が要るためユーザーの任意タイミングに委ねる。これでT4-1a〜T4-1fが全て完了し、設計書のサブPhase1(F6)の完了条件(実データ移行実行)を満たした。設計書§0のPhase順厳守により、次はサブPhase2(F1重回帰分析、T4-2a design_matrix.dartから)へ進める。
>
> **2026-07-21 T4-2a・T4-2b完了(F1重回帰分析、数値計算層):** `lib/services/math/design_matrix.dart`(`buildRegressionMatrix`、行フィルタ→連続変数中心化→産地ダミー(n<5は地域へ、地域も<5なら「その他」へ統合、最多水準が基準)→交互作用列(焙煎順序×湯温)の順で構築)と`lib/services/regression_service.dart`(`RegressionService.fit`/`fitDesign`/`predict`、Cholesky経由の正規方程式・SE/t/p値・R²・調整済みR²・AIC・VIF・95%予測区間)を実装。**設計書§4.4・§5.1のシグネチャには無いフィールドを一部追加**: `DesignMatrixResult`に`centerMeans`/`dummyLevels`/`baseLevel`(predict()が新規入力を訓練時と同じ基準で中心化・ダミー化するために必須、絶対規則の「発明しない」に抵触しうる拡張のため明記)。また設計書§4.4手順2の「経過日数」列(roastDate記録率70%以上で追加)は、roastDateが`BeanMaster`のフィールドであり`buildRegressionMatrix`のシグネチャ(CoffeeRecordとoriginByIdのみ)からは参照できないため本実装では見送った(次回要判断、詳細はNEXT_SESSION.md参照)。**設計書§9.4の期待値誤記を発見・訂正**: 固定10行データの期待値`β0=1.02667`等が実際の最小二乗解と一致しない(RSSが1.81→0.024に悪化する不整合)ことを`tools/verify_regression.py`(numpy.linalg.lstsq)で検証・確認し、ユーザー承認のもと正しい値(`β=[1.25,1.11,0.39]`等)に設計書・テストとも訂正した。`test/regression_service_test.dart`(§9.4、3ケース)・`test/math/design_matrix_test.dart`(新規、5ケース)全パス。`flutter analyze`新規issue0件(既存48件のまま)、`flutter test`全件パス(108→117件)。UI未実装(T4-2c1/c2)のため`flutter run`でのブラウザ確認は対象外。**T4-2c1/c2は設計書§12①によりUI画面デザインを上位モデル(Opus等)で検討する方針のため、本タスクでは実施していない。**
>
> **2026-07-21 T4-1c1/c2完了+実データ検証、T4-1b/d/eの重大バグを発見・修正:** ユーザーがPCで`npm i -g @google/clasp`→`clasp login`(kazuki21057@gmail.com)→Apps Script API有効化→scriptId(`1HIQ2fwz9UALrpmfg8Qzy9ZOxx0Sf-ED6Onf_1kyZ5Fpg8RPz2Nc5_2mW`)提供、まで実施。Claude Codeが`clasp push`→`clasp deploy --deploymentId AKfycbxqhFoge1C2jYwoyPcS3BDRypCyOjc7rV6qd3FwwMaPBQ42MyrtMv8-NdcAIlvpl0Ao`(URL維持)を実行しT4-1c1/c2を本番反映。curlで(a)`origin_master`/`analysis_history`/`recipe_suggestions`が`ensureSheet_`で自動生成されること、(b)ホワイトリスト外シート名が拒否されること、(c)既存`bean_master`が引き続き取得できること、を確認。`tools/seed_origin_masters.dart`で初期15件投入(実行時にPOSTの302リダイレクトを`package:http`が自動追従しない不具合を発見・手動追従するよう修正)。
> **重大バグ発見:** ブラウザで実際に012(新規豆追加)から産地・焙煎日を入力して登録し、GAS経由で保存内容を確認したところ、`BeanMaster.originId`/`roastDate`が**`SheetsService`の`getBeans()`keyMapと`_reverseMapBean()`reverseMapへの追加を忘れていたため、実際には一切保存されていなかった**(`origin`欄への後方互換コピーのみ動作)。`sheets_service.dart`を修正(`産地ID`/`焙煎日`列を追加)し、GAS側にも`bean_master`へ既存シートの新規列を冪等に追加する`ensureColumns_`ヘルパーを追加してpush/deploy、修正後に再度ブラウザから登録して`産地ID: "origin_1"`・`焙煎日: "2026-07-09T07:00:00.000Z"`が正しく保存されることを確認した。検証用に作成した豆2件はGAS delete経由で削除済み(実データへの影響なし)。詳細はNEXT_SESSION.md参照。
>
> **2026-07-21 T4-1a・T4-1b・T4-1d・T4-1e・T4-1fをまとめて実装(コードは完成、clasp login待ちでT4-1c1/c2と一部の実データ検証のみ未完了):** ユーザーから「F1」の呼称でT4-1(データ基盤/F6)の一括実施を依頼(マスタープランのID採番はサブPhase番号に対応するため、F1=重回帰分析は実際にはT4-2に当たる。依頼者にID対応を確認し、T4-1で進めることで合意)。
> - **完了**: T4-1a(`origin_master.dart`+初期15件データ+`tools/seed_origin_masters.dart`投入スクリプト)、T4-1b(`BeanMaster.originId/roastDate`・`CoffeeRecord.originId`・`brewRatio` getter・`encoding.dart`)はテストのみで完結する範囲のためフルに完了・検証済み。
> - **コード完成・実データ検証待ち**: T4-1d(`DataService`7メソッド+`SheetsService`実装。`AnalysisSnapshot`/`RecipeSuggestion`モデルもT4-4b/T4-5bから前倒しでここに作成)、T4-1e(012画面の産地ドロップダウン+焙煎日入力)、T4-1fの`migration_service.dart`+設定画面UIは実装・単体/widgetテストとも完了。ただし本物のGoogle SheetsにOrigin Masterシート等が存在しない(下記T4-1c1/c2が未デプロイのため)、実データでの動作確認は次回以降に持ち越し。
> - **未完了(ユーザー作業待ち)**: T4-1c1/c2(GAS改修)は`gas/Code.gs`・`appsscript.json`・`.clasp.json`(placeholder)・`README.md`をコード上は完成させたが、`clasp login`(ブラウザOAuth)はClaude Codeが代行できずスマホ単体でも困難と判明したためユーザーのPC作業待ち。ログイン後は`gas/.clasp.json`にscriptIdを記入するだけで、以降のpush/deployはClaude Codeが担当する(詳細は`gas/README.md`)。
> - 検証: `flutter analyze`(新規issue 0件、既存44件+新規4件は全て既存パターンと同種のinfo。48件)。`flutter test`全件パス(85→108件、新規23件)。画面変更(bean_create_screen.dart・settings_screen.dart)はwidgetテストのみで確認、実ブラウザでの実データ確認は上記の理由により今回未実施。
> - 詳細はNEXT_SESSION.md参照。
>
> **2026-07-21 T4-0b・T4-0c完了(F0=数値基盤タスク全て✅、Phase 4サブPhase0終了):** `lib/services/math/linear_solve.dart`(cholesky/choleskySolve/choleskyInverse/choleskyLogDet)と`lib/services/math/distributions.dart`(normalPdf/Cdf・erf・studentTCdf・regularizedIncompleteBeta・tQuantile)をまとめて実装。`tools/verify_linear_solve.py`・`tools/verify_distributions.py`(numpy/scipy突き合わせ)で§9.2/§9.3の期待値を事前検証。**検証中に設計書§9.3の`tQuantile(0.975,138)=1.977431`が誤記(実際はdf=137の値)と判明**、正しい値`1.977304`に設計書・テストとも訂正済み(詳細はNEXT_SESSION.md参照)。`test/math/linear_solve_test.dart`(3ケース)・`test/math/distributions_test.dart`(4ケース)全パス。`flutter analyze`新規issue0件(既存44件のまま)、`flutter test`全件パス(78→85件)。本タスクも既存コードへの結線が無いため`flutter run`でのブラウザ確認は対象外。これでF0(数値基盤、T4-0a〜0c)が全て✅になり、設計書§0のPhase順厳守によりT4-1a(データ基盤)から着手可能。
>
> **2026-07-21 T4-0a完了:** `lib/services/math/eigen.dart`に`eigenSymmetric`(古典的巡回Jacobi法、Golub & Van Loan §8.5)を新規実装。設計書§12②の運用方針に従い`tools/verify_eigen.py`(numpy突き合わせ)で§9.1の期待値を事前検証してからDartテストを作成。`test/math/eigen_test.dart`(4ケース: 2x2解析解・3x3対角・ランダム対称6x6の性質検証・非対称ArgumentError)全パス。既存の`_jacobiEigenvalueAlgorithm`(statistics_service.dart)の差し替えはT4-3aで実施予定のため、本タスクでは新規ファイル追加のみで既存コードへの結線は無し(画面上の見た目変化も無いため`flutter run`でのブラウザ確認は対象外、`flutter analyze`/`flutter test`のみ実施)。
>
> **2026-07-20 UI配置・運用方針の追加決定(設計書版1.2):** ①UI配置: F1/F2/F5は統計画面(040)、F3はダッシュボード(001)、**F4は統計画面ではなく抽出画面(030)**に配置(設計書§1.2.1、T4-6bに反映済み)。②画面デザインの新規検討は上位モデル(Opus等)で実施する運用とする(設計書§12①)。③Python検証が必要な場合は`tools/verify_*.py`としてスクリプト化しローカル実行する運用とする(設計書§12②)。④データ基盤拡張(F6, Phase1)はユーザーの手作業を初回の`clasp login`とscriptId記入のみに限定し、GAS改修・デプロイを含め全工程をClaude Codeが担当する(設計書§12③④、T4-1c1/c2に反映済み)。GAS完全撤廃(Sheets API直接利用)案は検討したが今回は不採用(設計書§11⑤)。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T4-0a | `lib/services/math/eigen.dart`(`eigenSymmetric`、巡回Jacobi法書き直し) | `test/math/eigen_test.dart`(設計書§9.1)全パス | — | M | ✅ |
| T4-0b | `lib/services/math/linear_solve.dart`(cholesky/choleskySolve/choleskyInverse/choleskyLogDet) | `test/math/linear_solve_test.dart`(§9.2)全パス | — | S | ✅ |
| T4-0c | `lib/services/math/distributions.dart`(normalPdf/Cdf・erf・studentTCdf・regularizedIncompleteBeta・tQuantile) | `test/math/distributions_test.dart`(§9.3)全パス | — | M | ✅ |
| T4-1a | `lib/models/origin_master.dart`新規(OriginMaster)+初期15件データの投入方針確定 | モデルのjson round-tripがテストでパス | T4-0a,T4-0b,T4-0c | S | ✅ |
| T4-1b | `BeanMaster`に`originId`/`roastDate`追加、`CoffeeRecord`に`originId`追加+`brewRatio` getter追加(非保存)。あわせて`lib/services/math/encoding.dart`(roastOrdinalMap、設計書§3.5)も実装 | 既存モデルテスト+新規フィールドのround-tripテストがパス | T4-1a | M | ✅ |
| T4-1c1 | `gas/`ディレクトリ新設(Code.gs/appsscript.json/.clasp.json)。**初回のみユーザー作業**(現行GASエディタ内容を`gas/Code.gs`へ手動コピー+`clasp login`、設計書§3.4.2②)、以降はClaude Codeが`clasp push`で反映。新規3シートは`ensureSheet_`ヘルパーで冪等自動生成(設計書§3.4.2⑤、手動シート作成不要) | `gas/Code.gs`がリポジトリにコミットされ、`clasp push`後に3シートが自動生成される | T4-1a | M | ✅ |
| T4-1c2 | シート名ホワイトリスト`ALLOWED_SHEETS`実装(doGet/doPost両方で検証、既存7+新規3シートを列挙)+`gas/README.md`にclaspデプロイ手順記載 | ホワイトリスト外シート名へのアクセスが拒否される。Claude Codeが`clasp push`→`clasp deploy --deploymentId <既存ID>`(URL維持)をBashから実行して反映できる | T4-1c1 | M | ✅ |
| T4-1d | `DataService`抽象に7メソッド追加(fetchOriginMasters等)+`SheetsService`実装(`FirestoreService`は`UnimplementedError`)。設計書§3.4.3のDataService実装にAnalysisSnapshot/RecipeSuggestionモデルが必要なため、`lib/models/analysis_snapshot.dart`・`recipe_suggestion.dart`(§7.2/§7.4のフィールド定義)も前倒しでこのタスクにて作成 | 新規メソッドの結合テストがパスし、実データでfetch/saveできる | T4-1b, T4-1c2 | M | ✅ |
| T4-1e | 豆登録画面(`bean_create_screen.dart`)の産地欄をOriginMaster選択ドロップダウン+新規追加ボタンに置換、焙煎日DatePicker追加 | 012画面で産地選択・焙煎日入力ができ、`origin`欄への同時コピーも維持される | T4-1d | M | ✅ |
| T4-1f | `migration_service.dart`(正規化辞書によるorigin→originId突合、冪等)+設定画面「データ移行」セクション | 移行が冪等に動作し、突合できなかったorigin一覧が画面表示され手動確定できる。**ユーザーが実データ移行を実行**(Phase 1完了条件) | T4-1e | M | ✅ |
| T4-2a | `lib/services/math/design_matrix.dart`(`buildRegressionMatrix`、中心化・産地ダミー統合・交互作用列) | 単体テスト(§9.4向けデータ含む)がパス | T4-1f | M | ✅ |
| T4-2b | `regression_service.dart`(fit/predict、Cholesky経由の正規方程式、VIF/AIC/予測区間) | `test/regression_service_test.dart`(§9.4)全パス | T4-2a | M | ✅ |
| T4-2c1 | `regression_section.dart`前半(モデルサマリ・係数テーブル・残差散布図) | 040画面に表示され実データで動作。**設計書§12①によりUI画面デザインは上位モデル(Opus等)での実施を検討** | T4-2b | M | ✅ |
| T4-2c2 | `regression_section.dart`後半(予測ミニフォーム+AI解釈`interpretRegression`連携) | 予測の点推定+95%予測区間が表示され、Gemini解釈が呼べる。**設計書§12①によりUI画面デザインは上位モデル(Opus等)での実施を検討** | T4-2c1 | M | ✅ |
| T4-2d | **抽出記録の`originId`バックフィル**(T4-2c1で発覚: 実データ`coffee_data`145件に`産地ID`列が無く全記録の`originId`が空 → F1回帰が実データで全行除外され「データ不足」表示になる)。(a) GAS `ensureColumns_`で`coffee_data`に`産地ID`列を追加、(b) 各記録の`beanId`→豆の`originId`を辿って`coffee_data.産地ID`を冪等にバックフィルするスクリプト/移行処理。焙煎度欠測の実態調査も併せて実施 | 実データの大半の記録で`originId`が解決し、040の回帰セクションがサマリ/係数/散布図をフル表示する。本番書き込みを伴うため実行前にユーザー確認 | T4-1c2 | M | ✅ |
| T4-3a | `statistics_service.dart`の`calculatePca()`改修(`eigenSymmetric`差し替え+相関行列化、`PcaComponent`拡張) | 既存PCAテストを新期待値へ更新(§9.7)し全パス | T4-2c2 | M | ✅ |
| T4-3b | `pca_detail_panel.dart`(寄与率バー・負荷量テーブル・AI深掘り`analyzeComponentsDeep`) | 040画面に表示され実データで動作 | T4-3a | M | ✅ |
| T4-4a | `preference_service.dart`(層別統計・Welch検定・Bonferroni補正・statements生成) | `test/preference_service_test.dart`(§9.6)全パス | T4-3b | M | ✅ |
| T4-4b | 評価登録後の自動更新フック(`brew_evaluation_screen.dart`からPreferenceService.build()を呼びAnalysisSnapshotとして保存)。**モデル・DataService保存は既にT4-1dで実装済み**、本タスクはフック配線のみ | 評価登録のたびpreferenceスナップショットが保存され、失敗しても記録保存は妨げない | T4-4a | S | ✅ |
| T4-4c | `preference_section.dart`(statementsカード・グループ統計テーブル・履歴折れ線グラフ) | 040画面に表示され実データで動作 | T4-4b | M | ✅ |
| T4-5a | 在庫豆定義の実コード調査+`suggestion_service.dart`(group_bestロジックのみ、GP未接続) | n_eff不足時のフォールバック提案が動作 | T4-4c | M | ✅ |
| T4-5b | `recipe_suggestion_card.dart`(ダッシュボード)。**モデル・DataService保存/更新は既にT4-1dで実装済み** | 手動E2E: 提案→淹れる→記録→resultRecordId紐付けを確認 | T4-5a | M | ✅ |
| T4-6a | `gp_service.dart`(fit/predict、Cholesky経由、固定グリッドハイパラ探索) | `test/gp_service_test.dart`(§9.5)全パス | T4-5b | L | ✅ |
| T4-6b | `optimize`(EIグリッド探索)+`lib/widgets/brew/gp_explorer_section.dart`(ヒートマップ) | **030画面(抽出レシピ)**に「レシピ探索」セクションが表示され実データで動作(設計書§1.2.1決定事項、統計画面ではなく抽出画面) | T4-6a | M | ✅ |
| T4-6c | `suggestion_service.dart`をGPへ接続(rationale='gp_mean'/'gp_ei') | F3のカードがGP予測を使い、7件に1回EI提案に切り替わることを確認 | T4-6b | S | ✅ |

### タスク共通の検証手順（全タスクに適用）

1. `flutter analyze` — 新規エラー/警告ゼロ
2. `flutter test` — 全パス(ロジックを追加したタスクは単体テストも追加)
3. `flutter run -d chrome` — 対象画面の表示・遷移・Sheets 疎通を確認(該当時は Playwright で視覚確認。snapshot は最小限)
4. OK → commit/push + マスタープラン進捗表更新 / NG → `NEXT_SESSION.md` 更新

---

## 4. 画面インベントリ（22画面）

| 画面 | 名称 | 主な要素 | 状態 |
|---|---|---|---|
| 001 | ダッシュボード | 残豆量(瓶)・直近5件履歴・黒板風 | ✅ |
| 002 | 抽出履歴(リスト) | 日時/豆/メソッド/点数、スワイプで021 | ⬜ |
| 003 | 抽出履歴(詳細) | 全情報表示・編集 | ⬜ |
| 010 | 豆管理(カード) | 焙煎所/豆名/煎り度/画像/残量、0%表示切替、＋ | ✅ |
| 011 | 豆管理(詳細) | 全情報・編集・関連履歴5件 | ✅ |
| 012 | 新規豆追加 | 入力フォーム | ✅ |
| 013 | ドリッパー管理 | 画像左/名前右リスト、＋ | ✅ |
| 014 | ドリッパー詳細 | 全情報・編集・関連履歴5件 | ✅ |
| 015 | 新規ドリッパー | 入力フォーム | ✅ |
| 016 | フィルター管理 | 画像左/名前右リスト、＋ | ✅ |
| 017 | フィルター詳細 | 全情報・編集・関連履歴5件 | ✅ |
| 018 | 新規フィルター | 入力フォーム | ✅ |
| 019 | メソッド管理 | 名前/発案者(小)/抽出回数、＋ | ✅ |
| 020 | メソッド詳細 | 全情報・動画再生(可能なら)・編集・履歴5件 | ✅(動画再生はT3-3で別途) |
| 021 | 新規メソッド | 入力フォーム | ✅ |
| 022 | グラインダー管理 | 画像左/名前リスト、＋ | ✅ |
| 023 | グラインダー詳細 | 全情報・編集・履歴5件 | ✅ |
| 024 | 新規グラインダー | 入力フォーム | ✅ |
| 030 | 抽出レシピ | Pouring Steps・タイマー・編集・評価ボタン→031 | ✅ |
| 031 | 抽出結果の評価 | 抽出情報登録＋評価・新規登録対応 | ✅ |
| 040 | 統計情報 | スタッツ・歯車→090 | ✅ |
| 090 | 設定 | メインカラー/APIキー/保存先 | ✅ |

---

## 5. 日次ループ運用（要点）

- **終了条件**（いずれかで停止）: ①タスク完了 ②連続3回失敗 ③1ループのコスト>$24 ④1ループのターン数>=30(2026-07-25〜、すべて直近の`/start`・`/full_loop`以降の累計。従来は当日累計だった)
- コスト・ターン数は `.claude/hooks/loop_guard.js` が transcript から算出し `.claude/loop_state.md` に出力。**この数値が真実**。
- 連続失敗は `.claude/loop_failures.txt`（`<ループ識別子> <回数>` 形式）を自分で更新（失敗+1／成功0リセット。識別子は `loop_state.md` の「ループ識別子」の値を使い、新しいループが始まると自動的に 0 扱い）。
- 停止時は新規着手せず、`NEXT_SESSION.md`（引き継ぎ）と本書の進捗表を更新してから終了。
- 詳細は `CLAUDE.md`「日次改修ループ運用ルール」を参照。

### 5.1 自動ループの省コスト運用方針（$24/ループ）

> 変更履歴: 2026-07-03 にコスト上限 $0.5→$1.5、ターン上限 10→30 に引き上げ。2026-07-04 にコスト上限 $1.5→$12 に引き上げ。2026-07-21 にコスト上限 $12→$24 に引き上げ（ユーザー指示、2倍）。2026-07-25 にコストの集計単位を「当日累計」→「1ループ単位(直近の`/start`・`/full_loop`以降)」に変更（ユーザー指示。`/loop`によるcron定期実行で同一セッション内に複数ループが連続すると当日累計がすぐ上限に達し、実質的に1回しか動かせなかったため)。以下の「$24/日」の記述は本節新設時点(2026-07-03)の実装を指す歴史的記述であり、現在のコスト上限の集計単位は上記のとおり1ループ単位。

コスト上限 $24/日 を守りながら自動化するため、**1日=最小1サブタスク**に限定し、**実装日と評価日を分離**する。役割をクラウドとローカルで分担:

| 担当 | 起動 | 役割 | やること | やらないこと |
|---|---|---|---|---|
| **クラウド schedule** | 毎日 05:00 JST（PC不要） | **実装日** | 最小1サブタスクのコード実装 → `flutter analyze` → `flutter test` → commit/push | `flutter run`（外部通信不可・高コスト） |
| **ローカル loop** | PC起動時 05:00 JST | **評価日** | `git pull` → `flutter run` → Playwright で疎通・見た目を評価 → 結果を `NEXT_SESSION.md` に記録 | 新規コード改修（NG項目は次の実装日へ回す） |

**省トークン徹底（コスト抑制の要）:**
- ファイルは全読みせず対象箇所のみ読む。大きな画面は「骨組み→部品→評価」と分割。
- Playwright の `snapshot` は最小限、スクショは要所のみ。
- $24 到達で `loop_guard` が停止 → その時点までを commit し引き継ぎ、翌日継続。

**競合回避:** クラウドは実装してpush、ローカルは pull して評価のみ（feature コードを commit しない）ので、同時刻でも git 競合は起きない。ローカルが評価するのは「直近 push 済みコミット」。

### コスト算出式（参考）
```
cost = Σ_model (
    input_tokens          × 単価_in
  + cache_creation_tokens × 単価_in × 1.25
  + cache_read_tokens     × 単価_in × 0.1
  + output_tokens         × 単価_out
) / 1e6        （直近の/start・/full_loop以降の分のみ。境界未検出時は当日分にフォールバック）
```
| モデル | in($/1M) | out($/1M) |
|---|---|---|
| Fable 5 | 10.00 | 50.00 |
| Opus 4.8 | 5.00 | 25.00 |
| Sonnet 4.6 | 3.00 | 15.00 |
| Haiku 4.5 | 1.00 | 5.00 |
