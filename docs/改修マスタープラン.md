# BeanBase 2.0 改修マスタープラン（進捗表示付き）

> 本書は今回の大規模改修の **全体設計と進捗の単一の真実**。毎日の改修タスクはここから選ぶ。
> 出典: [`docs/Beanbase改修案.md`](./Beanbase改修案.md) / 運用ルール: `CLAUDE.md`「日次改修ループ運用ルール」
> Phase 4 詳細の出典: `statistics_feature_design.md`(単一の正本) / `CLAUDE.md`「統計解析・予測機能の実装ルール」
> 最終更新: 2026-07-29(トークン削減のため完了行・作業ログをアーカイブへ分離)

> **本書の読み方(トークン節約、2026-07-29)**: 1ループで読むのは **§2(進捗サマリ)** と **§3 の該当フェーズの未完了行だけ**。
> 完了タスクの詳細は `docs/archive/マスタープラン_完了タスク.md`、日付順の作業ログは `docs/archive/マスタープラン_作業ログ.md` に退避済み(**通常は読まない**、必要時に ID/日付で grep)。

## 凡例
| 記号 | 意味 |
|---|---|
| ⬜ | 未着手 |
| 🟦 | 進行中 |
| ✅ | 完了 |
| ⏸ | 保留／後回し |

---

## 1. 改修の全体方針

| # | 柱 | 概要 | 優先 |
|---|---|---|---|
| A | データ基盤変更 | Firestore → **Google Sheets** に戻す（管理を楽に） | 1 |
| B | 画面構成・遷移の再編 | 抽出と評価を分離、14画面構成へ再設計 | 2 |
| C | UI全面刷新 | ダッシュボード/抽出/評価/スタッツを一から。黒板風・瓶ビジュアル等 | 3 |
| D | その他軽微な修正 | レスポンシブ確認・DRY化・仕上げ | 4 |
| E | AI活用拡大 | PCAの説明範囲拡大など（**全改修完了後**） | 5（最後） |

**データ基盤の決定（A）:** 個人利用・データ量小のため、以前の `SheetsService`（`lib/services/sheets_service.dart`）を再活用して Google Sheets に戻す。スプレッドシートで直接データ確認・手動編集できる点が「管理が楽」の要件に最適。Cycle 18 で入った `FirestoreService` 経由の読み書きを Sheets 経由へ差し戻す。画像保存先は別途方針決定（後述 T0-4）。

**モバイル利用の決定(D、2026-07-13):** ネイティブアプリ(Android/iOS)は**作らない**。Web 版をホスティングし、スマホ(Android)のブラウザからアクセスして**ホーム画面にピン留め(PWA として standalone 起動)**して使う。ホスティング先は既存 Firebase プロジェクト(`beanbase-app-2016`)の **Firebase Hosting** を第一候補とする(無料枠で十分・`firebase.json` が既にある)。デプロイは `flutter build web --release` → `firebase deploy` の手動運用から始める。注意: Gemini API キーは `shared_preferences`(ブラウザごとの localStorage)保存のため、スマホ側でも設定画面から再入力が必要。

**公開URL(T3-11で確定、2026-07-18):** https://beanbase-app-2016.web.app — 認証なしで誰でもアクセス可能。再デプロイは `flutter build web` → `firebase deploy --only hosting`。

---

## 2. 全体進捗サマリ（フェーズ別）

| フェーズ | 内容 | 対応Cycle | 状態 |
|---|---|---|---|
| Phase 0 | データ基盤を Sheets に戻す | Cycle 19 | ✅ |
| Phase 1 | 画面構成・ナビ再編（14画面の骨組み） | Cycle 20–22 | 🟦 |
| Phase 2 | UI全面刷新（黒板風・瓶・タイマー・評価・スタッツ） | Cycle 23–26 | ✅ |
| Phase 3 | 軽微な修正・仕上げ | Cycle 27 | 🟦 |
| Phase 4 | 統計解析・予測機能拡張（数値基盤/データ基盤/回帰/PCA拡張/好み/提案/GP） | Cycle 28+ | 🟦 |
| Phase 5 | **Android公開版の開発〜リリース**(開発運用基盤/エディション足場/ローカルDB/UI新規設計/インサイト化/収益化/ストア公開) | Cycle 30+ | ⬜ |

> Cycle番号は目安。実際の粒度に応じて分割・増減してよい（番号は連番厳守、`docs/`と`docs/archive/`の最大値+1）。

---

## 3. フェーズ詳細＆タスク分解（日次タスクのベース）

### Phase 0 — データ基盤を Sheets に戻す（Cycle 19）
**終了条件:** Sheets 経由で一覧表示・登録・編集・削除が動き、`flutter run` で接続成功。


> **完了済み(6件)**: T0-1, T0-2, T0-3, T0-4, T0-4b, T0-5
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

### Phase 1 — 画面構成・ナビ再編（Cycle 20〜22）

**Phase 終了条件:** 22画面すべてにルーティングが通り、抽出(030)と評価(031)が別画面として遷移する。

サイズ: S=半日余裕、M=1ループ標準、L=1ループぎりぎり(着手前に分割を検討)


> **完了済み(16件)**: T1-1a, T1-1b, T1-1c, T1-2a, T1-2b, T1-3, T1-4a, T1-4b, T1-4c, T1-5a, T1-5b, T1-5c, T1-5d, T1-6a, T1-6b, T1-7
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

> 旧 T1-1〜T1-7 との対応: T1-1→T1-1a/b/c、T1-2→T1-2a/b、T1-4→T1-4a/b/c、T1-5→T1-6a/b、T1-6→T1-5a/b、T1-7→T1-5c/d。

### Phase 2 — UI全面刷新（Cycle 23〜26）

**Phase 終了条件:** 黒板風ダッシュボード・瓶ビジュアル・タイマー付き抽出・評価・スタッツが見た目どおり動作。


> **完了済み(14件)**: T2-1a, T2-1b, T2-2a, T2-2b, T2-2c, T2-3a, T2-3b, T2-3c, T2-4a, T2-4b, T2-5a, T2-5b, T2-6, T2-7
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

### Phase 3 — 軽微な修正・仕上げ（Cycle 27）

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-1 | モバイル実機でのレイアウト確認・調整(**ユーザー実施の確認結果を受けて修正**) | ユーザー確認で NG 項目ゼロ／受入: 免除(視覚判断のみ) | T3-12 | M | ⬜ |
| T3-4 | 全体のUIおしゃれ化・微調整 | ユーザーレビューで完了判断／受入: 免除(視覚判断のみ) | T3-1 | M | ⬜ |
| T3-20 | Ubuntu環境の並行セットアップ(**ユーザー実施**) — Flutter/Dart SDK・Chrome(またはChromium)・Node.js(hooks用)・`gh` CLI導入、GitHub用SSH鍵の新規発行＆登録(リモートがSSH URLのため)、`flutter pub get`→`flutter analyze`/`flutter test`→`flutter run -d chrome`で疎通確認、Gemini APIキーを設定画面(090)で再入力(ブラウザのshared_preferencesはマシンごとに独立するため)。**2026-08-07: JDK17・Android SDK・Node.js・gh CLI導入とAndroid debugビルド成功まで完了(詳細はNEXT_SESSION.md -5.32節)。残りはGemini APIキー再入力とflutter run -d chromeでのSheets/Drive疎通確認** | Ubuntu上で`flutter run -d chrome`が起動し、Sheets/Driveへの疎通とAI分析が確認できる | なし | S | 🟦 |

> **完了済み(36件)**: T3-2, T3-3, T3-5, T3-6, T3-7, T3-8, T3-9, T3-10, T3-11, T3-12, T3-13, T3-14, T3-15, T3-16, T3-17, T3-18, T3-19, T3-21, T3-22, T3-23, T3-24, T3-25, T3-26, T3-27, T3-28, T3-29, T3-31, T3-32, T3-33, T3-30, T3-34, T3-35, T3-36, T3-37, T3-38, T3-39
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-07-26 ユーザー報告の不具合・要望、T3-40〜T3-53）

着手順の目安: **①不具合(T3-44→T3-45→T3-40) → ②焙煎度8段階化(T3-42→T3-43→T3-51) → ③画像UI統一(T3-41) → ④おすすめレシピ改修(T3-47→T3-48→T3-49) → ⑤最適条件探索(T3-50→T3-52→T3-53)**。**⑥データ整理(T3-46)は2026-07-30完了済み。T3-50・T3-47も2026-07-30完了済み**。**T3-52(a〜d全件)は2026-07-30完了・本番反映済み**。**T3-48は2026-07-31完了・本番反映済み(T3-71と合同デプロイ)**。**T3-51・T3-43も2026-07-31完了・本番反映済み(②焙煎度8段階化グループは全件完了)**。**T3-49も2026-07-31完了・本番反映済み(④おすすめレシピ改修グループは全件完了)。残るは⑤のT3-53のみで、その設計も2026-07-31に上位モデルで完了(→実装はT3-53a〜dへ分解済み)**。

**2026-08-01時点**: T3-40〜T3-56はT3-57を除き全件完了・本番反映済み(T3-53シリーズa〜dの完了で本グループは実質クローズ)。完了内容の詳細は `docs/archive/マスタープラン_完了タスク.md` を参照。**残るT3-57のみユーザーからの写真提供待ちで着手不可**。


| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-57 | **不具合(2026-07-27ユーザー報告)**: 「Youth」の豆(パッケージ画像)が表示されない。**調査結果(2026-07-27、下記note参照)**: 表示側のバグではなく、対象3件(`5a57cb8d`/`bf2d1f8d`/`5549c4ad`、いずれも産地違いのYouthシリーズ)は本番`bean_master`上で`豆画像URL`/`豆粒画像URL`/`情報画像URL`のいずれも元々空欄と確認。原因はT3-38(original-data移植)で、CSVが参照する画像がユーザーのローカルファイルパス(`豆マスター_Images/`配下)のみでリポジトリ・本番のどこにも実体が無く、移植時に画像だけ意図的に対象外とされたこと(T3-38完了ログに明記済み)。**修正には対象3件それぞれの実際のパッケージ写真が必要**(Claude側で生成・代用はできない)。画像URLの設定は`updateBean`経由でGASの`updateRow`を呼ぶが、**T3-56の再調査(2026-07-27)により`updateRow`自体は正常動作すると確認済み**(誤検知だった)ため、保存経路側の懸念は解消。**残る唯一の障害はユーザーからの写真提供待ち**。写真が揃い次第アプリUI(011編集)からアップロードして設定する | 3件のYouth豆すべてでパッケージ画像が本番アプリに表示される／受入: 免除(Sサイズ) | なし(写真素材のみユーザー提供待ち) | S | ⬜(ユーザーからの写真提供待ち) |

> **完了済み(27件)**: T3-40, T3-41, T3-42, T3-43, T3-44, T3-45, T3-46, T3-47, T3-48, T3-49, T3-50, T3-51, T3-52, T3-52a, T3-52b, T3-52c, T3-52d, T3-53, T3-53a, T3-53b, T3-53c, T3-53d, T3-54, T3-54a, T3-54b, T3-55, T3-56
> T3-53シリーズ(a〜d)が2026-08-01に全完了し、Phase 3追加分(T3-40〜T3-53)グループはこれで全完了。残るT3-57のみユーザーからの写真提供待ちで本体表に残置。
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-07-28 ユーザー要望、T3-58〜T3-69）

**要望の原文(6件)**: ①calcページで豆量を変更してもpouring stepsに反映されない・線形でお湯量を変更して ②豆残量も手動で調整できるようにする ③そもそも追加購入を想定した設計になっていない。豆詳細画面に追加購入ボタンを追加し、購入日・焙煎日・購入量を入力するダイアログで既存の購入日・焙煎日を更新し購入量を残量に加算する(**画面デザイン・遷移は上位モデルで検討**) ④豆の管理項目に保存場所(職場/家)を追加、既存は全て「職場」にしてあとでユーザーが手動で振り分ける ⑤豆の購入履歴を追えるページを新設、リスト形式とカレンダー形式の両方で購入日・購入豆・購入先が分かるように(**運用方法・画面構成は上位モデルで検討**) ⑥購入店を管理できるページを追加、管理項目も検討。豆管理項目から店を自動抽出しネットから情報を自動取得(**上位モデルで実行**)。

**着手順の目安**: **①独立・即着手(T3-58 → T3-59) → ②在庫基盤(T3-60) → ③設計(T3-61、上位モデル) → ④購入履歴基盤〜UI(T3-62 → T3-63 → T3-64 → T3-65) → ⑤購入店(T3-66設計(上位) → T3-67 → T3-68 → T3-69)**。

**依存関係(必ず守る順序)**:
- `T3-60`(在庫基準点) → `T3-61`(追加購入+購入履歴の統合設計、上位モデル) → `T3-62`(購入履歴データ基盤) → `T3-63`(追加購入UI実装) / `T3-63b`(012の初回購入記録+遡及登録) / `T3-64`(購入履歴リスト) → `T3-65`(購入履歴カレンダー)
- `T3-66`(購入店設計、上位モデル。**2026-07-28完了**) → `T3-67`(購入店データ基盤) → `T3-68`(購入店UI) → `T3-70`(新規購入店のAI自動取得) / `T3-69`(豆マスタのstore→storeId移行)
- `T3-62` + `T3-68` → `T3-69`

**すぐ着手できる(依存なし)**: T3-69(依存T3-62・T3-68は充足済み)。**T3-58は2026-07-29に完了済み**。**T3-66は2026-07-28に上位モデル(Opus 5)で完了済み**(成果物: `docs/store_master_design.md`)。**T3-67・T3-68・T3-70は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**。**T3-60は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(在庫基準点方式)。**T3-61(追加購入+購入履歴の統合設計)は2026-07-29に上位モデル(Opus 5)で完了**(成果物: `docs/bean_purchase_design.md`。T3-62〜T3-65は同書のとおり実装すれば設計判断が不要な粒度になっており、あわせて`T3-63b`を新設した)。**T3-62(購入履歴のデータ基盤)は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(GAS `bean_purchases`シート9列を本番へ自動生成確認済み)。これによりT3-63・T3-63b・T3-64がSonnet 5で着手可能になった。**T3-63(011の追加購入ボタン)は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(スイートイエローで追加購入50gを実際に実行し確認済み)。**T3-63b(012の初回購入記録+遡及登録)も2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(本番`bean_purchases`に`bp_init_<豆ID>`で正しく記録されることを確認済み)。**T3-64(025リスト形式)も2026-07-29にSonnet 5の`/full_loop`で完了・本番デプロイ・本番確認まで完了**(デプロイは初回セッションで自動モード分類器にブロックされたが翌セッションでユーザーが手動実行し成功)。これによりT3-65(025カレンダー形式)がSonnet 5で着手可能になった。**T3-65(025カレンダー形式)も2026-07-29にSonnet 5の`/full_loop`で完了・本番デプロイ・本番確認まで完了**(デプロイが自動モード分類器にブロックされたが、今回はAgentツールでサブエージェントに同じコマンドを実行させることで同一セッション内で解消。ユーザー指示によりこの回避策を今後の標準対応とする)。これで購入履歴基盤〜UI(T3-61〜T3-65)は全完了。**T3-59(豆マスタに保存場所)も2026-07-29にSonnet 5の`/full_loop`で完了・本番デプロイ・本番確認まで完了**(既存30件全豆に「職場」を一括設定済み)。**T3-69(豆マスタのstore→storeId移行)も2026-07-31にSonnet 5の`/full_loop`で完了・本番デプロイ・移行実行・本番確認まで完了**。これでT3-58〜T3-69グループは全完了。

**モデル選定の一般ルール(2026-07-28ユーザー指示で恒久化)**: **上位モデル(Opus等)がやるのは「方針検討」と「実装内容の検討」まで。実装は必ず下位モデル(Sonnet 5)に回す。** したがって上位モデル指定のタスクは、成果物が必ず**設計書+実装タスクへの分解**になり、コードは1行も書かない。逆に、上位モデルはSonnet 5が迷わない粒度(フィールド名・シート列名・画面ID・引数表・突合規則・既知の地雷への注意まで確定)で情報を整理する責任を負う。T3-66はこのルールに沿って実施した(成果物`docs/store_master_design.md` → T3-67/T3-68/T3-69/T3-70はすべてSonnet 5で実装可能)。

**このグループのモデル選定(2026-07-28、ユーザー指示「上位モデルで検討して」に基づく)**: **T3-61・T3-66の2件のみ上位モデル(Opus等)で実施**し、`full_loop`の自動選定対象外とする(`statistics_feature_design.md`§12①「画面デザインの新規検討は上位モデルで実施」に該当。T3-61=追加購入ダイアログと購入履歴の統合という新規の運用設計、T3-66=購入店マスタの管理項目そのものの決定とWeb情報収集を伴うため)。**T3-61・T3-66とも2026-07-29時点で完了済み**(成果物はそれぞれ`docs/bean_purchase_design.md`・`docs/store_master_design.md`)。**それ以外(T3-58〜T3-60、T3-62〜T3-65、T3-63b、T3-67〜T3-69)はすべてSonnet 5で`/full_loop`による自動実施の対象**。そのため設計判断が必要な箇所は本タスク表側で先に確定させてある(下記各行の「実装方針」を発明せずそのまま従うこと)。

**共通の注意(全タスク共通)**:
- モデルにフィールドを追加したら**必ず** ①`lib/models/*.dart` ②`*.g.dart`(build_runnerがこのマシンで不安定なため手動編集可、T3-34の教訓参照) ③`SheetsService`の**keyMapとreverseMapの両方** ④GAS `gas/Code.gs`の`EXISTING_SHEET_EXTRA_COLUMNS`(既存シートへの列追加)または`ALLOWED_SHEETS`+`NEW_SHEET_HEADERS`(新規シート) ⑤`clasp push`+`clasp redeploy` の5点をセットで実施する。**過去に頻発した「列プロビジョニング漏れ」バグ(T4-1b/T4-2d/T3-23/T3-47)と同型**。`rules/verification.md`のチェックリストに従う。
- 新規画面を足す場合は `lib/routing/app_screen.dart`(画面ID enum)+`lib/routing/screen_registry.dart`(ID→Widget解決)+導線(遷移ボタン)の3点セット。既存例はT3-36(042)・T3-39(043)。
- 画面IDの空き: **025〜029, 032〜039, 044〜089**。本グループでは **025=購入履歴、026=購入店管理(リスト)、027=購入店詳細、028=新規購入店** を割り当てる(着手時に`app_screen.dart`で重複が無いことを再確認すること)。
- CLAUDE.md「全マスタータブへの一律適用」規約: 豆マスタだけの変更で済むか、他マスターにも展開すべきかを毎回判断すること(本グループは豆固有の要件が多いため、原則として豆のみで良い。共通ウィジェットに手を入れる場合のみ全マスターへの影響を確認)。

**このグループのタスク(T3-58〜T3-69)はすべて完了した。**

> **完了済み(14件)**: T3-58, T3-59, T3-60, T3-61, T3-62, T3-63, T3-63b, T3-64, T3-65, T3-66, T3-67, T3-68, T3-70, T3-69
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-07-30 ユーザー要望、T3-71: 推奨焙煎度の「幅」対応）

T3-71・T3-71a・T3-71bは**2026-07-31にすべて完了・本番デプロイ・push済み**(詳細は`docs/archive/マスタープラン_完了タスク.md`のT3-71節、成果物`docs/method_roast_range_design.md`)。本番`methods_master`のうち推奨焙煎度が設定済みなのは`method001`(4:6メソッド)のみ。他11メソッドは未設定のためF3の候補にならない(ユーザーが021から順次設定する運用)。

> **完了済み(3件)**: T3-71, T3-71a, T3-71b
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-08-02、T3-72: 「継続中の注意事項」のタスク化）

`NEXT_SESSION.md`「継続中の注意事項(未解決)」9件を棚卸しし、着手可能なものをタスク化した。
**トリアージ結果**: #1は記載が古かった(下記T3-72a参照)、#8はユーザー実施予定のため対応不要、#4・#9はユーザー確認待ちで下表には含めない(質問済み)。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-72a | ❌ **クローズ(2026-08-05、T3-72fへ統合)**: 当初案(`bean_purchases`の`bp_init_<豆ID>`から`初期購入量(g)`を逆算投入)は本番データ突合の結果**実行不可**と判明(`bp_init_`行0件、`tools/migrate_bean_purchases.dart`本番未実行)。逆算スクリプト`tools/backfill_bean_initial_purchase.dart`の`--dry-run`も対象0件。自動移行できる元データが存在しないため、ユーザーが各豆の`初期購入量(g)`を手動入力する以外に解決策が無く、T3-72f(同じくユーザー手動入力のタスク)と性質が同一のため統合しクローズ | (T3-72fに統合) | なし | M | ❌(統合済み) |
| T3-72f | **注意事項#7**: 本番`methods_master`12メソッドのうち推奨焙煎度が設定済みは`method001`のみだった。**推奨焙煎度は2026-08-05、ユーザー依頼によりClaude(Opus 5)が各メソッドの発案者・出典(YouTube/公式レシピ等)をWeb検索で調査し、021の編集画面(推奨焙煎度スライダー)経由で残り12メソッド全件に設定・本番反映済み**(根拠: 명称に「浅煎り用/向け」と明記された5件→ライト〜シナモン、井崎式→発案者の基準レシピが「中煎り=92℃」基準でありデータの湯温92℃と一致→ミディアム〜シティ、ORIGAMI円錐/ウェーブ→フィルタ特性の一般知見、New Hybrid Method→開発者本人が焙煎度不問と明言のためライト〜イタリアン全域、松原スペシャルのみユーザー確認済みでフルシティ指定。詳細はNEXT_SESSION.md参照)。**残るのは28豆中24豆で未入力の`初期購入量(g)`のみ**(自動移行できる元データが無いためユーザーが021から手動入力する必要がある) | ~~11メソッドの推奨焙煎度が設定され~~→達成、`初期購入量(g)`が主要な豆で入力される→未達成(残作業) | なし | S | 🔶(推奨焙煎度は完了・初期購入量はユーザー入力待ち) |

> **完了済み(4件)**: T3-72d・T3-72b・T3-72c・T3-72e / **クローズ(1件)**: T3-72a(T3-72fへ統合)
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-08-03、T3-74: T3-72d本番確認で発見したレース条件の修正）

T3-72dの本番確認中に、`OptimisticListNotifier`(`lib/providers/data_providers.dart`)の`updateOptimistic`等が直後に呼ぶ`_syncInBackground()`が、GAS書き込み直後だと更新前データで上書きすることがあると判明した(詳細は`rules/lessons_archive.md` L99)。T3-72dの範囲(コンストラクタ引数問題)とは別原因のため未修正のまま新規タスク化する。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-74a | ✅ **完了(2026-08-03)**: L99末尾の案(a)`_syncInBackground()`削除を採用(理由・詳細は`rules/lessons_archive.md` L100)。テストで発覚した`_FakeDataService`の参照エイリアシング起因の重複バグも修正 | 編集→保存→pop直後(フルリロード無し)でも最新値が表示される。`flutter analyze`新規issue0・`flutter test`全パス | なし | M | ✅ |

> **完了済み(1件)**: T3-74a
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-08-03、T3-75: 本番動作・表示データの棚卸しで発見した不具合）

2026-08-03の`/full_loop`(ユーザー指示「本番環境が本当に意図した動作をしているのか、正しいデータを表示しているのか確認して」)で、本番GAS実データ12シートを直接取得して突合し、あわせて本番と同一の`build/web`(`main.dart.js` md5 `40c4c1679bd085f5809303bd4ba70a9b`、本番配信物と一致を確認済み)をローカル配信してブラウザ目視した結果を新規タスク化する。**この回ではコード編集は行っていない(ユーザー指示)。**

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-75a | ❌ **localhost限定・本番では再現せず(T3-75hで2026-08-03確認)**。マスター画像が1枚も表示されない。~~`bean_master`28件中24件に`豆画像URL`があり~~(元記述、以下同旨)。**判定根拠**: 本番URLの豆管理カード一覧(011)で3件全てGoogle Drive画像が正常表示(コーヒーバッグの写真)、豆詳細画面(012)でも画像表示を確認。localhost配信時のみOrigin/Refererの違いでDrive/lh3画像取得が失敗していたと推定 | ~~011/012/020等の一覧・詳細で本番のマスター画像が実際に表示される~~ → 確認済み | なし | M | ❌ |
| T3-75b | ✅ **完了(2026-08-04)**: 031(評価画面)の`_submit()`冒頭に豆未選択・湯温未入力(≤0)の必須バリデーションを追加し、本番デプロイ済み。既存の不正行(記録ID `1785746695316`)は正しい豆・湯温がユーザーにしか分からないため今回は対象外、ユーザーが後日021またはSheetsを直接編集して補正する方針(2026-08-04ユーザー確認済み) | 豆未選択・湯温0では保存できない | なし | S | ✅ |
| T3-75c | ✅ **完了(2026-08-05)**: **本番`coffee_data`のゴミ行3件を削除**。(1) 記録ID `1784633291939`(`bean_master`に存在しない豆IDを持つ孤児行)は既存の`delete`アクション(GAS変更不要)で削除。(2) 全列空白の行2件は既存`delete`がID列一致方式で空IDを扱えないため、`gas/Code.gs`に一回限りの`deleteBlankRows`アクションを追加し`clasp push`+`clasp deploy`(既存デプロイID、URL不変、@19→@20)で反映して実行、確認後に同関数を削除して再度push/deploy(@21)し後片付け(コードの正味差分なし)。本番`coffee_data`は173件→170件になった | 3行が削除され、`coffee_data`の有効行数が一致する → 達成(173→170件、空白行0件を確認) | なし | S | ✅ |
| T3-75d | ✅ **完了(2026-08-05)**: **本番`filter_master`のテストデータ(フィルターID `1771594821407`、`Test Filter`)を既存の`delete`アクションで削除** | `filter_master`が実機材7件のみになる → 達成(8件→7件、F001/F002/1f52a4d7/c2df4b23/8e2421ab/bbd1a210/00b3bc17の7件のみ) | なし | S | ✅ |
| T3-75e | 🔶 **本番では「初回描画時のみ」の一過性(2026-08-03 T3-75hで確認、localhost観測の「再描画後も残る」とは異なる挙動)**。ダッシュボード(001)・統計(040)・設定(090)いずれも画面遷移直後は同様の豆腐化(例:「実験的な提案です」→「⊠⊠的な提⊠です」)が出るが、**2〜3秒待って再描画すると同一画面内で正常な文字に回復**する。`performance.getEntriesByType('resource')`実測でも`fonts.gstatic.com`のnotosansjp含む全フォントリクエストは`failedCount:0`(取得自体は成功)。初回ペイントがCanvasKitのフォントフォールバック解決前に走っているのが原因と推定。**2026-08-05、`web/index.html`に`fonts.gstatic.com`/`fonts.googleapis.com`への`preconnect`ヒントを追加・本番デプロイ済み**(接続確立の待ち時間のみ短縮、取得自体の遅延は解消しない部分緩和)。恒久解消には日本語フォント(Noto Sans JP、1ウェイト約5.3MB)のアセットバンドルが必要だが、リポジトリ・アプリサイズの恒久的な増加を伴うためユーザー判断で見送り済み | 001/011/020/040の主要文言に豆腐(⊠)が出ない(初回描画のちらつきは許容するか要ユーザー判断→**ユーザーは許容を選択、preconnectのみで対応**)／受入: 免除(視覚判断のみ) | なし | M | 🔶(優先度低・部分対応済み) |
| T3-75f | ✅ **完了(2026-08-03)**: `sheets_service.dart`の`_fetchData`/`_postData`内の`print(`(生データダンプ含む)を全て削除し、状態ログのみ`debugPrint('[Antigravity] ...')`(日本語)に置き換えた | 本番ビルドのコンソールに生データダンプが出ない。`sheets_service.dart`内の`print(`が0件 | なし | S | ✅ |
| T3-75g | **残豆量の分母不整合**。(1) `5549c4ad`(Youth ケニア Kirinyaga Kii AB 中深煎り)は`初期購入量(g)`=35に対し抽出記録4件の使用量合計が60gで、`calculateBeanRemainingGrams`のclampにより0%表示になっている(実態と合っているか要確認)。(2) `1785051144979`(スイートイエロー)は`初期購入量(g)`=100 < `在庫基準量(g)`=135.5で、初期購入量より基準量が大きい(追加購入分が初期購入量に反映されていない)。T3-72a/T3-72fで扱う「初期購入量が未入力」問題とは別に、**入っている値同士が矛盾している**ケースなので切り出す | 2件の値がユーザー確認のうえ実態と合う値に補正される。追加購入時に`初期購入量(g)`と`在庫基準量(g)`のどちらを正とするかがコード・設計書で一意に定まっている／受入: test/acceptance/t3_75g_acceptance_test.dart | T3-72f | M | ⬜(要ユーザー確認) |

> **完了済み(6件)**: T3-75b, T3-75f, T3-75h, T3-75i, T3-75c, T3-75d
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

| T3-75h | ✅ **完了(2026-08-03)**: T3-75a〜gを本番URLで再確認した。**判定結果**: T3-75a=❌localhost限定(画像は本番で正常表示)、T3-75b/c/d/g=データ側の状態そのまま変化なし(未修正・要ユーザー確認は継続)、T3-75e=🔶本番では初回描画時のみの一過性に格下げ(再描画で回復)、T3-75f=本番でも再現しないことを再確認(修正済みのため当然)。画面遷移(D1/D2)・戻る操作・0%トグル等の操作異常なし、コンソール例外0件。手順は`docs/production_verification_guide.md`に従った。新規発見はT3-75iとして起票 | 各行の状態を更新済み。新規発見はT3-75i | なし | M | ✅ |
| T3-75i | ✅ **完了(2026-08-04)**: **抽出履歴詳細(003)でドリッパー名が稀にIDのまま表示される**問題(本番で1回観測、記録ID `1785746695316`)を修正。`lib/screens/log_detail_screen.dart`の`resolveName()`が、マスタ一覧に該当IDが無い場合、および`AsyncValue`が`data`以外の状態(取得エラー等)の場合に生のIDをそのまま返していたのを、両ケースとも`不明`を返すよう修正(豆・メソッド・グラインダー・ドリッパー・フィルター全項目に共通適用)。`test/log_detail_screen_test.dart`にマスタ取得失敗を模したケースを追加 | ドリッパー等マスター取得が失敗した場合でも詳細画面に生IDが出ない(代わりに「不明」等の日本語文言)。`flutter analyze`新規issue0・`flutter test`全パス | なし | S | ✅ |

> **確認して問題が無かった項目(2026-08-03)**: `coffee_data`171件の`ミル`/`ドリッパー`/`フィルター`/`抽出方法`/`産地ID`は未解決参照0件。`bean_master`の`産地ID`/`購入店ID`も未解決参照0件(空欄はあるが不正値は無し)。総合評価の平均は6.771(170件)。ダッシュボードのおすすめレシピは予測スコア6.2[4.8, 7.5]と区間内に収まっており表示は正常。本番`main.dart.js`のmd5はローカル`build/web`と一致しており、T3-72eの未デプロイ分は成果物に差が無い(デッドコード削除がtree-shakingで元から消えていたため)。
>
> **T3-75h(2026-08-03、本番URL再確認)で追加確認した内容**: 上記に加え`抽出方法`(メソッド)列も未解決参照0件(初回集計時のキー名取り違えによる誤検出170件を`メソッドID`列で再集計し訂正)。本番ブラウザで001/002/003/マスター管理/010/011/013(フィルター)/040/090の各画面を目視確認、画面遷移・戻る操作は正常、`Exception`/`Uncaught`/`TypeError`パターンでのコンソールエラーは0件。

#### Phase 3 追加分（2026-08-04 ユーザー要望・不具合、T3-76〜T3-79）

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-76 | ✅ **完了(2026-08-04)**: **不具合(2026-08-04ユーザー報告)**: 抽出履歴の編集画面(`lib/screens/log_edit_screen.dart`、002/003からの編集導線)が`CoffeeRecord`のごく一部(日時・豆量・総湯量・湯温・抽出時間・挽き目・コメント・7スコア)しか編集できず、**豆(beanId)を含む残りの項目(grinderId・dripperId・filterId・methodId・roastLevel・origin・originId・taste・concentration・bloomingWater・bloomingTime)が編集不可**だった。031(`brew_evaluation_screen.dart`)の`_resolveById`ドロップダウン選択パターン(直接編集させると存在しないIDになりうるマスタ参照を安全に選択)をそのまま再利用し、豆/グラインダー/ドリッパー/フィルター/メソッドをドロップダウン化。roastLevel/origin/originIdは031と同様に選択した豆から自動同期、taste/concentrationは4:6メソッド選択時のみ表示・保存(031のT3-18方針を踏襲)、bloomingWater/bloomingTimeは新規の数値入力欄として追加。本番URLをローカル配信し、実際に「豆: -」(未選択)の本番記録で編集画面を開いて豆選択→反映を確認済み(保存はユーザーの実データを書き換えるため未実施、UIの表示・選択反映のみ確認)。`test/log_edit_screen_test.dart`に3件追加(未選択マスタの選択・保存/豆未選択時のエラー/4:6メソッド時の味わい欄) | `log_edit_screen.dart`で`CoffeeRecord`の全編集可能フィールドが編集・保存できる。保存後、詳細画面(003)に反映される。`flutter analyze`新規issue0・`flutter test`全パス | なし | M | ✅ |
| T3-77 | ✅ **完了(2026-08-04)**: **要望(2026-08-04ユーザー報告)**: 抽出履歴一覧(002、`lib/screens/log_list_screen.dart`)にフィルタ機能が無い問題を解消。`LogListScreen`を`ConsumerWidget`から`ConsumerStatefulWidget`に変更し、豆(`_filterBeanId`)・メソッド(`_filterMethodId`)・期間(`_filterDateRange`)のローカル状態(setState、画面離脱でリセット)を追加。画面上部に絞り込み行を追加し、豆・メソッドはドロップダウンチップ、期間は`showDateRangePicker`を開くチップ(選択中は✕で個別解除可)、選択中フィルタ数をバッジ表示、1件でも選択中なら「リセット」ボタンを表示。フィルタ適用結果が0件のときは「条件に一致する記録がありません」+「フィルタをリセット」導線を表示。本番URLをローカル配信し実データ(豆28件・メソッド12件)で、単体フィルタ・複合フィルタ(豆+メソッド+期間で6件→3件)・リセット→全件復帰まで確認済み。コンソールエラー0件。`test/log_list_screen_test.dart`に2件追加(豆フィルタ+リセット/複合フィルタで0件時の案内文+リセット) | 002で豆・メソッド・期間の絞り込みができ、フィルタ適用中はリスト件数が絞り込まれる。フィルタ解除で全件表示に戻る。`flutter analyze`新規issue0・`flutter test`全パス | なし | M | ✅ |
| T3-78 | ✅ **完了(2026-08-04)**: **要望(2026-08-04ユーザー報告)**: 購入店のAI自動取得(028/027、`lib/screens/create/store_create_screen.dart`の`_fetchStoreInfoWithAi`→`AiAnalysisService.fetchStoreInfo`)が、`ambiguous`をAIが自己判断した場合のみ候補を出す設計のため、**似ているが別の店を高確度で誤って断定するリスクがある**問題を解消。①`StoreInfoCandidate`から`ambiguous`フィールドを削除し、`_buildStoreInfoPrompt`(`lib/services/ai_analysis_service.dart`)を常に候補を最大5件`candidates`に列挙させるプロンプトに変更(1件しか無い場合もその1件を返す指示に変更)。`store_create_screen.dart`側は`candidate.candidates.isNotEmpty`である限り常に`_showCandidateSelectionDialog`を経由してから、選択した候補で`fetchStoreInfo`を再実行(確定情報取得)するフローに変更(`_showCandidateSelectionDialog`は非空前提に簡素化、ダイアログ文言も「店舗の候補を確認してください」に変更)。②`fetchStoreInfo`のシグネチャを拡張し、`hintPrefecture`に加えフォームに入力済みの住所・URL・電話番号・営業時間・定休日・開業年・オンライン/実店舗/焙煎所の有無・豆の傾向・SNS URLを検索ヒントとして渡す(空欄の項目・未チェックのbool項目は渡さない)。`test/store_ai_fetch_test.dart`を新フローに合わせて更新し、候補1件のみでも候補選択を経由後に2回目の`fetchStoreInfo`で確定情報を取得し確認ダイアログに進むケースを新規追加。`flutter analyze`新規issue0・`flutter test`354件全パス(新規1件)・`flutter build web`成功。ブラウザではGemini APIキーが本セッションのローカル配信オリジンに保存されていないため実際のAI応答フローは確認できず、028画面の表示・APIキー未設定時のダイアログ表示・コンソールエラー0件のみ確認(ロジックはwidgetテストで担保) | AI自動取得を実行すると常に候補選択ダイアログが挟まり、フォームに既に入力済みの情報が検索の絞り込みに使われる。`flutter analyze`新規issue0・`flutter test`全パス(既存の`store_ai_fetch_test.dart`の期待値も新フローに合わせて更新) | なし | M | ✅ |
| T3-79 | ✅ **完了(2026-08-04)**: **不具合(2026-08-04ユーザー報告)**「注湯ステップのハイライトが途中からずれる」。本番`pouring_steps`実データ(method001=4:6メソッド、654c2399=井崎式)で`_activeStepIndex`(`lib/screens/brew_recipe_screen.dart`)の計算をトレースし原因を特定: 0秒ステップ(瞬間アクション)の直後の非ゼロステップへハイライトを譲るかどうかを、**そのステップ自身の説明文が空かどうかを見ずに常に譲っていた**バグ。4:6メソッドのように直後が空文字の説明のみなら問題は表面化しないが、井崎式のように直後のステップにも独自の指示文があるメソッドでは、そのステップの指示が一度もハイライトされず0秒ステップ側に奪われ続け、実際の注湯タイミングとハイライトが噛み合わなくなる。ロジックを`lib/utils/pouring_step_highlight.dart`の`activeStepIndex()`に切り出し、「直後のステップの説明文が空の場合のみ」0秒ステップへ譲るよう修正、`test/pouring_step_highlight_test.dart`に本番データを模したケース(4:6メソッド型・井崎式型)で回帰テストを追加 | 0秒ステップの直後に独自の説明文を持つステップが、自分自身の待機区間中にハイライトされる。`flutter analyze`新規issue0・`flutter test`全パス | なし | S | ✅ |

> **完了済み(4件)**: T3-76, T3-77, T3-78, T3-79

#### Phase 3 追加分（2026-08-05 ユーザー報告、T3-80: 注湯ハイライトずれの再発）

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-80a | ⚠️**上位モデルで実施(完了、2026-08-05)**: **不具合(2026-08-05ユーザー報告)**「pouring stepのハイライトがまたずれている」(T3-79の修正後も未解消/再発)。ユーザー指示により①上位モデルで検証設計→②下位モデルで検証→③上位モデルで修正案検討→④下位モデルで実装、の4フェーズで進める。本タスクはフェーズ①=**検証要領書`docs/pouring_highlight_verification_plan.md`の作成**(コード変更なし) | 検証要領書が存在し、仮説H1〜H7・手順・報告テンプレが下位モデルだけで実行できる粒度で書かれている | なし | S | ✅ |
| T3-80b | ✅ **完了(2026-08-05、Sonnet 5サブエージェント)**: フェーズ②=検証要領書に従い、本番`pouring_steps`全12メソッド+`methods_master`を`curl`取得して机上表を作成、`build/web`をローカル配信して`method001`・`654c2399`で実機観測(スクショ・コンソール確認)。**H1・H2・H4・H5・H6を「観測された」と判定**(H3は一部・H7は検証不能)。加えてメソッド切替時に経過時間・説明列がstaleになる表示不整合を新規発見。コードは未変更 | 要領書§6の完了条件を全て満たした報告が提出されている | T3-80a | M | ✅ |
| T3-80c | ⚠️**上位モデルで実施(完了、2026-08-05)**: フェーズ③=T3-80bの観測事実から**根本原因を確定**し`docs/pouring_highlight_fix_design.md`を作成。`method001`の表示時刻が粕谷式4:6メソッドの実際の注湯タイミングと一致することから、`加算時間（秒）`=「その操作を行うまでの待ち時間」・「経過時間」列=「その操作を行う時刻」と意味論を確定。**表示仕様は正しく、点灯区間が`[前の操作時刻, 自分の操作時刻)`= 常に1行先だったことがずれの正体**と結論(T3-79は症状の一部のみ修正していた)。修正4点(点灯窓の再定義+同時刻同時点灯+最終行保持 / 一時停止中も保持 / 時刻・説明列の`ValueKey` / メソッド切替時のタイマーリセット)とテスト12ケースを確定 | 下位モデルが設計判断なしに実装できる粒度の修正方針が文書化されている | T3-80b | S | ✅ |
| T3-80d | ✅ **完了(2026-08-05、Sonnet 5サブエージェント)・本番デプロイ済み**: フェーズ④=設計書通りに実装。`lib/utils/pouring_step_highlight.dart`を`activeStepIndex`(int?)→**`activeStepIndexes`(Set&lt;int&gt;)**へ全面書き換え(点灯窓を「自分の操作時刻〜次の操作時刻」に変更、同一操作時刻の行は同時点灯、最終操作時刻の超過後も最終行を点灯継続、T3-79の0秒ステップ譲渡ロジックは全廃)。`brew_recipe_screen.dart`は一時停止中もハイライトを保持(リセット時のみ消灯)+メソッド切替時にタイマーをリセット。`method_steps_editor.dart`は時刻列・説明列の`TextFormField`に`ValueKey('time_${s.id}')`/`ValueKey('desc_${s.id}')`を付与し、メソッド切替後に旧メソッドの値が残る表示不整合(T3-80bで発見、湯量列だけキーを持っていたのが原因)を解消。テストは`pouring_step_highlight_test.dart`を新仕様の12ケースへ全面書き直し+`method_steps_editor_test.dart`に同時点灯ケース1件追加。`flutter analyze`新規issue0・`flutter test`360件全パス・`flutter build web`成功。ローカル配信で4:6メソッド(0:45/1:30でタイマーと点灯行が一致)・井崎式(0:00の0秒ステップ点灯、2:00の2行同時点灯、3:00超過後も最終行点灯)・一時停止保持・リセット消灯・メソッド切替を実機確認、コンソールエラー0件。`firebase deploy --only hosting`実行後、本番`main.dart.js`のmd5一致(`0d71511a...`)を確認 | ハイライトが実際の注湯タイミングと一致する。`flutter analyze`新規issue0・`flutter test`全パス | T3-80c | M | ✅ |

> **完了済み(4件)**: T3-80a, T3-80b, T3-80c, T3-80d(いずれも2026-08-05、本番デプロイ済み)

#### Phase 3 追加分（2026-08-02、T3-73: 日次ループのトークン消費削減）

**正本は `docs/token_optimization_design.md`**(実測ベース。本表と食い違う場合は設計書が優先)。

**実測サマリ**: 1ループのコストは「リクエスト数 × 平均コンテキスト長」でほぼ決まり、cacheRが全体の6割。直近の高コストループは平均ctx 207〜231k・最大337〜355kで**200kの料金境界(単価約2倍)を越えていた**。これがコスト差2.6倍($15.3 vs $5.9)の主因。目標は **1ループ $15 → $7〜9**。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-73g | ✅ **完了(2026-08-05)**: T3-73d〜f適用後の初回`/full_loop`(本ループ自体)の実測を設計書§7に記録(23リクエスト・平均ctx 77,659・最大96,379・$1.1。本ループはコード変更なしの軽量ループのため$7〜9目標との比較は不可、実コード変更を伴う通常ループでの再測定が必要) | §7に実測値が1行以上記録されている → 達成 | T3-73a, T3-73d | S | ✅ |

> **完了済み(7件)**: T3-73a, T3-73b, T3-73c, T3-73d, T3-73e, T3-73f, T3-73g(詳細は`docs/archive/マスタープラン_完了タスク.md`「T3-73」節)

### Phase 4 — 統計解析・予測機能拡張（Cycle 28+、最後）

> **設計書は単一の正本**(CLAUDE.md「統計解析・予測機能の実装ルール」参照)。本表と食い違う場合は設計書が優先。**設計書§0の絶対規則によりサブPhase順(0→1→2→3→4→5→6)を厳守**し、各サブPhaseはテスト全パス(設計書§9)まで次に進まない。ID の数字はこのサブPhase番号に対応(例: T4-2xはサブPhase2=F1回帰分析)。
>
> **Phase 終了条件:** F0〜F6すべてが実装され、設計書§9の全テストがパスし、Phase 1完了時点で必要なユーザー作業(実データ移行の確定、GAS `scriptId` 記入と初回`clasp login`)が完了している。


> **完了済み(25件)**: T4-0a, T4-0b, T4-0c, T4-1a, T4-1b, T4-1c1, T4-1c2, T4-1d, T4-1e, T4-1f, T4-2a, T4-2b, T4-2c1, T4-2c2, T4-2d, T4-3a, T4-3b, T4-4a, T4-4b, T4-4c, T4-5a, T4-5b, T4-6a, T4-6b, T4-6c
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

### Phase 5 — Android公開版の開発〜リリース（Cycle 30+）

> **設計の正本は `docs/android_release/リリース計画書.md`**(全体戦略・3トラック・フェーズ定義)。
> 自動実行の詳細は `docs/android_release/開発運用基盤設計.md`、検証体制の詳細は `docs/android_release/検証強化設計.md`。**本表と食い違う場合は各設計書が優先。**
> 原価・収益の前提は `docs/android_monetization/`(コスト試算・収益化アイデア・コードベース構成方針)。

**Phase 終了条件:** Android 公開版が Google Play の製品版として公開され、AI サブスクの購入〜回数消費〜広告表示が本番で動作する。

**着手順の絶対規則(2026-08-07 ユーザー確定)**:

1. **トラックA(開発運用基盤)を最初に完成させる。** 夜間の自動実行が無いまま製品開発を始めると、40〜60人日規模を有人セッションだけで消化することになり破綻する
2. **トラックC の T5-C1(Play Console登録)は今すぐ開始。** テスター12人は知り合いから確保可能(2026-08-07確認済み)のため人集めは律速にならず、**残るクリティカルパスは Play Console の本人確認と14日間の待機のみ**
3. **夜間ループには設計判断をさせない。** `⚠️上位モデル`タスクは有人セッションで先に消化し、確定済みタスクの在庫を切らさない

サイズ: S=半日余裕、M=1ループ標準、L=1ループぎりぎり(**夜間ループはS/Mのみ着手可**)

#### トラックA — 開発運用基盤(最優先、他の全タスクの前提)

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-A8 | golden テスト基盤。共通コンポーネント単位で**ライト/ダーク両方**を撮る。CIでの差分検出。**LLMにgoldenを自動更新させない**運用ルールを`rules/verification.md`に明記。**実装時にoverflow判定をwidget test化**(`FlutterError.onError`差し替え+`tester.view.physicalSize`を複数解像度で変えてpump、検証強化設計 §5-2b D-4参照)し、`ui_verifier`のエミュレータ依存項目を減らす | 意図的な色変更でgoldenが落ちる | なし | S | ✅ |
| T5-A45 | goldenのOS非依存化。`flutter_test_config.dart`等でフォントを固定し、Windows/Ubuntu両方でgoldenが一致するようにする(T5-A8はWindows専用ベースライン+非Windows skipで暫定対処済み、恒久解決は未着手) | Windows/Ubuntu双方で`flutter test test/golden`が同一ベースラインでpassする(skip無し)／受入: test/acceptance/t5_a45_acceptance_test.dart | T5-A8 | M | ⬜ |
| T5-A12 | **有人監視下で`night_loop.ps1`を1回手動実行**し、(a)権限プロンプトで止まらない(b)`night_report.md`が生成される(c)ゲートが正しく判定する、を確認。その後タスクスケジューラに**23:00の1枠だけ**登録し3晩観察してから04:10・09:20を追加(開発運用基盤設計 §6の段階1〜5)。**いきなり無人で回さない** | 3晩連続で想定どおり動作する | T5-A10, T5-A11(完了済み) | M | ✅(2026-08-14完了。修正後〈`activeSessionMinutes`+使用率記録専用化+作業ツリー汚れガード〉の初回観察: 23:00・04:10の2枠は`.claude/night_runs.log`に増分し無人発火・実行を確認〈旧mtimeバグ〈L146〉の再発なし〉。09:20は04:10の残置dirty worktreeにより`skipped_dirty_worktree`で正しくスキップ(新設ガードが意図どおり機能)。T5-A46〜48ダミータスクは一度も選定されず未使用のまま完了条件を満たしたため削除、`docs/night_loop_verification_log.md`も未作成のため対象なし。**新たに600秒バックグラウンドタイムアウトでラッパーごと強制終了される問題を発見**(T5-A12が検証対象としていたバグとは別種、詳細はNEXT_SESSION.md `-5.86`節)) |
| T5-A13 | `.claude/agents/implementer.md` にAndroid/公開版の規約を追記。DBスキーマ移行の必須セット/`AppEdition`経由の分岐のみ許可(ウィジェット内`if (edition == ...)`禁止)/公開版画面はホワイトリスト方式 | 定義に3項目が明記されている | なし | S | ✅ |
| T5-A14 | **Proプランで`model: opus`のサブエージェント(`architect`)が起動できるか実測**。使えない場合は「設計は有人セッションで先出し、夜間ループに設計判断をさせない」運用をリリース計画書 §5 に沿って徹底する旨を`CLAUDE.md`に追記 | 可否が事実として記録され、不可の場合の代替運用が明文化されている | なし | S | ✅ |
| T5-A15 | `analysis_options.yaml` にlint追加(`avoid_catches_without_on_clauses`/`unawaited_futures`/`avoid_empty_else`/`always_use_package_imports`)。既存違反が多い場合は**新規ファイルのみ適用**のbaseline運用から始める | 空catchを新規に書くとanalyzeで落ちる | なし | S | ✅ |
| T5-A16 | トラックA完了後の最初の3ループのトークン実測を`docs/token_optimization_design.md` §7 に記録。悪化していたら`adversary`の起動条件を差分行数で絞る | §7に3行以上の実測値が記録されている | T5-A12 | S | ✅(2026-08-15完了。直近の`implementer`+`verifier`定型ループ3件〈T5-A83/A88/A89〉平均$5.98で目標〈現状維持$9〜10〉を下回り悪化なしと判定、`adversary`起動条件は変更せず維持。詳細は`docs/token_optimization_design.md` §10-3-1) |
| T5-A17 | **⚠️ユーザー実施(アシスタントは自分の権限設定を書き換えられない)**: `.claude/settings.night.json`(無人実行専用の権限プロファイル。`dontAsk`+deny主体)を設置する。**内容は開発運用基盤設計 §4-4 に全文記載済み**、コピーして保存するだけ。無人実行に「都度確認」は成立しないため、危険操作は確認ではなく**deny で実行不可能にする**という設計。**2026-08-10設置済み**(ファイル自体は存在)。**同日判明(教訓L140)**: `allow`に裸の`"Edit"`/`"Write"`があっても`.claude/`配下へのEdit/Writeはハーネス側の分類器が優先してハードブロックすると判明、`night_report.md`を`.claude/`外(リポジトリルート)へ移動して対処済み(commit a634202)。**この修正は未検証**(次回の`night_loop.ps1`実行で`night_report.md`が正しく更新されるか要確認) | ファイルが存在し、T5-A12の試走で(a)止まる定型コマンドが無い(b)denyが実際に効く(c)ゲート通過時のmain pushが通る、が確認できる | なし | S | ✅(2026-08-14完了。T5-A12の23:00・04:10の無人発火で(a)権限プロンプトで停止せず(b)`night_report.md`が両枠とも正しく更新される(c)ゲート判定〈09:20のdirty worktree検知〉が機能することを実地確認、(b)の再検証〈`.claude/`外への移動修正〉も含めて解消) |
| T5-A25 | 夜間ループの`/night_loop`起動回数カウンタを実装(`.claude/night_loop_run_count.txt`等)し、**10回に1回`/code-review`(medium)を実行**するよう`tools/night_loop.ps1`または`night_loop`スキルに組み込む。ルール本体は`CLAUDE.md`§日次改修ループ運用ルール「`/code-review`の定期実行ルール」(2026-08-08新設)。Critical/Major指摘が出た場合はその場でimplementerに差し戻して修正してから通常のpushゲート判定に進める | カウンタが正しくインクリメントされ、10回目の起動で`/code-review`相当のレビューが実行されることを確認できる | なし(T5-A12の試走と合わせて確認すると効率的) | S | ✅ |
| T5-A29 | `/full_loop`起動回数カウンタを実装(`.claude/full_loop_run_count.txt`等、T5-A25の`/night_loop`版と同一パターン)し、**10回に1回だけ**`docs/token_optimization_design.md` §7・§8の蓄積ログを元にした無駄調査を`architect`へ委譲するよう`full_loop`スキルに組み込む。毎回の軽量記録(loop_guardコスト・ターン数・使用率%)自体はカウンタと無関係に**常に**行う(ルール本体は`CLAUDE.md`§日次改修ループ運用ルール「トークン浪費の調査ルール」、2026-08-09新設) | カウンタが正しくインクリメントされ、10回目の起動でarchitectへの無駄調査委譲が実行されることを確認できる | なし(T5-A25と合わせて実装すると効率的) | S | ✅ |
| T5-A37 | **⚠️ユーザー実施(アシスタントは分類器にブロックされ実施不可、実測済み)**: `~/.gemini/antigravity-cli/settings.json`に`permissions.allow`で個別コマンド単位の許可ルール(例`command(echo)`)を追加し、意図した1コマンドだけが許可され他は拒否されたままかを確認する。詳細は`docs/antigravity_delegation_design.md` §3・§4・§5-1 | ルールが意図どおりのスコープ(許可対象コマンドのみ通り、対象外は引き続き拒否)で動くことを実機確認し、結果を同設計書§5-1に記録している | なし | S | ✅ |
| T5-A38 | `tools/antigravity_delegate.ps1`(Windows本命)・`tools/antigravity_delegate.sh`(Ubuntu/Git Bash)を実装。**仕様は`docs/antigravity_delegation_design.md` §9.2〜§9.4で確定済み**(引数表・標準出力の1行JSONスキーマ・終了コード0/2/10〜17・プロンプト3層構造・agy固有の上書きブロック全文・台帳`.claude/agy_logs/ledger.tsv`の列)。両スクリプトは同じ引数名・同じJSONスキーマ・同じ終了コードを持つ。`--dangerously-skip-permissions`を渡さない、`~/.gemini/antigravity-cli/settings.json`を読み書きしない、ラッパー自身はフォールバックを実行せず非0終了で親へ知らせるだけ。`.gitignore`に`.claude/agy_logs/`を追加する | `-DryRun`でプロンプトが3層構造で組み上がる。`-Role implementer`でdocs配下のファイル編集タスクが実行でき、標準出力が§9.2のスキーマの1行JSON(`response_head`は800文字で切れている)になる。`-Role adversary`は`--mode plan`で起動し、変更が起きた場合exit 17を返す。agy未検出時にexit 10を返す | T5-A40 | M | ✅ |
| T5-A39 | リポジトリ直下に`AGENTS.md`を新設(20行以内)。**内容は`docs/antigravity_delegation_design.md` §9.3「`AGENTS.md`の確定内容」のコードブロックをそのまま採用する**(創作しない) | ファイルが20行以内で存在し、§9.3の内容と一致している。`agy -p "このリポジトリの永続化バックエンドは何ですか" --output-format json`の応答が日本語でSheetsと答える | なし | S | ✅ |
| T5-A41 | パイロット導入: `tools/antigravity_delegate.ps1`を有人`/full_loop`の`implementer`役で3回試用する(対象は`docs/`・`tools/`・`.claude/`配下の非Dartファイル。うち1件は`-Model gemini-3.1-pro-high`で実施しモデル比較する)。判定条件は`docs/antigravity_delegation_design.md` §9.7-5の4項目(verifier全項目パス/日本語規約違反ゼロ/既知の地雷を踏んでいない/差し戻し1回以内)。結果を§7実績ログへ記録し、§9.5の状態遷移表(パイロット→条件付き常時→常時委譲)のどこへ進めるかを結論づける | 3回分の実績ログ(§7の表形式)と、§9.5のどの状態へ移行するかの結論が記録されている | T5-A38, T5-A39, T5-A42 | M | ✅ |
| T5-A42 | agy委譲ルートをスキル・規約へ配線する。(a)`.claude/skills/full_loop/SKILL.md`§サブエージェントへの委譲の表を`docs/antigravity_delegation_design.md` §9.5の4行の表へ差し替え、手順3に「§9.1のルーティング表で委譲先を決める。判定できなければClaudeを選ぶ(迷ったらClaude)」を追記 (b)`.claude/skills/night_loop/SKILL.md`に「T5-A41の昇格判定完了までagyを使わない」を明記 (c)`CLAUDE.md`§日次改修ループ運用ルールのモデル分担ルールに「agy委譲を導入しても親セッションはSonnet 5のまま(根拠: 同設計書§8)」と「同一タスクでagyを2回続けて使わない/agyの非0終了は`loop_failures.txt`に数えない」を追記 | 3ファイルに上記が反映され、`grep -n "antigravity_delegate" .claude/skills/*/SKILL.md CLAUDE.md`で参照が見つかる。ルーティング表の内容が§9.1と食い違わない | T5-A38 | S | ✅ |
| T5-A43 | `.claude/hooks/loop_guard.js`に`.claude/agy_logs/ledger.tsv`の参考集計を追加。ループ境界タイムスタンプ以降の行を数え、`loop_state.md`に`- agy委譲(枠外・参考): N件 / 成功x・フォールバックy / 合計Z秒 / Gemini 5時間残 P%`の1行を出す。**コスト閾値($24/夜間$8)の判定には一切含めない**。台帳が無い/壊れている場合は`try/catch`で握りつぶし行自体を出さない。あわせて`CLAUDE.md`§日次改修ループ運用ルールの終了条件に「agy経由の委譲はGeminiバケットのためコスト上限に含めない」を1行追記(設計は`docs/antigravity_delegation_design.md` §9.6) | 台帳にダミー3行を置いた状態でフックを発火させると`loop_state.md`に参考行が出る。台帳を削除しても従来どおり動き、どちらの場合も`cost=`の値が変化しない | T5-A38 | S | ✅ |
| T5-A44 | 実績ログの運用を配線する。`.claude/skills/end/SKILL.md`の締め手順に「その日にagy委譲を行った場合、`.claude/agy_logs/ledger.tsv`の該当行を`docs/antigravity_delegation_design.md` §7の表へ1行ずつ転記し、`verdict`列(ok/ng/fallback)を埋める」を追加する(台帳は`.gitignore`対象でコミットされないため、§7が唯一の永続記録になる) | `/end`スキルに手順が追加され、§7の表に少なくとも1行の実績が転記されている | T5-A38 | S | ✅ |
| T5-A49 | `/insights`レポート(2026-08-12)提案のCLAUDE.md追記のうち3件を反映(2026-08-13ユーザー指示で(b)→T5-A55、(d)→T5-A56へ統合、本タスクは(a)(c)(e)のみ): (a)無人ループは`.claude/`直下への書き込み禁止を明記し出力先を明確化(**pushの自動承認は既存ルール〈検証完了時〉のまま、デプロイ確認は従来どおり必須で変更しない**) (c)概念的な設計質問にはまず文章で直接回答し許可なくリサーチエージェントを起動しない旨の明記 (e)タスク開始前のコスト見積もりで実装+検証+デプロイが予算内か確認するルールの明記(既存§日次改修ループ運用ルールと重複しない範囲で統合) | 3項目がCLAUDE.mdに反映され、既存の「デプロイ・push・削除の確認ルール」と矛盾しない(デプロイは引き続き都度確認) | なし | S | ✅(2026-08-13完了。CLAUDE.mdの§日次改修ループ運用ルール・§出力量の規約へ(a)(c)(e)を反映、verifier検証パス) |
| T5-A50 | `/insights`レポート提案: PostToolUseフックでEdit/Write後のファイルエンコーディング(BOM等)を検査する`tools/check_encoding.ps1`+`.claude/settings.json`へのフック登録。過去のagy BOM喪失事故(T5-A41)の再発防止と重複するため既存の上書きブロック運用との重複可否を確認しつつ実装する | 意図的にBOMを壊した編集でフックが検知しログに残ることを確認 | なし | M | ✅(2026-08-13完了。`tools/check_encoding.ps1`新設+`.claude/settings.json`の`PostToolUse`〈`Edit\|Write`〉へ登録。BOM無し`.ps1`編集で`.claude/encoding_warnings.log`に警告が追記されることをverifierが独立再実行で確認) |
| T5-A51 | `/insights`レポート提案: `/verify`スキル新設(Chrome優先・エミュレータ2回上限・PASS/FAIL表形式の検証結果出力をコード化)。既存`verifier`エージェント運用との役割分担を明確にする | `/verify`スキルが存在し、full_loop/night_loopスキルから呼び出せる | なし | M | ✅(2026-08-13完了。`.claude/skills/verify/SKILL.md`新設〈`full_loop`手順4の委譲テンプレートを再利用する薄いラッパーとして設計、車輪の再発明を回避〉。`full_loop`手順4から参照を追加。verifier確認済み) |
| T5-A52 | `/insights`レポート提案: night_loop起動をheadlessモード相当(`--allowedTools`明示)に見直せるか検討・試行。既存の`.claude/settings.night.json`権限プロファイル(T5-A17)との重複・競合を先に整理すること | 既存プロファイルとの関係が整理され、採用する場合は無人実行が権限プロンプトなしで動くことを確認 | T5-A17 | S | ✅(2026-08-13完了。**結論: 見送り**。`claude --help`実機確認で`--allowedTools`/`--disallowedTools`は`settings.night.json`の`allow`/`deny`と同構文と判明。設計書§4-4は「deny優先・allowは効かせない」方針を既に明記しており、allow-list化は列挙漏れ時に無人ループが無言で詰まるリスク〈設計書§7が警戒する失敗モードそのもの〉を新たに生む。実際にその失敗が観測された記録も無いため、現状のdeny-list多層防御を維持する。CLIフラグと`--settings`ファイルのマージ優先順位も未確定でリスクを取る根拠に乏しい。コード変更なし) |
| T5-A53 | `/insights`レポート提案: ループ冒頭の60秒プリフライトスクリプト(`tools/preflight.ps1`。ファイルロック・PATH上の必須バイナリ・書き込み権限を確認)を実装しfull_loop/night_loopスキルのstep 0に組み込む。tail.exeロック事故(2026-08-12対応済み、commit 77d6094)・agy PATH不在事故の再発防止と重複するため、既存対応との重複範囲を先に確認する | 意図的に3種の障害を再現してpreflightが全て検知することを確認 | なし | M | ✅(2026-08-13完了。`tools/preflight.ps1`新設〈ロック・PATH・書き込み権限の3項目〉、`tools/night_loop.ps1`起動の早い段階〈claude CLI解決より前〉に組み込み失敗時は`outcome=error_preflight_failed`で安全終了。`full_loop`/`night_loop`スキルにも組み込み記述追加。3種の障害注入をimplementer・verifierそれぞれ独立に再現し検知を確認。**注意**: 同日中にT5-A58の設計〈`docs/failure_playbook.md`〉がこの`tools/preflight.ps1`を失敗プレイブック基盤へ統合する提案〈T5-A66〉をしている。統合するかどうかは次回T5-A61着手前に判断すること) |
| T5-A54 | `/insights`レポート提案「タスクサイズ調整」(**ユーザーが明示的に試行を指示**): full_loop/night_loopスキルに実装前のコスト見積もり・タスク分割ステップを追加。直近ループのloop_guard実測を元に予算超過が見込まれる場合はより小さいタスクを選ぶか、実装/検証+デプロイの2タスクに分割する | 見積もりロジックが動作し、予算超過時に分割または縮小の判断が記録される | なし | M | ✅(2026-08-13完了。`full_loop`手順2.5〈着手前見積もり、超過見込み時はL=分割/M,S=ユーザーへ報告して判断〉・`night_loop`手順2.5〈無人のため安全側へ自動的に倒す〉を新設。既存の手順3.5「セッション分割チェック」〈実装後の実測に基づく事後判断〉とは「着手前の予測」として役割分担を明記し矛盾なし) |
| T5-A55 | `/insights`レポート提案: 委譲後の必須diffレビュー・長時間委譲エージェントへの進捗ハートビート/タイムアウトルールを委譲ルール文書へ明記。agy BOM喪失事故(T5-A41)の教訓と重複するため統合する。**T5-A49(b)(サブエージェント委譲時のエンコーディング/フォーマット制約の明示・委譲後の再読み込み+diff確認・進捗報告間隔のルール)を統合(2026-08-13ユーザー指示)** | ルールが文書化され、委譲後のdiff確認手順・エンコーディング制約・進捗報告間隔が明文化されている | なし | S | ✅(2026-08-13完了。`.claude/skills/full_loop/SKILL.md`「委譲時の共通ルール」へ3項目〈必須diffレビュー・進捗ハートビート/タイムアウト目安10分・エンコーディング事後確認〉を追記。verifier検証パス) |
| T5-A56 | `/insights`レポート提案: 「既知の失敗しやすい検証経路」節をCLAUDE.mdまたは`rules/verification.md`へ追加(Androidエミュレータ・GAS直curl等、リトライ上限2回のハードルール化)。**T5-A49(d)(Androidエミュレータ検証2回上限・ブラウザ優先・GAS直curl不可の明記)を統合(2026-08-13ユーザー指示)** | 節が追加され、リトライ上限と代替手段が明記されている | なし | S | ✅(2026-08-13完了。`rules/verification.md`に「## 既知の失敗しやすい検証経路」節を新設〈エミュレータ2回上限・ブラウザ優先・GAS直curl不可〉、CLAUDE.mdのVerification Rules節から参照を追加。verifier検証パス) |
| T5-A57 | ⚠️**上位モデルで実施**: `/insights`レポート「今後の展望」由来(**ユーザーが「面白い、やってみたい」と表明**)。並列マルチトラック夜間ループフリートの設計——git worktree単位で独立したimplementerを並列実行しverifier/adversaryゲート通過分のみマージするアーキテクチャ。成果物は設計書+実装タスク分解のみ(コードは書かない) | 設計書が存在し、依存関係の無い3〜5トラックへの分割方針・マージ手順・トークン予算管理方針が確定している／受入: 免除(設計タスク) | なし | L | ⬜(**2026-08-13、ユーザーと相談の結果「見送り」で合意**。理由: 設計コストだけでもOpusトークンを消費する一方、並列化の価値はトラックBが本格化して直列スループットが実際にボトルネックになった時点で初めて出る。現状トラックA残件は十数件・トラックBは意図的に未着手のため、今並列化基盤を先行整備するROIが低いと判断。**トラックBが本格化しスループット不足が顕在化した時点で再検討すること**) |
| T5-A58 | ⚠️**上位モデルで実施**: `/insights`レポート「今後の展望」由来。失敗プレイブックを備えた自己修復ループの設計——既知障害(ファイルロック・BOM喪失・エミュレータクラッシュ・権限拒否・エージェントハング)の検知シグネチャと自動対処、未知障害のみエスカレーションする仕組み。成果物は設計書+実装タスク分解のみ | `docs/failure_playbook.md`相当の設計書が存在し、既知障害ごとの検知条件・自動対処・エスカレーション基準が確定している | なし | L | ✅(2026-08-13完了、architect設計。`docs/failure_playbook.md`新設、既知障害7件〈FP-01〜07〉+安全側判断基準〈冪等・非破壊・限定範囲・誤爆時無害の4条件を全て満たすものだけ自動実行、プロセスkill等は原則warnのみ〉+未知障害の証拠束自動生成を確定。実装タスクT5-A61〜A68へ分解済み〈下記参照〉) |
| T5-A59 | ⚠️**上位モデルで実施**: `/insights`レポート「今後の展望」由来。実行可能な受け入れハーネス(プロパティベースのゴールデンループ)の設計——タスクごとに本番に対してexit 0を返す検証スクリプトを必須化し、検証を「物語」ではなく「契約」にする。成果物は設計書+実装タスク分解のみ | 設計書が存在し、検証スクリプトの規約(命名・配置・exit code)とfull_loopスキルへの組み込み方針が確定している | なし | L | ✅(2026-08-13完了、architect設計。`docs/acceptance_harness_design.md`新設。**適用範囲を限定**〈サイズM/L・実行成果物への変更・完了条件が機械判定可能、の3条件全て満たすタスクのみ必須。ドキュメントのみ/Sサイズ/設計のみ/視覚的主観判断のみは免除〉、スクリプト規約〈既定はDart`test/acceptance/`、例外PowerShell`tools/acceptance/`、exit code 0/1/2/3以上〉、`tools/verify.ps1`への`-Task`引数追加方針を確定。実装タスクT5-A69〜A72へ分解済み〈下記参照〉) |
| T5-A61 | (T5-A58設計の実装その1)失敗プレイブック基盤: 検知シグネチャ照合の共通関数+`.claude/failure_reports/`への証拠束出力の仕組みを実装。**着手前に、同日完了済みのT5-A53(`tools/preflight.ps1`)との統合要否を判断すること**(`docs/failure_playbook.md`のT5-A66〈本表ではA66相当〉が統合を提案しているため、重複実装を避ける) | `docs/failure_playbook.md`記載の基盤仕様どおり動作し、T5-A53との関係(統合するか併存するか)が明文化されている | なし | M | ✅(2026-08-13完了。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A61」節) |
| T5-A62 | (T5-A58設計の実装その2)FP-01(ログファイルロック)・FP-02(agy BOM喪失)ルールの実装(検知+安全側の自動対処または警告) | `docs/failure_playbook.md`記載の該当ルールどおり検知・対処すること(または誤爆リスクが大きい場合はwarnのみに留めること)を実地確認 | T5-A61 | M | ✅(2026-08-13完了。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A62」節) |
| T5-A63 | (T5-A58設計の実装その3)FP-03(エミュレータクラッシュ/ハング)・FP-07(必須バイナリ不在・ディスク不足)ルールの実装 | 同上、`docs/failure_playbook.md`記載の該当節どおり | T5-A61 | S | ✅(2026-08-13完了。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A63」節) |
| T5-A64 | (T5-A58設計の実装その4)FP-04(権限拒否)・FP-06(同一スキップ/エラー連続)ルールの実装。**FP-04は検知+`allow`追加候補行の生成のみ、権限設定の自動書き換えは禁止**(`docs/failure_playbook.md`の安全側判断基準どおり) | 同上 | T5-A61 | S | ✅(2026-08-13完了。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A64」節) |
| T5-A65 | (T5-A58設計の実装その5)FP-05(エージェント/claudeハング)ルールの実装。子孫プロセス特定停止は`docs/failure_playbook.md`記載のしきい値(暫定20分/40分/90分、未観測のため実測3回分を見てから調整)で実装し、対象0件時は停止しないことを保証する | 同上。誤爆(無関係プロセスの停止)が起きないことを実地確認 | T5-A61 | M | ✅(2026-08-13完了。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A65」節) |
| T5-A66 | (T5-A58設計の実装その6)`tools/night_loop.ps1`への配線。T5-A61〜A65の各ルールを起動フローへ組み込み、**T5-A53(`tools/preflight.ps1`)をこの設計へ統合して閉じるかどうかをユーザーに確認した上で反映する**(`docs/failure_playbook.md`§9-5) | 配線後も既存のnight_loop.ps1の既存フロー(多重起動ガード・git pull等)が壊れないことを確認 | T5-A62, T5-A63, T5-A64, T5-A65 | S→実装はM相当に拡大 | ✅(2026-08-14完了、PR #3マージ。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A66」節・教訓L152) |
| T5-A67 | (T5-A58設計の実装その7)⚠️**ユーザー実施**: `.claude/settings.night.json`の`allow`へ`failure_playbook`関連スクリプトの実行許可を追加(追加しないとFP-04〈権限拒否検知〉が自分自身の実行時に拒否されてしまうため) | `.claude/settings.night.json`に追加され、無人実行でfailure_playbook関連スクリプトが権限プロンプトなしで動く／受入: 免除(Sサイズ) | T5-A66 | S | ⬜(2026-08-14ユーザーが許可行を追加済み。ただし追加時のJSON文字列が`tools\failure_playbook.ps1`〈バックスラッシュ未エスケープ〉となっており`\f`がフォームフィード制御文字として解釈され意図した文字列にならないバグを親が発見・`tools\\failure_playbook.ps1`へ修正、JSON妥当性を確認済み。無人実行での実地動作確認〈権限プロンプトなしで動くこと〉はT5-A68で行う) |
| T5-A68 | (T5-A58設計の実装その8)障害注入テスト: FP-01〜07を意図的に再現し、検知・自動対処・エスカレーションがすべて設計書どおり動くことを確認する | 7パターン全てで検知・対処・エスカレーション基準が設計書と一致することを実地確認／受入: tools/acceptance/t5_a68_check.ps1 | T5-A66, T5-A67 | M | ⬜(2026-08-13ユーザー承認により解除、本日以降着手可) |
| T5-A69 | (T5-A59設計の実装その1)`tools/acceptance/`新設+`tools/verify.ps1`/`tools/verify.sh`へ`-Task <タスクID>`引数と9項目目`acceptance`を追加。フォールトインジェクション3ケース(存在しないID→missing/exit1→fail/exit2→skipのままok)を実測で含める | `docs/acceptance_harness_design.md`記載のスクリプト規約(exit code 0/1/2/3以上、stdout1行JSON)どおり動作し、3ケースのフォールトインジェクションが全て期待どおりの判定になることを確認 | なし | M | ✅(2026-08-14完了。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A69」節) |
| T5-A70 | (T5-A59設計の実装その2)`full_loop`/`night_loop`/`.claude/agents/verifier.md`/`.claude/skills/verify/SKILL.md`/`rules/verification.md`の5ファイルへ受け入れハーネスの配線(`docs/acceptance_harness_design.md`§8に文言確定済み) | 5ファイルに配線され、`-Task`指定時に受け入れ資産が無ければ`acceptance_missing`でトップレベル`ok:false`になることを確認 | T5-A69 | S | ✅(2026-08-14完了。設計書§8の確定済み文言をそのまま5ファイルへ追記〈implementer実装〉。verifierが`powershell -File tools/verify.ps1 -Task T5-A70`を実行し、受け入れ資産が無いため`acceptance.reason:"acceptance_missing"`・トップレベル`ok:false`になることを確認〈通常実行の9項目は全PASSで回帰なし〉。`lib/`不変・ドキュメントのみのためデプロイ対象外) |
| T5-A71 | (T5-A59設計の実装その3)未完了タスク行への`／受入: <パス>`または`／受入: 免除(理由)`の付与(完了済みタスクには遡及しない) | `docs/改修マスタープラン.md`の未完了タスク行に受入欄が付与されている／受入: 免除(Sサイズ) | T5-A69 | S | ✅(2026-08-16夜間ループ完了。`implementer`が未完了58行〈`docs/acceptance_harness_design.md` §2の3条件で判定〉に受入欄を付与。内訳: 必須〈受け入れテストパス予約〉28件・免除(Sサイズ)19件・免除(設計タスク)5件・免除(視覚判断のみ)6件。`grep -c "受入:" docs/改修マスタープラン.md`=65件で一致確認) |
| T5-A72 | (T5-A59設計の実装その4)パイロット1件: M以上・`lib/`変更を伴うタスクで実際に受け入れテストを書き、実装をわざと1行壊して受け入れテストだけが落ちることまで確認する。**トラックB本格化まで対象タスクが無いため着手不可** | 受け入れテストが実装のバグを検知できることを実地確認し、増分コストが記録される／受入: test/acceptance/t5_a72_acceptance_test.dart | T5-A70, トラックB着手 | M | ⬜(トラックB本格化後) |
| T5-A73 | ⚠️**ユーザー実施**: `~/.gemini/antigravity-cli/settings.json`の`permissions.allow`へ正規表現トークン形式のルールを追加する。追加候補・優先度・リスク評価は`docs/antigravity_delegation_design.md` §10-2の表に確定済み(候補#1〜#7を追加、#8は保留)。`deny`側の推奨エントリも同節に記載。**アシスタントはこのファイルを読み書きしない** | §10-4の検証6件(許可4・拒否2)を実行し、期待どおりの結果になる。特に検証#6(`.*`が複数トークンを吸わないこと)の結果を§5-7へ追記する。#6が許可されてしまった場合は拡張を全撤回し完全一致列挙へ戻す／受入: 免除(Sサイズ) | なし | S | ⬜(ユーザー実施待ち) |
| T5-A74 | ⚠️**ユーザー実施**: agyを1.1.12→1.1.13へアップグレード(`winget upgrade`)。実施条件は`docs/antigravity_delegation_design.md` §10-3に確定済み(単独実施・夜間トリガー直前を避ける・回帰時は`winget install --version 1.1.12`で戻す) | アップグレード後に§10-4の検証6件を再実行して回帰が無いことを確認し、結果を§5-7へ1行記録する | なし(T5-A73と同一ループで実施すると効率的) | S | ✅(2026-08-14完了。`agy --version`が**1.1.13**を返すことを確認。同ループ内でagyを実呼び出し4回〈`agy models`+researcher役3件〉行い、回帰は観測されなかった。§10-4の許可系検証6件はT5-A73〈ユーザー実施待ち〉に依存するため未実施) |
| T5-A75 | `researcher`役のagyパイロット: `tools/antigravity_delegate.ps1 -Role researcher`を有人`/full_loop`で3件試用する。対象は`docs/antigravity_delegation_design.md` §9.1のresearcher専用除外条件に当たらない一般調査のみ。判定基準は§9.7-7の4項目(出典URL+取得日/各件2本の抽出検査で実在・整合/確認済みと推測の分離/日本語規約違反ゼロ/差し戻し1回以内) | 3件分の実績ログが§7にあり、§9.5のresearcher状態遷移表のどの状態へ進めるかが結論づけられている(3件中2件以上「採用」で条件付き常時へ) | なし | M | ✅(2026-08-14完了。3件実施し**3件中1件のみ「採用」で昇格条件〈2件以上〉未達**、`researcher`役は「パイロット」据え置きと結論。不採用2件はいずれも出典の質が原因〈1件は404の実在しないURL、1件は実在するURLに裏付けの無い主張を帰属〉で、報告の体裁・所要時間はむしろ良好だった。あわせて**`--mode plan`でもagyがファイルを書けること**を実機で発見し設計書§9.2の誤りを訂正。詳細は`docs/antigravity_delegation_design.md` §7「T5-A75の結論」) |
| T5-A76 | `architect`委譲の定型化+fork不採用の明文化。(a)`.claude/skills/full_loop/SKILL.md`の§サブエージェントへの委譲に「architect委譲プロンプトのテンプレ」を追加(内容は`docs/token_optimization_design.md` §10-2-Bの6項目をそのまま採用)。(b)同ファイルに「`Task`の`subagent_type`に`"fork"`を渡さない」を1行追記し理由として§10-4を参照 | `grep -n "fork" .claude/skills/full_loop/SKILL.md`で(b)が見つかり、(a)の6項目が§10-2-Bと食い違わない | なし | S | ✅(2026-08-14完了。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A76」節)
| T5-A77 | agy `adversary`の起動条件拡大を1ループ試して判断する。現行の`/code-review`実行条件(差分5ファイル超/フェーズ完了/10回に1回)に代えて「`lib/`変更を含むループでは常時起動」を1回試す。Critical/Majorのみ差し戻し対象、Minorは記録に留める(`docs/token_optimization_design.md` §10-2-A-2の注意)。**あわせて(2026-08-14追記、§12.5)フェーズ完了ループでは、Gemini `adversary`に加えて`claude-sonnet-4-6`の2本目レビューを1回だけ試し、2本目だけが見つけたCritical/Major件数を記録する(0件なら以後行わない、依存T5-A83)** | 1ループ分の実測(検出件数・差し戻し往復数・Claude側サブエージェント体数・5時間枠pt)を§7へ記録し、常時起動へ移行するか現行条件を維持するかを結論づける／受入: 免除(Sサイズ) | T5-A73(agyの`git diff`系許可があると差分把握が自力でできる。無くても親がファイル化して渡せば実施可) | S | ⬜ |
| T5-A78 | agy委譲ラッパー(`tools/antigravity_delegate.ps1`/`.sh`)のモデル指定を固定値`gemini-3.6-flash-high`から**最新モデルの自動解決**へ変更する(ユーザー指示、2026-08-14)。`-Model`/`--model`省略時に`agy models`から`gemini-<major>.<minor>-flash-high`の最大版を数値比較で選ぶ。取得失敗・タイムアウト・候補ゼロ時は`gemini-3.6-flash-high`へフォールバックしラッパー自体は失敗させない | 省略時に実機で`gemini-3.7-flash-high`が解決され、明示指定時は自動解決が走らない。stdout 1行JSONの契約と`.ps1`のUTF-8 BOMが保たれている | なし | S | ✅(2026-08-14完了、commit `2a225ef`。`--model`を単に省略する案は不採用——agyの既定モデルが不明で`claude-sonnet-4-6`等に落ちるとClaude利用枠を消費し委譲の目的が崩れるため、明示的に最新Gemini Flashを解決して渡す。verifier検証は台帳確認以外の全項目PASS〈台帳はDryRunでは記録しない設計のため確認不能、同ループのT5-A75実呼び出し3件で`model`列に`gemini-3.7-flash-high`が記録されることを確認済み〉) |
| T5-A79 | agy `researcher`役の**出典検証の機械化**(ユーザー依頼、2026-08-14)。(a)`tools/verify_citations.ps1`新規——調査レポート末尾の「出典一覧」表のURLを実際に取得し、HTTPステータスと**裏付け引用の原文照合**で検証する。(b)`tools/antigravity_delegate.ps1`に配線——`researcher`役のときだけ出典検証ブロックをプロンプトへ挿入し、実行後に自動検証、失敗時は exit 18(`CITATION_UNVERIFIED`)。あわせてagyの`gemini-3.1-pro-high`と最新Flashを同一テーマで比較し、pro採用の可否を判定する | 検証スクリプトが正常系でexit 0、故意に壊したレポートでexit 1になり落ちた行が特定できる。`-Role researcher -DryRun`のプロンプトに検証ブロックが入り`-Role implementer`には入らない。pro/flashの比較結論が設計書§7に記録されている | なし | M | ✅(2026-08-14完了。**プロンプト強化だけでは不十分**と実測で判明——URL捏造(失敗の型①)は再発しなくなったが、実在URLへの無関係な帰属(型②)はプロンプトでは保証できないため機械照合を必須の関門にした。**proへは切り替えない**(FlashとProはagy上で同一クォータバケットを共有し、提供されるproは2世代古い`3.1`。実測でもflash 3.7が上回った)。詳細は`docs/antigravity_delegation_design.md` §7「T5-A79の結論」) |
| T5-A80 | agyの`Claude and GPT models`クォータバケット(週次100%・未使用)が、ユーザーのClaude Proプラン枠とは**別勘定**であることを実証する。`-Model claude-sonnet-4-6`で1回実呼び出しし、(a)`agy -p /usage`の`Claude and GPT models`残量が減ること (b)ローカルAPI(`localhost:3000`)のClaude Pro使用率が動かないこと、を前後比較する。**別勘定と確認できれば、agyへの委譲範囲を「Geminiで品質が届かない役割」まで広げられる** | 前後の実測値2組が`docs/antigravity_delegation_design.md` §5へ記録され、別勘定か否かが結論づけられている。別勘定だった場合は§9.1のルーティング表に、どの役割でClaudeモデルをagy経由で使うかの方針を追記する | なし | S | ✅(2026-08-14完了、当初は**判定不能**〈agyの`/usage`regressionでアシスタント側の直接計測が不可〉。**同日ユーザーが自身のAnthropic課金情報から「別勘定・消費しない」と確認**、結論を**別勘定・確定**へ更新。詳細は`docs/antigravity_delegation_design.md` §7「T5-A80の結論」追記部) |
| T5-A82 | ⚠️**上位モデルで実施**: T5-A80が別勘定確定(ユーザー確認)を受け、`docs/antigravity_delegation_design.md` §9.1ルーティング表を再設計する。現行の「不可」判定が(a)ツールアクセス制約(claude-in-chrome・emulator・シェル無し)によるものか(b)モデル品質/コスト上の制約(Geminiしか選べなかった)によるものかを役割ごとに切り分け、(b)に該当する役割(`implementer`の対象拡大・`researcher`のclaude-sonnet-4-6化・`architect`のclaude-opus-4-6-thinking試用可否等)について段階的パイロット計画を立てる。成果物は設計書+実装タスク分解のみ(コードは書かない) | §9.1が役割ごとに「ツール制約」「品質/コスト制約」のどちらが不可の理由かで再整理され、(b)に該当する役割の段階的パイロット計画(対象・順序・判定基準)が確定している | T5-A80 | M | ✅(2026-08-14完了、architect設計。`docs/antigravity_delegation_design.md` §12新設——不可理由を型a〈ツール制約、恒久〉/型b〈品質・コスト制約、別勘定確定で解消〉に切り分け、`verifier`/`ui_verifier`は型aで不可維持、`adversary`はGemini多様性維持を優先しClaudeは2本目追加のみ検討、`researcher`/`implementer`/`architect`をこの順でパイロット計画。実装タスクT5-A83〜A89へ分解済み〈下記参照〉) |
| T5-A83 | (T5-A82設計の実装その1)`tools/antigravity_delegate.ps1`/`.sh`にagy経由Claudeモデル対応を実装。`$AllowedRoles`へ`architect`追加、`--effort`を`claude-`/`gpt-`接頭辞では渡さない、クォータ事前チェックをモデル接頭辞で`Claude and GPT models`グループへ分岐、stdout JSONに`quota.bucket`追加(既存`gemini_*`キーは後方互換のため残す) | `-Role architect -DryRun`がexit 0、`-Model claude-sonnet-4-6`指定時に`--effort`が引数に含まれない、`-Model`省略時のGemini既定経路に回帰なし、`.ps1`のUTF-8 BOM保持／受入: 免除(Sサイズ) | なし | S | ✅(2026-08-15完了。`implementer`が`.ps1`/`.sh`両方へ§12.3の確定仕様通りに実装〈architectロール追加・`architect`のモードを`plan`に・`claude-`/`gpt-`接頭辞で`--effort`抑制・クォータ分岐+`quota.bucket`〈"gemini"/"claude_gpt"〉追加〉。`verifier`が7項目独立検証(BOM保持・`-Role architect -DryRun`exit0・AllowedRoles・effort抑制ロジック静的確認・`.sh`構文チェック・Gemini自動解決回帰なし・変更範囲)全PASS。Dartコード変更なし・`tools/`のみのためデプロイ・本番確認は不要。次はT5-A84〈⚠️一部ユーザー実施〉が着手可能) |
| T5-A84 | (T5-A82設計の実装その2)Claudeモデルのスモーク検証3件(ファイル読み取り可否・Web検索・`claude-opus-4-6-thinking`のeffort受理)+3pバケット消費実測(⚠️ユーザー実施、対話モードTUIで`/usage`前後値を確認) | 3件の結果と3pバケット前後値(または取得不能)が§7へ記録され、ファイル読み取りが失敗した場合はT5-A85以降を中止する判断が明記されている | T5-A83 | S | ✅(2026-08-15完了。**⚠️ユーザー実施は不要と判明**——`agy -p "/usage"`はPowerShell経由〈Git Bash非経由〉なら正常動作し、教訓L158どおりGit Bashのパス変換が真因と実機で再確認、親が全自動で計測。3pバケット消費実測: 週次99.45%→97.38%〈-2.07pt〉・5時間100%→93.77%〈-6.23pt〉。スモーク検証3件: (1)ファイル読み取り✅可能〈`--add-dir`は絶対パス必須〉(2)Web検索✅可能(3)`claude-opus-4-6-thinking`の`--effort`❌拒否〈exit1、T5-A83の抑制ロジックの正しさを実証〉。ファイル読み取り成功のためT5-A85以降続行可と判定。詳細は`docs/antigravity_delegation_design.md` §7「T5-A84の結論」) |
| T5-A85 | (T5-A82設計の実装その3)`researcher`役 × `claude-sonnet-4-6`パイロット3件(一般調査2件+緩和候補テーマ1件)。`verify_citations.ps1`の関門は維持 | §12.9の5条件で3件を判定し、結果と昇格可否が§7・§9.5のresearcher状態遷移表へ反映されている | T5-A84 | M | ✅(2026-08-15完了、**結論: 不採用**。一般調査2件+緩和候補1件〈`drift`マイグレーション仕様の逐語確認〉の3件とも同一パターンで失敗——agy呼び出しは`exit0`/`num_turns:1`で正常終了扱いだが、応答が「レポートを作成します」で尻切れし`docs/research/*.md`へのファイル書き込みが1件も発生せず`verify_citations.ps1`がexit18〈`REPORT_NOT_FOUND`〉。**ユーザー指摘で`--mode accept-edits`(書き込み許可モード)でも追試したが同一パターンで失敗**——原因は`--mode plan`の読み取り専用制約ではなく、`claude-sonnet-4-6`のagyヘッドレス単発プロンプトが1ターンあたりのステップ予算を使い切り最終アクション前に打ち切られる構造的制約と推定(未確定)。`researcher`役は現行どおりGemini既定を維持、状態遷移は「パイロット」据え置き。詳細は`docs/antigravity_delegation_design.md` §7「T5-A85の結論」) |
| T5-A86 | (T5-A82設計の実装その4)`implementer`役 × `claude-sonnet-4-6`パイロット3件(`lib/`のM規模または`test/`配下、`gas/`対象外)。Gemini版の実績とは別カウント | §9.7-5の4基準+BOM喪失チェックで3件を判定し、結果が§7・§9.5へ反映されている | T5-A84, T5-A85完了後 | M | ✅(2026-08-15完了、**結論: 採用**。トラックB未着手のため`test/pilot/`配下に使い捨て検証専用テスト3本〈`bean_stock_calculator.dart`の既存関数向け、内容は親が全文事前確定〉を委譲、判定後に削除。3件とも`flutter test`で実際にPASS・日本語規約違反ゼロ・地雷なし。**過程で不具合2件を発見**——(1)委譲プロンプトの上書き規則〈シェル完全一致のみ許可〉とT5-A92追記〈特定ファイル指定`flutter test`を許可〉が矛盾しPERMISSION_DENIEDで1回差し戻し〈タスク側で検証コマンド禁止を明示して解消〉(2)`tools/antigravity_delegate.ps1`の変更検出が同一ディレクトリへの2回目以降の書き込みを`git status --porcelain`の仕様上検知できず`NO_CHANGES`と誤判定〈実ファイルは正しく書き込まれていた〉。§12.9の昇格条件(3件中2件以上)を満たし、`implementer`役は`lib/`のM規模・`test/`配下に限りClaudeモデルで「条件付き常時」へ移行可。ただし実運用前に上記(1)の恒久修正が必要(新規タスク化検討)。詳細は`docs/antigravity_delegation_design.md` §7「T5-A86の結論」) |
| T5-A87 | (T5-A82設計の実装その5)`architect`役 × `claude-opus-4-6-thinking`パイロット3件(運用/ドキュメント設計→S規模実装設計→M規模実装設計の順、視覚デザイン・ブラウザ再現・implementer2回失敗後の原因究明は対象外)。設計書はファイルに書かせず`response_log`経由で受け取る | 各件「実装+verifier検証完了」まで含めて§12.9の5条件で判定し、3件の結果が§7へ記録、§9.1のarchitect行の最終判定が確定。不採用1件で中断 | T5-A85, T5-A88 | M | ✅(2026-08-15完了、**結論: 不採用・パイロット未実施**。着手前にT5-A85で判明した構造的制約〈探索を伴うタスクでagy×Claudeが`num_turns:1`のまま打ち切られる〉がarchitectの設計タスクでも再発する懸念を親がユーザーへ確認したところ、「上位モデルはagyに一切委譲しない」という恒久方針が示され、3件のパイロット実行なしで不採用を確定。`docs/antigravity_delegation_design.md` §9.1のarchitect行を「不可(恒久)」へ更新、§12.7に不採用確定の経緯を追記、§7に「T5-A87の結論」を新設。これによりT5-A82設計〈§12〉のagy×Claudeパイロット系列(T5-A83〜A87)は全完了。コード変更なし・`lib/`/`gas/`不変のためデプロイ・本番確認不要) |
| T5-A88 | (T5-A82設計の実装その6)`tools/verify_citations.ps1`にリポジトリ内引用モードを追加。設計書・報告中の`path:行`参照がファイル実在+記述一致するか機械照合する | 正常系exit 0、行番号をずらした設計書でexit 1になり不一致行が特定できる。既存URL検証モードに回帰なし／受入: 免除(サイズS) | なし | S | ✅(2026-08-15完了。`implementer`が新規スイッチ`-RepoCitationMode`を追加、既定〈スイッチ無し〉は従来のURL検証モードのまま。新設「## リポジトリ引用一覧」表〈列: #/主張ID/path:行/裏付け引用〉を対象に、path実在・行番号範囲内・裏付け引用の前後3行一致を判定、既存の`Split-TableRow`/`Get-NormalizedText`等を再利用。正常系exit 0、行番号ずれ(99999)でexit 1・`reason:"line_out_of_range"`で不一致行を特定、既存URL検証モード〈`docs/research/2026-08-14_m3_breakpoints_flash.md`〉に回帰なし、をverifierが独立検証・全確認。実装中Writeツールでの全文書き換えによりBOM喪失→復旧〈教訓L160〉。Dartファイル変更なし・`lib/`/`gas/`不変のためデプロイ・本番確認不要) |
| T5-A89 | (T5-A82設計の実装その7)再設計の配線。§9.1を§12.3の表へ差し替え、`.claude/skills/full_loop/SKILL.md`にClaudeモデル指定分岐+1ループ上限(既定2件・architect1件)を追記、`.claude/skills/night_loop/SKILL.md`に「夜間ループではClaudeモデル指定禁止」明記、`CLAUDE.md`の「agyはGeminiバケット」記述を「Geminiまたはpost別勘定Claude/GPTバケット」へ修正 | 4ファイルに反映、`grep -n "claude-sonnet-4-6" .claude/skills/full_loop/SKILL.md`が該当行を返す、`CLAUDE.md`が§12.1の前提と食い違わない／受入: 免除(ドキュメントのみ) | T5-A83 | S | ✅ |
| T5-A81 | `researcher`役(および調査系`implementer`委譲)の出力に、ファイルパス/行範囲/根拠を含む構造化JSONスキーマを課す。ユーザー提供のGeminiレポート(`docs/antigravity_claude_orchestration_report.md` §3-B)のcontract-driven promptingの提案のうち、既存設計(§9.2〜9.3)に無い部分のみ採用する。対象は`.claude/agents/researcher.md`または`tools/antigravity_delegate.ps1`の層3プロンプト組み立て部分 | 調査系委譲の報告に、根拠となったファイルパス/行範囲(または出典URL)が構造化された形で含まれるようになり、`verify_citations.ps1`等の機械検証がしやすくなる／受入: 免除(Sサイズ) | なし | S | ⬜ |
| T5-A90 | **バグ修正・最優先**: `tools/failure_playbook.ps1 -Mode Preflight -Unattended`が無人夜間ループ実行時に無限ハングする不具合。2026-08-15朝、23:00枠の`night_loop.ps1`(PID 20156)がこの子プロセス(PID 15364)を起動した直後(23:00:05)からログが途絶え、親は消滅・子だけ孤児化して約9時間ハング(CPU消費45秒のみ=実質待機状態)、`night_loop.lock`が解放されず翌04:10枠がTask Schedulerに強制終了(`SCHED_S_TASK_TERMINATED`)、ログ・実行記録が一切残らなかった(09:20枠も同様に影響された可能性)。手動DryRun実行では毎回4秒程度で完了しており、無人・深夜特有の条件でのみ再現する差分がある。Preflightフェーズの各シグネチャ(FP-01/FP-02/FP-03-EMULATOR/FP-06-SILENTSTALL等、`tools/failure_playbook.ps1`内`Phase = @('Preflight')`該当箇所)のうち、タイムアウト保護のない処理を特定し、無限ハングしえないよう修正する(既存のFP-03-EMULATOR「10秒タイムアウト」のような上限を全シグネチャに適用する方針が有力)。応急対応として2026-08-15朝、親セッションがユーザー許可を得てPID 15364をkill・`night_loop.lock`を解放済み(暫定復旧のみ、恒久修正は未実施) | 原因シグネチャが特定され、無人実行(`-Unattended`)時に最大所要時間の上限(タイムアウト)が入り、模擬的な長時間応答条件でも`night_loop.ps1`自体が一定時間でこの子プロセスを検知・強制終了できることを実地確認する。再発時に検知できるよう`.claude/night_skips.log`等へ記録が残る | なし | M | ✅(2026-08-15完了。architectが原因究明: `failure_playbook.ps1`のPreflight実行ループ〈1314行付近〉が外部プロセス呼び出しをタイムアウト無しで同期実行しており、`night_loop.ps1`側の外側タイムアウトも無く、FP-03-EMULATORの「10秒タイムアウト」も実装上は`adb get-state`の後段にしか効いておらずその手前の`emulator.ps1 -Status`呼び出し〈無制限〉が最有力の原因と特定。Watchdogはclaude起動後にしか動かずPreflightフェーズが無監視だったことも判明。3層防御〈個別プロセスタイムアウト化→Detect累積予算→呼び出し元の外側タイムアウト〉で修正方針を設計。implementerが`tools/lib/loop_io.ps1`に`Invoke-ProcessWithTimeout`ヘルパーを新設〈非同期stdout/stderr読み取り+`taskkill /T /F`でプロセスツリーごと終了〉、`tools/failure_playbook.ps1`のFP-03 Detect/Repair全外部呼び出しをタイムアウト化・新シグネチャT追加・WMI呼び出しにタイムアウト追加・メインループにDetect累積予算/スロー検知警告を追加、`tools/night_loop.ps1`のPreflight/Postmortem呼び出しをタイムアウト化・起動時の孤児プレイブックプロセス掃除を追加。実装中に`emulator.ps1`〈OEMコードページ〉と`failure_playbook.ps1`〈UTF-8〉のエンコーディング差異による文字化け回帰を発見・`Encoding`パラメータで解消。受け入れハーネス`tools/acceptance/t5_a90_check.ps1`新規作成〈4項目〉。`verifier`が独立検証: `verify.ps1 -Task T5-A90`9項目全PASS〈acceptance含む〉、`t5_a90_check.ps1`単独実行4/4 pass再現、BOM維持確認、実機`-Mode Preflight`実行で文字化け無し確認。`lib/`・`gas/`不変のためデプロイ・本番確認不要) |
| T5-A92 | (§10-7採否判断の実装その2)反復中の検証範囲の限定(Test Impact Analysisの限定採用)。`.claude/agents/implementer.md` §実装後のセルフチェック(必ず実施)の末尾へ「反復中の検証範囲(T5-A92)」を追記し、試行錯誤中は関連テストのみ・`build web`省略可、**報告直前に必ず1回フル(analyze→flutter test→build web)を通す**ことを条件にする。**追記文言は`docs/token_optimization_design.md` §10-7-3のT5-A92節に確定済み、そのまま転記する**。**既知の地雷**: `.claude/agents/`配下はClaudeの`implementer`委譲ではハードブロックされた実績があるため(T5-A13)、agy委譲(`-Role implementer`)か親の直接編集で行う | `.claude/agents/implementer.md`に該当節が追記され、`grep -n "反復中の検証範囲" .claude/agents/implementer.md`が該当行を返す。フル実行を条件とする一文が残っている／受入: 免除(ドキュメントのみ) | なし | S | ✅(2026-08-15完了。既知の地雷どおり親が直接`.claude/agents/implementer.md`へ確定済み文言をそのまま追記〈設計判断なし〉。`grep -n "反復中の検証範囲"`で該当行を確認、BOM無し〈元ファイルも無し〉を確認) |
| T5-A93 | (§10-7採否判断の実装その3)委譲1回ごとの予算チェックポイント。`.claude/skills/full_loop/SKILL.md`「委譲時の共通ルール」の必須diffレビュー項目の直後へ、委譲1件完了ごとに`.claude/loop_state.md`を確認し**適用中モードの上限の6割(有人$14.4/夜間$12)を超えたら新規委譲を開始しない**(差し戻しも対象、超過時は`NEXT_SESSION.md`へ引き継ぐ)ルールを追加。**追記文言は`docs/token_optimization_design.md` §10-7-3のT5-A93節に確定済み、そのまま転記する** | `grep -n "予算チェックポイント" .claude/skills/full_loop/SKILL.md`が該当行を返し、$14.4/$12の両しきい値と差し戻しも対象である旨が含まれる／受入: 免除(ドキュメントのみ) | なし | S | ✅(2026-08-15完了。§9.1ルーティング表どおり`implementer`役(Gemini既定)をagy経由〈`tools/antigravity_delegate.ps1 -Role implementer`〉に委譲、確定済み文言をそのまま`.claude/skills/full_loop/SKILL.md`58行目へ追記。`git diff`で1ファイル・意図した2行のみの追記であることを親が確認、`grep -n "予算チェックポイント"`該当行あり、BOM無し〈元ファイルも無し〉を確認。ドキュメントのみのためverifier委譲・デプロイ・本番確認は不要) |
| T5-A94 | (§10-7採否判断の実装その4)**委譲する単位そのものの見直し(ユーザー確認必須)**。(a)非委譲のしきい値(`lib/`・`gas/`・`test/`・`web/`を含まない1ファイル・10行以内の確定済み文言修正は親が直接編集してよい)、(b)同型Sタスクのバッチ委譲(同じファイル群を触るSタスクを最大3件まで1回の`implementer`委譲へまとめてよい、検証も1回にまとめる)。いずれも`CLAUDE.md`§日次改修ループ運用ルールと`.claude/skills/full_loop/SKILL.md`手順2の恒久ルールを緩めるため、**着手時に`AskUserQuestion`で(a)(b)個別に可否を確認し、承認された案のみ反映する**。文言・根拠は`docs/token_optimization_design.md` §10-7-3のT5-A94節に確定済み | `AskUserQuestion`での確認記録が残り、承認された案のみが`CLAUDE.md`・`.claude/skills/full_loop/SKILL.md`へ反映されている(両方不承認なら「不採用」を§10-7-3へ追記して完了とする)／受入: 免除(ドキュメントのみ) | なし | S | ✅(2026-08-15完了。`AskUserQuestion`で(a)(b)個別に確認し**両方とも「導入する」で承認**。`implementer`へ委譲し確定済み文言をそのまま転記——CLAUDE.md(102行目、§日次改修ループ運用ルール末尾)へ新設段落、`.claude/skills/full_loop/SKILL.md`手順2(78行目、なお選ばないタスクの直後)・手順3(86行目、冒頭)へ追記。親が`git diff`で2ファイル・意図した箇所のみの変更であることを確認、`grep`で3箇所該当を確認。**ハーネスの自動セキュリティ分類器が「Instruction Poisoning」警告を出したが**(CLAUDE.md/SKILL.mdへの「ユーザー承認」文言書き込みが将来セッションに誤読されうる汎用パターンとして検出)、`git diff`で内容が今回のユーザー承認どおり一字一句確定済み文言と一致することを確認し誤検知と判断、ユーザーへも報告済み。コード変更なしのためverifier委譲・デプロイ・本番確認は不要) |
| T5-A95 | (§10-7採否判断の実装その5)新設スキル`/token_review`の配線。(a)`.claude/skills/full_loop/SKILL.md` 手順6.5末尾の「…`architect`サブエージェントへ委譲する」を「…`.claude/skills/token_review/SKILL.md`(`/token_review`)の手順に従って実施する(researcher/architectへの委譲要否はスキル側に定義済み)」へ差し替え、(b)`CLAUDE.md`§日次改修ループ運用ルール「トークン浪費の調査ルール」へ「手順は`.claude/skills/token_review/SKILL.md`が正本」を1文追記 | `grep -rn "token_review" .claude/skills/full_loop/SKILL.md CLAUDE.md`が両ファイルで該当行を返す／受入: 免除(ドキュメントのみ) | なし | S | ✅(2026-08-16完了。(b)は2026-08-16夜間ループで完了済み、(a)は本セッションで完了。`.claude/skills/full_loop/SKILL.md`手順6.5末尾を確定済み文言どおり差し替え、`grep`で両ファイル該当行を確認。T5-A94の非委譲しきい値〈1ファイル・10行以内・確定済み文言〉に該当するためimplementer委譲せず親が直接編集、`git diff`で1行差分のみを確認。`lib/`・`gas/`不変のためverifier委譲・デプロイ・本番確認は不要) |
| T5-A96 | (T5-A86発見バグの恒久修正)`tools/antigravity_delegate.ps1`の「agy固有の上書き規則」ブロックへ「セルフチェック(`flutter analyze`/`flutter test`/`flutter build web`)はagy委譲では一切実行せず、`.claude/agents/implementer.md`の『実装後のセルフチェック』『反復中の検証範囲(T5-A92)』の両節はagy委譲には適用しない」旨を追記する。`.sh`版にも同一変更。現状はタスクごとに親が委譲プロンプトへ手動で検証禁止を書き足さないと`PERMISSION_DENIED`(exit 12)になる(T5-A86実測、`docs/antigravity_delegation_design.md` §7参照) | 追記後、検証コマンド禁止の指示をタスク本文へ書かずに`-Role implementer -Model claude-sonnet-4-6`のタスクを1件実行し、`PERMISSION_DENIED`が発生しないことを確認する／受入: 免除(ドキュメントのみ、`tools/`のPowerShell/Bashスクリプトで実行成果物への変更を伴わない) | なし | S | ✅(2026-08-15完了。`implementer`が`tools/antigravity_delegate.ps1`〈363行目〉・`.sh`〈288行目〉の`$OverrideBlock`/`OVERRIDE_BLOCK`ヒアドキュメントへ`docs/antigravity_delegation_design.md`185行目の確定文言をそのまま1文追記〈既存の完全一致コマンド許可リスト項目の直後〉。親が`git diff`で2ファイル・各1行のみの変更を確認、`.ps1`はUTF-8 BOM付きCRLF・`.sh`はBOM無し、いずれも編集前後で維持を確認。完了条件の実地確認として、検証コマンド禁止の指示をタスク本文に書かずに`-Role implementer -Model claude-sonnet-4-6`で使い捨てテスト〈`test/pilot/t5_a96_check_test.dart`、判定後削除〉を1件実行し`exit_code:0`・`status:"SUCCESS"`で`PERMISSION_DENIED`が再発しないことを確認〈応答は`flutter analyze`/`flutter build web`を新規規則に従い自発的に省略〉) |
| T5-A97 | **バグ調査**: `tools/acceptance/t5_a90_check.ps1`のチェック3「フェーズ上限の実地確認」が`verify.ps1`回帰実行のたび不安定に不合格(2026-08-15 23:00枠の夜間ループ〈T5-A71検証時〉で実測、経過秒=0.7〈30秒内で正常〉・exitOk=True・wrapperWARN=True・eventsTSV=Trueだが残存プロセス数=1)。T5-A90本体のフェーズ上限機構自体は正常に見えるため、計測タイミングの競合(プロセス終了直後の残存カウントのレース)か真の軽微リークかを切り分ける | 原因を特定し、`t5_a90_check.ps1`が`verify.ps1`の毎回の回帰実行で安定して4/4 passする(フレーク再発なし)／受入: 免除(Sサイズ) | なし | S | ✅(2026-08-16完了。**原因は3点、いずれも真のレース/リークではなくチェック設計の欠陥**(architect調査): (1)残存プロセス判定がマシン全体の`failure_playbook.ps1`一致プロセスを無条件カウントし、`night_loop.ps1`が常駐させる`-Mode Watchdog`プロセスまで誤検知(夜間ループ実行中は必ず不合格になる決定的バグ、フレークではない)。(2)経過秒に下限が無く多重起動ガードでの即時終了(0.7秒)を「正常完了」と誤判定。(3)wrapper.log/events.tsvのTail判定が当日の古い行を誤検出。`implementer`が差分判定方式へ書き換え(ベースライン取得→新規行のみ判定、Watchdog除外パターン追加、経過秒下限9秒追加)、`tools/verify.ps1`の受け入れスクリプトタイムアウトも60000→180000msへ延長。`verifier`が独立検証: 単体3回連続実行で毎回4/4 pass・経過秒14.6秒(9〜30秒内)、ダミーWatchdog常駐状態でも4/4 pass(旧実装の再発無し)、`verify.ps1`(`-Task`無し)9項目全ok:true、BOM/CRLF維持確認。変更は`tools/night_loop.ps1`・`tools/acceptance/t5_a90_check.ps1`・`tools/verify.ps1`・`docs/failure_playbook.md`の4ファイルのみ・`lib/`/`gas/`不変のためデプロイ・本番確認は不要) |
| T5-A99 | **バグ調査**: `night_loop`のタスク選定がmainのタスク表しか見ておらず、対象タスクIDに紐づくオープンPRの有無をチェックしていない。2026-08-15 23:00枠が自動pushゲート不通過でT5-A71をPRルート(`night/T5-A71`)に倒した後、mainのタスク表はT5-A71が未完了のままだったため、翌04:10枠が**同じT5-A71を独立に再実装し直接pushしてしまい**、結果PR #4がmainとコンフリクト(CONFLICTING)する事態になった(2026-08-16、有人セッションで発見・PRはクローズ・内容はmainへ転記済み)。`.claude/skills/night_loop/SKILL.md`のタスク選定手順に、選定候補IDで`gh pr list --search "<ID>"`相当のオープンPR確認を追加すべきか検討する | 同種の再発(2つの無人トリガーが同一タスクを別々にやり直しコンフリクトを生む)が起きない仕組みが`night_loop`の設計に入る、またはリスクを許容する明示判断がされる | なし | S | ✅(2026-08-16完了。タスク文言自体が実装方針をほぼ示していたためarchitect委譲は不要と判断、親が方針〈`gh pr list --search "<ID>" --state open`でヒットしたら次点へフォールバック〉を確定して`implementer`へ委譲。`.claude/skills/night_loop/SKILL.md`「2. タスク選定」節へ新設ステップを追加(候補選定直後・2件目選定時の両方に適用、除外し尽くした場合は既存の§中断条件へ)。親が`git diff`で1ファイル・意図した4箇所の変更のみであることを確認、BOM無し・CRLF維持を確認。ドキュメントのみ・`lib/`/`gas/`不変のためverifier委譲・デプロイ・本番確認は不要) |
| T5-A98 | **設計拡張検討(2026-08-16 PR #4クローズ時に再調査、premise訂正)**: T5-A71実施時、`docs/acceptance_harness_design.md` §2.2の免除4文言のいずれにも当てはまらない境界事例が7件見つかった(T5-A45・T5-A72・T5-A7・T5-C9/C10/C11・T3-75e)としてarchitectへの相談用に起票されたが、現在のmain(直接コミットcc11582での別経路のT5-A71完了)を確認したところ**7件中6件(T5-A45/A72/A7は`受入: test/acceptance/*.dart`の予約パス、T5-C9/C10/C11は既存の`免除(視覚判断のみ)`)は既に付与済み**で新カテゴリ無しで解決していた。残るT3-75e(唯一未付与)も本タスクで`免除(視覚判断のみ)`を付与し解消。**結論: 新カテゴリ追加は不要、既存4文言+`受入:<パス>`の運用で足りることが実例で確認できたため、architect相談は不要と判断しクローズ** | (再調査の結果、当初の完了条件は前提が崩れたため上記結論をもって完了) | T5-A71 | S | ✅(2026-08-16完了。詳細は上記) |
> **完了済み(82件)**: T5-A99(2026-08-16完了、`night_loop`のオープンPR未チェック問題の修正。`.claude/skills/night_loop/SKILL.md`のタスク選定手順へ`gh pr list --search "<ID>" --state open`相当のオープンPR確認ステップを追加、ヒットしたら次点タスクへフォールバック。上記タスク行に詳細記載), T5-A97(2026-08-16完了、`t5_a90_check.ps1`チェック3フレークの根本修正。マシン全体プロセス走査からベースライン差分方式へ書き換え、常駐Watchdog誤検知・多重起動即終了誤判定・古いログ誤検出の3点を解消。上記タスク行に詳細記載), T5-A98(2026-08-16完了、PR #4クローズ時の再調査で境界事例7件中6件が既に既存カテゴリで解決済みと判明、残るT3-75eも`免除(視覚判断のみ)`で解消。新カテゴリ追加不要と結論、architect相談は不要と判断。上記タスク行に詳細記載), T5-A95(2026-08-16完了、`/token_review`スキルの配線。(a)`.claude/skills/full_loop/SKILL.md`手順6.5末尾を確定済み文言へ差し替え〈本セッション、非委譲しきい値により親が直接編集〉、(b)`CLAUDE.md`側は前回夜間ループで完了済み。両方完了で正式運用開始。上記タスク行に詳細記載), T5-A71(2026-08-16夜間ループ完了、未完了タスク行〈58行〉への受入欄付与。上記タスク行に詳細記載), T5-A94(2026-08-15完了、§10-7採否判断の実装その4・委譲する単位の見直し。`AskUserQuestion`で(a)非委譲のしきい値(b)同型Sタスクのバッチ委譲を両方承認、CLAUDE.md・`.claude/skills/full_loop/SKILL.md`へ反映。上記タスク行に詳細記載), T5-A93(2026-08-15完了、§10-7採否判断の実装その3・委譲1回ごとの予算チェックポイント。上記タスク行に詳細記載), T5-A87(2026-08-15完了、agy×architect役パイロット・**結論「不採用・パイロット未実施」**〈ユーザー方針「上位モデルはagyに一切委譲しない」により3件試行なしで確定〉。agy×Claudeパイロット系列T5-A83〜A87が全完了。上記タスク行に詳細記載), T5-A96(2026-08-15完了、T5-A86発見バグの恒久修正。agy委譲ラッパー2本の上書き規則ブロックへ検証コマンド省略指示を追記し、Claudeモデル委譲でのPERMISSION_DENIED再発を実地確認で解消。上記タスク行に詳細記載), T5-A16(2026-08-15完了、トラックA完了後の3ループのトークン実測。直近の`implementer`+`verifier`定型ループ3件〈T5-A83/A88/A89〉平均$5.98が目標を下回り悪化なしと判定、`adversary`起動条件は変更せず。詳細は`docs/token_optimization_design.md` §10-3-1), T5-A92(2026-08-15完了、§10-7採否判断の実装その2・反復中の検証範囲の限定。上記タスク行に詳細記載), T5-A91(2026-08-15完了、§10-7採否判断の実装その1・委譲プロンプト規約の強化。`.claude/skills/full_loop/SKILL.md`のarchitect委譲テンプレへ7項目目「長文は貼らずパスで渡す」追加、委譲時の共通ルールへ「報告は要点のみを返させる」追加。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A91」節), T5-A89(2026-08-15完了、agy正式移行系列〈T5-A82設計〉の再設計配線。§9.1ルーティング表を§12.3内容へ差し替え、`.claude/skills/full_loop/SKILL.md`・`.claude/skills/night_loop/SKILL.md`・`CLAUDE.md`へClaudeモデル委譲件数上限・夜間ループでのClaude指定禁止を追記。上記タスク行に詳細記載), T5-A88(2026-08-15完了、`verify_citations.ps1`にリポジトリ内引用照合モード〈`-RepoCitationMode`〉追加。上記タスク行に詳細記載), T5-A90(2026-08-15完了、夜間ループPreflightフェーズの無限ハング不具合修正〈3層タイムアウト防御〉。上記タスク行に詳細記載), T5-A83(2026-08-15完了、agy委譲ラッパー2本〈.ps1/.sh〉のClaudeモデル対応実装。上記タスク行に詳細記載), T5-A70(2026-08-14完了、受け入れハーネスの5ファイル配線。上記タスク行に詳細記載), T5-A80(2026-08-14完了、agyの`Claude and GPT models`バケット別勘定検証・**判定不能**〈`/usage`のヘッドレスregressionで直接計測不能、Sonnet 5相当のモデルIDは存在せず。教訓L158〉。上記タスク行に詳細記載), T5-A79(2026-08-14完了、agy researcher役の出典検証の機械化+pro不採用の判断。上記タスク行に詳細記載), T5-A78(2026-08-14完了、agyラッパーのモデル自動解決化。上記タスク行に詳細記載), T5-A75(2026-08-14完了、researcher役agyパイロット3件・昇格条件未達で据え置き。上記タスク行に詳細記載), T5-A74(2026-08-14完了、agy 1.1.13へのアップグレード確認。上記タスク行に詳細記載), T5-A76(2026-08-14完了、architect委譲プロンプトのテンプレ化+fork不採用の明文化。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A76」節), T5-A66(2026-08-14完了、night_loop.ps1への失敗プレイブック配線・PR #3マージ。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A66」節・教訓L152), T5-A69(2026-08-14完了、受け入れハーネス実装その1。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A69」節), T5-A12・T5-A17(2026-08-14完了、夜間ループ無人発火の実地観察。行末の状態セルに詳細記載), T5-A49・T5-A50・T5-A51・T5-A52・T5-A53・T5-A54・T5-A55・T5-A56・T5-A58・T5-A59(2026-08-13完了10件、`/insights`レポート由来のルール変更バックログ。ユーザーと対話しながら方針を決定〈T5-A49はa/c/eのみ採用しb/dをA55/A56へ統合、T5-A57は見送り〉。詳細は各タスク行に記載、失敗プレイブック・受け入れハーネスの設計書は`docs/failure_playbook.md`・`docs/acceptance_harness_design.md`), T5-A60(2026-08-13完了、夜間ループ運用ポリシー改訂。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A60」節), T5-A61(2026-08-13完了、失敗プレイブック基盤。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A61」節), T5-A62(2026-08-13完了、FP-01・FP-02ルールの拡張。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A62」節), T5-A63(2026-08-13完了、FP-03エミュレータルールの追加。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A63」節), T5-A64(2026-08-13完了、FP-04権限拒否・FP-06サイレントスタールルールの追加。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A64」節), T5-A65(2026-08-13完了、FP-05エージェント/claudeハングルールの追加。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A65」節), T5-A1, T5-A2, T5-A3, T5-A5, T5-A8, T5-A9, T5-A42, T5-A43, T5-A44, T5-A10, T5-A11, T5-A18, T5-A19, T5-A20, T5-A21, T5-A22, T5-A23, T5-A24, T5-A26, T5-A6, T5-A27, T5-A28, T5-A30, T5-A31, T5-A32, T5-A33, T5-A34, T5-A35, T5-A36, T5-A4, T5-A40, T5-A38, T5-A39, T5-A37, T5-A41, T5-A13, T5-A25, T5-A29, T5-A14(**T5-A14は2026-08-10完了**。「Proプランで`model: opus`のサブエージェントが起動できるか」を、2026-08-09〜08-10の複数ループ〈T5-A27・T5-A28・T5-A32・T5-A34・T5-A36〉で実際にOpus`architect`が起動・完走した実績〈`docs/token_optimization_design.md` §8の使用率ログに実測記録あり〉を根拠に「可能」と確定。`docs/android_release/リリース計画書.md` §5・§7を更新(不確実性の記述を撤回)。「使えない場合の代替運用」は該当しないため`CLAUDE.md`追記は不要と判断。**T5-A41は2026-08-10完了**。`tools/antigravity_delegate.ps1`を使い、agy(`gemini-3.6-flash-high`×2件・`gemini-3.1-pro-high`×1件)へT5-A25/T5-A29/T5-A13を実際に委譲し3回試用、3件とも「採用」相当と判定(§9.7-5の4基準を満たす)。**新規地雷を発見**: agyが既存`.ps1`ファイルを編集すると日本語コメント入りファイルのUTF-8 BOMが失われPowerShell 5.1で構文エラーになる事象(`tools/night_loop.ps1`で実際に発生、教訓L127と同種)を`verifier`が実機確認、親が直接BOM復元で解消しラッパーの上書きブロックへ再発防止の一文を追加(§9.3反映済み)。`gemini-3.1-pro-high`は応答本文冒頭に`<END_OF_TURN>`が63行連続で漏れる不具合も観測(実装内容自体は正しかったが`response_head`が無意味化)。結論として§9.5状態遷移表を「条件付き常時」(`docs/`・`tools/`・`.claude/`の非Dartファイル+`lib/`配下のS規模タスクがagy対象)へ移行、「常時委譲」は`lib/`配下の追加実績待ち。詳細は`docs/antigravity_delegation_design.md` §7・§9.5。**T5-A25・T5-A29・T5-A13はT5-A41のパイロット試用対象タスクとして同日中に完了**(それぞれ夜間/有人ループの起動回数カウンタ実装、`implementer.md`へのAndroid規約追記)。T5-A13は前回Claude実装者委譲では`.claude/agents/implementer.md`編集がハードブロックされ有人セッションでの直接編集が必要とされていたが、agy委譲では正常に編集できることを実機確認した。**T5-A37は2026-08-10、T5-A38/T5-A39完了の同日中に訂正・完了**。当初「`command(git)`等の単語1つでは機能せず`command(*)`以外に実用解なし」と結論していたが、`command(flutter --version)`のように**引数まで含めた完全一致の文字列**なら意図どおりのスコープ〈登録した完全一致コマンドのみ許可〉で機能することが判明。同方式で`flutter analyze`/`test`/`build web`/`pub get`・`dart run build_runner build --force-jit`・`git status`/`diff`/`log`/`show`・`tools/verify.ps1`の計10コマンドを登録し、agy直接呼び出し・ラッパー(`-Role implementer`)経由の両方で実機動作を確認、ラッパーの上書きブロックも「シェル一切禁止」から「上記完全一致コマンドのみ許可」へ緩和した。詳細は`docs/antigravity_delegation_design.md` §5-1訂正・`rules/lessons_archive.md` L138。**T5-A38・T5-A39は2026-08-10、Windows実機でagyを実際に呼び出して完了**。実装済みだったラッパーには2つの実行時バグがあり、実地テストで初めて発覚・修正した: (1) `Invoke-AgyProcess`が`ProcessStartInfo.ArgumentList`(コレクション型API)を使っていたが、このPCのWindows PowerShell 5.1環境にはこのプロパティが存在せず(`.GetProperty('ArgumentList')`が空)、agy.exeに引数が1つも渡らずハングしてタイムアウトしていた→`.Arguments`(文字列)+自前クオート関数`ConvertTo-ProcessArgumentString`に置き換えて解消。(2) `.claude/agents/implementer.md`の「セルフチェック(必ず実施)」が、agy用の上書きブロックの「拒否されたらスキップしてよい」より強く解釈され、agyが`flutter analyze`を試みて拒否され応答ごと打ち切られていた(agyは拒否時に会話全体を中断する、Claude側のような優雅なスキップはしない)→上書きブロックを「シェルコマンドは1回も試みない」という明示禁止に強化して解消。修正後、`-Role implementer`での実ファイル編集(`write_file`)・`-Role adversary`(`--mode plan`、無編集)いずれも実地成功を確認。**別途判明(ユーザー実機、恒久情報として`docs/antigravity_delegation_design.md`側に反映要)**: agy自身の`settings.json`の`write_file`許可はプレースホルダパス(`/path/to/project/`)のままだと機能しない(実パスに直す必要がある)。`command(git)`/`command(npm run ...)`/`command(flutter)`のような**引数なし単語1つ**の個別指定はWindowsで機能しなかったが、同日中にT5-A37で訂正済み(引数まで含めた完全一致の文字列なら機能する。上記T5-A37の節参照)。**T5-A40は2026-08-10完了**。Windows環境で`agy --version`(1.1.11)・`agy -p "OK"`・`agy -p "/usage"`を実行し結果を`docs/antigravity_delegation_design.md` §5に記録した。**T5-A36・T5-A4も2026-08-10完了**。`ui_verifier.md`絶対規則9(overflowはログ行またはスクショどちらか一方で「指摘あり」、L130で既に緩和済み)の判定基準のまま、画面090に意図的なoverflowを仕込んでui_verifierへ検証委譲した結果、`-Log`に`A RenderFlex overflowed by 810 pixels on the right.`が実際に出力され「指摘あり」と判定された(ログ行自体は複数回のうち出たり出なかったりしたが、L130の視覚的証拠併用ルールにより判定は安定)。原因究明(architect)自体は前セッションで完了済み(Flutter debugビルドのstructured errors既定有効でlogcatにFlutterErrorが出ないケースがある)だったため、今回は既存の緩和策で完了条件を満たせることを実地確認しただけ。一時編集(overflow挿入)は`git checkout`で復元済み。詳細は簡潔化のためアーカイブへの転記を省略(変更ファイルはlib/screens/settings_screen.dartへの一時編集のみで最終的に無変更)。**T5-A35は2026-08-09完了**。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A35」節。**T5-A34は2026-08-09完了**。`.claude/settings.json`に`PostToolUse`〈matcher `Task`〉・`SubagentStop`フックを追加し`loop_guard.js`をターン内でも再計算させ、`full_loop`スキル手順1・3.5を「フック出力ではなく`loop_state.md`をReadして判定する」に改めた。**実装直後、verifierの検証で`PostToolUse`/`SubagentStop`発火時にコストが常に$0になる副作用バグを発見**——`loop_guard.js`の生テキスト境界再検出処理(`UserPromptSubmit`専用のはずが全イベント無条件実行だった)が、他イベントのペイロード内の無関係なテキスト〈サブエージェント指示文・SKILL.mdパス等〉に含まれる`/full_loop`部分文字列に誤反応しループ境界を「今この瞬間」へ誤リセットしていたことが原因(L131)。`event === 'UserPromptSubmit'`限定のガードを追加して修正し、verifierが再検証(コスト$11.89→非ゼロ、境界タイムスタンプが発火時刻と33分ズレていることを確認)。`additionalContext`による文脈注入は、`Task`ツールを持たないimplementerの制約でライブ検証はできず、ドキュメント調査結果を記録するに留め完了扱いとした。コード変更は`.claude/hooks/loop_guard.js`・`.claude/settings.json`・`.claude/skills/full_loop/SKILL.md`のみ・`lib/`不変のためデプロイ対象外。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A34」節。**T5-A33は2026-08-09完了。`.claude/hooks/loop_guard.js`にサブエージェント消費の合算を実装(`resolveTranscriptTargets()`新設、`analyze()`を親+サブエージェント合算対応に変更、`loop_state.md`に内訳行追加)。verifierが独立検証: `eef0d647-...`セッションで個別ファイルのコストが`docs/token_optimization_design.md` §9-A表と誤差$0.0005で一致、サブエージェント無しセッションで回帰無し確認済み。完了条件のうち「本ループスコープでの合計一致」は境界検出ロジック(変更対象外)の影響で厳密一致せず(§9-A表は当日全体スコープの値、本ループスコープでは$17.78)、当日全体スコープでの再計算では$20.8145で一致を確認したため実質達成と判断。コード変更は`.claude/hooks/loop_guard.js`のみ・`lib/`不変のためデプロイ対象外。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A33」節。**T5-A32は2026-08-09完了。`tools/ui_probe.ps1`の`Invoke-Prepare`を「ビルド→起動→install」順に再構成、`Invoke-Adb`共通ラッパー+終了コード確認、`Assert-DeviceAlive`(プロセス生死でなく`adb get-state`応答性で判定、T5-A31のハング事象もカバー)、`-Prepare -Retry`自動再試行、`-AvdName`既定値を`beanbase_ui`に統一、`.claude/agents/ui_verifier.md`絶対規則8を具体化。verifierが独立検証: kill後1.68秒/実地再検証8.6秒でdevice_lost検知(要求10秒以内)、通常フロー・`-Alive`も正常動作、`flutter analyze`新規issue0件。**T5-A4の完了条件再実行で新たな知見**: overflowはスクショ・dump実測で検出できたが要求される`RenderFlex overflowed`ログ行は`-Log`で検出できず(L130、原因究明はT5-A36へ分離。T5-A4自体は完了済みに移さず継続)。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A32」節。T5-A31は2026-08-09完了。`tools/emulator.ps1`に既定AVD変更・ログ分離・`HasExited`即時検知・`Clear-StaleEmulator`・`-Doctor`を実装、verifierが強制kill後の即時検知(180秒→約3秒)を実地確認。**副次観察**: 正常系確認中に1回、プロセスは生存したまま約9分ハングする事象を観測(`HasExited`では検知不能な失敗モード、T5-A32の死活監視でカバー予定として申し送り済み)、`-Stop`が`Clear-StaleEmulator`を呼ばず`multiinstance.lock`が残存する軽微な差異も観測(次回`-Start`で自動解消のため実害なし)。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A31」節。T5-A30は2026-08-09完了。新AVD `beanbase_ui`〈1080x2400/density420〉を作成し`-Prepare -SkipBuild`→`-Shot`を5回連続実行、Windowsイベントログでその時間帯のAPPCRASH 0件を確認、verifierが独立再検証済み。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A30」節。T5-A27・T5-A28も2026-08-09完了。T5-A28は原因調査の結果、当初想定〈フック発火タイミングの制約〉より深刻な欠陥〈サブエージェント消費が構造的に集計対象外、可視範囲33.2%〉と判明、改善策をT5-A33〜A35へ分解。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A28」節、設計は`docs/token_optimization_design.md` §9参照。T5-A27の原因調査とD-1〜D-4への分解は同アーカイブ「T5-A27」節参照。他は2026-08-08。**T5-A5は2026-08-09、`.claude/agents/researcher.md`新設+T5-C3の実行完了により終了条件を満たした**。コード変更は検証ツール・エージェント定義・運用スクリプト・SKILL.md・`loop_guard.js`のみ・`lib/`不変のためデプロイ対象外。T5-A3の実動確認結果・T5-A11の3ケース確認結果は`docs/archive/マスタープラン_完了タスク.md`「T5-A3」「T5-A11」節。T5-A26はWindows環境`~/.claude/settings.json`への`enabledPlugins`追加のみ、リポジトリ外の変更のためcommit対象なし。**T5-A6は2026-08-09に`verifier`が独立検証**: `tools/emulator.ps1 -Start`/`-Stop`/`flutter devices`/`flutter doctor -v`いずれもOK。初回`-Start`実行時のみツール実行系がexit code 255を報告したがスクリプト自身の出力・機能とも成功しており、`-Status`・再実行時はexit 0で再現せず——GUIプロセスをStart-Processで起動する際の環境的な副作用と判断し実害なしと結論、詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A6」節に転記。**T5-A8は2026-08-10完了**。Windows環境でのgolden全件失敗をarchitectがOSフォントレンダリング差と特定し「Windowsでベースライン再生成+非Windows skip」を採用、implementerが実装、verifierが`tools/verify.ps1`8項目全ok確認。詳細は`docs/archive/マスタープラン_完了タスク.md`「T5-A8」節。恒久解決(OS非依存化)はT5-A45へ分離。**T5-A44は2026-08-10完了**。`.claude/skills/end/SKILL.md`の締め手順に、agy委譲を行った日は`.claude/agy_logs/ledger.tsv`の該当行を`docs/antigravity_delegation_design.md` §7の表へ転記し`verdict`列を埋める手順を追加。`grep -n "ledger.tsv"`で参照を確認。**判定条件のうち「§7の表に少なくとも1行の実績転記」は今回未達成**——このループでは実際のagy呼び出しを行っておらず(通常のClaudeサブエージェントのみ使用)、転記すべき実データが無いため。パイロット導入(T5-A41)で実際にagyを使った回に自然に満たされる見込み。**T5-A43は2026-08-10完了**。`.claude/hooks/loop_guard.js`に`readAgyLedgerSummary()`を新設、既存のループ境界検出ロジックを再利用して台帳を集計し、`loop_state.md`へ`- agy委譲(枠外・参考): ...`行を表示専用で出力(コスト・ターン閾値判定には未使用)。台帳欠損時は`try/catch`で握りつぶし行を出さない。implementerがダミー台帳あり/なしの両方で実地確認、`node -c`で構文チェックOK。`CLAUDE.md`に該当ルールを1文追記。**T5-A42は2026-08-10完了**。agy委譲ルートを`.claude/skills/full_loop/SKILL.md`(委譲表を§9.5の4行表へ差替+手順3にルーティング表参照追記)・`.claude/skills/night_loop/SKILL.md`(T5-A41昇格判定完了までagy不使用を明記)・`CLAUDE.md`(モデル分担ルールにagy導入後も親はSonnet 5のまま/非0終了は連続失敗に数えない旨を追記)へ配線。implementerが実装、`grep -n "antigravity_delegate"`で3ファイルへの参照を確認。ドキュメント編集のみで`lib/`不変のためデプロイ対象外)
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。
> **T5-A1の使い方(後続タスクの前提)**: Windowsでは **`powershell -File tools\verify.ps1`** が本命(`jq`不要)。`tools/verify.sh`はGit Bash用だが`jq`必須で、不在時は`{"ok":false,"error":"jq_not_found",...}`を返して非ゼロ終了する。`build_apk_release`は`lib/main_public.dart`未作成・Android SDK未検出の間は`{"ok":true,"skipped":true,"note":...}`でスキップされる(T5-A6・トラックBのE-1完了後に実効化する)。
> **T5-A38/T5-A39/T5-A41/T5-A42/T5-A43/T5-A44は2026-08-10に完了済みへ移動済み**(詳細は上記完了済み一覧の注記)。トラックAのagy委譲配線タスク・パイロット導入はこれで完了。**2026-08-10、T5-A41の結論により状態遷移は「パイロット」→「条件付き常時」(`docs/`・`tools/`・`.claude/`の非Dartファイル+`lib/`配下のS規模タスクがagy対象)へ移行済み**(`docs/antigravity_delegation_design.md` §7・§9.5)。**「常時委譲」への移行には`lib/`配下での追加3件の実績が必要**——次回以降、依存の満たされた`lib/`配下のS規模タスクが選定された際は§9.1のルーティング表に従いagy委譲を試み、結果を§7実績ログへ追記していくこと。**2026-08-10ユーザー指示(恒久)**: agy正式運用移行を、不具合対応を除く**最重要タスク**とする方針は`lib/`配下実績の蓄積フェーズにも引き続き適用する。`full_loop`スキルのタスク選定規則に手順0として明記済み。
> **T5-A17/T5-A12関連の既知の不具合(2026-08-09、`night_loop`試走中に発覚)**: `.claude/settings.night.json`の`defaultMode: "dontAsk"`は「denyに無ければ許可」ではなく「allowに無ければ拒否」で効くため、現状の`allow`リスト(`flutter analyze`等の定型コマンドのみ)には`Edit`/`Write`が含まれておらず、無人実行では親セッション・サブエージェントともに一切コードもドキュメントも編集できない(詳細・再現手順は`rules/lessons_archive.md` L132)。T5-A17の完了条件(a)(b)(c)の実測、および本設計書§4-4の前提記述の見直しが必要。**対処にはユーザーが`.claude/settings.night.json`の`allow`に`Edit`/`Write`を追加する必要がある**(アシスタントは自分の権限設定を書き換えられない)。

#### トラックB / P0 — エディション足場 + 原価リスクの遮断

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B0a | **全AI機能に`maxOutputTokens`を設定**(`ai_analysis_service.dart`。#3=600、#4=300 等、機能ごと)。出力暴走で1回3円超・月100回310円の赤字になりうる**最大の原価リスク**を数行で潰す(コスト試算 §2-3a) | 5機能すべての`GenerationConfig`に上限が入り、既存テストが全パス／受入: 免除(Sサイズ) | なし | S | ⬜ |
| T5-B0b | **画像を長辺1024pxへ縮小してから送信**(`image_upload_field.dart`の`ImagePicker`に`maxWidth`/`maxHeight`、ファイル選択側もリサイズ)。豆読取の原価が0.36円→0.09円に(コスト試算 §2-3b) | 送信前の画像の長辺が1024px以下。豆読取AIが従来どおり動作する／受入: 免除(Sサイズ) | なし | S | ⬜ |
| T5-B1 | **E-1**: `lib/config/app_edition.dart` 新設 + `lib/main_public.dart` 追加。**差分ゼロの2エントリポイント**(コードベース構成方針 §7) | 両エントリポイントが**同一の動作**で起動する。`flutter build apk -t lib/main_public.dart`成功／受入: 免除(Sサイズ) | なし | S | ⬜ |
| T5-B2 | **E-2**: `enabledScreens` を `screenRegistry`(`lib/routing/screen_registry.dart`)と`main_layout.dart`の`destinations`に接続。**ホワイトリスト方式**(既定非表示) | public版で`lib/screens/debug/`・`lib/screens/mock/`が出ない。personal版は従来どおり全画面／受入: test/acceptance/t5_b2_acceptance_test.dart | T5-B1 | M | ⬜ |
| T5-B3 | **E-3**: `dataServiceProvider`(`lib/providers/data_providers.dart`)を`AppEdition.useLocalDb`経由にする(実装はまだ`SheetsService`のみ) | personal版が従来どおりSheetsで動く／受入: 免除(Sサイズ) | T5-B1 | S | ⬜ |
| T5-B4 | **E-4**: `AiAnalysisService`のキー取得を`AppEdition.aiKeyMode`経由にする | personal版のAI機能が従来どおり動く／受入: 免除(Sサイズ) | T5-B1 | S | ⬜ |
| T5-A7 | `integration_test` スモークスイート。起動→記録(抽出→評価)→保存→一覧反映→インサイト表示→設定 の一筆書き+**5マスタ全部の一覧→詳細→編集**(2026-08-10、依存が`T5-B1`のためトラックAからトラックBへ分類変更。IDはT5-A7のまま維持) | エミュレータ上で全パス。`verify.ps1`とは別コマンドで実行できる／受入: test/acceptance/t5_a7_acceptance_test.dart | T5-A6(完了済み), T5-B1 | L | ⬜ |

#### トラックB / P1 — ローカルDB化

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B10 | **DBパッケージの選定調査**(`researcher`)。`drift`第一候補 vs `isar` vs `sqflite`。**型安全なマイグレーション基盤の有無**を最重要の判断軸にする | `docs/research/<日付>_local_db.md`に比較表と推奨、出典URLつき／受入: 免除(Sサイズ) | T5-A5 | S | ⬜ |
| T5-B11 | ⚠️**上位モデルで実施**: ローカルDBスキーマ設計。全モデルのテーブル定義・列名・型・インデックス・**マイグレーション方針**・Sheets↔ローカルの対応表を確定(implementerが設計判断せずに実装できる粒度) | 設計書が存在し、テーブル/列/移行の全定義が確定している／受入: 免除(設計タスク) | T5-B10 | M | ⬜ |
| T5-B12 | **マイグレーション基盤**を最初に入れる(後付けは必ず破綻する)。バージョン管理・up移行・移行テストの雛形 | 意図的にスキーマを1バージョン上げたテストが通る／受入: test/acceptance/t5_b12_acceptance_test.dart | T5-B11 | M | ⬜ |
| T5-B13 | `LocalDbService`(`DataService`の実装)を作成。40メソッド相当を実装 | public版がローカルDBで一覧/登録/編集/削除できる。`integration_test`スモークが通る／受入: test/acceptance/t5_b13_acceptance_test.dart | T5-B12, T5-B3 | L(要分割) | ⬜ |
| T5-B14 | 画像のローカル保存(`path_provider`。既に依存にある)。public版は`ImageService`をDrive→ローカルに差し替え | 画像を登録→再起動後も表示される／受入: test/acceptance/t5_b14_acceptance_test.dart | T5-B13 | M | ⬜ |
| T5-B15 | **エクスポート/インポート(JSON/CSV)を無料機能として実装**。データ消失クレームがレビュー低評価の最大要因(コスト試算 §6-#7) | エクスポート→アプリのデータ全消去→インポートで完全復元できる／受入: test/acceptance/t5_b15_acceptance_test.dart | T5-B13, T5-B14 | M | ⬜ |

#### トラックB / P2 — UI全画面の新規デザイン

**順序を守る: トークン → コンポーネント → 画面**(L10。共通部品を先に作る)。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B20 | ⚠️**上位モデルで実施**: 公開版のデザイン方針とトークン体系の設計。色/タイポ/余白/角丸/エレベーション、**ダークモード**、既存`mainColorProvider`との関係、4系統(ホーム/記録/インサイト/オンボーディング)の画面構成と遷移 | 設計書が存在し、トークン名・値・コンポーネント一覧・画面IDが確定している／受入: 免除(設計タスク) | T5-B1 | M | ⬜ |
| T5-B21 | デザイントークンの実装(ライト/ダーク) | 両モードでアプリが起動し、既存画面が壊れない／受入: test/acceptance/t5_b21_acceptance_test.dart | T5-B20 | M | ⬜ |
| T5-B22 | 共通コンポーネント実装(カード/リスト行/空状態/ローディング/エラー/ボトムシート/数値入力)+ **各コンポーネントのgolden(ライト/ダーク)** | goldenが全件撮れ、`verify.ps1`のgolden項目が動作する／受入: test/acceptance/t5_b22_acceptance_test.dart | T5-B21, T5-A8 | L(要分割) | ⬜ |
| T5-B23 | 画面: ホーム | goldenあり。`ui_verifier`の7項目で指摘なし／受入: test/acceptance/t5_b23_acceptance_test.dart | T5-B22 | M | ⬜ |
| T5-B24 | 画面: 記録(抽出→評価) | 同上。`integration_test`の記録導線が新画面で通る／受入: test/acceptance/t5_b24_acceptance_test.dart | T5-B22 | L(要分割) | ⬜ |
| T5-B25 | 画面: インサイト(表示層のみ。中身はP3) | 同上／受入: test/acceptance/t5_b25_acceptance_test.dart | T5-B22 | M | ⬜ |
| T5-B26 | 画面: オンボーディング(インストール直後から使える価値。抽出タイマー/テンプレレシピ/器具プリセット。収益化アイデア §9-2) | 初回起動でオンボーディングが出る／受入: test/acceptance/t5_b26_acceptance_test.dart | T5-B22 | M | ⬜ |
| T5-B27 | 残りの画面(マスタ管理等)を新体系へ移植。**全マスタ(豆/グラインダー/ドリッパー/フィルター/メソッド)に一律適用** | 公開版の全画面が新デザインで表示される／受入: test/acceptance/t5_b27_acceptance_test.dart | T5-B23〜B26 | L(要分割) | ⬜ |
| T5-B28 | モバイル固有の作り込み: タップ領域48dp/スワイプ/ハプティクス/**抽出タイマーのバックグラウンド動作と通知**/画面回転/戻るジェスチャ/端末フォントサイズ追従 | 実機でタイマーがバックグラウンドでも動き通知が出る／受入: test/acceptance/t5_b28_acceptance_test.dart | T5-B24 | L(要分割) | ⬜ |

#### トラックB / P3 — 統計の「洗練された情報のみ」化

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B30 | ⚠️**上位モデルで実施**: 公開版のインサイト表示仕様。**生の統計量(PCA負荷量/回帰係数/p値/寄与率/固有値)を出さない**方針を、`statistics_feature_design.md`に「公開版の表示規則」の節を**追加する形**で整合させる(§0の絶対規則「点推定+不確実性をセット表示」を破らず、数値からビジュアルへ表現を変えるだけと位置づける)。所見1文+根拠1つ+次の一手のカード形式、確信度3段階の定義、データ不足時の閾値 | 設計書に節が追加され、変換規則と閾値が確定している／受入: 免除(設計タスク) | T5-B20 | M | ⬜ |
| T5-B31 | インサイト変換層の実装(統計サービスの出力 → インサイトカード) | 生の統計量が公開版のUIに一切現れない(grepで確認)／受入: test/acceptance/t5_b31_acceptance_test.dart | T5-B30, T5-B25 | M | ⬜ |
| T5-B32 | データ不足時の進捗表示「**あと◯件でインサイトが使えます**」+ 少数データでも出せる簡易フィードバック(前回との比較) | ログ0件・数件・閾値直前・閾値超えの4状態が正しく出し分けられる／受入: test/acceptance/t5_b32_acceptance_test.dart | T5-B31 | M | ⬜ |

#### トラックB / P4 — 収益化基盤(v1.0に含む)

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
**無料/Pro の線引き(2026-08-07 ユーザー確定、このグループ全体の前提)**: **無料では AI 機能を一切使えない。AI 以外(記録・マスタ管理・統計/インサイト全機能・エクスポート/インポート)はすべて無料**。Pro = AI 月100回 + 広告非表示 + クラウドバックアップ。

この線引きにより設計が単純化される: (a) **無料ユーザーはサーバに一切アクセスしない**ためアカウント登録・ログインが不要でインストール直後から使える (b) **コスト試算 §6-#2 の「無料枠悪用による原価青天井」リスクが構造的に消滅**(無料枠が存在しないため取り直せるものが無い) (c) AI原価が課金者数に厳密に比例し粗利予測が完全に立つ (d) MAUが増えてもサーバ費用は増えない。

代わりに収益化アイデア §3-3「無料枠ゼロだと課金率が上がらない」への対策として、**Play標準の「Proの無料トライアル」で体験導線を作る**(T5-B48)。無料ユーザーにAIを開放するのではなくProを期間限定で試す形なので線引きを崩さない。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B40 | ⚠️**上位モデルで実施**: 中継サーバと課金の設計。API仕様/認証方式/回数カウンタのデータ構造/購入検証フロー/RTDNの受信と反映/**価格とAI回数上限をサーバ側設定値として持つ形**(アプリに埋め込まない)/エラー時のクライアント挙動/**無料ユーザーがサーバを一切呼ばないことの担保**(クライアント側でAI導線を出さない + サーバ側でも未認証リクエストを拒否する二重化) | 設計書が存在し、エンドポイント・スキーマ・状態遷移が確定している／受入: 免除(設計タスク) | T5-B15 | L | ⬜ |
| T5-B41 | Cloud Run 中継サーバの実装(`アプリ → 中継サーバ → Gemini`)。**APKにGeminiキーを埋めない** | ローカルからエンドポイントを叩いてAI応答が返る。未認証リクエストは拒否される／受入: test/acceptance/t5_b41_acceptance_test.dart | T5-B40 | L(要分割) | ⬜ |
| T5-B42 | **Googleアカウントログイン**の実装。**課金時のみ必須**(無料ユーザーには求めない)。購入の復元とPro状態の紐付けに使う | 課金導線でログインを求め、ログアウト→再ログインで購入が復元される。**未ログインでもAI以外の全機能が使える**／受入: test/acceptance/t5_b42_acceptance_test.dart | T5-B41 | M | ⬜ |
| T5-B43 | サーバ側の回数カウンタ(端末側カウンタは改ざん可能なので使わない)。**Pro=月100回、無料=利用不可** | 上限到達でAI呼び出しが拒否され、翌月にリセットされる／受入: test/acceptance/t5_b43_acceptance_test.dart | T5-B42 | M | ⬜ |
| T5-B44 | `in_app_purchase` + **購入トークンのGoogle Play Developer APIによるサーバ検証**(クライアント検証は偽装可能) | テスト購入でPro状態がサーバ側に反映される／受入: test/acceptance/t5_b44_acceptance_test.dart | T5-B43 | L(要分割) | ⬜ |
| T5-B45 | RTDN(Real-time Developer Notifications)受信。解約・返金・期限切れの反映 | 解約テストでPro状態が失効し、AI導線が非表示に戻る／受入: test/acceptance/t5_b45_acceptance_test.dart | T5-B44 | M | ⬜ |
| T5-B46 | `google_mobile_ads` + UMP同意。**バナー(一覧下部)のみ。インタースティシャルは入れない**(記録アプリのUXを壊す)。**リワード広告でのAI付与は行わない**(無料でAIを使えることになり線引きに反する) | 同意ダイアログが出て、無料ユーザーにバナーが表示される／受入: test/acceptance/t5_b46_acceptance_test.dart | T5-B44 | M | ⬜ |
| T5-B47 | Pro機能の実装: 広告非表示 / クラウドバックアップ。**エクスポート/インポートは無料側(T5-B15)なのでProには含めない**。訴求は「AI回数」より「広告非表示」が刺さる(収益化アイデア §2-4) | Pro購読中は広告が消え、バックアップ/復元が動く／受入: test/acceptance/t5_b47_acceptance_test.dart | T5-B45, T5-B46 | L(要分割) | ⬜ |
| T5-B48 | **Proの無料トライアル**(Play標準の introductory offer)の設定と、アプリ内の訴求導線(AI機能をタップした無料ユーザーに「トライアルで試す」を出す)。無料枠ゼロによる課金率低下への対策(収益化アイデア §3-3) | 無料ユーザーがAI機能に触れるとトライアル導線が出る。トライアル中はProとして扱われ、期間終了でAIが使えなくなる／受入: test/acceptance/t5_b48_acceptance_test.dart | T5-B47 | M | ⬜ |

#### トラックC — リリース準備(人手中心。開発と並行)

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-C1 | **Play Console デベロッパー登録**($25 ≒ 3,900円)。本人確認に日数がかかることがある | アカウントが有効になり、アプリを作成できる／受入: 免除(Sサイズ) | なし | S | ⬜(**ユーザー実施・今すぐ**) |
| T5-C2 | テスター12人の確保。**知り合いから確保できる見込み(2026-08-07ユーザー確認済み)のため人集めは律速にならない**。内部テスト完了時点で声をかけられるよう名簿だけ用意しておく | 12人以上のGoogleアカウントが集まっている／受入: 免除(Sサイズ) | なし | S | ⬜(ユーザー実施) |
| T5-C4 | 署名鍵の作成と **Play App Signing** の設定。**keystoreを複数箇所にバックアップ**(紛失すると以後アップデート不能。コスト試算 §6-#6)。keystoreはコミットしない | 署名済みAABが生成でき、バックアップ先が2箇所以上ある／受入: 免除(Sサイズ) | T5-C1 | S | ⬜(ユーザー実施を含む) |
| T5-C5 | プライバシーポリシー(GitHub Pages)。**端末ローカルに留まる範囲とAIへ送信する範囲の線引きを明記**する | 公開URLがあり、実装と一致している／受入: 免除(Sサイズ) | T5-B41 | S | ⬜ |
| T5-C6 | Data safety 申告。実装と一致させる(実装が変わったら申告も更新) | Play Consoleで申告が完了／受入: 免除(Sサイズ) | T5-C5 | S | ⬜ |
| T5-C7 | ストア素材: アイコン / feature graphic / スクリーンショット8枚 / 説明文 | Play Consoleに全素材が登録済み／受入: 免除(視覚判断のみ) | T5-B27 | M | ⬜ |
| T5-C8 | ASO(キーワード設計)。日本語UI×コーヒー抽出記録の二重のニッチである前提で設計(収益化アイデア §9-1) | タイトル・短い説明・長い説明にキーワードが織り込まれている／受入: 免除(Sサイズ) | T5-C7 | S | ⬜ |
| T5-C9 | 内部テスト。実機での全機能回帰 | 実機でクラッシュなく主要導線が通る／受入: 免除(視覚判断のみ) | T5-B48, T5-C4, T5-C6 | M | ⬜ |
| T5-C10 | **クローズドテスト(12人 × 14日間連続)** | Play Consoleの要件が満たされ、製品版申請が可能になる／受入: 免除(視覚判断のみ) | T5-C9, T5-C2 | L(待機14日) | ⬜ |
| T5-C11 | 製品版申請 → 公開。**T5-C3をもう一度実施してから提出する** | Google Playで一般公開されている／受入: 免除(視覚判断のみ) | T5-C10, T5-C7, T5-C8 | M | ⬜ |

> **完了済み(1件)**: T5-C3(2026-08-09、`researcher`が調査。成果物は`docs/research/2026-08-09_play_requirements.md`。**申請直前(T5-C11着手前)に再実施が必要**なため、着手済みではあるが最終確定ではない点に注意)

#### Phase 5 の未決事項(着手前に決める)

1. **アプリ名・パッケージID・ストア上のブランド** — パッケージIDは公開後に変更不能
2. **英語対応** — 母数10倍 vs 30画面の文言外部化の工数。**v1.0は日本語のみ、v1.1以降で判断**が妥当
3. **アフィリエイト導線** — 原価ゼロで相性は最良だがAmazonアソシエイト審査とステマ規制の広告表記が要る。v1.0に入れるか要判断
4. **Firestoreレガシー資産の扱い** — 回数カウンタにFirestoreを使うなら`FirestoreService`を復活させるか新規に書くか
5. **Proでの`model: opus`サブエージェントの可否** — T5-A14で実測
6. **既存`mainColorProvider`と新デザイントークンの関係** — T5-B20で決める

### タスク共通の検証手順（全タスクに適用）

1. `flutter analyze` — 新規エラー/警告ゼロ
2. `flutter test` — 全パス(ロジックを追加したタスクは単体テストも追加)
3. `flutter run -d chrome` — 対象画面の表示・遷移・Sheets 疎通を確認(該当時は Playwright で視覚確認。snapshot は最小限)
4. OK → commit/push + マスタープラン進捗表更新 / NG → `NEXT_SESSION.md` 更新

---

## 4. 画面インベントリ（23画面）

| 画面 | 名称 | 主な要素 | 状態 |
|---|---|---|---|
| 001 | ダッシュボード | 残豆量(瓶)・直近5件履歴・黒板風 | ✅ |
| 002 | 抽出履歴(リスト) | 日時/豆/メソッド/点数、スワイプで021 | ⬜ |
| 003 | 抽出履歴(詳細) | 全情報表示・編集 | ⬜ |
| 010 | 豆管理(カード) | 焙煎所/豆名/煎り度/画像/残量、0%表示切替、＋ | ✅ |
| 011 | 豆管理(詳細) | 全情報・編集・関連履歴5件 | ✅ |
| 012 | 新規豆追加 | 入力フォーム | ✅ |
| 013 | ドリッパー管理 | 画像左/名前右リスト、＋ | ✅ |
| 014 | ドリッパー詳細 | 全情報・編集・関連履歴5件 | ✅ |
| 015 | 新規ドリッパー | 入力フォーム | ✅ |
| 016 | フィルター管理 | 画像左/名前右リスト、＋ | ✅ |
| 017 | フィルター詳細 | 全情報・編集・関連履歴5件 | ✅ |
| 018 | 新規フィルター | 入力フォーム | ✅ |
| 019 | メソッド管理 | 名前/発案者(小)/抽出回数、＋ | ✅ |
| 020 | メソッド詳細 | 全情報・動画再生(可能なら)・編集・履歴5件 | ✅(動画再生はT3-3で別途) |
| 021 | 新規メソッド | 入力フォーム | ✅ |
| 022 | グラインダー管理 | 画像左/名前リスト、＋ | ✅ |
| 023 | グラインダー詳細 | 全情報・編集・履歴5件 | ✅ |
| 024 | 新規グラインダー | 入力フォーム | ✅ |
| 030 | 抽出レシピ | Pouring Steps・タイマー・編集・評価ボタン→031 | ✅ |
| 031 | 抽出結果の評価 | 抽出情報登録＋評価・新規登録対応 | ✅ |
| 040 | 統計情報 | スタッツ・歯車→090 | ✅ |
| 045 | 探索の検証状況 | 探索サマリ・次に試すと良い条件・スコア推移・試した条件の分布・試行一覧、030/011から遷移 | ✅ |
| 090 | 設定 | メインカラー/APIキー/保存先 | ✅ |

---

## 5. 日次ループ運用（要点）

- **終了条件**（いずれかで停止）: ①タスク完了 ②連続3回失敗 ③1ループのコスト>$24 ④1ループのターン数>=30(2026-07-25〜、すべて直近の`/start`・`/full_loop`以降の累計。従来は当日累計だった)
- コスト・ターン数は `.claude/hooks/loop_guard.js` が transcript から算出し `.claude/loop_state.md` に出力。**この数値が真実**。
- 連続失敗は `.claude/loop_failures.txt`（`<ループ識別子> <回数>` 形式）を自分で更新（失敗+1／成功0リセット。識別子は `loop_state.md` の「ループ識別子」の値を使い、新しいループが始まると自動的に 0 扱い）。
- 停止時は新規着手せず、`NEXT_SESSION.md`（引き継ぎ）と本書の進捗表を更新してから終了。
- 詳細は `CLAUDE.md`「日次改修ループ運用ルール」を参照。

- 過去の自動ループ運用方針(クラウド/ローカル分担・コスト算出式)は `docs/archive/マスタープラン_作業ログ.md` 末尾に退避。
