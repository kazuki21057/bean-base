# BeanBase 2.0 改修マスタープラン（進捗表示付き）

> 本書は今回の大規模改修の **全体設計と進捗の単一の真実**。毎日の改修タスクはここから選ぶ。
> 出典: [`docs/Beanbase改修案.md`](./Beanbase改修案.md) / 運用ルール: `CLAUDE.md`「日次改修ループ運用ルール」
> Phase 4 詳細の出典: `statistics_feature_design.md`(単一の正本) / `CLAUDE.md`「統計解析・予測機能の実装ルール」
> 最終更新: 2026-07-29(トークン削減のため完了行・作業ログをアーカイブへ分離)

> **本書の読み方(トークン節約、2026-07-29)**: 1ループで読むのは **§2(進捗サマリ)** と **§3 の該当フェーズの未完了行だけ**。
> 完了タスクの詳細は `docs/archive/マスタープラン_完了タスク.md`、日付順の作業ログは `docs/archive/マスタープラン_作業ログ.md` に退避済み(**通常は読まない**、必要時に ID/日付で grep)。

## 凡例
| 記号 | 意味 |
|---|---|
| ⬜ | 未着手 |
| 🟦 | 進行中 |
| ✅ | 完了 |
| ⏸ | 保留／後回し |

---

## 1. 改修の全体方針

| # | 柱 | 概要 | 優先 |
|---|---|---|---|
| A | データ基盤変更 | Firestore → **Google Sheets** に戻す（管理を楽に） | 1 |
| B | 画面構成・遷移の再編 | 抽出と評価を分離、14画面構成へ再設計 | 2 |
| C | UI全面刷新 | ダッシュボード/抽出/評価/スタッツを一から。黒板風・瓶ビジュアル等 | 3 |
| D | その他軽微な修正 | レスポンシブ確認・DRY化・仕上げ | 4 |
| E | AI活用拡大 | PCAの説明範囲拡大など（**全改修完了後**） | 5（最後） |

**データ基盤の決定（A）:** 個人利用・データ量小のため、以前の `SheetsService`（`lib/services/sheets_service.dart`）を再活用して Google Sheets に戻す。スプレッドシートで直接データ確認・手動編集できる点が「管理が楽」の要件に最適。Cycle 18 で入った `FirestoreService` 経由の読み書きを Sheets 経由へ差し戻す。画像保存先は別途方針決定（後述 T0-4）。

**モバイル利用の決定(D、2026-07-13):** ネイティブアプリ(Android/iOS)は**作らない**。Web 版をホスティングし、スマホ(Android)のブラウザからアクセスして**ホーム画面にピン留め(PWA として standalone 起動)**して使う。ホスティング先は既存 Firebase プロジェクト(`beanbase-app-2016`)の **Firebase Hosting** を第一候補とする(無料枠で十分・`firebase.json` が既にある)。デプロイは `flutter build web --release` → `firebase deploy` の手動運用から始める。注意: Gemini API キーは `shared_preferences`(ブラウザごとの localStorage)保存のため、スマホ側でも設定画面から再入力が必要。

**公開URL(T3-11で確定、2026-07-18):** https://beanbase-app-2016.web.app — 認証なしで誰でもアクセス可能。再デプロイは `flutter build web` → `firebase deploy --only hosting`。

---

## 2. 全体進捗サマリ（フェーズ別）

| フェーズ | 内容 | 対応Cycle | 状態 |
|---|---|---|---|
| Phase 0 | データ基盤を Sheets に戻す | Cycle 19 | ✅ |
| Phase 1 | 画面構成・ナビ再編（14画面の骨組み） | Cycle 20–22 | 🟦 |
| Phase 2 | UI全面刷新（黒板風・瓶・タイマー・評価・スタッツ） | Cycle 23–26 | ✅ |
| Phase 3 | 軽微な修正・仕上げ | Cycle 27 | 🟦 |
| Phase 4 | 統計解析・予測機能拡張（数値基盤/データ基盤/回帰/PCA拡張/好み/提案/GP） | Cycle 28+ | 🟦 |
| Phase 5 | **Android公開版の開発〜リリース**(開発運用基盤/エディション足場/ローカルDB/UI新規設計/インサイト化/収益化/ストア公開) | Cycle 30+ | ⬜ |

> Cycle番号は目安。実際の粒度に応じて分割・増減してよい（番号は連番厳守、`docs/`と`docs/archive/`の最大値+1）。

---

## 3. フェーズ詳細＆タスク分解（日次タスクのベース）

### Phase 0 — データ基盤を Sheets に戻す（Cycle 19）
**終了条件:** Sheets 経由で一覧表示・登録・編集・削除が動き、`flutter run` で接続成功。


> **完了済み(6件)**: T0-1, T0-2, T0-3, T0-4, T0-4b, T0-5
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

### Phase 1 — 画面構成・ナビ再編（Cycle 20〜22）

**Phase 終了条件:** 22画面すべてにルーティングが通り、抽出(030)と評価(031)が別画面として遷移する。

サイズ: S=半日余裕、M=1ループ標準、L=1ループぎりぎり(着手前に分割を検討)


> **完了済み(16件)**: T1-1a, T1-1b, T1-1c, T1-2a, T1-2b, T1-3, T1-4a, T1-4b, T1-4c, T1-5a, T1-5b, T1-5c, T1-5d, T1-6a, T1-6b, T1-7
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

> 旧 T1-1〜T1-7 との対応: T1-1→T1-1a/b/c、T1-2→T1-2a/b、T1-4→T1-4a/b/c、T1-5→T1-6a/b、T1-6→T1-5a/b、T1-7→T1-5c/d。

### Phase 2 — UI全面刷新（Cycle 23〜26）

**Phase 終了条件:** 黒板風ダッシュボード・瓶ビジュアル・タイマー付き抽出・評価・スタッツが見た目どおり動作。


> **完了済み(14件)**: T2-1a, T2-1b, T2-2a, T2-2b, T2-2c, T2-3a, T2-3b, T2-3c, T2-4a, T2-4b, T2-5a, T2-5b, T2-6, T2-7
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

### Phase 3 — 軽微な修正・仕上げ（Cycle 27）

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-1 | モバイル実機でのレイアウト確認・調整(**ユーザー実施の確認結果を受けて修正**) | ユーザー確認で NG 項目ゼロ | T3-12 | M | ⬜ |
| T3-4 | 全体のUIおしゃれ化・微調整 | ユーザーレビューで完了判断 | T3-1 | M | ⬜ |
| T3-20 | Ubuntu環境の並行セットアップ(**ユーザー実施**) — Flutter/Dart SDK・Chrome(またはChromium)・Node.js(hooks用)・`gh` CLI導入、GitHub用SSH鍵の新規発行＆登録(リモートがSSH URLのため)、`flutter pub get`→`flutter analyze`/`flutter test`→`flutter run -d chrome`で疎通確認、Gemini APIキーを設定画面(090)で再入力(ブラウザのshared_preferencesはマシンごとに独立するため)。**2026-08-07: JDK17・Android SDK・Node.js・gh CLI導入とAndroid debugビルド成功まで完了(詳細はNEXT_SESSION.md -5.32節)。残りはGemini APIキー再入力とflutter run -d chromeでのSheets/Drive疎通確認** | Ubuntu上で`flutter run -d chrome`が起動し、Sheets/Driveへの疎通とAI分析が確認できる | なし | S | 🟦 |

> **完了済み(36件)**: T3-2, T3-3, T3-5, T3-6, T3-7, T3-8, T3-9, T3-10, T3-11, T3-12, T3-13, T3-14, T3-15, T3-16, T3-17, T3-18, T3-19, T3-21, T3-22, T3-23, T3-24, T3-25, T3-26, T3-27, T3-28, T3-29, T3-31, T3-32, T3-33, T3-30, T3-34, T3-35, T3-36, T3-37, T3-38, T3-39
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-07-26 ユーザー報告の不具合・要望、T3-40〜T3-53）

着手順の目安: **①不具合(T3-44→T3-45→T3-40) → ②焙煎度8段階化(T3-42→T3-43→T3-51) → ③画像UI統一(T3-41) → ④おすすめレシピ改修(T3-47→T3-48→T3-49) → ⑤最適条件探索(T3-50→T3-52→T3-53)**。**⑥データ整理(T3-46)は2026-07-30完了済み。T3-50・T3-47も2026-07-30完了済み**。**T3-52(a〜d全件)は2026-07-30完了・本番反映済み**。**T3-48は2026-07-31完了・本番反映済み(T3-71と合同デプロイ)**。**T3-51・T3-43も2026-07-31完了・本番反映済み(②焙煎度8段階化グループは全件完了)**。**T3-49も2026-07-31完了・本番反映済み(④おすすめレシピ改修グループは全件完了)。残るは⑤のT3-53のみで、その設計も2026-07-31に上位モデルで完了(→実装はT3-53a〜dへ分解済み)**。

**2026-08-01時点**: T3-40〜T3-56はT3-57を除き全件完了・本番反映済み(T3-53シリーズa〜dの完了で本グループは実質クローズ)。完了内容の詳細は `docs/archive/マスタープラン_完了タスク.md` を参照。**残るT3-57のみユーザーからの写真提供待ちで着手不可**。


| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-57 | **不具合(2026-07-27ユーザー報告)**: 「Youth」の豆(パッケージ画像)が表示されない。**調査結果(2026-07-27、下記note参照)**: 表示側のバグではなく、対象3件(`5a57cb8d`/`bf2d1f8d`/`5549c4ad`、いずれも産地違いのYouthシリーズ)は本番`bean_master`上で`豆画像URL`/`豆粒画像URL`/`情報画像URL`のいずれも元々空欄と確認。原因はT3-38(original-data移植)で、CSVが参照する画像がユーザーのローカルファイルパス(`豆マスター_Images/`配下)のみでリポジトリ・本番のどこにも実体が無く、移植時に画像だけ意図的に対象外とされたこと(T3-38完了ログに明記済み)。**修正には対象3件それぞれの実際のパッケージ写真が必要**(Claude側で生成・代用はできない)。画像URLの設定は`updateBean`経由でGASの`updateRow`を呼ぶが、**T3-56の再調査(2026-07-27)により`updateRow`自体は正常動作すると確認済み**(誤検知だった)ため、保存経路側の懸念は解消。**残る唯一の障害はユーザーからの写真提供待ち**。写真が揃い次第アプリUI(011編集)からアップロードして設定する | 3件のYouth豆すべてでパッケージ画像が本番アプリに表示される | なし(写真素材のみユーザー提供待ち) | S | ⬜(ユーザーからの写真提供待ち) |

> **完了済み(27件)**: T3-40, T3-41, T3-42, T3-43, T3-44, T3-45, T3-46, T3-47, T3-48, T3-49, T3-50, T3-51, T3-52, T3-52a, T3-52b, T3-52c, T3-52d, T3-53, T3-53a, T3-53b, T3-53c, T3-53d, T3-54, T3-54a, T3-54b, T3-55, T3-56
> T3-53シリーズ(a〜d)が2026-08-01に全完了し、Phase 3追加分(T3-40〜T3-53)グループはこれで全完了。残るT3-57のみユーザーからの写真提供待ちで本体表に残置。
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-07-28 ユーザー要望、T3-58〜T3-69）

**要望の原文(6件)**: ①calcページで豆量を変更してもpouring stepsに反映されない・線形でお湯量を変更して ②豆残量も手動で調整できるようにする ③そもそも追加購入を想定した設計になっていない。豆詳細画面に追加購入ボタンを追加し、購入日・焙煎日・購入量を入力するダイアログで既存の購入日・焙煎日を更新し購入量を残量に加算する(**画面デザイン・遷移は上位モデルで検討**) ④豆の管理項目に保存場所(職場/家)を追加、既存は全て「職場」にしてあとでユーザーが手動で振り分ける ⑤豆の購入履歴を追えるページを新設、リスト形式とカレンダー形式の両方で購入日・購入豆・購入先が分かるように(**運用方法・画面構成は上位モデルで検討**) ⑥購入店を管理できるページを追加、管理項目も検討。豆管理項目から店を自動抽出しネットから情報を自動取得(**上位モデルで実行**)。

**着手順の目安**: **①独立・即着手(T3-58 → T3-59) → ②在庫基盤(T3-60) → ③設計(T3-61、上位モデル) → ④購入履歴基盤〜UI(T3-62 → T3-63 → T3-64 → T3-65) → ⑤購入店(T3-66設計(上位) → T3-67 → T3-68 → T3-69)**。

**依存関係(必ず守る順序)**:
- `T3-60`(在庫基準点) → `T3-61`(追加購入+購入履歴の統合設計、上位モデル) → `T3-62`(購入履歴データ基盤) → `T3-63`(追加購入UI実装) / `T3-63b`(012の初回購入記録+遡及登録) / `T3-64`(購入履歴リスト) → `T3-65`(購入履歴カレンダー)
- `T3-66`(購入店設計、上位モデル。**2026-07-28完了**) → `T3-67`(購入店データ基盤) → `T3-68`(購入店UI) → `T3-70`(新規購入店のAI自動取得) / `T3-69`(豆マスタのstore→storeId移行)
- `T3-62` + `T3-68` → `T3-69`

**すぐ着手できる(依存なし)**: T3-69(依存T3-62・T3-68は充足済み)。**T3-58は2026-07-29に完了済み**。**T3-66は2026-07-28に上位モデル(Opus 5)で完了済み**(成果物: `docs/store_master_design.md`)。**T3-67・T3-68・T3-70は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**。**T3-60は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(在庫基準点方式)。**T3-61(追加購入+購入履歴の統合設計)は2026-07-29に上位モデル(Opus 5)で完了**(成果物: `docs/bean_purchase_design.md`。T3-62〜T3-65は同書のとおり実装すれば設計判断が不要な粒度になっており、あわせて`T3-63b`を新設した)。**T3-62(購入履歴のデータ基盤)は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(GAS `bean_purchases`シート9列を本番へ自動生成確認済み)。これによりT3-63・T3-63b・T3-64がSonnet 5で着手可能になった。**T3-63(011の追加購入ボタン)は2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(スイートイエローで追加購入50gを実際に実行し確認済み)。**T3-63b(012の初回購入記録+遡及登録)も2026-07-29にSonnet 5の`/full_loop`で完了・本番反映済み**(本番`bean_purchases`に`bp_init_<豆ID>`で正しく記録されることを確認済み)。**T3-64(025リスト形式)も2026-07-29にSonnet 5の`/full_loop`で完了・本番デプロイ・本番確認まで完了**(デプロイは初回セッションで自動モード分類器にブロックされたが翌セッションでユーザーが手動実行し成功)。これによりT3-65(025カレンダー形式)がSonnet 5で着手可能になった。**T3-65(025カレンダー形式)も2026-07-29にSonnet 5の`/full_loop`で完了・本番デプロイ・本番確認まで完了**(デプロイが自動モード分類器にブロックされたが、今回はAgentツールでサブエージェントに同じコマンドを実行させることで同一セッション内で解消。ユーザー指示によりこの回避策を今後の標準対応とする)。これで購入履歴基盤〜UI(T3-61〜T3-65)は全完了。**T3-59(豆マスタに保存場所)も2026-07-29にSonnet 5の`/full_loop`で完了・本番デプロイ・本番確認まで完了**(既存30件全豆に「職場」を一括設定済み)。**T3-69(豆マスタのstore→storeId移行)も2026-07-31にSonnet 5の`/full_loop`で完了・本番デプロイ・移行実行・本番確認まで完了**。これでT3-58〜T3-69グループは全完了。

**モデル選定の一般ルール(2026-07-28ユーザー指示で恒久化)**: **上位モデル(Opus等)がやるのは「方針検討」と「実装内容の検討」まで。実装は必ず下位モデル(Sonnet 5)に回す。** したがって上位モデル指定のタスクは、成果物が必ず**設計書+実装タスクへの分解**になり、コードは1行も書かない。逆に、上位モデルはSonnet 5が迷わない粒度(フィールド名・シート列名・画面ID・引数表・突合規則・既知の地雷への注意まで確定)で情報を整理する責任を負う。T3-66はこのルールに沿って実施した(成果物`docs/store_master_design.md` → T3-67/T3-68/T3-69/T3-70はすべてSonnet 5で実装可能)。

**このグループのモデル選定(2026-07-28、ユーザー指示「上位モデルで検討して」に基づく)**: **T3-61・T3-66の2件のみ上位モデル(Opus等)で実施**し、`full_loop`の自動選定対象外とする(`statistics_feature_design.md`§12①「画面デザインの新規検討は上位モデルで実施」に該当。T3-61=追加購入ダイアログと購入履歴の統合という新規の運用設計、T3-66=購入店マスタの管理項目そのものの決定とWeb情報収集を伴うため)。**T3-61・T3-66とも2026-07-29時点で完了済み**(成果物はそれぞれ`docs/bean_purchase_design.md`・`docs/store_master_design.md`)。**それ以外(T3-58〜T3-60、T3-62〜T3-65、T3-63b、T3-67〜T3-69)はすべてSonnet 5で`/full_loop`による自動実施の対象**。そのため設計判断が必要な箇所は本タスク表側で先に確定させてある(下記各行の「実装方針」を発明せずそのまま従うこと)。

**共通の注意(全タスク共通)**:
- モデルにフィールドを追加したら**必ず** ①`lib/models/*.dart` ②`*.g.dart`(build_runnerがこのマシンで不安定なため手動編集可、T3-34の教訓参照) ③`SheetsService`の**keyMapとreverseMapの両方** ④GAS `gas/Code.gs`の`EXISTING_SHEET_EXTRA_COLUMNS`(既存シートへの列追加)または`ALLOWED_SHEETS`+`NEW_SHEET_HEADERS`(新規シート) ⑤`clasp push`+`clasp redeploy` の5点をセットで実施する。**過去に頻発した「列プロビジョニング漏れ」バグ(T4-1b/T4-2d/T3-23/T3-47)と同型**。`rules/verification.md`のチェックリストに従う。
- 新規画面を足す場合は `lib/routing/app_screen.dart`(画面ID enum)+`lib/routing/screen_registry.dart`(ID→Widget解決)+導線(遷移ボタン)の3点セット。既存例はT3-36(042)・T3-39(043)。
- 画面IDの空き: **025〜029, 032〜039, 044〜089**。本グループでは **025=購入履歴、026=購入店管理(リスト)、027=購入店詳細、028=新規購入店** を割り当てる(着手時に`app_screen.dart`で重複が無いことを再確認すること)。
- CLAUDE.md「全マスタータブへの一律適用」規約: 豆マスタだけの変更で済むか、他マスターにも展開すべきかを毎回判断すること(本グループは豆固有の要件が多いため、原則として豆のみで良い。共通ウィジェットに手を入れる場合のみ全マスターへの影響を確認)。

**このグループのタスク(T3-58〜T3-69)はすべて完了した。**

> **完了済み(14件)**: T3-58, T3-59, T3-60, T3-61, T3-62, T3-63, T3-63b, T3-64, T3-65, T3-66, T3-67, T3-68, T3-70, T3-69
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-07-30 ユーザー要望、T3-71: 推奨焙煎度の「幅」対応）

T3-71・T3-71a・T3-71bは**2026-07-31にすべて完了・本番デプロイ・push済み**(詳細は`docs/archive/マスタープラン_完了タスク.md`のT3-71節、成果物`docs/method_roast_range_design.md`)。本番`methods_master`のうち推奨焙煎度が設定済みなのは`method001`(4:6メソッド)のみ。他11メソッドは未設定のためF3の候補にならない(ユーザーが021から順次設定する運用)。

> **完了済み(3件)**: T3-71, T3-71a, T3-71b
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-08-02、T3-72: 「継続中の注意事項」のタスク化）

`NEXT_SESSION.md`「継続中の注意事項(未解決)」9件を棚卸しし、着手可能なものをタスク化した。
**トリアージ結果**: #1は記載が古かった(下記T3-72a参照)、#8はユーザー実施予定のため対応不要、#4・#9はユーザー確認待ちで下表には含めない(質問済み)。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-72a | ❌ **クローズ(2026-08-05、T3-72fへ統合)**: 当初案(`bean_purchases`の`bp_init_<豆ID>`から`初期購入量(g)`を逆算投入)は本番データ突合の結果**実行不可**と判明(`bp_init_`行0件、`tools/migrate_bean_purchases.dart`本番未実行)。逆算スクリプト`tools/backfill_bean_initial_purchase.dart`の`--dry-run`も対象0件。自動移行できる元データが存在しないため、ユーザーが各豆の`初期購入量(g)`を手動入力する以外に解決策が無く、T3-72f(同じくユーザー手動入力のタスク)と性質が同一のため統合しクローズ | (T3-72fに統合) | なし | M | ❌(統合済み) |
| T3-72f | **注意事項#7**: 本番`methods_master`12メソッドのうち推奨焙煎度が設定済みは`method001`のみだった。**推奨焙煎度は2026-08-05、ユーザー依頼によりClaude(Opus 5)が各メソッドの発案者・出典(YouTube/公式レシピ等)をWeb検索で調査し、021の編集画面(推奨焙煎度スライダー)経由で残り12メソッド全件に設定・本番反映済み**(根拠: 명称に「浅煎り用/向け」と明記された5件→ライト〜シナモン、井崎式→発案者の基準レシピが「中煎り=92℃」基準でありデータの湯温92℃と一致→ミディアム〜シティ、ORIGAMI円錐/ウェーブ→フィルタ特性の一般知見、New Hybrid Method→開発者本人が焙煎度不問と明言のためライト〜イタリアン全域、松原スペシャルのみユーザー確認済みでフルシティ指定。詳細はNEXT_SESSION.md参照)。**残るのは28豆中24豆で未入力の`初期購入量(g)`のみ**(自動移行できる元データが無いためユーザーが021から手動入力する必要がある) | ~~11メソッドの推奨焙煎度が設定され~~→達成、`初期購入量(g)`が主要な豆で入力される→未達成(残作業) | なし | S | 🔶(推奨焙煎度は完了・初期購入量はユーザー入力待ち) |

> **完了済み(4件)**: T3-72d・T3-72b・T3-72c・T3-72e / **クローズ(1件)**: T3-72a(T3-72fへ統合)
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-08-03、T3-74: T3-72d本番確認で発見したレース条件の修正）

T3-72dの本番確認中に、`OptimisticListNotifier`(`lib/providers/data_providers.dart`)の`updateOptimistic`等が直後に呼ぶ`_syncInBackground()`が、GAS書き込み直後だと更新前データで上書きすることがあると判明した(詳細は`rules/lessons_archive.md` L99)。T3-72dの範囲(コンストラクタ引数問題)とは別原因のため未修正のまま新規タスク化する。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-74a | ✅ **完了(2026-08-03)**: L99末尾の案(a)`_syncInBackground()`削除を採用(理由・詳細は`rules/lessons_archive.md` L100)。テストで発覚した`_FakeDataService`の参照エイリアシング起因の重複バグも修正 | 編集→保存→pop直後(フルリロード無し)でも最新値が表示される。`flutter analyze`新規issue0・`flutter test`全パス | なし | M | ✅ |

> **完了済み(1件)**: T3-74a
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

#### Phase 3 追加分（2026-08-03、T3-75: 本番動作・表示データの棚卸しで発見した不具合）

2026-08-03の`/full_loop`(ユーザー指示「本番環境が本当に意図した動作をしているのか、正しいデータを表示しているのか確認して」)で、本番GAS実データ12シートを直接取得して突合し、あわせて本番と同一の`build/web`(`main.dart.js` md5 `40c4c1679bd085f5809303bd4ba70a9b`、本番配信物と一致を確認済み)をローカル配信してブラウザ目視した結果を新規タスク化する。**この回ではコード編集は行っていない(ユーザー指示)。**

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-75a | ❌ **localhost限定・本番では再現せず(T3-75hで2026-08-03確認)**。マスター画像が1枚も表示されない。~~`bean_master`28件中24件に`豆画像URL`があり~~(元記述、以下同旨)。**判定根拠**: 本番URLの豆管理カード一覧(011)で3件全てGoogle Drive画像が正常表示(コーヒーバッグの写真)、豆詳細画面(012)でも画像表示を確認。localhost配信時のみOrigin/Refererの違いでDrive/lh3画像取得が失敗していたと推定 | ~~011/012/020等の一覧・詳細で本番のマスター画像が実際に表示される~~ → 確認済み | なし | M | ❌ |
| T3-75b | ✅ **完了(2026-08-04)**: 031(評価画面)の`_submit()`冒頭に豆未選択・湯温未入力(≤0)の必須バリデーションを追加し、本番デプロイ済み。既存の不正行(記録ID `1785746695316`)は正しい豆・湯温がユーザーにしか分からないため今回は対象外、ユーザーが後日021またはSheetsを直接編集して補正する方針(2026-08-04ユーザー確認済み) | 豆未選択・湯温0では保存できない | なし | S | ✅ |
| T3-75c | ✅ **完了(2026-08-05)**: **本番`coffee_data`のゴミ行3件を削除**。(1) 記録ID `1784633291939`(`bean_master`に存在しない豆IDを持つ孤児行)は既存の`delete`アクション(GAS変更不要)で削除。(2) 全列空白の行2件は既存`delete`がID列一致方式で空IDを扱えないため、`gas/Code.gs`に一回限りの`deleteBlankRows`アクションを追加し`clasp push`+`clasp deploy`(既存デプロイID、URL不変、@19→@20)で反映して実行、確認後に同関数を削除して再度push/deploy(@21)し後片付け(コードの正味差分なし)。本番`coffee_data`は173件→170件になった | 3行が削除され、`coffee_data`の有効行数が一致する → 達成(173→170件、空白行0件を確認) | なし | S | ✅ |
| T3-75d | ✅ **完了(2026-08-05)**: **本番`filter_master`のテストデータ(フィルターID `1771594821407`、`Test Filter`)を既存の`delete`アクションで削除** | `filter_master`が実機材7件のみになる → 達成(8件→7件、F001/F002/1f52a4d7/c2df4b23/8e2421ab/bbd1a210/00b3bc17の7件のみ) | なし | S | ✅ |
| T3-75e | 🔶 **本番では「初回描画時のみ」の一過性(2026-08-03 T3-75hで確認、localhost観測の「再描画後も残る」とは異なる挙動)**。ダッシュボード(001)・統計(040)・設定(090)いずれも画面遷移直後は同様の豆腐化(例:「実験的な提案です」→「⊠⊠的な提⊠です」)が出るが、**2〜3秒待って再描画すると同一画面内で正常な文字に回復**する。`performance.getEntriesByType('resource')`実測でも`fonts.gstatic.com`のnotosansjp含む全フォントリクエストは`failedCount:0`(取得自体は成功)。初回ペイントがCanvasKitのフォントフォールバック解決前に走っているのが原因と推定。**2026-08-05、`web/index.html`に`fonts.gstatic.com`/`fonts.googleapis.com`への`preconnect`ヒントを追加・本番デプロイ済み**(接続確立の待ち時間のみ短縮、取得自体の遅延は解消しない部分緩和)。恒久解消には日本語フォント(Noto Sans JP、1ウェイト約5.3MB)のアセットバンドルが必要だが、リポジトリ・アプリサイズの恒久的な増加を伴うためユーザー判断で見送り済み | 001/011/020/040の主要文言に豆腐(⊠)が出ない(初回描画のちらつきは許容するか要ユーザー判断→**ユーザーは許容を選択、preconnectのみで対応**) | なし | M | 🔶(優先度低・部分対応済み) |
| T3-75f | ✅ **完了(2026-08-03)**: `sheets_service.dart`の`_fetchData`/`_postData`内の`print(`(生データダンプ含む)を全て削除し、状態ログのみ`debugPrint('[Antigravity] ...')`(日本語)に置き換えた | 本番ビルドのコンソールに生データダンプが出ない。`sheets_service.dart`内の`print(`が0件 | なし | S | ✅ |
| T3-75g | **残豆量の分母不整合**。(1) `5549c4ad`(Youth ケニア Kirinyaga Kii AB 中深煎り)は`初期購入量(g)`=35に対し抽出記録4件の使用量合計が60gで、`calculateBeanRemainingGrams`のclampにより0%表示になっている(実態と合っているか要確認)。(2) `1785051144979`(スイートイエロー)は`初期購入量(g)`=100 < `在庫基準量(g)`=135.5で、初期購入量より基準量が大きい(追加購入分が初期購入量に反映されていない)。T3-72a/T3-72fで扱う「初期購入量が未入力」問題とは別に、**入っている値同士が矛盾している**ケースなので切り出す | 2件の値がユーザー確認のうえ実態と合う値に補正される。追加購入時に`初期購入量(g)`と`在庫基準量(g)`のどちらを正とするかがコード・設計書で一意に定まっている | T3-72f | M | ⬜(要ユーザー確認) |

> **完了済み(6件)**: T3-75b, T3-75f, T3-75h, T3-75i, T3-75c, T3-75d
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

| T3-75h | ✅ **完了(2026-08-03)**: T3-75a〜gを本番URLで再確認した。**判定結果**: T3-75a=❌localhost限定(画像は本番で正常表示)、T3-75b/c/d/g=データ側の状態そのまま変化なし(未修正・要ユーザー確認は継続)、T3-75e=🔶本番では初回描画時のみの一過性に格下げ(再描画で回復)、T3-75f=本番でも再現しないことを再確認(修正済みのため当然)。画面遷移(D1/D2)・戻る操作・0%トグル等の操作異常なし、コンソール例外0件。手順は`docs/production_verification_guide.md`に従った。新規発見はT3-75iとして起票 | 各行の状態を更新済み。新規発見はT3-75i | なし | M | ✅ |
| T3-75i | ✅ **完了(2026-08-04)**: **抽出履歴詳細(003)でドリッパー名が稀にIDのまま表示される**問題(本番で1回観測、記録ID `1785746695316`)を修正。`lib/screens/log_detail_screen.dart`の`resolveName()`が、マスタ一覧に該当IDが無い場合、および`AsyncValue`が`data`以外の状態(取得エラー等)の場合に生のIDをそのまま返していたのを、両ケースとも`不明`を返すよう修正(豆・メソッド・グラインダー・ドリッパー・フィルター全項目に共通適用)。`test/log_detail_screen_test.dart`にマスタ取得失敗を模したケースを追加 | ドリッパー等マスター取得が失敗した場合でも詳細画面に生IDが出ない(代わりに「不明」等の日本語文言)。`flutter analyze`新規issue0・`flutter test`全パス | なし | S | ✅ |

> **確認して問題が無かった項目(2026-08-03)**: `coffee_data`171件の`ミル`/`ドリッパー`/`フィルター`/`抽出方法`/`産地ID`は未解決参照0件。`bean_master`の`産地ID`/`購入店ID`も未解決参照0件(空欄はあるが不正値は無し)。総合評価の平均は6.771(170件)。ダッシュボードのおすすめレシピは予測スコア6.2[4.8, 7.5]と区間内に収まっており表示は正常。本番`main.dart.js`のmd5はローカル`build/web`と一致しており、T3-72eの未デプロイ分は成果物に差が無い(デッドコード削除がtree-shakingで元から消えていたため)。
>
> **T3-75h(2026-08-03、本番URL再確認)で追加確認した内容**: 上記に加え`抽出方法`(メソッド)列も未解決参照0件(初回集計時のキー名取り違えによる誤検出170件を`メソッドID`列で再集計し訂正)。本番ブラウザで001/002/003/マスター管理/010/011/013(フィルター)/040/090の各画面を目視確認、画面遷移・戻る操作は正常、`Exception`/`Uncaught`/`TypeError`パターンでのコンソールエラーは0件。

#### Phase 3 追加分（2026-08-04 ユーザー要望・不具合、T3-76〜T3-79）

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-76 | ✅ **完了(2026-08-04)**: **不具合(2026-08-04ユーザー報告)**: 抽出履歴の編集画面(`lib/screens/log_edit_screen.dart`、002/003からの編集導線)が`CoffeeRecord`のごく一部(日時・豆量・総湯量・湯温・抽出時間・挽き目・コメント・7スコア)しか編集できず、**豆(beanId)を含む残りの項目(grinderId・dripperId・filterId・methodId・roastLevel・origin・originId・taste・concentration・bloomingWater・bloomingTime)が編集不可**だった。031(`brew_evaluation_screen.dart`)の`_resolveById`ドロップダウン選択パターン(直接編集させると存在しないIDになりうるマスタ参照を安全に選択)をそのまま再利用し、豆/グラインダー/ドリッパー/フィルター/メソッドをドロップダウン化。roastLevel/origin/originIdは031と同様に選択した豆から自動同期、taste/concentrationは4:6メソッド選択時のみ表示・保存(031のT3-18方針を踏襲)、bloomingWater/bloomingTimeは新規の数値入力欄として追加。本番URLをローカル配信し、実際に「豆: -」(未選択)の本番記録で編集画面を開いて豆選択→反映を確認済み(保存はユーザーの実データを書き換えるため未実施、UIの表示・選択反映のみ確認)。`test/log_edit_screen_test.dart`に3件追加(未選択マスタの選択・保存/豆未選択時のエラー/4:6メソッド時の味わい欄) | `log_edit_screen.dart`で`CoffeeRecord`の全編集可能フィールドが編集・保存できる。保存後、詳細画面(003)に反映される。`flutter analyze`新規issue0・`flutter test`全パス | なし | M | ✅ |
| T3-77 | ✅ **完了(2026-08-04)**: **要望(2026-08-04ユーザー報告)**: 抽出履歴一覧(002、`lib/screens/log_list_screen.dart`)にフィルタ機能が無い問題を解消。`LogListScreen`を`ConsumerWidget`から`ConsumerStatefulWidget`に変更し、豆(`_filterBeanId`)・メソッド(`_filterMethodId`)・期間(`_filterDateRange`)のローカル状態(setState、画面離脱でリセット)を追加。画面上部に絞り込み行を追加し、豆・メソッドはドロップダウンチップ、期間は`showDateRangePicker`を開くチップ(選択中は✕で個別解除可)、選択中フィルタ数をバッジ表示、1件でも選択中なら「リセット」ボタンを表示。フィルタ適用結果が0件のときは「条件に一致する記録がありません」+「フィルタをリセット」導線を表示。本番URLをローカル配信し実データ(豆28件・メソッド12件)で、単体フィルタ・複合フィルタ(豆+メソッド+期間で6件→3件)・リセット→全件復帰まで確認済み。コンソールエラー0件。`test/log_list_screen_test.dart`に2件追加(豆フィルタ+リセット/複合フィルタで0件時の案内文+リセット) | 002で豆・メソッド・期間の絞り込みができ、フィルタ適用中はリスト件数が絞り込まれる。フィルタ解除で全件表示に戻る。`flutter analyze`新規issue0・`flutter test`全パス | なし | M | ✅ |
| T3-78 | ✅ **完了(2026-08-04)**: **要望(2026-08-04ユーザー報告)**: 購入店のAI自動取得(028/027、`lib/screens/create/store_create_screen.dart`の`_fetchStoreInfoWithAi`→`AiAnalysisService.fetchStoreInfo`)が、`ambiguous`をAIが自己判断した場合のみ候補を出す設計のため、**似ているが別の店を高確度で誤って断定するリスクがある**問題を解消。①`StoreInfoCandidate`から`ambiguous`フィールドを削除し、`_buildStoreInfoPrompt`(`lib/services/ai_analysis_service.dart`)を常に候補を最大5件`candidates`に列挙させるプロンプトに変更(1件しか無い場合もその1件を返す指示に変更)。`store_create_screen.dart`側は`candidate.candidates.isNotEmpty`である限り常に`_showCandidateSelectionDialog`を経由してから、選択した候補で`fetchStoreInfo`を再実行(確定情報取得)するフローに変更(`_showCandidateSelectionDialog`は非空前提に簡素化、ダイアログ文言も「店舗の候補を確認してください」に変更)。②`fetchStoreInfo`のシグネチャを拡張し、`hintPrefecture`に加えフォームに入力済みの住所・URL・電話番号・営業時間・定休日・開業年・オンライン/実店舗/焙煎所の有無・豆の傾向・SNS URLを検索ヒントとして渡す(空欄の項目・未チェックのbool項目は渡さない)。`test/store_ai_fetch_test.dart`を新フローに合わせて更新し、候補1件のみでも候補選択を経由後に2回目の`fetchStoreInfo`で確定情報を取得し確認ダイアログに進むケースを新規追加。`flutter analyze`新規issue0・`flutter test`354件全パス(新規1件)・`flutter build web`成功。ブラウザではGemini APIキーが本セッションのローカル配信オリジンに保存されていないため実際のAI応答フローは確認できず、028画面の表示・APIキー未設定時のダイアログ表示・コンソールエラー0件のみ確認(ロジックはwidgetテストで担保) | AI自動取得を実行すると常に候補選択ダイアログが挟まり、フォームに既に入力済みの情報が検索の絞り込みに使われる。`flutter analyze`新規issue0・`flutter test`全パス(既存の`store_ai_fetch_test.dart`の期待値も新フローに合わせて更新) | なし | M | ✅ |
| T3-79 | ✅ **完了(2026-08-04)**: **不具合(2026-08-04ユーザー報告)**「注湯ステップのハイライトが途中からずれる」。本番`pouring_steps`実データ(method001=4:6メソッド、654c2399=井崎式)で`_activeStepIndex`(`lib/screens/brew_recipe_screen.dart`)の計算をトレースし原因を特定: 0秒ステップ(瞬間アクション)の直後の非ゼロステップへハイライトを譲るかどうかを、**そのステップ自身の説明文が空かどうかを見ずに常に譲っていた**バグ。4:6メソッドのように直後が空文字の説明のみなら問題は表面化しないが、井崎式のように直後のステップにも独自の指示文があるメソッドでは、そのステップの指示が一度もハイライトされず0秒ステップ側に奪われ続け、実際の注湯タイミングとハイライトが噛み合わなくなる。ロジックを`lib/utils/pouring_step_highlight.dart`の`activeStepIndex()`に切り出し、「直後のステップの説明文が空の場合のみ」0秒ステップへ譲るよう修正、`test/pouring_step_highlight_test.dart`に本番データを模したケース(4:6メソッド型・井崎式型)で回帰テストを追加 | 0秒ステップの直後に独自の説明文を持つステップが、自分自身の待機区間中にハイライトされる。`flutter analyze`新規issue0・`flutter test`全パス | なし | S | ✅ |

> **完了済み(4件)**: T3-76, T3-77, T3-78, T3-79

#### Phase 3 追加分（2026-08-05 ユーザー報告、T3-80: 注湯ハイライトずれの再発）

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-80a | ⚠️**上位モデルで実施(完了、2026-08-05)**: **不具合(2026-08-05ユーザー報告)**「pouring stepのハイライトがまたずれている」(T3-79の修正後も未解消/再発)。ユーザー指示により①上位モデルで検証設計→②下位モデルで検証→③上位モデルで修正案検討→④下位モデルで実装、の4フェーズで進める。本タスクはフェーズ①=**検証要領書`docs/pouring_highlight_verification_plan.md`の作成**(コード変更なし) | 検証要領書が存在し、仮説H1〜H7・手順・報告テンプレが下位モデルだけで実行できる粒度で書かれている | なし | S | ✅ |
| T3-80b | ✅ **完了(2026-08-05、Sonnet 5サブエージェント)**: フェーズ②=検証要領書に従い、本番`pouring_steps`全12メソッド+`methods_master`を`curl`取得して机上表を作成、`build/web`をローカル配信して`method001`・`654c2399`で実機観測(スクショ・コンソール確認)。**H1・H2・H4・H5・H6を「観測された」と判定**(H3は一部・H7は検証不能)。加えてメソッド切替時に経過時間・説明列がstaleになる表示不整合を新規発見。コードは未変更 | 要領書§6の完了条件を全て満たした報告が提出されている | T3-80a | M | ✅ |
| T3-80c | ⚠️**上位モデルで実施(完了、2026-08-05)**: フェーズ③=T3-80bの観測事実から**根本原因を確定**し`docs/pouring_highlight_fix_design.md`を作成。`method001`の表示時刻が粕谷式4:6メソッドの実際の注湯タイミングと一致することから、`加算時間（秒）`=「その操作を行うまでの待ち時間」・「経過時間」列=「その操作を行う時刻」と意味論を確定。**表示仕様は正しく、点灯区間が`[前の操作時刻, 自分の操作時刻)`= 常に1行先だったことがずれの正体**と結論(T3-79は症状の一部のみ修正していた)。修正4点(点灯窓の再定義+同時刻同時点灯+最終行保持 / 一時停止中も保持 / 時刻・説明列の`ValueKey` / メソッド切替時のタイマーリセット)とテスト12ケースを確定 | 下位モデルが設計判断なしに実装できる粒度の修正方針が文書化されている | T3-80b | S | ✅ |
| T3-80d | ✅ **完了(2026-08-05、Sonnet 5サブエージェント)・本番デプロイ済み**: フェーズ④=設計書通りに実装。`lib/utils/pouring_step_highlight.dart`を`activeStepIndex`(int?)→**`activeStepIndexes`(Set&lt;int&gt;)**へ全面書き換え(点灯窓を「自分の操作時刻〜次の操作時刻」に変更、同一操作時刻の行は同時点灯、最終操作時刻の超過後も最終行を点灯継続、T3-79の0秒ステップ譲渡ロジックは全廃)。`brew_recipe_screen.dart`は一時停止中もハイライトを保持(リセット時のみ消灯)+メソッド切替時にタイマーをリセット。`method_steps_editor.dart`は時刻列・説明列の`TextFormField`に`ValueKey('time_${s.id}')`/`ValueKey('desc_${s.id}')`を付与し、メソッド切替後に旧メソッドの値が残る表示不整合(T3-80bで発見、湯量列だけキーを持っていたのが原因)を解消。テストは`pouring_step_highlight_test.dart`を新仕様の12ケースへ全面書き直し+`method_steps_editor_test.dart`に同時点灯ケース1件追加。`flutter analyze`新規issue0・`flutter test`360件全パス・`flutter build web`成功。ローカル配信で4:6メソッド(0:45/1:30でタイマーと点灯行が一致)・井崎式(0:00の0秒ステップ点灯、2:00の2行同時点灯、3:00超過後も最終行点灯)・一時停止保持・リセット消灯・メソッド切替を実機確認、コンソールエラー0件。`firebase deploy --only hosting`実行後、本番`main.dart.js`のmd5一致(`0d71511a...`)を確認 | ハイライトが実際の注湯タイミングと一致する。`flutter analyze`新規issue0・`flutter test`全パス | T3-80c | M | ✅ |

> **完了済み(4件)**: T3-80a, T3-80b, T3-80c, T3-80d(いずれも2026-08-05、本番デプロイ済み)

#### Phase 3 追加分（2026-08-02、T3-73: 日次ループのトークン消費削減）

**正本は `docs/token_optimization_design.md`**(実測ベース。本表と食い違う場合は設計書が優先)。

**実測サマリ**: 1ループのコストは「リクエスト数 × 平均コンテキスト長」でほぼ決まり、cacheRが全体の6割。直近の高コストループは平均ctx 207〜231k・最大337〜355kで**200kの料金境界(単価約2倍)を越えていた**。これがコスト差2.6倍($15.3 vs $5.9)の主因。目標は **1ループ $15 → $7〜9**。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T3-73g | ✅ **完了(2026-08-05)**: T3-73d〜f適用後の初回`/full_loop`(本ループ自体)の実測を設計書§7に記録(23リクエスト・平均ctx 77,659・最大96,379・$1.1。本ループはコード変更なしの軽量ループのため$7〜9目標との比較は不可、実コード変更を伴う通常ループでの再測定が必要) | §7に実測値が1行以上記録されている → 達成 | T3-73a, T3-73d | S | ✅ |

> **完了済み(7件)**: T3-73a, T3-73b, T3-73c, T3-73d, T3-73e, T3-73f, T3-73g(詳細は`docs/archive/マスタープラン_完了タスク.md`「T3-73」節)

### Phase 4 — 統計解析・予測機能拡張（Cycle 28+、最後）

> **設計書は単一の正本**(CLAUDE.md「統計解析・予測機能の実装ルール」参照)。本表と食い違う場合は設計書が優先。**設計書§0の絶対規則によりサブPhase順(0→1→2→3→4→5→6)を厳守**し、各サブPhaseはテスト全パス(設計書§9)まで次に進まない。ID の数字はこのサブPhase番号に対応(例: T4-2xはサブPhase2=F1回帰分析)。
>
> **Phase 終了条件:** F0〜F6すべてが実装され、設計書§9の全テストがパスし、Phase 1完了時点で必要なユーザー作業(実データ移行の確定、GAS `scriptId` 記入と初回`clasp login`)が完了している。


> **完了済み(25件)**: T4-0a, T4-0b, T4-0c, T4-1a, T4-1b, T4-1c1, T4-1c2, T4-1d, T4-1e, T4-1f, T4-2a, T4-2b, T4-2c1, T4-2c2, T4-2d, T4-3a, T4-3b, T4-4a, T4-4b, T4-4c, T4-5a, T4-5b, T4-6a, T4-6b, T4-6c
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。

### Phase 5 — Android公開版の開発〜リリース（Cycle 30+）

> **設計の正本は `docs/android_release/リリース計画書.md`**(全体戦略・3トラック・フェーズ定義)。
> 自動実行の詳細は `docs/android_release/開発運用基盤設計.md`、検証体制の詳細は `docs/android_release/検証強化設計.md`。**本表と食い違う場合は各設計書が優先。**
> 原価・収益の前提は `docs/android_monetization/`(コスト試算・収益化アイデア・コードベース構成方針)。

**Phase 終了条件:** Android 公開版が Google Play の製品版として公開され、AI サブスクの購入〜回数消費〜広告表示が本番で動作する。

**着手順の絶対規則(2026-08-07 ユーザー確定)**:

1. **トラックA(開発運用基盤)を最初に完成させる。** 夜間の自動実行が無いまま製品開発を始めると、40〜60人日規模を有人セッションだけで消化することになり破綻する
2. **トラックC の T5-C1(Play Console登録)は今すぐ開始。** テスター12人は知り合いから確保可能(2026-08-07確認済み)のため人集めは律速にならず、**残るクリティカルパスは Play Console の本人確認と14日間の待機のみ**
3. **夜間ループには設計判断をさせない。** `⚠️上位モデル`タスクは有人セッションで先に消化し、確定済みタスクの在庫を切らさない

サイズ: S=半日余裕、M=1ループ標準、L=1ループぎりぎり(**夜間ループはS/Mのみ着手可**)

#### トラックA — 開発運用基盤(最優先、他の全タスクの前提)

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-A4 | `.claude/agents/ui_verifier.md` を新設。エミュレータでアプリを起動し変更画面を操作してスクショ判定。チェック項目は検証強化設計 §5-2 の7項目 | 意図的にoverflowさせた画面で指摘が出る | T5-A6 | M | ⬜ |
| T5-A5 | `.claude/agents/researcher.md` を新設。Webでの情報収集専任(Play要件・競合/ASO・パッケージ選定・Gemini価格)。成果物は`docs/research/<日付>_<主題>.md`、**出典URLと取得日を必ず記載**、推測と事実を分けて書く | T5-C3(Play要件の最新確認)を実際に1件実行し成果物が出る | なし | S | ⬜ |
| T5-A6 | Androidエミュレータ環境の整備。AVD作成、起動/停止スクリプト(`tools/emulator.ps1`)、`flutter devices`で認識されることの確認。Windows/Ubuntu両対応 | スクリプト1本でエミュレータが起動し`flutter run -d <emulator>`が通る | なし | M | ⬜ |
| T5-A7 | `integration_test` スモークスイート。起動→記録(抽出→評価)→保存→一覧反映→インサイト表示→設定 の一筆書き+**5マスタ全部の一覧→詳細→編集** | エミュレータ上で全パス。`verify.ps1`とは別コマンドで実行できる | T5-A6, T5-B1 | L | ⬜ |
| T5-A8 | golden テスト基盤。共通コンポーネント単位で**ライト/ダーク両方**を撮る。CIでの差分検出。**LLMにgoldenを自動更新させない**運用ルールを`rules/verification.md`に明記 | 意図的な色変更でgoldenが落ちる | なし | S | ⬜ |
| T5-A11 | `.claude/hooks/loop_guard.js` のしきい値を夜間ループ用に分岐。夜間=**$8/ターン40/連続失敗2**、有人=従来($24/30/3)。判別できない場合は安全側(夜間の値)。**【T5-A9からの申し送り】`loop_guard.js`はループ境界を`/start`・`/full_loop`でしか検出しておらず(`:110-111`・`:242`)、`/night_loop`は境界として認識されない**。境界未検出時は当日累計にフォールバックする(`:255`)ため、同日に複数回発火する夜間ループが互いの消費を合算してしまう。**`night_loop`も境界に加えること** | `loop_state.md`に適用中のモードとしきい値が出力される。`/night_loop`がループ境界として認識される | なし | S | ⬜ |
| T5-A12 | **有人監視下で`night_loop.ps1`を1回手動実行**し、(a)権限プロンプトで止まらない(b)`night_report.md`が生成される(c)ゲートが正しく判定する、を確認。その後タスクスケジューラに**23:00の1枠だけ**登録し3晩観察してから04:10・09:20を追加(開発運用基盤設計 §6の段階1〜5)。**いきなり無人で回さない** | 3晩連続で想定どおり動作する | T5-A10, T5-A11 | M | ⬜ |
| T5-A13 | `.claude/agents/implementer.md` にAndroid/公開版の規約を追記。DBスキーマ移行の必須セット/`AppEdition`経由の分岐のみ許可(ウィジェット内`if (edition == ...)`禁止)/公開版画面はホワイトリスト方式 | 定義に3項目が明記されている | なし | S | ⬜ |
| T5-A14 | **Proプランで`model: opus`のサブエージェント(`architect`)が起動できるか実測**。使えない場合は「設計は有人セッションで先出し、夜間ループに設計判断をさせない」運用をリリース計画書 §5 に沿って徹底する旨を`CLAUDE.md`に追記 | 可否が事実として記録され、不可の場合の代替運用が明文化されている | なし | S | ⬜ |
| T5-A15 | `analysis_options.yaml` にlint追加(`avoid_catches_without_on_clauses`/`unawaited_futures`/`avoid_empty_else`/`always_use_package_imports`)。既存違反が多い場合は**新規ファイルのみ適用**のbaseline運用から始める | 空catchを新規に書くとanalyzeで落ちる | なし | S | ⬜ |
| T5-A16 | トラックA完了後の最初の3ループのトークン実測を`docs/token_optimization_design.md` §7 に記録。悪化していたら`adversary`の起動条件を差分行数で絞る | §7に3行以上の実測値が記録されている | T5-A12 | S | ⬜ |
| T5-A17 | **⚠️ユーザー実施(アシスタントは自分の権限設定を書き換えられない)**: `.claude/settings.night.json`(無人実行専用の権限プロファイル。`dontAsk`+deny主体)を設置する。**内容は開発運用基盤設計 §4-4 に全文記載済み**、コピーして保存するだけ。無人実行に「都度確認」は成立しないため、危険操作は確認ではなく**deny で実行不可能にする**という設計 | ファイルが存在し、T5-A12の試走で(a)止まる定型コマンドが無い(b)denyが実際に効く(c)ゲート通過時のmain pushが通る、が確認できる | なし | S | ⬜ |

> **完了済み(12件)**: T5-A1, T5-A2, T5-A3, T5-A9, T5-A10, T5-A18, T5-A19, T5-A20, T5-A21, T5-A22, T5-A23, T5-A24(いずれも2026-08-08。コード変更は検証ツール・エージェント定義・運用スクリプト・SKILL.mdのみ・`lib/`不変のためデプロイ対象外。T5-A3の実動確認結果は`docs/archive/マスタープラン_完了タスク.md`「T5-A3」節)
> 完了行の詳細(実装内容・検証結果)は `docs/archive/マスタープラン_完了タスク.md` を参照。
> **T5-A1の使い方(後続タスクの前提)**: Windowsでは **`powershell -File tools\verify.ps1`** が本命(`jq`不要)。`tools/verify.sh`はGit Bash用だが`jq`必須で、不在時は`{"ok":false,"error":"jq_not_found",...}`を返して非ゼロ終了する。`build_apk_release`は`lib/main_public.dart`未作成・Android SDK未検出の間は`{"ok":true,"skipped":true,"note":...}`でスキップされる(T5-A6・トラックBのE-1完了後に実効化する)。

#### トラックB / P0 — エディション足場 + 原価リスクの遮断

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B0a | **全AI機能に`maxOutputTokens`を設定**(`ai_analysis_service.dart`。#3=600、#4=300 等、機能ごと)。出力暴走で1回3円超・月100回310円の赤字になりうる**最大の原価リスク**を数行で潰す(コスト試算 §2-3a) | 5機能すべての`GenerationConfig`に上限が入り、既存テストが全パス | なし | S | ⬜ |
| T5-B0b | **画像を長辺1024pxへ縮小してから送信**(`image_upload_field.dart`の`ImagePicker`に`maxWidth`/`maxHeight`、ファイル選択側もリサイズ)。豆読取の原価が0.36円→0.09円に(コスト試算 §2-3b) | 送信前の画像の長辺が1024px以下。豆読取AIが従来どおり動作する | なし | S | ⬜ |
| T5-B1 | **E-1**: `lib/config/app_edition.dart` 新設 + `lib/main_public.dart` 追加。**差分ゼロの2エントリポイント**(コードベース構成方針 §7) | 両エントリポイントが**同一の動作**で起動する。`flutter build apk -t lib/main_public.dart`成功 | なし | S | ⬜ |
| T5-B2 | **E-2**: `enabledScreens` を `screenRegistry`(`lib/routing/screen_registry.dart`)と`main_layout.dart`の`destinations`に接続。**ホワイトリスト方式**(既定非表示) | public版で`lib/screens/debug/`・`lib/screens/mock/`が出ない。personal版は従来どおり全画面 | T5-B1 | M | ⬜ |
| T5-B3 | **E-3**: `dataServiceProvider`(`lib/providers/data_providers.dart`)を`AppEdition.useLocalDb`経由にする(実装はまだ`SheetsService`のみ) | personal版が従来どおりSheetsで動く | T5-B1 | S | ⬜ |
| T5-B4 | **E-4**: `AiAnalysisService`のキー取得を`AppEdition.aiKeyMode`経由にする | personal版のAI機能が従来どおり動く | T5-B1 | S | ⬜ |

#### トラックB / P1 — ローカルDB化

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B10 | **DBパッケージの選定調査**(`researcher`)。`drift`第一候補 vs `isar` vs `sqflite`。**型安全なマイグレーション基盤の有無**を最重要の判断軸にする | `docs/research/<日付>_local_db.md`に比較表と推奨、出典URLつき | T5-A5 | S | ⬜ |
| T5-B11 | ⚠️**上位モデルで実施**: ローカルDBスキーマ設計。全モデルのテーブル定義・列名・型・インデックス・**マイグレーション方針**・Sheets↔ローカルの対応表を確定(implementerが設計判断せずに実装できる粒度) | 設計書が存在し、テーブル/列/移行の全定義が確定している | T5-B10 | M | ⬜ |
| T5-B12 | **マイグレーション基盤**を最初に入れる(後付けは必ず破綻する)。バージョン管理・up移行・移行テストの雛形 | 意図的にスキーマを1バージョン上げたテストが通る | T5-B11 | M | ⬜ |
| T5-B13 | `LocalDbService`(`DataService`の実装)を作成。40メソッド相当を実装 | public版がローカルDBで一覧/登録/編集/削除できる。`integration_test`スモークが通る | T5-B12, T5-B3 | L(要分割) | ⬜ |
| T5-B14 | 画像のローカル保存(`path_provider`。既に依存にある)。public版は`ImageService`をDrive→ローカルに差し替え | 画像を登録→再起動後も表示される | T5-B13 | M | ⬜ |
| T5-B15 | **エクスポート/インポート(JSON/CSV)を無料機能として実装**。データ消失クレームがレビュー低評価の最大要因(コスト試算 §6-#7) | エクスポート→アプリのデータ全消去→インポートで完全復元できる | T5-B13, T5-B14 | M | ⬜ |

#### トラックB / P2 — UI全画面の新規デザイン

**順序を守る: トークン → コンポーネント → 画面**(L10。共通部品を先に作る)。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B20 | ⚠️**上位モデルで実施**: 公開版のデザイン方針とトークン体系の設計。色/タイポ/余白/角丸/エレベーション、**ダークモード**、既存`mainColorProvider`との関係、4系統(ホーム/記録/インサイト/オンボーディング)の画面構成と遷移 | 設計書が存在し、トークン名・値・コンポーネント一覧・画面IDが確定している | T5-B1 | M | ⬜ |
| T5-B21 | デザイントークンの実装(ライト/ダーク) | 両モードでアプリが起動し、既存画面が壊れない | T5-B20 | M | ⬜ |
| T5-B22 | 共通コンポーネント実装(カード/リスト行/空状態/ローディング/エラー/ボトムシート/数値入力)+ **各コンポーネントのgolden(ライト/ダーク)** | goldenが全件撮れ、`verify.ps1`のgolden項目が動作する | T5-B21, T5-A8 | L(要分割) | ⬜ |
| T5-B23 | 画面: ホーム | goldenあり。`ui_verifier`の7項目で指摘なし | T5-B22 | M | ⬜ |
| T5-B24 | 画面: 記録(抽出→評価) | 同上。`integration_test`の記録導線が新画面で通る | T5-B22 | L(要分割) | ⬜ |
| T5-B25 | 画面: インサイト(表示層のみ。中身はP3) | 同上 | T5-B22 | M | ⬜ |
| T5-B26 | 画面: オンボーディング(インストール直後から使える価値。抽出タイマー/テンプレレシピ/器具プリセット。収益化アイデア §9-2) | 初回起動でオンボーディングが出る | T5-B22 | M | ⬜ |
| T5-B27 | 残りの画面(マスタ管理等)を新体系へ移植。**全マスタ(豆/グラインダー/ドリッパー/フィルター/メソッド)に一律適用** | 公開版の全画面が新デザインで表示される | T5-B23〜B26 | L(要分割) | ⬜ |
| T5-B28 | モバイル固有の作り込み: タップ領域48dp/スワイプ/ハプティクス/**抽出タイマーのバックグラウンド動作と通知**/画面回転/戻るジェスチャ/端末フォントサイズ追従 | 実機でタイマーがバックグラウンドでも動き通知が出る | T5-B24 | L(要分割) | ⬜ |

#### トラックB / P3 — 統計の「洗練された情報のみ」化

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B30 | ⚠️**上位モデルで実施**: 公開版のインサイト表示仕様。**生の統計量(PCA負荷量/回帰係数/p値/寄与率/固有値)を出さない**方針を、`statistics_feature_design.md`に「公開版の表示規則」の節を**追加する形**で整合させる(§0の絶対規則「点推定+不確実性をセット表示」を破らず、数値からビジュアルへ表現を変えるだけと位置づける)。所見1文+根拠1つ+次の一手のカード形式、確信度3段階の定義、データ不足時の閾値 | 設計書に節が追加され、変換規則と閾値が確定している | T5-B20 | M | ⬜ |
| T5-B31 | インサイト変換層の実装(統計サービスの出力 → インサイトカード) | 生の統計量が公開版のUIに一切現れない(grepで確認) | T5-B30, T5-B25 | M | ⬜ |
| T5-B32 | データ不足時の進捗表示「**あと◯件でインサイトが使えます**」+ 少数データでも出せる簡易フィードバック(前回との比較) | ログ0件・数件・閾値直前・閾値超えの4状態が正しく出し分けられる | T5-B31 | M | ⬜ |

#### トラックB / P4 — 収益化基盤(v1.0に含む)

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
**無料/Pro の線引き(2026-08-07 ユーザー確定、このグループ全体の前提)**: **無料では AI 機能を一切使えない。AI 以外(記録・マスタ管理・統計/インサイト全機能・エクスポート/インポート)はすべて無料**。Pro = AI 月100回 + 広告非表示 + クラウドバックアップ。

この線引きにより設計が単純化される: (a) **無料ユーザーはサーバに一切アクセスしない**ためアカウント登録・ログインが不要でインストール直後から使える (b) **コスト試算 §6-#2 の「無料枠悪用による原価青天井」リスクが構造的に消滅**(無料枠が存在しないため取り直せるものが無い) (c) AI原価が課金者数に厳密に比例し粗利予測が完全に立つ (d) MAUが増えてもサーバ費用は増えない。

代わりに収益化アイデア §3-3「無料枠ゼロだと課金率が上がらない」への対策として、**Play標準の「Proの無料トライアル」で体験導線を作る**(T5-B48)。無料ユーザーにAIを開放するのではなくProを期間限定で試す形なので線引きを崩さない。

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-B40 | ⚠️**上位モデルで実施**: 中継サーバと課金の設計。API仕様/認証方式/回数カウンタのデータ構造/購入検証フロー/RTDNの受信と反映/**価格とAI回数上限をサーバ側設定値として持つ形**(アプリに埋め込まない)/エラー時のクライアント挙動/**無料ユーザーがサーバを一切呼ばないことの担保**(クライアント側でAI導線を出さない + サーバ側でも未認証リクエストを拒否する二重化) | 設計書が存在し、エンドポイント・スキーマ・状態遷移が確定している | T5-B15 | L | ⬜ |
| T5-B41 | Cloud Run 中継サーバの実装(`アプリ → 中継サーバ → Gemini`)。**APKにGeminiキーを埋めない** | ローカルからエンドポイントを叩いてAI応答が返る。未認証リクエストは拒否される | T5-B40 | L(要分割) | ⬜ |
| T5-B42 | **Googleアカウントログイン**の実装。**課金時のみ必須**(無料ユーザーには求めない)。購入の復元とPro状態の紐付けに使う | 課金導線でログインを求め、ログアウト→再ログインで購入が復元される。**未ログインでもAI以外の全機能が使える** | T5-B41 | M | ⬜ |
| T5-B43 | サーバ側の回数カウンタ(端末側カウンタは改ざん可能なので使わない)。**Pro=月100回、無料=利用不可** | 上限到達でAI呼び出しが拒否され、翌月にリセットされる | T5-B42 | M | ⬜ |
| T5-B44 | `in_app_purchase` + **購入トークンのGoogle Play Developer APIによるサーバ検証**(クライアント検証は偽装可能) | テスト購入でPro状態がサーバ側に反映される | T5-B43 | L(要分割) | ⬜ |
| T5-B45 | RTDN(Real-time Developer Notifications)受信。解約・返金・期限切れの反映 | 解約テストでPro状態が失効し、AI導線が非表示に戻る | T5-B44 | M | ⬜ |
| T5-B46 | `google_mobile_ads` + UMP同意。**バナー(一覧下部)のみ。インタースティシャルは入れない**(記録アプリのUXを壊す)。**リワード広告でのAI付与は行わない**(無料でAIを使えることになり線引きに反する) | 同意ダイアログが出て、無料ユーザーにバナーが表示される | T5-B44 | M | ⬜ |
| T5-B47 | Pro機能の実装: 広告非表示 / クラウドバックアップ。**エクスポート/インポートは無料側(T5-B15)なのでProには含めない**。訴求は「AI回数」より「広告非表示」が刺さる(収益化アイデア §2-4) | Pro購読中は広告が消え、バックアップ/復元が動く | T5-B45, T5-B46 | L(要分割) | ⬜ |
| T5-B48 | **Proの無料トライアル**(Play標準の introductory offer)の設定と、アプリ内の訴求導線(AI機能をタップした無料ユーザーに「トライアルで試す」を出す)。無料枠ゼロによる課金率低下への対策(収益化アイデア §3-3) | 無料ユーザーがAI機能に触れるとトライアル導線が出る。トライアル中はProとして扱われ、期間終了でAIが使えなくなる | T5-B47 | M | ⬜ |

#### トラックC — リリース準備(人手中心。開発と並行)

| ID | タスク | 終了条件 | 依存 | サイズ | 状態 |
|---|---|---|---|---|---|
| T5-C1 | **Play Console デベロッパー登録**($25 ≒ 3,900円)。本人確認に日数がかかることがある | アカウントが有効になり、アプリを作成できる | なし | S | ⬜(**ユーザー実施・今すぐ**) |
| T5-C2 | テスター12人の確保。**知り合いから確保できる見込み(2026-08-07ユーザー確認済み)のため人集めは律速にならない**。内部テスト完了時点で声をかけられるよう名簿だけ用意しておく | 12人以上のGoogleアカウントが集まっている | なし | S | ⬜(ユーザー実施) |
| T5-C3 | **Play要件の最新確認**(`researcher`)。クローズドテスト要件・targetSdk要件・データセーフティ・課金/広告のポリシー。**要件は変動するため申請直前にもう一度実施する** | `docs/research/<日付>_play_requirements.md`に出典URLと取得日つきでまとまっている | T5-A5 | S | ⬜ |
| T5-C4 | 署名鍵の作成と **Play App Signing** の設定。**keystoreを複数箇所にバックアップ**(紛失すると以後アップデート不能。コスト試算 §6-#6)。keystoreはコミットしない | 署名済みAABが生成でき、バックアップ先が2箇所以上ある | T5-C1 | S | ⬜(ユーザー実施を含む) |
| T5-C5 | プライバシーポリシー(GitHub Pages)。**端末ローカルに留まる範囲とAIへ送信する範囲の線引きを明記**する | 公開URLがあり、実装と一致している | T5-B41 | S | ⬜ |
| T5-C6 | Data safety 申告。実装と一致させる(実装が変わったら申告も更新) | Play Consoleで申告が完了 | T5-C5 | S | ⬜ |
| T5-C7 | ストア素材: アイコン / feature graphic / スクリーンショット8枚 / 説明文 | Play Consoleに全素材が登録済み | T5-B27 | M | ⬜ |
| T5-C8 | ASO(キーワード設計)。日本語UI×コーヒー抽出記録の二重のニッチである前提で設計(収益化アイデア §9-1) | タイトル・短い説明・長い説明にキーワードが織り込まれている | T5-C7 | S | ⬜ |
| T5-C9 | 内部テスト。実機での全機能回帰 | 実機でクラッシュなく主要導線が通る | T5-B48, T5-C4, T5-C6 | M | ⬜ |
| T5-C10 | **クローズドテスト(12人 × 14日間連続)** | Play Consoleの要件が満たされ、製品版申請が可能になる | T5-C9, T5-C2 | L(待機14日) | ⬜ |
| T5-C11 | 製品版申請 → 公開。**T5-C3をもう一度実施してから提出する** | Google Playで一般公開されている | T5-C10, T5-C7, T5-C8 | M | ⬜ |

#### Phase 5 の未決事項(着手前に決める)

1. **アプリ名・パッケージID・ストア上のブランド** — パッケージIDは公開後に変更不能
2. **英語対応** — 母数10倍 vs 30画面の文言外部化の工数。**v1.0は日本語のみ、v1.1以降で判断**が妥当
3. **アフィリエイト導線** — 原価ゼロで相性は最良だがAmazonアソシエイト審査とステマ規制の広告表記が要る。v1.0に入れるか要判断
4. **Firestoreレガシー資産の扱い** — 回数カウンタにFirestoreを使うなら`FirestoreService`を復活させるか新規に書くか
5. **Proでの`model: opus`サブエージェントの可否** — T5-A14で実測
6. **既存`mainColorProvider`と新デザイントークンの関係** — T5-B20で決める

### タスク共通の検証手順（全タスクに適用）

1. `flutter analyze` — 新規エラー/警告ゼロ
2. `flutter test` — 全パス(ロジックを追加したタスクは単体テストも追加)
3. `flutter run -d chrome` — 対象画面の表示・遷移・Sheets 疎通を確認(該当時は Playwright で視覚確認。snapshot は最小限)
4. OK → commit/push + マスタープラン進捗表更新 / NG → `NEXT_SESSION.md` 更新

---

## 4. 画面インベントリ（23画面）

| 画面 | 名称 | 主な要素 | 状態 |
|---|---|---|---|
| 001 | ダッシュボード | 残豆量(瓶)・直近5件履歴・黒板風 | ✅ |
| 002 | 抽出履歴(リスト) | 日時/豆/メソッド/点数、スワイプで021 | ⬜ |
| 003 | 抽出履歴(詳細) | 全情報表示・編集 | ⬜ |
| 010 | 豆管理(カード) | 焙煎所/豆名/煎り度/画像/残量、0%表示切替、＋ | ✅ |
| 011 | 豆管理(詳細) | 全情報・編集・関連履歴5件 | ✅ |
| 012 | 新規豆追加 | 入力フォーム | ✅ |
| 013 | ドリッパー管理 | 画像左/名前右リスト、＋ | ✅ |
| 014 | ドリッパー詳細 | 全情報・編集・関連履歴5件 | ✅ |
| 015 | 新規ドリッパー | 入力フォーム | ✅ |
| 016 | フィルター管理 | 画像左/名前右リスト、＋ | ✅ |
| 017 | フィルター詳細 | 全情報・編集・関連履歴5件 | ✅ |
| 018 | 新規フィルター | 入力フォーム | ✅ |
| 019 | メソッド管理 | 名前/発案者(小)/抽出回数、＋ | ✅ |
| 020 | メソッド詳細 | 全情報・動画再生(可能なら)・編集・履歴5件 | ✅(動画再生はT3-3で別途) |
| 021 | 新規メソッド | 入力フォーム | ✅ |
| 022 | グラインダー管理 | 画像左/名前リスト、＋ | ✅ |
| 023 | グラインダー詳細 | 全情報・編集・履歴5件 | ✅ |
| 024 | 新規グラインダー | 入力フォーム | ✅ |
| 030 | 抽出レシピ | Pouring Steps・タイマー・編集・評価ボタン→031 | ✅ |
| 031 | 抽出結果の評価 | 抽出情報登録＋評価・新規登録対応 | ✅ |
| 040 | 統計情報 | スタッツ・歯車→090 | ✅ |
| 045 | 探索の検証状況 | 探索サマリ・次に試すと良い条件・スコア推移・試した条件の分布・試行一覧、030/011から遷移 | ✅ |
| 090 | 設定 | メインカラー/APIキー/保存先 | ✅ |

---

## 5. 日次ループ運用（要点）

- **終了条件**（いずれかで停止）: ①タスク完了 ②連続3回失敗 ③1ループのコスト>$24 ④1ループのターン数>=30(2026-07-25〜、すべて直近の`/start`・`/full_loop`以降の累計。従来は当日累計だった)
- コスト・ターン数は `.claude/hooks/loop_guard.js` が transcript から算出し `.claude/loop_state.md` に出力。**この数値が真実**。
- 連続失敗は `.claude/loop_failures.txt`（`<ループ識別子> <回数>` 形式）を自分で更新（失敗+1／成功0リセット。識別子は `loop_state.md` の「ループ識別子」の値を使い、新しいループが始まると自動的に 0 扱い）。
- 停止時は新規着手せず、`NEXT_SESSION.md`（引き継ぎ）と本書の進捗表を更新してから終了。
- 詳細は `CLAUDE.md`「日次改修ループ運用ルール」を参照。

- 過去の自動ループ運用方針(クラウド/ローカル分担・コスト算出式)は `docs/archive/マスタープラン_作業ログ.md` 末尾に退避。
