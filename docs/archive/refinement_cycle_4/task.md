# 修正と改善タスク (Cycle 4)

## 概要
ユーザーからの追加要望（Time表示形式変更）および、実機動作（`flutter run`）での表示不具合（変更が反映されていない）の調査・修正を行う。

## タスクリスト
- [x] **Phase 1: 表示形式の変更**
    - [x] Calculator: Time列を `秒` から `分:秒` (mm:ss) 表記に変更
- [x] **Phase 2: 実機表示不具合の調査と修正**
    - [x] 変更が反映されない原因の調査（ホットリロード/再起動、キャッシュ、ビルドエラー） -> **原因特定**: キャッシュではなく、データ読み込み時の型不一致エラーによりデータが0件になっていた。
    - [x] `flutter clean` および再ビルドの実施 -> `build_runner` 再実行済。
    - [x] `SheetsService` が実機環境で正しくデータを取得できているか確認（API通信ログ出力の強化） -> ログ実装済。
- [x] **Phase 3: プロセス改善**
    - [x] 検証手順に `flutter clean` を含める
