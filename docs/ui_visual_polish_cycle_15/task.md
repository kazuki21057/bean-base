# Visual Polish & Data Management (Cycle 15)

## 1. 画像表示の修正 (優先度高)
- [x] **ローカル画像対応**:
  - [x] `ImageUtils` または UI側で、URLがローカルパス (`C:\...` や `/data/...`) の場合に `Image.file` を使用するように修正 (`BeanImage`)。
  - [x] 現在の `Image.network` ではPC/Mobileでインポートした画像が表示されない問題を解決。
- [x] **Heroアニメーション**:
  - [x] リスト画面から詳細画面への遷移時に画像を滑らかに拡大縮小する `Hero` ウィジェットを導入。

## 2. Method (レシピ) 編集の完遂
- [x] **ステップ読み込み**:
  - [x] `MasterAddScreen` (Methodフォーム) で既存メソッドを編集する際、保存されたステップ (`PouringStep`) を読み込んで表示する。
  - [x] 現在は新規作成のみ想定されており、編集時にステップが空になる問題を修正。
- [x] **編集フロー確認**:
  - [x] 詳細画面 (`MethodDetailScreen`) から編集画面への遷移とデータ受け渡しを確認。

## 3. UI/UX 改善
- [x] **Empty State (空の状態)**:
  - [x] ログや豆リストが空の場合に、「データがありません」だけでなく、アイコンや「追加する」ボタンを表示するリッチな表示にする。
- [x] **Equipment (器具) 編集**:
  - [x] Grinder/Dripper/Filter の編集・更新が正しく動作することを確認（基本CRUD）。

## 4. その他
- [ ] **不要なインポートの整理**: コードベースのクリーンアップ。
