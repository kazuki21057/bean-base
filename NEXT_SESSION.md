# 次回の作業予定 (Next Steps / Next Session)

本日の改善サイクル(Cycle 17)において、以下の機能が完了・テスト済みです：
1. 画像の最適化と削除対応（各マスターデータ削除時にFirebase/ローカルの両方で対応）
2. `ImageUploadField` の共通化によるコードのDRY適用（マスター登録全フォームへの反映）
3. デスクトップ＆モバイル両方での動作・レイアウト確認（Statistics画面、マスター画面等）

---

## 次回以降の推奨タスク (Next Session)

1. **抽出記録 (CoffeeRecord) への応用**:
   - 現状マスターデータ(豆、ミル等)に対して画像アップロード共通化を行いました。今後は毎回の「抽出記録」に対しても、複数枚の画像アップロード（抽出時の様子やサーバーの色など）ができるようにUI拡張を検討します。
   
2. **AI(Gemini) 機能のさらなる活用**:
   - 現在PCA成分分析の結果をAIで言語化する機能が存在しますが、よりパーソナライズされた「おすすめの豆」や「次回試すべき抽出レシピの提案」などの機能を `ai_analysis_service.dart` に追加することを検討します。

3. **クラウド同期の実運用テスト (Firebase Firestore)**:
   - 現状Google SheetsとFirebase Storageを併用していますが、将来的なフルクラウドアプリ化（Firestoreへの移行等）のフィージビリティ検証やアーキテクチャ再設計が必要になる可能性があります。

4. **ルールとドキュメントの整理**:
   - `docs/` 配下のディレクトリ設計が複雑(Cycle1〜17が混在)になってきているため、過去の実装ドキュメント群のアーカイブ整理を行うと見通しが良くなります。
